# G2.2 D-131 混合裁决（compute=A / 图形=B）决策包 + 栈处置计划 + Owner 合并清单

> 生成:2026-06-25 · `Assisted-by: kiro:claude-opus-4-8`
> 地位:**AI 代录 owner 裁决的决策包,供 owner FCP-lite 批准**;**AI 不代决 / 不自合 / 不自批**,state-flip + 合并均由 owner(硬规则 1)。
> 本文件为 review 产物,可不入库(owner 确认后删);若入库则随勘误 PR。

---

## 1. 裁决摘要（owner 已裁，AI 代录）

D-131 由**单选 A** 增补为 **混合:compute=A（LLVM DirectX 后端直 emit DXIL）/ 图形=B（MIR→SPIR-V→SPIRV-Cross→HLSL→dxc→DXIL 转译）**。

| 维度 | 路径 | 依据（证据指针） |
|---|---|---|
| compute kernel | **A**（RFC-0003 既有） | 结构首选 + round-8 浅修 PSV（validator pre 0/25→post 25/25 accept，RD-011 dev 偏差） |
| 图形着色阶段 | **B**（本包 RFC-0004） | A 路图形签名 ISG1/OSG1 `elemcount=0` 不可达（`dxil_slice3_rxs0159_sig_disasm_round8.md`，#90504 + FFI ABI 禁区）;A-graphics ~800-1500 LOC 上游大功能无在途（`dxil_a_graphics_sig_effort_report.md`）;B 图形签名 `elemcount>0`、SV 端到端存活、validator accept、确定性 **measured 可行**（`dxil_b_graphics_sig_report.md`） |
| A-graphics | 迁移目标（RD-015） | 上游 #90504/#57928 成熟 + D-205 pin bump 后由 B 迁回 A |

**P-01 达标要求**（owner 裁断，AI 代录）:owner 裁定 **P-01(strict-only)不开例外、不设边界**——RFC-0004 §4.4 由「P-01 边界/例外声明占位」改写为 **strict-only 达标要求**:图形=B 严禁任何对用户声明/可观察签名元素的静默降级或丢弃,留不住即显式 6xxx 编译错;经用户语义名 by-construction 保真 + **强制译后签名一致性校验门**(译后 DXIL ISG1/OSG1 vs MIR 意图签名比对 → 6xxx,不可裁剪)达标(RFC-0004 §4.4/§4.2,measured 可行见 `dxil_b_strict_only_report.md`)。核心规范句为 AI 代录 owner 裁断(owner 合并 #100 生效);细化处(「用户可观察」精确边界 / 未用输入保留 vs 诊断)留〈待 owner〉,AI 不替补(P-13/硬规则 1)。

---

## 2. 决策包产物清单（本勘误 PR diff）

| 文件 | 变更 | 类型 | guardrails |
|---|---|---|---|
| `13_DECISION_LOG.md` | §3 D-131 行（A→混合）+ §7 待决行 + 修订记录 v1.4 | 规划文档勘误 | **预期红**（check_planning_docs，需 `--admin`） |
| `rfcs/0003-dxil-backend.md` | §9 Q-D131 追加混合增补（追加式不改 A）+ 修订记录 | RFC 追加式回填 | 通过（rfcs 不受 planning-docs 守卫） |
| `rfcs/0004-spirv-dxil-graphics-backend.md` | **新建** Draft v0.1 + **Draft v0.2**（§4.4 改为 strict-only 达标要求(代录 owner 裁断)+ §4.2 强制签名一致性校验门设计面 + §9 Q-P01-Boundary 代录;🔒 §4.6 禁区占位待 owner） | 新 RFC（Draft）/ 追加式修订 | 通过（rfcs 不受 planning-docs 守卫） |
| `registry/deferred.json` | RD-010 close + 新增 RD-014/RD-015 + RD-014 history 追加(strict-only 校验门义务) + revision_log v1.30/v1.31 | 只追加（status 非冻结字段） | 通过（append-only） |
| `G2.2_HYBRID_DECISION_PACKAGE.md` | 本决策包 + 栈处置 + 合并清单 | review 产物 | — |

**未触碰**（边界遵守）:src/、spec/ 条款体、codegen、evidence/ 既有文件（byte-unchanged）、D-205 pin / toolchain.rs、🔒 禁区子节（仅占位）、P-01 例外规范性裁断（留 owner）。

---

## 3. 未合并栈现状与处置

main = `7277a60`（#95 slice1，RXS-0157 + D-131=A）。未合并栈（基于 slice1）:

```
main(#95 slice1)
 └─ #96 feat/g2.2-pr-c2-slice2-rxs0158   (RXS-0158 阶段→着色器类型)
     └─ #97 feat/g2.2-pr-c2-slice3-rxs0159 (RXS-0159 阶段 I/O 签名,类型面+body stub)
         └─ #98 spike/g2.2-b-graphics-sig-forensic (B 取证 evidence)
             └─ #99 spike/g2.2-a-graphics-sig-effort (A 评估 evidence)
                 └─ (本包) errata/d131-hybrid-graphics-b
```

| PR/分支 | 内容 | 处置建议 | 理由 |
|---|---|---|---|
| #96 slice2 RXS-0158 | 着色阶段→DXIL 着色器**类型**降级（compute/vertex/fragment 已落 passing；mesh/task/RT 映射登记 deferred RD-012） | **可独立合** | 着色器**类型**映射是类型面、**后端无关**（A/B 都产合法 shader type），与图形=B 裁决不冲突;RXS-0158 不触签名（签名在 RXS-0159） |
| #97 slice3 RXS-0159 | 阶段 I/O→签名/系统值语义降级（**A 路类型面 + void body stub**，disasm SKIP） | **HOLD / 由 RFC-0004 PR-D2 按 B 重构后落** | slice3 evidence 已证 **A 路签名产物不可达（elemcount=0）**;RXS-0159 的 A 路实现（`!rurix.dxil.sig.*` 被后端忽略）在混合下应改 B（真达 SV）。**不合一个产物不可达的 A 路签名条款**;RXS-0159 的 SV 名映射类型面可在 B 重构中复用 |
| #98 B 取证 | `dxil_b_graphics_sig_report.md` + JSON + schema + 探针 | **可独立合** | 纯 evidence/spike，是混合裁决依据;byte-unchanged 不可篡改 |
| #99 A 评估 | `dxil_a_graphics_sig_effort_report.md` + JSON + schema + 探针 | **可独立合** | 纯 evidence/spike，是混合裁决依据 |
| 本包 errata | D-131 混合增补 + RFC-0004 + registry | **末位合（依赖证据先落）+ `--admin`** | 规划文档勘误（00 §6.3 独立 PR）;依赖 #96/#98/#99 evidence + slice3 evidence 已在仓库以解析引用 |

> **#97 HOLD 的依据**:任务红线「不合一个产物不可达的条款」。RXS-0159 的 A 路 `.dxil-ll` 含 `!rurix.dxil.sig.*` 元数据但后端产 `elemcount=0`（slice3 round-8 实测）。混合裁定图形=B 后,RXS-0159 应经 RFC-0004 / PR-D2 按 B 路径重构（SPIR-V `BuiltIn`/`Location` → dxc 产真实 ISG1/OSG1）使 SV 真达。其 SV 语义名映射表（类型面，后端无关）可复用。

---

## 4. Owner 合并清单（建议序；命令非交互）

> 命令需 owner 以 ADMIN 身份执行;保留 merge commit（勿 squash）以存栈边界。本包 AI 不代合、不代按（硬规则 1）。

**前置说明**:本 errata 分支当前 git base = #99（栈尖，含 #96~#99 + 全部 RD 编号 RD-001~013 + 证据文件）。若 owner 采纳 #97 HOLD,本 errata 的规划/ registry 变更需在 #97 排除后 reconcile（见步骤 5 注）。

### 步骤 1 — 合 evidence + slice2（可独立）
```
gh pr merge 96 --merge   # slice2 RXS-0158（着色器类型，后端无关）
gh pr merge 98 --merge   # B 取证 evidence
gh pr merge 99 --merge   # A 评估 evidence
```
（注:#98/#99 当前 base 为栈内分支;owner 视情况 retarget→main 或按栈序合。）

### 步骤 2 — slice3 #97 处置
```
# 建议 HOLD:在 PR #97 标 hold/superseded,留待 RFC-0004 批准后 PR-D2 按 B 重构 RXS-0159
gh pr comment 97 --body "HOLD: RXS-0159 A 路签名产物不可达(slice3 elemcount=0);D-131 混合裁定图形=B,RXS-0159 由 RFC-0004/PR-D2 按 B 重构后落。详见 G2.2_HYBRID_DECISION_PACKAGE.md §3。"
```

### 步骤 3 — 合本包（D-131 混合勘误，需 `--admin`）
```
# 13_DECISION_LOG.md 改动触 check_planning_docs 红(errata 设计行为),必须 --admin
gh pr merge <本PR号> --admin --merge --subject "errata(d131): 单选 A 增补为混合 compute=A/图形=B + RFC-0004 草案 + RD-010 close/RD-014/RD-015"
```

### 步骤 4 — 合并后验证
```
git fetch origin main; git log --oneline origin/main -8
py -3 ci/check_schemas.py        # PASS
py -3 ci/trace_matrix.py --check # 159/159 PASS
py -3 ci/check_guardrails.py     # main 上对 13 的勘误已落，base 切换后应 PASS
```

### 步骤 5 — RFC-0004 FCP-lite + 下游
- owner 对 **RFC-0004** 走 FCP-lite（10 §2.2）:批准范围含 §4.4 🔒 P-01 边界声明 + §4.6 🔒 禁区 + §9 全部裁决项（含 Q-Hybrid-RFC:新建 RFC-0004 vs RFC-0003 增补）。
- 批准后启 **PR-D1**（spec 脚手架:RXS-0159 重构说明 + B 新增面预留，不落裸条款头）→ **PR-D2**（RXS-0159 按 B 重构 + RXS-0160 + MIR→SPIR-V + 转译链 + golden + 供应链 pin + 6xxx 错误码）。
- **PR-D2 硬义务（代录 owner 裁断，RFC-0004 §4.4/§4.2 + RD-014 history）**:PR-D2 图形=B 实现**必含强制签名一致性校验门**——译后比对 DXIL ISG1/OSG1 vs MIR 意图签名(用户语义名 / 系统值 / 被使用元素,含声明但未用的输入),任何用户声明/可观察元素未保真即 **6xxx 显式编译错**,strict no-silent、无静默降级/丢弃。校验门是图形=B codegen **不可裁剪**组成(不存在「跳过校验直接产物」的配置)。
- **G-G2-2 真实红绿（含签名校验门）**:除既有 device 真跑 / dxc validator / DXIL golden 三样证据外,**真实红绿须含**『篡改 DXIL 签名(如改名/删用户声明元素)→ 与 MIR 意图不符 → 校验门触发 **6xxx 红** → 复原绿』,run URL 归档(反 YAML-only)。
- **注（reconcile）**:若 #97 HOLD,本 errata 的 13/RFC-0003/RFC-0004 规划变更与 #97 spec 内容无耦合（可净挑）;但 `registry/deferred.json` 的 RD-014/015 编号假定 RD-012/013（来自 #96/#97）已存在,RD-010 close history 追加于 B/A round 之后。若 #97 不入 main,owner 合并本包时需确认 deferred.json 的 entries/ history 顺序在 main 实际状态上 append-only 自洽（RD-013 由 #97 引入;若 #97 hold 则 RD-013 不在 main，RD-014/015 编号需顺延或 #97 的 RD-012/013 单独 cherry）。建议:**#96 含 RD-012、#97 含 RD-013**——若 HOLD #97，可将 RD-013 登记并入 RFC-0004/PR-D1，保编号连续。owner 终裁。
- **注（strict-only 取证栈）**:RFC-0004 §4.4 达标机制引 `evidence/dxil_b_strict_only_report.md`(B strict-only 达标取证,本栈 **PR #101**,随 #100 待 owner 批);该 evidence 与 RD-014 history v1.31 的校验门可行性论证同源。owner 合并时确认 #101 evidence 已在栈内(或先于 #100 落 main),使 RFC-0004 §4.4/§11 的引用解析。

---

## 5. 边界遵守声明（硬规则自检）

- **硬规则 1**:AI 代录 owner 裁决,**不代决 / 不自合 / 不自批**;state-flip（D-131 行、RD-010 close、RFC 状态）以 owner 合并生效。
- **硬规则 5 / P-13**:🔒 §4.6 禁区子节（签名/FFI ABI 布局、纹理内存模型、UB）维持占位不落笔;§4.4 strict-only 达标要求的**核心规范句为代录 owner 已给裁断**(owner 合并 #100 生效),**不自创超裁断的规范内容**,细化处(「用户可观察」精确边界 / 未用输入保留 vs 诊断)留〈待 owner〉占位;未落任何禁区内容。
- **硬规则 6 / 00 §6.3**:规划文档勘误（13）独立 PR、追加式;evidence/ 既有 byte-unchanged。
- **硬规则 7**:spec-first;本包不落 codegen / 不落条款体;下游实现待 owner 批准 RFC-0004 后另起 PR。
- **验证**:check_schemas PASS / check_structure PASS / trace_matrix 159/159 PASS / check_guardrails 仅 13_DECISION_LOG.md 预期红（errata，需 --admin）/ 全文件 LF（CR=0，尾 0x0a）。
