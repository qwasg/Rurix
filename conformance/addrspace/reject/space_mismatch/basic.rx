//@ spec: RXS-0067
//@ expect-error: RX3002
// addrspace negative: a `global` view is passed where a `constant` view is expected.

device fn consume(v: View<constant, f32>) {}

kernel fn k(g: View<global, f32>) {
    consume(g);
}

fn main() {}
