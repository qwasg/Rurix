//@ spec: RXS-0022
// fn pointer types: params, returns, no-arg, as fields.

fn takes_callback(cb: fn(i32) -> i32, hook: fn(), gen: fn(f32, f32) -> (f32, f32)) -> i32 {
    cb(1)
}

struct Dispatch {
    handler: fn(u32) -> u32,
}

type Op = fn(f32) -> f32;
