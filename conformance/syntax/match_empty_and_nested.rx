//@ spec: RXS-0029
// Empty match bodies parse; nested matches in arm bodies.

enum Never {}

fn nested(a: i32, b: i32) -> i32 {
    match a {
        0 => match b {
            0 => 0,
            _ => 1,
        },
        _ => 2,
    }
}

fn empty(n: Never) {
    match n {};
}
