//@ spec: RXS-0255
//@ expect-error: RX6031
// export_c reject: 签名合法，但导出体含数组索引 `a[i]`（数组越界 panic 面）→ RX6031
// （RXS-0255 导出体须无 panic 面 by-construction）。

#[export(c)]
pub fn read_at(i: i32) -> i32 {
    let a = [1, 2, 3, 4];
    a[i]
}
