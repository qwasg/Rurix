//@ spec: RXS-0351
// G9.3 M94 RED 语料（spec/virtual_geometry.md RXS-0351 L2/L3：CLAS 主腿与传统 BLAS
// 回退腿 ray query 逐命中一致〔任意 hit 集合 + 最近 hit 距离容差 0〕；可见集与
// BLAS 内容错开一簇的注入必须判 RED）。
// **转正（G9.3 波语料转正批）**：负例数据面（最小说明性 fixture，未来 CI 消费）——
//   可见集 = {C0, C1, C2}（selection 输出）；当帧拼装 BLAS 输入 = {C0, C2}
//   （C1 被故意漏装——可见集 ↔ BLAS 内容错开一簇）；
//   或拼装输入 = {C0, C1, C2, C3}（C3 不在可见集——多装一簇）。
//   两种错开 variant 均必须被一致性校验判 RED（与 RXS-0352 L3 provenance 校验同一
//   负例轴）。静态帧（可见集 digest 逐帧不变）触发 CLAS/BLAS 构建计数非零的
//   variant 同判 RED。
// 预期 RED 面（实现 PR 接线）：src/rurix-render/src/rt/as_manager.rs ClasBlasKey
//   校验负例 + ci/g9_clas_rt_convergence_smoke.py 门错开一簇臂/静态帧构建计数臂
//   （symbolic key g9.p0.m94.clas_rt_convergence，G9.1 冻结字面不动）。
fn main() {}
