//@ spec: RXS-0350
// G9.3 M93 RED 语料（spec/virtual_geometry.md RXS-0350 L2/L4：屏幕空间误差
// selection cut 逐帧无重叠无空洞——每条根→叶路径恰好一个选中簇，覆盖性机器核验；
// 空洞注入负例 RED 臂独立有效）。
// **转正（G9.3 波语料转正批）**：负例数据面（最小说明性 fixture，未来 CI 消费）——
//   DAG：root R → {A, B}；A → {A0, A1}（parent→child LOD 细化边）。
//   合法 cut（正例对照，见 accept/visible_cluster_set_valid_cut.rx）= {A0, A1, B}。
//   注入空洞 cut = {A0, B}：根→叶路径 R→A→A1 无选中簇（A 未选且 A1 未选）= 空洞；
//   注入重叠 cut = {A, A0, B}：A 与 A0 父子同选 = 重叠。
//   两种 variant 均必须被 selection 覆盖性校验 fail-closed 判 RED，负例臂独立于
//   正例臂成立。
// 预期 RED 面（实现 PR 接线）：src/rurix-render/src/geometry/cull.rs 覆盖性校验
//   负例 + ci/g9_visible_cluster_set_smoke.py 门空洞注入臂（symbolic key
//   g9.p0.m93.visible_cluster_set，G9.1 冻结字面不动）。
fn main() {}
