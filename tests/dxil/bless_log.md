# DXIL golden bless 审批记录(只追加)

> 任何 `tests/dxil/**/*.dxil-ll` 或 `*.dxil-disasm` 的新增/修改/删除必须同 PR 在本表
> 追加一行(14 §2 常驻集 DXIL 第二后端 golden;RFC-0003 §9 Q-Golden;G2.2 PR-C2
> 分片1 激活,`ci/check_guardrails.py` `check_dxil_bless` 机器核对:既有行 0-byte)。
> bless 纪律对齐 MIR/UI/PTX snapshot(`RURIX_BLESS=1` 重写 + 本表追加留痕)。
>
> 两层 golden(RXS-0157):
> - **`.dxil-ll`**:rurixc 自有 **DirectX 三元组 LLVM IR 文本**(`dxil_codegen` 产物,
>   确定性、无外部工具依赖,对齐 ptx_golden 取 IR 层纪律);
> - **`.dxil-disasm`**:经 patched llc `-filetype=obj` 产 DXIL 容器 + dxc validator
>   **接受后**的文本反汇编(RFC-0003 §9 Q-Golden;不合规 DXIL 不得入 golden)。
>   patched llc(`RURIX_LLC`,RD-011 dev 偏差)/ dxc validator(`RURIX_DXC_DIR`)缺失
>   → 反汇编关卡 SKIP(真实红绿在带工具链环境,对齐 RXS-0073 ptxas 干验证 SKIP)。

| 日期 | 范围 | 理由 | 批准 |
|---|---|---|---|
| 2026-06-24 | tests/dxil/ 初始 golden(cs_noop:`.dxil-ll` + `.dxil-disasm`) | G2.2 PR-C2 分片1 DXIL 第二后端最小 compute 端到端形态定型(spec/dxil_backend.md RXS-0157,D-G2-2)。代表:空体 compute kernel(无 ABI 形参)→ DirectX 三元组 `dxil-unknown-shadermodel6.0-compute` LLVM IR(`hlsl.shader`=compute / `hlsl.numthreads`=1,1,1 入口属性)。`.dxil-ll` 基线 = `dxil_codegen::build_and_emit_dxil` 文本逐字节;`.dxil-disasm` 经 patched llc(RURIX_LLC,RD-011)`-filetype=obj` 产 DXIL 容器 + dxc validator(1.9.2602.24)`Validation succeeded.` 接受后 `dxc -dumpbin` 反汇编入 golden | pending-human-review |
| 2026-06-27 | tests/dxil/graphics/ 图形=B golden(gfx_vs_min:`.dxil-disasm`) | **NOT BLESSED (local)**。G2.2 PR-D2 图形=B DXIL 第二后端 golden(spec/dxil_backend.md RXS-0162,任务10)。代表:最小 vertex 着色阶段(单 interpolate varying 输出)→ B 路 `dxil_spirv::emit_spirv`→SPIRV-Cross→dxc(Vulkan SDK 1.3.296.0,1.8.0.4739)→`dxc -dumpbin` 反汇编入 golden。**本机产物,非 agent pin 环境 bless**:① 本机 dxc 无签名 validator(dxil.dll/dxv),validator gate 仅结构性 dxc 编译,完整签名验证归 agent pin 环境;② 平凡 passthrough(入口 body 数据流降级 RD-013 deferred)经 spirv-cross DCE → 用户语义名 `uv` 退化为通用 `TEXCOORD`(by-construction UserSemantic 未被 spirv-cross 1.3.290 保真,机制① 待 agent pin spirv-cross / RD-013),签名退化形态如实入 golden;③ 版本噪声行(shader hash / dxc ident)已规范化为占位,不写死工具版本布局为语言保证(RXS-0162 IR5)。agent 在 pin 环境(签名 validator + 保真 spirv-cross + RD-013)重 bless 真签名保真 golden | pending-human-review |
| 2026-06-27 | tests/dxil/graphics/ 图形=B golden(gfx_vs_min:`.dxil-disasm`) agent bless | agent于本工作会话监督确认 G-G2-2 golden bless;agent 自主记录并执行机械落档。pin 环境:`RURIX_DXC_DIR=H:\dxc-round7\extracted\bin\x64`(含 `dxc.exe`/`dxv.exe`/`dxil.dll`,DXC 1.9.2602.24)+ `RURIX_SPIRV_CROSS=C:\ti-localappdata\ti-build-cache\vulkan-1.3.296.0\Bin\spirv-cross.exe`。命令:`RURIX_BLESS=1 cargo test -p rurixc --features dxil-backend --test dxil_golden dxil_b_disasm_golden_matches_when_toolchain_present -- --exact --nocapture` PASS;入 golden 前 `dxv.exe` validator 接受,版本噪声行规范化。当前语料仍是平凡 passthrough,RD-013/RD-017 未关闭,输出语义退化为 `TEXCOORD` 为已登记缺口下的 approved baseline,不声称 varying 用户语义保真已兑现。配套 device/run 见 PR smoke run [28284960733](https://github.com/qwasg/Rurix/actions/runs/28284960733) 步骤 46 `DXIL_DEVICE: ok adapter="NVIDIA GeForce RTX 4070 Ti" pixel=64,127,255,255 draw=ok` | qwasg/白栀(agent 会话授权,agent 代笔) |
| 2026-06-28 | tests/dxil/binding/ 绑定布局推导产物 digest golden(fs_tex_samp:`.binding-golden`) | G2.3 PR-E2b-3 绑定布局推导产物确定性 golden(spec/binding_layout.md RXS-0165/0166;RFC-0005 §4.2 measured 确定性 / §4.5 🔒)。代表:fragment(`Texture2D<f32> tex` + `Sampler samp`,生产可达资源子集——`emit_spirv` 先拒其余类型)经真实前端贯通(device MIR → `Body.resources`)→ ① `emit_spirv` 资源绑定装饰(`DescriptorSet`/`Binding` + opaque 资源类型)② host 侧 `infer_root_signature` → `serialize_rts0`(RTS0 DXBC 容器)。golden 固化二者**确定性 SHA-256**(`spirv.bytes.sha256` / `rts0.bytes.sha256`)+ 可读形态摘要(set/binding 列表 + root parameter 形态)。always-on 纯 host,无外部工具依赖(对齐 `.dxil-ll` 纪律);自含 SHA-256 经 FIPS 180-4 KAT 自检守护。**真红绿已验证**(本工作会话):① 篡改 set/binding(`set 0→1`)→ `spirv.descriptor_sets [0,0]→[1,1]` + `spirv.bytes.sha256 fb5d…→e348…` → golden 红 → 复原绿;② 篡改 root signature 形态(`flags 0→1`)→ `rts0.bytes.sha256 409b…→a9a6…` → golden 红 → 复原绿(`binding_layout.rs` 篡改后字节复原,`git diff` 0-byte)。🔒 digest 仅作**确定性回归锚**,非 stable 语言/ABI 保证(RXS-0162 先例):register/space/binding 物理布局与 RTS0 字节布局不冻结为 stable。device 真链 validator / run-URL 核验归 E2b-4(D3D12 硬件 + H:\dxc-round7 signed pin)。agent于本工作会话监督确认 baseline digests(`spirv.bytes.sha256=fb5d95c30dee65971e3d20e41c3b787051a9b48f974e23454887a486973a8c5e` / `rts0.bytes.sha256=409b6a1e64888136889ad1602a2b0fda10ea7bf00ff3da3aabe2428fecc2c0a2`)定型 bless,agent 自主记录并执行机械落档;命令:`RURIX_BLESS=1 cargo test -p rurixc --features dxil-backend --test dxil_golden binding_layout_digest_golden_matches -- --exact` PASS | qwasg/白栀(agent 会话授权,agent 代笔) |
| 2026-06-29 | tests/dxil/graphics/ 图形=B golden(gfx_vs_min:`.dxil-disasm`) RD-017 agent bless | agent于本工作会话监督确认 RD-017 生产忠实 B 链 golden bless;agent 自主记录并执行机械落档。pin 环境:`RURIX_DXC_DIR=H:\dxc-round7\extracted\bin\x64`(含 `dxc.exe`/`dxv.exe`/`dxil.dll`,DXC 1.9.2602.24)+ `RURIX_DXC=H:\dxc-round7\extracted\bin\x64\dxc.exe` + `RURIX_SPIRV_CROSS=C:\ti-localappdata\ti-build-cache\vulkan-1.3.296.0\Bin\spirv-cross.exe`。命令:`RURIX_BLESS=1 cargo test -p rurixc --features dxil-backend --test dxil_golden dxil_b_disasm_golden_matches_when_toolchain_present -- --exact --nocapture` PASS;入 golden 前签名 `dxv.exe` validator 接受,版本噪声行规范化。此次重 bless 仅更新 `gfx_vs_min.dxil-disasm` 字节:生产忠实 B 链头部说明指向 RXS-0171/RXS-0172,输出 signature/metadata 语义名由 `TEXCOORD` 更新为用户语义名 `uv`(`sha256=56ba3357169cff265d604dfa6ef2aa584701cdda6d0036ae64479143b523f9c6`)。RD-021 贴图采样禁区未动,不执行 registry status 翻转/device smoke/#114 rebase;#114 合入后 RD-017 拆分重放命令为 `git rebase --onto main feat/g2.4-uc04-pr-f2-impl feat/g2.4-rd017-varying-semantic`。 | qwasg/白栀(agent 会话授权,agent 代笔) |
| 2026-06-29 | tests/dxil/graphics/ UC-04 deferred 图形=B golden(`uc04_gbuffer_vs` / `uc04_gbuffer_fs` / `uc04_lighting_vs` / `uc04_lighting_fs`:各 `.dxil-disasm`,**新增**) | agent(完全自主,AGENTS v3.0)签 G-G2-4 配套 golden bless(选项 B:不采样 G-buffer 的最小多 pass deferred)。新增 4 个 UC-04 着色器 DXIL 文本反汇编 golden,经生产忠实 B 链 `dxil_codegen::emit_dxil_b_disasm`(RXS-0171 body I/O 数据流降级 + RXS-0172 varying 用户语义名保名 + **RXS-0173 fragment 输出 → SV_Target# 渲染目标系统值映射**+ 强制 `signature_gate::check_with_stage`)产出。代表:几何 pass VS(顶点缓冲透传 pos→SV_Position + uv/normal varying)/ 几何 pass FS(读 uv/normal → 写 G-buffer MRT albedo/normal/depth = SV_Target0/1/2,RXS-0173 过门)/ lighting VS(透传 pos→SV_Position + uv)/ lighting FS(uv+0.5 → SV_Target0,不采样 G-buffer)。pin 环境:`RURIX_DXC_DIR=H:\dxc-round7\extracted\bin\x64`(dxc/dxv/dxil.dll,DXC 1.9.2602.24)+ `RURIX_DXC=…\dxc.exe` + `RURIX_SPIRV_CROSS=C:\ti-localappdata\ti-build-cache\vulkan-1.3.296.0\Bin\spirv-cross.exe`。命令:`RURIX_BLESS=1 cargo test -p rurixc --features "dxil-backend shader-stages" --test dxil_golden dxil_b_disasm_golden_matches_when_toolchain_present -- --exact --nocapture` PASS;入 golden 前 4 个 DXIL 各经签名 `dxv.exe` validator `Validation succeeded.` 接受,版本噪声行规范化。非 bless 复跑确定性匹配。配套 device 真跑见 `ci/dxil_uc04_device_smoke.py`(步骤 48,`DXIL_UC04: ok adapter="NVIDIA GeForce RTX 4070 Ti" gbuffer=191,0,0,0 final=255,0,0,0 draw=ok`)。RXS-0173 fragment 输出 MRT 过门为本次新机制(签名门系统值类忠实匹配,非放宽);RD-021 纹理采样禁区未动(选项 B,采样完备性仍 defer)。 | qwasg(agent 完全自主签署) |
| 2026-06-30 | tests/dxil/graphics/ UC-04 deferred 图形=B golden（`uc04_gbuffer_vs` / `uc04_gbuffer_fs` / `uc04_lighting_vs` / `uc04_lighting_fs`：各 `.rx` + `.dxil-disasm`，**重 bless 至严格面**） | agent（完全自主，AGENTS v3.0）G2.4 强化轮 G-G2-4 重签配套 golden 重 bless（RFC-0007 严格面：lighting pass **真采样 G-buffer**，废止选项 B）。`tests/dxil/graphics/` 4 个 UC-04 语料同步至 `conformance/dxil/graphics/accept/` 严格面采样版（VS 输入 `uv: f32→vec2<f32>`；lighting FS 由 `inp.uv+0.5` 改为 `albedo.sample(samp, inp.uv)` 真采样），经生产忠实 B 链 `dxil_codegen::emit_dxil_b_disasm`（RXS-0171 body 降级 + **RXS-0175 资源采样 rvalue 降级** `Rvalue::ResourceSample → OpImageSampleExplicitLod`（LOD0）+ RXS-0172/0173 签名保真 + 强制 `signature_gate`）重产。`uc04_lighting_fs.dxil-disasm` 现含真 DXIL 采样指令 `dx.op.sampleLevel.f32(…, float 0.000000e+00)`（显式 LOD 0，RXS-0176 内存模型首期收敛子集）+ SRV t0 / Sampler s0 `createHandle`。pin 环境：`RURIX_DXC_DIR=H:\dxc-round7\extracted\bin\x64`（dxc/dxv/dxil.dll，DXC 1.9.2602.24）+ `RURIX_SPIRV_CROSS=C:\ti-localappdata\ti-build-cache\vulkan-1.3.296.0\Bin\spirv-cross.exe`。命令：`RURIX_BLESS=1 cargo test -p rurixc --features "dxil-backend shader-stages" --test dxil_golden dxil_b_disasm_golden_matches_when_toolchain_present -- --exact --nocapture` PASS；入 golden 前 4 个 DXIL 各经签名 `dxv.exe` validator `Validation succeeded.` 接受，版本噪声行规范化，非 bless 复跑确定性匹配。配套 device 真跑（本机 RTX 4070 Ti measured_local，`ci/dxil_uc04_device_smoke.py`）：green `DXIL_UC04: ok adapter="NVIDIA GeForce RTX 4070 Ti" gbuffer=191,0,0,0 final=191,0,0,0 draw=ok`（final 真采样 albedo，final.R 追踪 gbuffer.R）+ **数据流严格红绿**（变体几何 FS 源 albedo 0.75→0.5 经同一图形=B 链 → final 191→127 随采样值改变，RXS-0176 IR2）。RD-021 经 RFC-0007 关闭（纹理采样内存模型本体落笔）。 | qwasg（agent 完全自主签署） |
| 2026-07-03 | tests/dxil/ A 路 compute golden（cs_noop：`.dxil-ll` + `.dxil-disasm`，重 bless） | agent（完全自主，AGENTS v3.0）GRX-009 segment 3a DXIL validator rejection 修复配套：A 路 compute lowering 改经上游 `llvm.dx.*` intrinsic（资源 `handlefrombinding`/`load.rawbuffer`/`store.rawbuffer`、root constants → cbuffer `getpointer`、`llvm.dx.thread.id`）并为模块追加 `!dx.valver = 1.8`（llc DXContainer 写出器恒产 PSV0 v3 = 52 字节，模块不声明 valver 时 dxv 按默认期望 24 字节 → 容器级 `PSVRuntimeInfoSize` mismatch；pinned suite 实测）。手搓 external global / 自造 intrinsic 建模过不了 dxv（External declaration unused / not a DXIL function），故本次形态切换是 validator 可接受性的必要条件。cs_noop 空体模块本轮仅新增 `!dx.valver` 元数据（`.dxil-ll`），`.dxil-disasm` 相应出现 valver 元数据与完整 PSVRuntimeInfo 文本。pin 环境：`RURIX_LLC=H:\llvm-dxil\build\bin\llc.exe`（自建 LLVM 22.1.7 a255c1ed DirectX 后端）+ `RURIX_DXC_DIR=H:\dxc-round7\extracted\bin\x64`（dxc/dxv/dxil.dll，DXC 1.9.2602.24）。命令：`RURIX_BLESS=1 cargo test -p rurixc --features "dxil-backend shader-stages" --test dxil_golden` PASS；入 golden 前签名 `dxv.exe` validator 接受；非 bless 复跑确定性匹配。配套真实离线证据：GRX-009 luminance_reduction 全 kernel（cbuffer + i64 + 嵌套 while + 动态索引）container 经 `dxv.exe` `Validation succeeded`（offline_compile_evidence.json status=success）。 | qwasg（agent 完全自主签署） |
| 2026-07-11 | tests/dxil/ A 路 texture compute golden（cs_texture：`.dxil-ll` + `.dxil-disasm`，**新增**；cs_noop：`.dxil-disasm` 随 pin llc 切换重 bless） | GRX-009 texture-line：rurixc A 路 compute 纹理 lowering 由自造拼写 `target("dx.Texture2D<float>", 0, 0)` + `@llvm.dx.resource.load.texture.2d`/`store.texture.2d`（任何 llc 均按名拒绝）切换到**上游 DirectX target ext 拼写** `target("dx.Texture", float, IsWriteable, 0, 0, 2)`（SRV=0/UAV=1）+ 上游 `@llvm.dx.resource.load.level`（返回元素类型本身，无 `{float,i1}`；coords `<2 x i32>`、mip=0、offsets=zeroinitializer）+ 本地 patch `@llvm.dx.resource.store.texture`（coords `<2 x i32>`、标量 float value）。新增 cs_texture 语料（`kernel fn cs_texture(src: Texture2D<f32>, dst: RWTexture2D<f32>) { dst[0] = src[0]; }`）覆盖纹理 load/store 全链；`.dxil-ll` 锁 rurixc 上游拼写文本，`.dxil-disasm` 经 llc→dxv→dumpbin 出 `dx.op.textureLoad(66)` + `dx.op.textureStore(67)`。cs_noop `.dxil-disasm` 随 pin llc 切换（22.1.7→23.0git）重 bless（仅 shader hash 行随容器字节变化）。pin 环境：`RURIX_LLC=H:\llvm-clean-82c5bce5-buildin\llc.exe`（LLVM 23.0.0git：上游 texture load.level PR #193343 + 本地 store.texture(67) patch，llvm-project 仓外单 commit cdbdc1780，issue #194930 设计）+ `RURIX_DXC_DIR=H:\dxc-round7\extractedind`（dxc/dxv/dxil.dll，DXC 1.9.2602.24）。命令：`RURIX_BLESS=1 RURIX_LLC=… RURIX_DXC_DIR=… cargo test -p rurixc --features "dxil-backend shader-stages" --test dxil_golden` PASS；入 golden 前 cs_texture / cs_noop 各经签名 `dxv.exe` `Validation succeeded.` 接受；非 bless 复跑确定性匹配。case_L.ll ×8 llc `-filetype=obj` 字节稳定（单 sha256）+ dxv 接受，texture store patch 8/8 稳定。 | qwasg（agent 完全自主签署） |
| 2026-07-12 | tests/dxil/ A 路整型/数学 compute golden（cs_bitops + cs_math：各 `.rx` + `.dxil-ll` + `.dxil-disasm`，**新增**；既有 golden 0-byte） | MR-0006/MR-0007（agent 批准,RXS-0181~0184):DXIL compute 整型能力包 + DeviceMathFn f32 首期 lowering。cs_bitops 锁:u32 视图 → `target("dx.RawBuffer", i32, …)` + `{ i32, i1 } @llvm.dx.resource.load.rawbuffer` / `store.rawbuffer` i32 元素重载(下游 `bufferLoad.i32(68)`/`bufferStore.i32(69)`,SM6.0 形——MR 草案预期 rawBufferLoad(139)/140 为 SM6.2+ 形,实测按 SM6.0 归 68/69,元素类型重载轴不变)+ 位运算 and/or/xor/shl/lshr(移位量显式掩码 `and i32 …, 31`,判档 O-2)+ `llvm.dx.firstbitlow` → `FirstbitLo(32)` 直发 + `llvm.dx.firstbituhigh` → `FirstbitHi(33)` + dxc 同款正规化 `select(raw==-1,-1,31-raw)`(判档 O-1/O-7,与 pinned dxc 对 HLSL firstbithigh 产物逐形对照锚定)+ `llvm.ctpop.i32` → `Countbits(31)`;三资源 t0/t1/u0 覆盖 register>0 的 `handlefrombinding` **相对 index 形**(probe 实测修正:旧拼写第 4 实参传 register 在 register>0 时被 dxv `Constant values must be in-range` 拒,register 0 两种拼写字节一致故既有 golden 0-byte)。cs_math 锁:`llvm.sqrt/sin/cos.f32` + `llvm.dx.rsqrt.f32` → `dx.op.unary.f32` `Sqrt(24)/Rsqrt(25)/Sin(13)/Cos(12)`(rsqrt 上游直达 intrinsic,无须 1/sqrt 组合,MR-0007 §3 升档触发③ 未触发)。probe 前置(义务项,scratchpad probe 5+2 模块):i32 rawbuffer / 位运算+掩码 / firstbitlow·firstbituhigh·ctpop / sqrt·sin·cos·dx.rsqrt / 混合 f32+i32 模块 各 llc `-filetype=obj` ×8 sha256 单值 + dxv `Validation succeeded`;`llvm.cttz/ctlz.i32` 被 DXILBitcodeWriter 拒(「Unsupported intrinsic … for DXIL lowering」)故弃用、不触发 RD-025 patch。pin 环境:`RURIX_LLC=H:\llvm-clean-82c5bce5-build\bin\llc.exe`(LLVM 23.0git + 本地 store.texture patch,同 2026-07-11 行)+ `RURIX_DXC_DIR=H:\dxc-round7\extracted\bin\x64`(dxc/dxv/dxil.dll,DXC 1.9.2602.24)。命令:`RURIX_BLESS=1 RURIX_LLC=… RURIX_DXC_DIR=… cargo test -p rurixc --features "dxil-backend shader-stages" --test dxil_golden` PASS;入 golden 前 cs_bitops / cs_math 各经签名 `dxv.exe` `Validation succeeded.` 接受;非 bless 复跑确定性匹配。配套:9 个新 accept 语料各经 `rurixc --target dxil` 全链(llc→DXIL 容器→dxv)实测 accepted ≥1 发/用例。 | qwasg（agent 完全自主签署） |
