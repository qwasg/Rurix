# GRX CI 门禁增量

> 所属契约:[GRX_CONTRACT.md](GRX_CONTRACT.md)
> 版本:v1.37(2026-07-13)
> 基线:沿用现有 Rurix host/device 回归网;本文只规定 Godot/Rurix 集成增量门禁。
> 铁律:建设期 SKIP 必须写明原因;close-out strict 不允许 SKIP、estimated 或无证据性能声明。

---

## 1. PR Smoke

| # | 步骤 | 命令 | 失败处理 |
|---|---|---|---|
| GRX-S1 | Rust 格式 | `cargo fmt --check` | 失败即红 |
| GRX-S2 | Rurix Godot ABI 单测 | `cargo test -p rurix-godot` | 失败即红 |
| GRX-S3 | DXIL/shader-stages 回归 | `cargo test -p rurixc --features "dxil-backend shader-stages"` | 失败即红 |
| GRX-S4 | Godot/Rurix bridge smoke | `py -3 ci/godot_rurix_bridge_smoke.py` | 失败即红 |
| GRX-S5 | GRX-009 segment 3a validation_failed regression | `py -3 ci\godot_rurix_toolchain_probe_validation_failed_test.py` | 失败即红 |
| GRX-S6 | GRX-009 DXIL toolchain preflight evidence | `py -3 ci\godot_rurix_toolchain_probe.py` | 失败即红 |
| GRX-S7 | ignored source/state 检查 | `git check-ignore -v --no-index external/godot-master .cursor/settings.json .kiro/state.json .kimi/state.json .trae/state.json .claude/state.json .vscode/settings.json .idea/workspace.xml .windsurf/state.json .zed/settings.json` | 失败即红 |
| GRX-S8 | Godot source 不入 Git | `git status --porcelain -- external/godot-master`; `git ls-files external/godot-master .cursor .kiro .kimi .trae .claude .vscode .idea .windsurf .zed` | 任一输出非空即红 |

`GRX-S5` 是 `ci/godot_rurix_toolchain_probe.py` 的 GRX-009 segment 3a 红绿回归,不属于 bridge-only smoke。该脚本必须使用 temp fixture 与 `RURIX_GRX009_PASS_DIR`,不得改写真正的 `spike/godot-rurix/passes/luminance_reduction/pass_manifest.json` 或 `offline_compile_evidence.json`;它不需要真实 Godot build、SCons、GPU 或 D3D12 runtime。

`GRX-S6` 必须写出 `target/grx/dxil_toolchain_probe.json`,记录 `RURIX_LLC` patched DXIL llc 与由 `RURIX_DXC_DIR` / `RURIX_DXC_NEW_DIR` 指向的 signed DXC validator suite(`dxc.exe`、`dxv.exe`、`dxil.dll`)状态;signed suite 不只是文件存在,也不得只凭 exit code 0 PASS,`dxc.exe` / `dxv.exe` 必须通过稳定 runnable probe 与大小写不敏感 identity marker 检查并记录 probe command、exit code、output、timeout evidence,`dxil.dll` 只记录 `sha256` / `is_file`;所有外部 probe 命令必须带 timeout,timeout 必须写入 evidence 或 missing reason;它只报告缺口与下一条可执行定位命令,不得安装或下载工具链。

## 2. Local Godot Gate

`GRX.0` 只固化文档基线;本节及 `GRX-001` detector / `GRX-002` build / `GRX-003` load smoke 的实际执行都属于 `GRX.1`。本地推荐先跑 wrapper gate 记录稳定命令与产物 evidence:

```powershell
py -3 ci\godot_rurix_scons_build.py
```

裸 SCons 仍是规范目标命令:

```powershell
scons platform=windows target=template_debug d3d12=yes module_rurix_accel_enabled=yes disable_path_overrides=no
```

建设期规则:

- 若 `scons` 或 Python `SCons` 不存在,GRX-001 detector 必须输出明确 SKIP reason。
- 若 detector 未发现可用 SCons launcher,必须输出 `recommended_scons_command: null` 与明确下一步(如 `install_or_enable_scons`),不得给出已知会失败的 `scons ...` 推荐命令。
- 不允许安装或修改全局工具链作为 CI 步骤副作用。
- detector 默认以 workspace-local `LOCALAPPDATA=H:\rurix\target\grx\localappdata` 解析 Godot Windows build deps。
- `build_ready PASS` 必须同时覆盖 Godot 自身配置阶段会检查的 workspace-local AccessKit 与 D3D12 Mesa/NIR deps,不能只看 SCons/MSVC/Windows SDK/DXC。
- 若 AccessKit deps 缺失,detector 必须保持 `build_ready = false`,并给出 workspace-local AccessKit 安装命令;默认口径下不得一边维持 `build_ready = false` 一边推荐默认 SCons build。若未来要支持 `accesskit=no`,必须新增单独 readiness 字段,不能复用 `build_ready`。
- 若 D3D12 Mesa/NIR deps 缺失,detector 必须保持 `build_ready = false`,并给出 workspace-local 安装命令;`d3d12=yes` 目标下不允许建议 `d3d12=no`。
- `dxv.exe` 缺失默认记为 warning/optional tool missing,服务于后续 DXIL/device validation;它不阻塞 `GRX-002` Godot SCons build。
- 仅当 detector 输出 `build_ready PASS` 时才实际运行 `scons platform=windows target=template_debug d3d12=yes module_rurix_accel_enabled=yes disable_path_overrides=no`。
- 裸 SCons 是规范目标;当前本机 MSVC 14.44 因 C1001 ICE,推荐通过 `py -3 ci\godot_rurix_scons_build.py` 固定 wrapper 内部 `num_jobs=1 verbose=yes angle=no silence_msvc=no` workaround 记录 evidence。
- wrapper summary 必须归档 Godot build log,并在 `target/grx/godot_scons_build_summary.json` 中记录 Godot exe、console exe、module lib 的 `path / exists / size_bytes / mtime_utc / sha256`。
- wrapper summary 的 `command` / `ice_workaround_command` 必须包含 `disable_path_overrides=no`,并输出 `required_scons_args_satisfied` 或 `path_overrides_ready` 之类的 readiness 字段。
- probe 的 `build_artifacts_ready` 不得只看文件存在;必须同时要求 summary 中的 required artifacts evidence 完整且 `disable_path_overrides=no` 条件满足。若命令证据缺失,必须输出 `next_action=rebuild_godot_with_path_overrides` 与 `next_command=py -3 ci\godot_rurix_scons_build.py`。
- 若 wrapper 最新一次非零退出,但 required artifacts 与 path-overrides evidence 已齐备,probe 可以把该状态降为 warning,而不是阻塞 `GRX-003` / `GRX-004` 接续。
- 任一关键产物缺失时,即使 SCons exit 0,也不得写 `status: success`。
- `rurix_godot.dll` present/missing 两种启动路径都必须验证;`godot_rurix_load_smoke.py` 生成的项目文件只允许落在 `target/grx/godot-load-smoke`,Godot 启动必须通过 `--path` 指向该目录。
- 若需清理 `external/godot-master/bin` 的旧残留,`project.godot` / `main.gd` 只允许按 marker 删除;`main.tscn` 只允许按 marker 或精确 legacy fingerprint 删除,不得泛删 Godot bin 文件。
- missing DLL case 必须保持 fallback,且若日志出现 `RurixAccel: D3D12 Forward+ bridge session ready.` 必须判失败并在 summary 记录 `unexpected_markers`。

## 3. Benchmark Gate

建设期 normal gate:

- `GRX-004` 已以 fresh per-scene smoke 通过:`target/grx/godot_bench_project_smoke_summary.json` 当前应记录 `scene_count=7`、`failure_count=0`,且 7 个 scene 都有独立 Godot load evidence。
- `GRX-005` 建设期 runner 的目标产物是 raw frame sample JSON 与 `target/grx/godot_bench_runner_summary.json`,而不是 baseline/Rurix 对比结果。
- `GRX-005` runner 必须扫描 Godot 日志 failure marker(对齐 `bench_project_smoke.py`):允许已知 `ERROR: Could not load global script cache.`(含其 `at: ProjectSettings::get_global_class_list` 上下文行)作为 warning,但其它 `ERROR:` / `SCRIPT ERROR:` / `Parser Error:` / `Parse Error:` / `Failed loading resource:` / `Failed loading script` 必须让该 scene fail;`per_scene_results` 必须记录 `failure_markers` / `warnings`,summary 必须给出聚合 `warning_count`。runner 不带 `--verbose`,因此不复用 smoke 的 “缺 Loading resource 载入证据即失败” 规则。
- `GRX-006` 已交付 baseline/perf evidence schema 与 strict perf gate 输入格式:`spike/godot-rurix/bench/schemas/baseline_evidence.schema.json`、`spike/godot-rurix/bench/schemas/perf_gate_input.schema.json`(draft-07);`perf_gate.py` 支持 `--kind {perf_gate,baseline}`、`--strict`、`--validate-only`,并把 `FORMAT FAIL` 与 `PERF FAIL` 分开输出。
- strict perf gate(`--strict`)必须拒绝:任何 SKIP / estimated 标记、`quick_smoke` 或非 full run_mode、缺任一固定 scene、缺 `*_raw_artifact_path`、`warmup/sample != 300/2000`、backend/resolution/vsync/evidence_level 不符。
- `GRX-006` hardening 后的三条强化规则:
  - strict forbidden marker 采用词边界正则(下划线视作词字符),命中文档任意字符串或 key 中独立出现的 `skip` / `skipped` / `estimated`;必须命中 `SKIP: missing`、`skip-reason`、`status=SKIP`、`estimated:true`、`estimated local`,不得误伤 `spike`、`quick_smoke`、普通路径分隔片段。
  - baseline reader(`--kind baseline`)必须校验每个 scene 的 `sample_count` 为正整数,且当顶层 `sample_frames` 为正整数时要求 `sample_count == sample_frames`,与 `baseline_evidence.schema.json` 对齐。
  - strict perf gate 若输入含 `thresholds`,其三项必须等于固定值 `geomean_fps_ratio_min=1.5` / `p95_frame_time_reduction_min=0.3` / `single_scene_fps_ratio_min=0.95`,任一被覆盖为其它值即 FORMAT FAIL。
- GRX-006 红测样例(必须可复现):`samples/perf_gate_forbidden_skip_example.json`(含独立 `SKIP: missing` 标记,`--strict` 必 FORMAT FAIL);`samples/baseline_missing_sample_count_example.json`(缺一个 scene 的 `sample_count`,`--kind baseline --validate-only` 必 FORMAT FAIL)。
- `quick_smoke` 证据(含 baseline schema 的 quick_smoke 文档)只能作为 smoke evidence,不能作为 strict perf gate 输入。
- benchmark runner 可 SKIP,但必须说明缺少哪一项:Godot build、scene project、D3D12 runtime、GPU timestamp、visual capture、baseline JSON。
- `gpu_timestamps_available=false` 在 `GRX-005` 建设期可接受,但必须显式写入 raw JSON;禁止伪造 GPU timestamp 或把占位值写成真实测量值。
- quick/full 模式都只能声明采样 runner 已完成并给出帧数、路径与 raw artifacts,不能宣称 perf gate、baseline 对比或任何性能提升。
- 任何已有 evidence JSON 必须可被 schema/reader 解析。
- 不允许把 estimated 写入 close-out 输入。

Close-out strict gate(`GRX-006+ / close-out`,本次不触发):

```powershell
py -3 spike/godot-rurix/bench/perf_gate.py --strict <results.json>
```

`perf_gate.py` 输入格式见 `spike/godot-rurix/bench/schemas/perf_gate_input.schema.json`;strict close-out 必须用 `--strict` 且提供 full-mode measured_local 输入。必须同时满足:

| 指标 | 阈值 | 字段名 |
|---|---:|---|
| 几何平均 FPS ratio | >= 1.5 | `geomean_fps_ratio_min` |
| p95 frame time reduction | >= 0.30 | `p95_frame_time_reduction_min` |
| 单场景 FPS ratio 下限 | >= 0.95 | `single_scene_fps_ratio_min` |

Close-out 证据必须包含:

- scene-level baseline FPS / Rurix FPS。
- scene-level baseline p95 ms / Rurix p95 ms。
- raw sample path 或 run artifact path。
- `measured_local` evidence level。
- 同画质、同分辨率、同 Godot scene、同 D3D12 Forward+ 后端说明。

## 4. Visual And Fallback Gate

每个新增 pass 必须过:

| 门 | 要求 |
|---|---|
| visual reference | reference frame capture 与 Rurix frame capture 均存在 |
| diff policy | LDR pass 用 per-channel absolute diff; HDR/temporal pass 用 SSIM/PSNR + temporal stability |
| fallback telemetry | compile_failed、validation_failed、unsupported_device、visual_diff_failed、manual_disabled 至少覆盖实际失败路径 |
| pass matrix | pass 默认 enabled/disabled、适用场景、失败原因可查询 |
| red/green | 至少一种真实红绿:pass 输出错误、禁用 fallback、视觉 diff 超阈值 |

任一 visual 或 validation 失败时,该 pass 默认 disabled,Godot 原 pass 接管。

`GRX-007` scaffold 建设期规则:

- visual scaffold 产物:`spike/godot-rurix/bench/capture_reference_frames.py`、`visual_diff.py`、`schemas/visual_diff_evidence.schema.json`、`samples/visual_diff_placeholder.json`。
- schema 覆盖 7 个固定 scene,每 scene 至少一个 capture frame;`status ∈ {pass, skip}`。
- LDR 支持 per-channel absolute diff:仅当 `reference_frame_path` 与 `candidate_frame_path` 指向真实存在的帧文件时才计算逐通道 max/mean 绝对差;否则输出 SKIP。
- HDR/temporal 只在 schema 中声明字段(`ssim`/`psnr`/`temporal_stability` 等),值保持 null,建设期不产出结果、不伪造。
- 建设期允许 SKIP,但每个 SKIP 的 capture frame 必须写明具体缺失原因之一:`missing capture backend`、`missing Godot full run`、`missing frame artifact`。
- 除非真的有 reference 与 candidate frame 文件以及计算出的 diff,否则不得写“视觉验证已通过”;`visual_diff.py` 在无真实帧时打印 `SCAFFOLD ... visual verification is NOT done`。
- `visual_diff.py --validate-only` 只校验格式(7 scene、每 scene ≥1 frame、`status=skip` 必须有非空 `skip_reason`、`status=pass` 必须有 reference+candidate path 与 `ldr_diff`),格式通过(含全 SKIP)退出码 0,格式错误退出码 1。
- `GRX-007` hardening:`status=skip` 的 capture frame 必须为纯 skip,不得携带伪造 diff/帧路径——`reference_frame_path`、`candidate_frame_path`、`ldr_diff`、`hdr_diff`、`temporal_diff` 必须为 null 或缺省;任一非 null 即 FORMAT FAIL(`visual_diff.py` 与 schema `else` 分支双侧约束)。红测样例:`samples/visual_diff_skip_with_fake_ldr_example.json`(skip 带伪造 `ldr_diff`,`--validate-only` 必 FORMAT FAIL)。已有红绿保持:`samples/visual_diff_pass_missing_ldr_example.json`(pass 缺 `ldr_diff` FORMAT FAIL)、`samples/visual_diff_mismatch_example.json`(pass 真实帧但记录 diff 不一致 DIFF FAIL)、`samples/visual_diff_ldr_pass_example.json`(pass 记录 diff 与计算一致 PASS);`--write-output` 生成带 computed `ldr_diff` 的 evidence JSON。
- `GRX-007` 收尾 hardening:`status=pass` 的 capture frame 承诺存在可比对的真实帧对,若其 `reference_frame_path`/`candidate_frame_path` 帧文件在磁盘上缺失、不可读、不是合法 channel 文档、或两帧 channel 数量不一致,`visual_diff.py` 必须 `DIFF FAIL` 且非零退出,不得降级为 SKIP;`--write-output` 模式在任一 `status=pass` 帧算不出 diff 时必须失败且不写出 evidence。红测样例:`samples/visual_diff_pass_missing_frame_artifact_example.json`(pass 帧带合法 `ldr_diff` 但帧文件不存在,运行 `visual_diff.py` 必 DIFF FAIL / 非零退出)。`ci/godot_rurix_toolchain_probe.py` 的 `grx007_visual_ready` 已把该红测纳入红绿证据。

`GRX-008` scaffold/hardening 建设期规则:

- fallback telemetry scaffold 产物:`spike/godot-rurix/bench/schemas/fallback_telemetry.schema.json`、`fallback_telemetry.py`、`samples/fallback_telemetry_placeholder.json`。`fallback_reason` 枚举五值:`compile_failed`、`validation_failed`、`unsupported_device`、`visual_diff_failed`、`manual_disabled`。
- `fallback_telemetry.py` 与 schema 区分 scaffold 与 full/measured_local:
  - scaffold(`run_mode=scaffold` / `evidence_level=scaffold`)允许 `telemetry_timestamp=null`、`telemetry_frame=null`,但每个 pass 必须 `enable_state=disabled` 且 `godot_fallback_active=true`。
  - full(`run_mode=full` 或 `evidence_level=measured_local`)要求 `telemetry_timestamp` 非空、`telemetry_frame` 为非负整数;null 即 FORMAT FAIL。
  - `evidence_level=measured_local` 不允许 `pass_id` 以 `placeholder_` 开头(`fallback_telemetry.py` 与 `fallback_telemetry.schema.json` 双侧约束,schema-only 校验不再比脚本松)。
- `GRX-008` 红测样例(必须可复现):`samples/fallback_telemetry_full_null_timestamp_example.json`(full/measured_local 但 timestamp/frame 为 null,`--validate-only` 必 FORMAT FAIL);`samples/fallback_telemetry_scaffold_fallback_inactive_example.json`(scaffold 但 `godot_fallback_active=false`,`--validate-only` 必 FORMAT FAIL)。`samples/fallback_telemetry_placeholder.json` 仍 FORMAT PASS,且明确它不是实际 fallback telemetry(不代表任何 pass 已接入或发生真实 fallback)。

`GRX-009` luminance reduction pass 准备期 + gated implementation 分段规则(pass 本体仍未完成):

- 准备产物:`spike/godot-rurix/passes/luminance_reduction/PASS_CONTRACT.md` 与 `pass_manifest.json`。二者仅记录 `pass_id=luminance_reduction`、目标场景(`post_fx_chain`、`mixed_forward_plus`)、Godot 侧候选 hook/source 调查(路径 + 函数,不改 `external/godot-master`)、输入/输出资源占位、dispatch 形态占位、fallback reason(对齐 GRX-008 五枚举)、visual/perf evidence 要求;pass 默认 `disabled`,任何 compile/validation/visual/perf 失败走 Godot 原生 luminance 路径。
- `ci/godot_rurix_toolchain_probe.py` 的 `grx009_prep_ready` 只负责准备产物与 Godot manifest path 证据判定:两个准备产物存在、`pass_manifest.json` 可解析且 `pass_id==luminance_reduction`、`implemented==false`、`default_enable_state==disabled`、`target_scenes` 含 `post_fx_chain` 与 `mixed_forward_plus`;manifest `godot_hook_investigation` 记录的 effect_class header/source、全部 shaders、全部 call_sites file 必须为相对路径且确实存在于 `external/godot-master` 之下(resolve 后不得逃逸快照根;检查只读,不修改快照),任一缺失即 not ready。未就绪时 `next_action=start_grx009_luminance_reduction_pass_contract`;准备就绪后仅表示可以进入实现阶段,**不代表任何实际 pass 已实现或任何性能/视觉结论已达成**。
- `grx009_segment1_ready` 代表第一段 gated scaffold 的历史交付点:manifest `implementation_status.segment == 1`、`real_gpu_pass == false`、`godot_core_call_site_wired == false`,且 0002 patch、`samples/fallback_telemetry_luminance_disabled_example.json`、`src/rurix-godot` 中的 `LuminanceReductionGate` 关键标记齐备,并且 disabled sample 必须能通过 `fallback_telemetry.py --validate-only`。当 manifest 已推进到 segment 2 或更后段时,该布尔值可能为 `false`;这表示当前状态已越过 segment 1,**不**表示回归。命中时 `next_action=start_grx009_luminance_core_callsite_fallback_wiring`。
- gated implementation 第一段(gated scaffold)规则:`src/rurix-godot` 的 `LuminanceReductionGate` 默认 disabled,`request_enable` 恒失败 `compile_failed`(无已编译 Rurix DXIL luminance kernel),`rxgd_record_pass` 对 `RXGD_PASS_LUMINANCE_REDUCTION` 恒返回 `RXGD_STATUS_FALLBACK` 且不得累加 estimated GPU/CPU time(C ABI v1 不变);红绿由 `cargo test -p rurix-godot` 承担(default-disabled 回退 + enable 失败)。Godot 侧栈式 `0002-rurix-accel-luminance-pass-gate.patch` 基于 0001、仅改 `modules/rurix_accel/*`;per-pass 设置 `rendering/rurix_accel/passes/luminance_reduction/enabled` 默认 false,`try_record_luminance_reduction()` 在设置关闭、session/符号缺失或 bridge 返回非 `RXGD_STATUS_OK` 时必须返回 false(调用方走 Godot 原生 luminance 路径)。
- `grx009_segment2_ready` 代表第二段 core call-site fallback wiring 已交付:manifest `implementation_status.segment == 2`、`real_gpu_pass == false`、`godot_core_call_site_wired == true`,且 0002/0003 patch、`samples/fallback_telemetry_luminance_callsite_wired_disabled_example.json` 与 `LuminanceReductionGate` 关键标记齐备。这里不允许只靠 patch 文本或 JSON 可解析来判 ready:callsite-wired sample 必须真能通过 `fallback_telemetry.py --validate-only`,同时 patch stack 必须通过共享四态检查并真实落在 `0001+0002+0003`。命中后 `next_action=start_grx009_luminance_reduction_real_gpu_pass`。
- gated implementation 第二段(core call-site fallback wiring)规则:Godot core 修改只能通过 `spike/godot-rurix/patches/0003-rurix-accel-luminance-core-callsite-wiring.patch` 管理,不得把 `external/godot-master` 纳入 Git。0003 只在 Auto Exposure `luminance_reduction` 调用点前加入 opt-in gate:只有 module 设置开启、bridge session/record symbol 可用且 `rxgd_record_pass` 返回 `RXGD_STATUS_OK` 时才跳过 Godot 原生 luminance;否则必须执行原来的 `luminance_reduction`。当前 bridge 对 `RXGD_PASS_LUMINANCE_REDUCTION` 恒返回 `RXGD_STATUS_FALLBACK`,所以 segment 2 实测语义仍是 Godot 原生路径接管。
- `ci/godot_rurix_bridge_smoke.py` 必须校验 patch 栈四态(base 未应用 / 仅 0001 / 0001+0002 / 0001+0002+0003),任何 drift 即红;并校验 0002 与 0003 的关键标记(setting key、`RXGD_PASS_LUMINANCE_REDUCTION`、`rxgd_record_pass`、`try_record_luminance_reduction`、`D3D12Hooks::get_singleton`、`renderer_scene_render_rd.cpp`)。当 patch 栈真实落在 `0001+0002+0003` 时,smoke 还必须对 `0004-rurix-accel-luminance-resource-mapping-scaffold.patch` 执行 `git apply --check`,确认 segment 3b scaffold patch 可应用;0004 不可应用即红,不得只靠 patch 文本 marker 放行。此外还必须在临时 scratch 副本中(已应用 0004)对 `0005-rurix-accel-luminance-runtime-binding-preflight.patch` 做 stacked `git apply --check`,并在已应用 0004+0005 的 scratch 副本中对 `0006-rurix-accel-luminance-gated-dispatch-bringup.patch` 做 stacked `git apply --check`;0005 或 0006 不可叠加即红,且这些检查只在临时 scratch 副本进行,绝不污染 `external/godot-master`。
- GRX-009 disabled/fallback wiring 状态记录样例:`samples/fallback_telemetry_luminance_disabled_example.json` 与 `samples/fallback_telemetry_luminance_callsite_wired_disabled_example.json` 均为 scaffold 级,`--validate-only` 必 FORMAT PASS;它们**不是** measured telemetry,不得当作真实 fallback 事件或接入证据。
- `grx009_segment3a_compile_ready` 代表离线 compile artifact 已真正齐备:manifest 与 `offline_compile_evidence.json` 必须一致记录 `status=success`,且 latest compile attempt 真实产生并可追溯的 `DXIL container + root signature + descriptor layout` current artifacts 三者齐备,同时 `runtime_state` 仍为 `fallback_only`。`.dxil` 若是 LLVM IR 文本、`artifact_kind=dxil_ir_text`、`semantic_status=entry_shell_only`,或 stderr 记录 patched llc / validator SKIP,只能作为 debug/non-ready evidence,一律不得 ready。若离线脚本真实运行后得到 `compile_failed`、`toolchain_missing` 或 `validation_failed`,这只能算 blocker evidence complete,不得把 manifest 推进到 segment 3,也不得把 blocker 写成 ready;`next_action` 应继续指向修复真实 DXIL container/body lowering/validator blocker。`validation_failed` evidence 必须保留 validator rejection 的 raw stdout/stderr/exit code,并在 `offline_compile_evidence.json.commands[]` 中可追溯 validator 子命令;不得只写泛化 `dxc validator rejected emitted DXIL container`。current artifact 路径必须是 tracked evidence artifact;`.dxil.ll` 等中间输入若保留,必须纳入 `debug_artifacts` 或明确清理,不得裸 untracked。`py -3 ci\godot_rurix_toolchain_probe_validation_failed_test.py` 是 `validation_failed` 分支的正式红绿回归,必须通过 temp fixture 与 `RURIX_GRX009_PASS_DIR` 构造 evidence,不得改写真正的 luminance `pass_manifest.json` 或 `offline_compile_evidence.json`。当前真实状态是 GRX-009 segment 4i canonical 离线 compile `compile_failed`:canonical `offline_compile_evidence.json` 记录 `status=compile_failed`、`blocker_category=dxil_container_missing`、`runtime_mappable=false`、`attempted_binding_kinds=[texture2d,rwtexture2d]`(segment 4i texture-capable kernel 源 `src/lib_texture.rx` 已就位但 patched llc 不支持 `llvm.dx.resource.load.texture.2d` intrinsic);canonical artifacts 路径携带 raw-buffer 字节复制自 `artifacts/raw_buffer_historical/`;historical raw-buffer segment 3a success 保留在 `offline_compile_evidence_raw_buffer.json`(`status=success`,based on `src/lib.rx` raw-buffer kernel);manifest 顶层 `offline_compile_status=compile_failed`,evidence 仍 `runtime_state=fallback_only`、`real_gpu_pass=false`;current first blocker 是 `kernel_binding_kind_mismatch`(bridge tracked package 仍为 raw-buffer,Godot runtime 提供 Texture2D 资源),`math_pyramid_parity_not_proven` 仅 future-only;不得据此宣称 runtime pass、visual/perf 或 measured telemetry 已完成。
- `grx009_segment3b_resource_mapping_ready` 代表 resource mapping scaffold 已完成,不是 runtime ready:必须存在 `spike/godot-rurix/passes/luminance_reduction/resource_mapping.md`,manifest 必须保持 `implemented=false`,`default_enable_state=disabled`,`implementation_status.segment=3`,`segment_detail=3b_resource_mapping_scaffold`,`real_gpu_pass=false`,descriptor layout 必须记录 root constants / `b0`、SRV `src_luminance=t0`、UAV `dst_luminance=u0`、`requires_64bit_integer_shader_capability=true`,patch `0004-rurix-accel-luminance-resource-mapping-scaffold.patch` 必须存在并只承载 module/bridge 级 mapping scaffold marker,且必须在当前 `external/godot-master` 已处于 `0001+0002+0003` 的前提下通过 `git apply --check`;bridge 必须仍对 `RXGD_PASS_LUMINANCE_REDUCTION` fallback 且不得累计 estimated GPU/CPU time。任一资源缺失、cap 不支持、ABI mismatch、descriptor 不一致或 0004 patch 不可应用都必须让 ready=false;当 resource mapping 证据齐全但 0004 不可应用时,probe 的 `next_action` 必须指向修复 0004 patch artifact。该 gate 只允许 next_action 进入未来 gated runtime binding slice,不得宣称 real GPU pass、visual diff、measured telemetry 或性能提升。
- `grx009_segment4a_runtime_binding_preflight_ready` 只表示 runtime binding preflight/fallback-only ready,不是 D3D12 dispatch 或 real GPU pass ready:必须同时满足 `grx009_segment4a_runtime_binding_preflight_inputs_ready=true` 与 `grx009_patch_0005_applyable=true`,manifest/evidence 必须保持 `implemented=false`,`default_enable_state=disabled`,`runtime_state=fallback_only`,`real_gpu_pass=false`。`0005-rurix-accel-luminance-runtime-binding-preflight.patch` 的 stacked applyability 必须通过 scratch copy 验证:在已应用 0001+0002+0003+0004 的 scratch 状态下检查 0005,不得把检查副作用写入或污染 `external/godot-master`。segment 4a 即使 preflight 成功,bridge `rxgd_record_pass` 对 `RXGD_PASS_LUMINANCE_REDUCTION` 仍必须返回 `RXGD_STATUS_FALLBACK`,不得录制 D3D12 dispatch,不得把 logical RID/resource preflight 写成 native GPU handle 或 runtime pass 完成。ready 后 `next_action` 只能指向 `start_grx009_luminance_segment4b_gated_dispatch_bringup`,并且该入口不代表 segment 4b 已开始。
- `grx009_segment4b_gated_dispatch_bringup_ready` 只表示"显式 opt-in、失败即 fallback"的 gated dispatch bring-up 接线就绪,**不是** real D3D12 dispatch 或 real GPU pass ready:必须同时满足 `grx009_segment4b_gated_dispatch_bringup_inputs_ready=true` 与 `grx009_patch_0006_applyable=true`;manifest/evidence 必须保持 `implemented=false`、`default_enable_state=disabled`、`runtime_state=fallback_only`、`real_gpu_pass=false`、`real_d3d12_dispatch_recorded=false`,`implementation_status.segment=4`、`segment_detail=4b_gated_dispatch_bringup`、`segment_4a_runtime_binding_preflight=true`、`segment_4b_gated_dispatch_bringup=true`、`godot_core_call_site_wired=true`。
- `0006-rurix-accel-luminance-gated-dispatch-bringup.patch` 的 stacked applyability 必须在已应用 0001+0002+0003+0004+0005 的临时 scratch 副本中通过 `git apply --check`,不得把检查副作用写入或污染 `external/godot-master`;bridge smoke 与 `ci/godot_rurix_patch_stack.py` 均纳入该检查(`grx009_patch_0006_applyable`)。0006 只做 gated dispatch bring-up 接线:新增默认 `false` 的 `rendering/rurix_accel/passes/luminance_reduction/dispatch_bringup` opt-in、声明保留能力 flag `RXGD_CAP_LUMINANCE_DISPATCH_BRINGUP (1u << 1)`(承载在既有 `RxGdCaps.flags`,不改 ABI struct layout、`RXGD_ABI_VERSION` 保持 1、header/patch/smoke 同步),并仅在 opt-in 开启时于 `try_create_session` 置入该 flag;Godot 原生 luminance path 必须保留。
- bridge dispatch bring-up gate(`src/rurix-godot/src/lib.rs`,C ABI v1 不变):`rxgd_record_pass` 对 `RXGD_PASS_LUMINANCE_REDUCTION` 走 `record_gated_dispatch_bringup`——先跑完整 segment 4a preflight,再跑 dispatch eligibility(opt-in flag `RXGD_CAP_LUMINANCE_DISPATCH_BRINGUP`、64-bit integer 能力、native D3D12 device/queue handle 非空、resource `native_handle` 非空、compiled package 可用且 descriptor layout 与离线 evidence 的 DXIL/root signature/descriptor layout SHA-256 匹配)。任一失败返回 `RXGD_STATUS_FALLBACK` 并记录清晰 fallback reason(`manual_disabled`/`unsupported_device`/`validation_failed`/`compile_failed`),不累计 estimated GPU/CPU time;**即便全部 eligible,且 segment 4c 已有 standalone measured D3D12 dispatch smoke,explicit dispatch gate 仍关闭:因为没有 bridge-linked runtime dispatch recording path、没有 measured bridge telemetry,且不能让 `rxgd_record_pass` 返回 OK,仍返回 `RXGD_STATUS_FALLBACK`**,`enabled` 保持 false,不录制 dispatch。红绿由 `cargo test -p rurix-godot` 承担,至少覆盖:disabled fallback(opt-in flag 未置)、missing native device/queue/resource handle fallback、layout/hash mismatch fallback、valid eligibility 仍受 explicit dispatch gate 控制;单元测试里的 fake/null handle 一律不得返回 `RXGD_STATUS_OK`,ready 不得只靠脚本存在。
- `grx009_real_d3d12_dispatch_smoke_ready`(segment 4c standalone smoke gate)只在 `real_d3d12_dispatch_smoke.json` 记录 `status=success`、`pass_id=luminance_reduction`、`segment=4c`、`runtime_state=fallback_only`、`real_gpu_pass=false`,且其 artifact SHA-256 同时与 on-disk artifacts 和 segment 3a offline compile evidence 一致、descriptor/root-signature/PSO/dispatch/fence/readback 各 check 均为 true 时才 ready;缺失 / SKIP / FAIL / hash 漂移一律不 ready。ready 后 probe `next_action=start_grx009_bridge_real_d3d12_dispatch_recording`。segment 4c smoke success 只是 standalone measured evidence:它**不**让 bridge 录制 dispatch、**不**让 `rxgd_record_pass` 返回 OK、**不**启用 Godot luminance Rurix path,manifest/evidence 保持 `runtime_state=fallback_only`、`real_gpu_pass=false`、`real_d3d12_dispatch_recorded=false`,不得写成 real GPU pass、visual diff、measured telemetry 或性能提升;除非真实 bridge dispatch recording slice 明确产出 measured evidence,否则不得累计 estimated GPU/CPU time。若运行环境无真实 D3D12 device harness,该 smoke 必须显式 SKIP 并保留缺失原因,SKIP 不推进 readiness。
- `grx009_bridge_real_d3d12_dispatch_recording_ready`(segment 4d bridge recording smoke gate)只在 `grx009_real_d3d12_dispatch_smoke_ready=true`(4c 已 ready)**且** `bridge_dispatch_recording_evidence.json` 记录 `status=success`、`pass_id=luminance_reduction`、`segment=4d`、`runtime_state=fallback_only`、`real_gpu_pass=false`、`bridge_recorded_d3d12_dispatch=true`、`godot_runtime_luminance_path_enabled=false`、`default_enable_state=disabled`、`gpu_timestamp_status=not_yet`,其 artifact SHA-256 同时与 on-disk artifacts 和 segment 3a offline compile evidence 一致,`checks`(含 `recording_shim_linked`、`rxgd_record_pass_returned_ok`、`bridge_recorded_one_pass`、`no_fallback_passes`、`gpu_time_ns_zero`、`fence_completed`、`dst_uav_readback`)全绿,bridge frame stats `recorded_passes=1`/`fallback_passes=0`/`gpu_time_ns=0`/`last_error=0`,且 `bridge.shim_available=1` 时才 ready;缺失 / SKIP / FAIL / hash 漂移一律不 ready。ready 后 probe `next_action=start_grx009_godot_native_resource_handle_mapping`;bridge smoke 缺失 / SKIP / FAIL 时保持 `start_grx009_bridge_real_d3d12_dispatch_recording`,不推进。segment 4d 的 bridge 录制路径**默认关闭**:仅在 `cargo build/test --features d3d12-recording-shim` 编入,且仅由 harness-only flag `RXGD_CAP_LUMINANCE_DISPATCH_RECORD (1u << 2)`(承载在既有 `RxGdCaps.flags`,不改 ABI struct layout、`RXGD_ABI_VERSION` 保持 1)武装;Godot module 从不设置该 flag。默认 `cargo test -p rurix-godot` 不需要 Windows SDK/D3D12 link 且 `rxgd_record_pass` 对 `RXGD_PASS_LUMINANCE_REDUCTION` 保持 `RXGD_STATUS_FALLBACK`。`cargo test -p rurix-godot --features d3d12-recording-shim` 必须绿(shim 经 `cc` 编译 + 链接 `d3d12`/`dxgi`;含 SHA-256 自检与内嵌 artifact 摘要匹配单测)。fake/null handle 一律不得返回 OK;shim 只对**内存副本**签名 DXIL,tracked 磁盘 artifact 字节不变。segment 4d bridge smoke success 只是 measured bridge evidence:它**不**启用 Godot luminance Rurix path、**不**完成 Godot runtime pass、**不**把 `real_gpu_pass`/`real_d3d12_dispatch_recorded` 改成 true(除非 Godot runtime path、visual diff、telemetry gate 同时完成),不得写成 visual diff、measured fallback telemetry、GPU timestamp 或性能提升。若运行环境无真实 D3D12 device / 无签名 DXC / 无 MSVC,该 smoke 必须显式 SKIP 并保留缺失原因,SKIP 不推进 readiness。
- segment 4d evidence artifact hygiene(historical run artifact):`bridge_dispatch_recording_evidence.json` 是**某一次历史 measured run** 的产物。smoke 在 feature build 成功后必须记录当次 feature-built DLL 指纹 `dll_fingerprint`(`dll_path_at_run`/`dll_sha256`/`dll_size_bytes`/`dll_mtime_utc`/`build_profile=debug`/`features=["d3d12-recording-shim"]`),并把该 DLL 复制到 `target/grx009_bridge_recording_smoke/rurix_godot_d3d12_recording_shim.dll`(记 `snapshot_dll_path`/`snapshot_dll_sha256`;二进制在 `target/` 下,gitignored,不纳入 Git)。`target/debug/rurix_godot.dll` 是 mutable artifact,后续 feature-off `cargo build -p rurix-godot` 会原地覆盖它。因此 `grx009_bridge_real_d3d12_dispatch_recording_ready` **不**把当前 `target/debug/rurix_godot.dll` hash 纳入 gate,当前 DLL 被覆盖时 4d 仍 ready=true(historical measured evidence);probe 输出 `grx009_bridge_recording_evidence_dll_sha256`、`grx009_bridge_recording_current_dll_sha256`、`grx009_bridge_recording_current_dll_matches_evidence`,不匹配时追加 warning(非 failure),提示重跑 `ci/grx009_luminance_bridge_recording_smoke.py` 可刷新当前 artifact 指纹并精确复现当次 feature-built DLL。
- probe predecessor gate 采用 historical/cumulative 语义:当 manifest 已推进到 segment 4b(且 4c smoke 已 success、4d bridge smoke 已 success)而对应 evidence 仍完整时,`grx009_segment3a_compile_ready`、`grx009_segment3b_resource_mapping_ready`、`grx009_segment4a_runtime_binding_preflight_ready`、`grx009_real_d3d12_dispatch_smoke_ready` 必须仍为 `true`,不得因后段推进而回落 false;这些 gate 依据 segment 序号下限 + 累积里程碑 flag(`segment_3b_resource_mapping_scaffold` / `segment_4a_runtime_binding_preflight`)判定,artifact hash、DXIL container、patch applyability、`runtime_state=fallback_only`、`real_gpu_pass=false` 检查一律未削弱。
- `grx009_segment4e_native_resource_handle_mapping_ready`(segment 4e native D3D12 resource handle mapping gate)只在 `grx009_bridge_real_d3d12_dispatch_recording_ready=true`(4d 已 ready)**且** `grx009_segment4e_native_resource_handle_mapping_inputs_ready=true` **且** `grx009_patch_0007_applyable=true` 时才 ready。inputs 要求 manifest 仍 `implemented=false`、`default_enable_state=disabled`、`segment=4`、`runtime_state=fallback_only`、`real_gpu_pass=false`、`real_d3d12_dispatch_recorded=false`、`godot_runtime_luminance_path_enabled=false`,且 `segment_4e_native_resource_handle_mapping=true`,并校验 0007 patch 的关键 marker(`native resource handle mapping`、`RenderingDevice::get_driver_resource`、`DRIVER_RESOURCE_TEXTURE`、`p_source_native_handle`/`p_dest_native_handle`、`ID3D12Resource*`、`rb->get_internal_texture()`、`luminance_buffers->reduce[0]`、`native Godot luminance path`、`RXGD_STATUS_FALLBACK`、`does not set RXGD_CAP_LUMINANCE_DISPATCH_RECORD`)。`0007-rurix-accel-luminance-native-resource-handle-mapping.patch` 的 stacked applyability 必须在已应用 0001+0002+0003+0004+0005+0006 的临时 scratch 副本中通过 `git apply --check`,不得污染 `external/godot-master`;`ci/godot_rurix_patch_stack.py` 与 bridge smoke 均纳入该检查。0007 只把 Godot runtime luminance pass 传给 bridge 的资源从 logical RID id 改成真实 `ID3D12Resource*` native handle:`renderer_scene_render_rd.cpp` 经 `RenderingDevice::get_driver_resource(DRIVER_RESOURCE_TEXTURE, RID, 0)` 解析 source(`rb->get_internal_texture()`)/dest(`luminance_buffers->reduce[0]`),native handle 为 0 或 `RenderingDevice` 不可用时必须 fallback 到 Godot 原生 luminance path。ready 后 probe `next_action=start_grx009_godot_runtime_bridge_dispatch_recording_smoke`;0007 缺失/不可叠加时 `next_action=fix_grx009_luminance_segment4e_patch_0007_applyability`。segment 4e 只完成 native handle mapping + preflight/evidence:`RXGD_ABI_VERSION` 不变、Godot module 仍不设置 `RXGD_CAP_LUMINANCE_DISPATCH_RECORD`、shipping/feature-off bridge 对 `RXGD_PASS_LUMINANCE_REDUCTION` 仍返回 `RXGD_STATUS_FALLBACK`,不启用默认 Rurix luminance runtime pass、不完成 Godot runtime pass,不得写成 real GPU pass、Godot-runtime-driven D3D12 dispatch、visual diff、measured telemetry、GPU timestamp 或性能提升。
- segment 4f evidence hygiene(latest vs historical success)：smoke 追踪**两个** tracked evidence——`godot_runtime_bridge_recording_evidence.json` 是 **latest 证据**,每次运行改写,未设 `RURIX_GRX009_SEGMENT4F_GODOT_EXE`(scratch Godot exe env var)时诚实记 `status=skip`,**本身不推进** readiness;`godot_runtime_bridge_recording_success_evidence.json` 是 **historical measured success 证据**,**仅**在严格 `status=success` 运行时写入/更新(记录 Godot exe fingerprint、0001..0008 patch stack identity、feature-built DLL fingerprint、artifact hashes、`godot_exit_code_zero=true`、marker `recorded=1`,并注明 scratch build 二进制不入 Git),之后的 SKIP/FAIL 运行**绝不**删除或覆盖它。
- `grx009_segment4f_godot_runtime_bridge_recording_ready`(segment 4f Godot-runtime bridge dispatch recording smoke gate)只在 `grx009_segment4e_native_resource_handle_mapping_ready=true`(4e 已 ready)**且** `grx009_segment4f_godot_runtime_bridge_recording_inputs_ready=true`(含 `grx009_patch_0008_applyable=true` 与 0008 关键 marker)**且 historical success 证据** `godot_runtime_bridge_recording_success_evidence.json`(**不是** reproducible-default SKIP 的 latest 文件)记 `status=success`、`pass_id=luminance_reduction`、`segment=4f`、`runtime_state=fallback_only`、`real_gpu_pass=false`、`real_d3d12_dispatch_recorded=false`、`godot_runtime_bridge_recorded_dispatch=true`、`godot_runtime_luminance_path_enabled=false`、`default_enable_state=disabled`、`gpu_timestamp_status=not_yet`,artifact SHA-256 同时与 on-disk artifacts 和 segment 3a offline compile evidence 一致,`checks`(`artifact_hashes_match_offline_evidence`、`descriptor_layout_matches_resource_mapping`、`recording_shim_linked`、`godot_runtime_session_ready`、`godot_runtime_call_site_recorded`、`recorded_one_pass`、`godot_exit_code_zero`)全绿,且 `recording.recorded=1` 时才 ready;historical success 缺失 / SKIP / FAIL / hash 漂移 / 被篡改一律不 ready。probe 同时报告 latest smoke status(`grx009_segment4f_godot_runtime_bridge_recording_latest_status`,可为 reproducible-default SKIP)与 historical success readiness(`..._success_status`);latest 回落为 SKIP 时,只要曾录得一次 measured success,readiness 不回退。ready 后 probe `next_action=start_grx009_luminance_real_visual_diff_and_measured_fallback_telemetry`;historical success 缺失(含 latest 为 SKIP / FAIL、从未录得 success)时保持 `start_grx009_godot_runtime_bridge_dispatch_recording_smoke`,不推进;若 4f inputs 齐备但 0008 不可叠加则 `next_action=fix_grx009_luminance_segment4f_patch_0008_applyability`。
- `0008-rurix-accel-luminance-godot-runtime-bridge-recording-smoke.patch` 的 stacked applyability 必须在已应用 0001+0002+0003+0004+0005+0006+0007 的临时 scratch 副本中通过 `git apply --check`,不得污染 `external/godot-master`;`ci/godot_rurix_patch_stack.py` 与 bridge smoke 均纳入该检查(`grx009_patch_0008_applyable`)。0008 只做 harness-only Godot-runtime 录制 arm 接线:新增默认 `false` 的 `rendering/rurix_accel/passes/luminance_reduction/dispatch_recording_smoke` opt-in、在 module 声明 harness-only flag `RXGD_CAP_LUMINANCE_DISPATCH_RECORD (1u << 2)`(承载在既有 `RxGdCaps.flags`,不改 ABI struct layout、`RXGD_ABI_VERSION` 保持 1、header/patch/smoke 同步),仅在 opt-in 开启时于 `try_create_session` 置入该 flag,并在 `try_record_luminance_reduction` 仅当 `rxgd_record_pass` 返回 `RXGD_STATUS_OK` 时打印 `RXGD_GODOT_RUNTIME_LUMINANCE_RECORD` marker。默认(未开 opt-in)Godot module 仍不设置该 flag,shipping/feature-off bridge 与默认 Godot config 仍对 `RXGD_PASS_LUMINANCE_REDUCTION` 返回 `RXGD_STATUS_FALLBACK`。
- segment 4f smoke(`ci/grx009_godot_runtime_bridge_recording_smoke.py`)只在真实 D3D12 device、MSVC/SCons、signed DXC、以及**用完整 0001..0008 patch 栈重建的 Godot scratch build**(`RURIX_GRX009_SEGMENT4F_GODOT_EXE`,不得复用只带 0001+0002+0003 的 tracked `external/godot-master` build)全部可用时才运行;任一缺失必须写明确 SKIP reason 并保留在 evidence,不推进 readiness。它先校验 artifact/offline evidence hash 与 descriptor layout(不一致即 FAIL),再 build `d3d12-recording-shim` DLL、生成开启 `dispatch_recording_smoke` opt-in 的最小 Godot 项目、运行 Godot 并扫描 `RXGD_GODOT_RUNTIME_LUMINANCE_RECORD` marker。marker 出现且 `recorded=1` 但 Godot 进程 `exit_code != 0` 一律记 hard FAIL,绝不 success。smoke 总是改写 latest 证据 `godot_runtime_bridge_recording_evidence.json`;**仅**在严格 success 时把同一份 success 文档另写入 historical 证据 `godot_runtime_bridge_recording_success_evidence.json`(SKIP/FAIL 运行不触碰该 historical 文件)。segment 4f success 只是 test-only / harness-only Godot-runtime measured evidence:`RXGD_ABI_VERSION` 不变,不启用默认 Rurix luminance runtime pass、不完成 Godot runtime pass,不得写成 real GPU pass、default Godot-runtime-driven D3D12 dispatch、visual diff、measured telemetry、GPU timestamp 或性能提升。
- `grx009_segment4g_visual_fallback_ready`(segment 4g real visual diff + measured fallback telemetry gate)只在 segment 4f 已 ready **且** historical success 证据 `visual_fallback_success_evidence.json` 通过 `grx009_segment4g_visual_fallback_issue` 全量审计时才 ready:frame artifacts 磁盘复核 + in-process LDR diff 重算(pinned `max_abs<=2`/`mean_abs<=0.25`)、fallback matrix 双腿一致性(candidate 腿必须实测 patch-0002 marker、reference 腿必须没有)、GRX-008 measured_local telemetry 复核。**segment 4h preflight hardening(2026-07-06)加严**:success 证据必须携带 `runtime_log_audit`(reference/candidate 双腿全量 stdout+stderr 审计:只容忍带 rationale 的 `Could not load global script cache`,其余 `ERROR:` 行与未 tracked 的 `RXGD_DIAG` 一律 FAIL,probe 复审 recorded stdout)、`visual.capture_frame_index` 为实测帧号且 telemetry 条目 `enable_state=enabled`、`fallback_reason=validation_failed`、`godot_fallback_active=true`、`telemetry_frame == visual.capture_frame_index`(stale frame / 错 reason / 错 state / 意外 ERROR 均有 red 回归覆盖)。
- `grx009_segment4h_real_pass_enablement_ready`(segment 4h gated real-pass enablement gate)只在 segment 4g 已 ready **且** historical success 证据 `real_pass_enablement_success_evidence.json` 通过 `grx009_segment4h_real_pass_enablement_issue` 全量审计(real dispatch 真正执行且完成的 `RXGD_GODOT_RUNTIME_LUMINANCE_REAL_PASS` marker line 入证、三腿 matrix 一致、frames 磁盘复核 + diff 重算、forced-failure red leg 实测 `unsupported_device`、0001..0009 patch-stack identity 与 scratch 溯源、runtime log audit、telemetry 复核、DLL 必须 shipping feature-off、无性能宣称)时才 ready。**用 tracked segment 3a artifact 此 success 按设计不可达**:bridge real-pass attempt(`RXGD_CAP_LUMINANCE_REAL_PASS`,由默认 false 的 `.../dispatch_real_pass` opt-in 武装,patch 0009)在 preflight/eligibility 之后跑 kernel-binding-kind conformance check(tracked kernel = raw-buffer views,Godot runtime = Texture2D 句柄)并恒 fail closed,打印机读 `RXGD_REAL_PASS_BLOCKED first_missing_prerequisite=kernel_binding_kind_mismatch` 诊断。latest 证据 `real_pass_enablement_evidence.json` 区分 `skip_kind=environment`(前置缺失,`RURIX_REQUIRE_REAL=1` 升级 FAIL)与 `skip_kind=measured_prerequisite_blocked`(真机全腿实测、如实记录 first missing prerequisite,不升级、不推进);完整性违规一律 FAIL。`0009-rurix-accel-luminance-real-pass-optin.patch` 的 stacked applyability 必须在已应用 0004..0008 的 scratch 副本中通过 `git apply --check`(`grx009_patch_0009_applyable`)。4g ready 且 4h 未 ready 时 `next_action` 按 latest 证据指向第一缺失前置(缺 0001..0009 scratch exe → `start_grx009_segment4h_real_pass_enablement_smoke`;measured blocked → `provide_grx009_runtime_mappable_luminance_kernel_artifact`;fail → `fix_grx009_segment4h_real_pass_enablement_failure`)。segment 4h 是 bring-up gate,不是 default enablement:pass 默认 `disabled`、`runtime_state=fallback_only`、`real_gpu_pass=false`,不得写成 real GPU pass、visual 成功、GPU timestamp 或性能提升。

## 5. ABI Gate

`RXGD_ABI_VERSION` 是稳定边界。以下任一变化必须同 PR 更新 Rust、header、smoke 和 Godot patch:

- exported function signature。
- `RxGdCaps`、`RxGdResource`、`RxGdFrameStats` layout。
- status code、backend/render method 常量、pass id 常量。
- resource ownership 或 lifetime 约定。

验证命令:

```powershell
cargo test -p rurix-godot
py -3 ci/godot_rurix_bridge_smoke.py
cargo build -p rurix-godot
```

Godot 侧不得静默接受 ABI mismatch;必须 fallback 并记录 telemetry。

## 6. Red/Green 程序

每个 pass PR 至少执行一条:

1. **pass 输出错误**:注入错误输出或资源映射错误 -> visual/perf/validation gate 红 -> 复原绿。
2. **禁用 fallback**:强制 pass 返回 fallback -> Godot 原 pass 接管且 telemetry 记录 -> 复原绿。
3. **视觉 diff 超阈值**:调低阈值或篡改 capture -> diff gate 红 -> 复原绿。

close-out 前必须汇总 red/green evidence URL 或本地 evidence 路径。

## 7. 当前验证状态

截至本文修订时,`GRX.0` 文档任务本身不要求 Godot benchmark,也不能据此宣称任何性能提升。`GRX-001/002/003` 已按 fresh evidence 在本地通过并分别产出 detector/build/load evidence;`GRX-004` 已以 fresh per-scene smoke 通过,`GRX-005` runner 已存在并硬化,`GRX-006` 已交付并硬化 baseline/perf schema,`GRX-007` 已完成 visual scaffold + hardening,`GRX-008` 已完成 fallback telemetry scaffold + hardening。GRX-009 第二段 core call-site fallback wiring 已完成,`segment 3a` historical raw-buffer offline compile 已达 success(保留在 `offline_compile_evidence_raw_buffer.json`,based on `src/lib.rx` raw-buffer kernel,latest compile attempt 产出 `artifact_kind=dxil_container` 的 DXIL container、root signature 与 descriptor layout,签名 `dxv.exe` 接受该 container,compute lowering 改经 `llvm.dx.*` resource/thread-id intrinsic + `!dx.valver` 后过验证);canonical `offline_compile_evidence.json` 当前 `status=compile_failed`(`blocker_category=dxil_container_missing`、`runtime_mappable=false`、`attempted_binding_kinds=[texture2d,rwtexture2d]`):segment 4i texture-capable kernel 源 `src/lib_texture.rx` 已就位但 patched llc 不支持 `llvm.dx.resource.load.texture.2d` intrinsic,canonical artifacts 路径携带 raw-buffer 字节复制自 `artifacts/raw_buffer_historical/`,manifest 顶层 `offline_compile_status=compile_failed`,evidence 仍 `runtime_state=fallback_only`、`real_gpu_pass=false`;current first blocker 是 `kernel_binding_kind_mismatch`(bridge tracked package 仍为 raw-buffer),`math_pyramid_parity_not_proven` 仅 future-only。`segment 3b` resource mapping scaffold 已记录 Godot luminance source/reduce/current 资源流、source dimensions、min/max/exposure 参数、descriptor `b0/t0/u0` 映射与 64-bit integer shader capability gate。`segment 4a` runtime binding preflight 已交付并保持。`segment 4b` gated dispatch bring-up 已 ready,`segment 4c` standalone real D3D12 dispatch smoke 已 success:`ci/grx009_luminance_d3d12_dispatch_smoke.py` 在真实 D3D12 adapter(NVIDIA GeForce RTX 4070 Ti)上完成一次最小 compute dispatch —— tracked luminance DXIL container + RTS0 root signature + descriptor layout 被接受(RTS0 直建 root signature、DXIL 建 compute PSO、SRV t0 / UAV u0 / b0 root constants 绑定、dispatch=1,1,1、fence_completed_value=1、dst UAV readback checksum=0x4b95f515),evidence `real_d3d12_dispatch_smoke.json` 记录 `status=success`、`runtime_state=fallback_only`、`real_gpu_pass=false`,artifact SHA-256 与 segment 3a offline evidence 一致。`segment 4d` bridge real D3D12 dispatch recording smoke 已 success:`ci/grx009_luminance_bridge_recording_smoke.py` 在真实 D3D12 adapter(NVIDIA GeForce RTX 4070 Ti)上经 bridge C ABI 录制一次最小 luminance compute dispatch —— `rurix_godot.dll` 以默认关闭的 `d3d12-recording-shim` feature 编译后,`rxgd_record_pass(RXGD_PASS_LUMINANCE_REDUCTION)` 返回 `RXGD_STATUS_OK`(`recorded_passes=1`、`fallback_passes=0`、`gpu_time_ns=0`、`dispatch=1,1,1`、`fence=1`、`checksum=0x4b95f515`、`dxil_signed=yes`),evidence `bridge_dispatch_recording_evidence.json` 记录 `status=success`、`bridge_recorded_d3d12_dispatch=true`、`godot_runtime_luminance_path_enabled=false`、`default_enable_state=disabled`、`gpu_timestamp_status=not_yet`、`runtime_state=fallback_only`、`real_gpu_pass=false`,artifact SHA-256 与 segment 3a offline evidence 一致。`segment 4e` native D3D12 resource handle mapping 已 ready:栈式 patch `0007-rurix-accel-luminance-native-resource-handle-mapping.patch` 把 Godot runtime luminance pass 传给 bridge 的资源从 logical RID id 改成真实 `ID3D12Resource*` native handle,`grx009_patch_0007_applyable=true`、`grx009_segment4e_native_resource_handle_mapping_ready=true`。`segment 4f` Godot-runtime bridge dispatch recording smoke 已接线:新增栈式 patch `0008-rurix-accel-luminance-godot-runtime-bridge-recording-smoke.patch`(叠在 0001..0007 之后,scratch copy 校验,不污染 `external/godot-master`)、默认关闭的 `rendering/rurix_accel/passes/luminance_reduction/dispatch_recording_smoke` opt-in、module 侧 harness-only flag `RXGD_CAP_LUMINANCE_DISPATCH_RECORD (1u << 2)`(承载在既有 `RxGdCaps.flags`,`RXGD_ABI_VERSION` 保持 1)与 `RXGD_GODOT_RUNTIME_LUMINANCE_RECORD` marker,以及 harness `ci/grx009_godot_runtime_bridge_recording_smoke.py` 与**两个** tracked evidence:latest 证据 `godot_runtime_bridge_recording_evidence.json`(每次运行改写、未设 `RURIX_GRX009_SEGMENT4F_GODOT_EXE` 时诚实记 `status=skip`、本身不推进 readiness)与 historical measured success 证据 `godot_runtime_bridge_recording_success_evidence.json`(仅在严格 success 时写入、之后 SKIP/FAIL 绝不覆盖,readiness gate 只看此文件)。probe 当前输出 `grx009_patch_0006_applyable=true`、`grx009_segment4b_gated_dispatch_bringup_ready=true`、`grx009_real_d3d12_dispatch_smoke_ready=true`、`grx009_bridge_real_d3d12_dispatch_recording_ready=true`、`grx009_patch_0007_applyable=true`、`grx009_segment4e_native_resource_handle_mapping_ready=true`、`grx009_patch_0008_applyable=true`、`grx009_segment4f_godot_runtime_bridge_recording_inputs_ready=true`,并把 `next_action` 指向 `start_grx009_godot_runtime_bridge_dispatch_recording_smoke`;predecessor gate 采用 historical 语义,`grx009_segment3a_compile_ready` / `grx009_segment3b_resource_mapping_ready` / `grx009_segment4a_runtime_binding_preflight_ready` / `grx009_real_d3d12_dispatch_smoke_ready` 仍为 `true`。0006/0007/0008 stacked applyability gate 均已纳入,必须在已应用其各自前置栈的 scratch copy 中通过检查,不得污染 `external/godot-master`。当前本机运行环境无用完整 0001..0008 栈重建的 Godot scratch build(`RURIX_GRX009_SEGMENT4F_GODOT_EXE` 未设置),因此 latest 证据 `godot_runtime_bridge_recording_evidence.json` 诚实记 `status=skip`;由于尚无 historical measured success 证据 `godot_runtime_bridge_recording_success_evidence.json`,`grx009_segment4f_godot_runtime_bridge_recording_ready=false`,readiness 不推进,`next_action` 保持 `start_grx009_godot_runtime_bridge_dispatch_recording_smoke`。一旦以 `RURIX_GRX009_SEGMENT4F_GODOT_EXE` 指向 full-stack scratch exe 跑出一次严格 success,historical success 证据即写入并推进 readiness;之后 unset env var 重跑,latest 回落 SKIP 而 historical readiness 保持 true。segment 4c smoke success 只是 standalone measured smoke evidence;segment 4d bridge smoke success 是 measured bridge evidence(经 test-only feature + harness-only record-arm flag 录制),segment 4e 只完成 native handle mapping preflight,segment 4f 目前是 SKIP 接线态:四者均**不**启用 Godot luminance Rurix path、**不**完成 Godot runtime pass:shipping(feature-off)bridge 与默认 Godot config 对 `RXGD_PASS_LUMINANCE_REDUCTION` 仍返回 `RXGD_STATUS_FALLBACK`,`real_d3d12_dispatch_recorded`(默认 Godot runtime 口径)仍 false,本次只完成 segment 4f harness/patch/evidence 接线,不宣称真实 visual diff、measured telemetry、FPS、p95 或性能提升。`validation_failed` 分支仍由 `py -3 ci\godot_rurix_toolchain_probe_validation_failed_test.py` 作为 temp fixture 红绿回归纳入门禁,该回归不得改写真正 evidence。**当前默认语义仍是 disabled/fallback:bridge 对 `RXGD_PASS_LUMINANCE_REDUCTION` 即使 preflight 与 dispatch eligibility 全部通过也仍返回 `RXGD_STATUS_FALLBACK`,runtime 仍为 `fallback_only`,`real_gpu_pass=false`,`real_d3d12_dispatch_recorded=false`,所以实测仍由 Godot 原生 luminance 路径接管。baseline full 实测对比、真实 visual diff、真实 fallback 接入(引擎内实测)、bridge-recorded D3D12 dispatch、real GPU luminance pass、任何实际加速 pass 与性能提升声明仍属未完成;visual/telemetry evidence 仍不是 measured_local close-out 证据。** 当前已知可用验证包括:

- `cargo fmt --check`
- `cargo test -p rurix-godot`
- `cargo test -p rurix-godot --features d3d12-recording-shim`(segment 4d bridge 录制 shim 编译 + 单测绿;含 SHA-256 自检与内嵌 artifact 摘要匹配)
- `cargo build -p rurix-godot`
- `$env:RURIX_DXC_DIR='H:\dxc-round7\extracted\bin\x64'; py -3 ci\grx009_luminance_bridge_recording_smoke.py`(segment 4d bridge real D3D12 dispatch recording smoke;真实 adapter 上 `status=success`,无 adapter/签名 DXC/MSVC 时 SKIP)
- `py -3 ci/godot_rurix_bridge_smoke.py`
- `py -3 ci/godot_rurix_scons_build.py`
- `py -3 ci/godot_rurix_load_smoke.py`
- `cargo test -p rurixc --features "dxil-backend shader-stages"`
- `py -3 spike\godot-rurix\bench\generate_benchmark_project.py`
- `py -3 spike\godot-rurix\bench\bench_project_smoke.py`
- `py -3 spike\godot-rurix\bench\run_benchmark_scenes.py --quick-smoke`
- `py -3 spike\godot-rurix\bench\perf_gate.py --kind baseline --validate-only spike\godot-rurix\bench\samples\baseline_smoke_example.json`(格式校验 PASS)
- `py -3 spike\godot-rurix\bench\perf_gate.py --strict spike\godot-rurix\bench\samples\perf_gate_failing_example.json`(格式可解析,性能门 PERF FAIL,属预期红)
- `py -3 spike\godot-rurix\bench\perf_gate.py --strict spike\godot-rurix\bench\samples\perf_gate_forbidden_skip_example.json`(含 `SKIP: missing`,FORMAT FAIL,预期红)
- `py -3 spike\godot-rurix\bench\perf_gate.py --kind baseline --validate-only spike\godot-rurix\bench\samples\baseline_missing_sample_count_example.json`(缺 `sample_count`,FORMAT FAIL,预期红)
- `py -3 spike\godot-rurix\bench\visual_diff.py --validate-only spike\godot-rurix\bench\samples\visual_diff_placeholder.json`(格式 PASS,7 场景全部 SKIP)
- `py -3 spike\godot-rurix\bench\visual_diff.py --validate-only spike\godot-rurix\bench\samples\visual_diff_skip_with_fake_ldr_example.json`(skip 带伪造 ldr_diff,FORMAT FAIL,预期红)
- `py -3 spike\godot-rurix\bench\visual_diff.py --validate-only spike\godot-rurix\bench\samples\visual_diff_pass_missing_ldr_example.json`(pass 缺 ldr_diff,FORMAT FAIL,预期红)
- `py -3 spike\godot-rurix\bench\visual_diff.py spike\godot-rurix\bench\samples\visual_diff_mismatch_example.json`(pass 真实帧但记录 diff 不一致,DIFF FAIL,预期红)
- `py -3 spike\godot-rurix\bench\visual_diff.py spike\godot-rurix\bench\samples\visual_diff_ldr_pass_example.json`(pass 记录 diff 与计算一致,PASS)
- `py -3 spike\godot-rurix\bench\fallback_telemetry.py --validate-only spike\godot-rurix\bench\samples\fallback_telemetry_placeholder.json`(scaffold placeholder,FORMAT PASS)
- `py -3 spike\godot-rurix\bench\fallback_telemetry.py --validate-only spike\godot-rurix\bench\samples\fallback_telemetry_full_null_timestamp_example.json`(full/measured_local 但 timestamp/frame 为 null,FORMAT FAIL,预期红)
- `py -3 spike\godot-rurix\bench\fallback_telemetry.py --validate-only spike\godot-rurix\bench\samples\fallback_telemetry_scaffold_fallback_inactive_example.json`(scaffold 但 `godot_fallback_active=false`,FORMAT FAIL,预期红)
- `py -3 spike\godot-rurix\bench\visual_diff.py spike\godot-rurix\bench\samples\visual_diff_pass_missing_frame_artifact_example.json`(pass 帧指向不存在的帧文件,DIFF FAIL,预期红)
- `py -3 spike\godot-rurix\bench\fallback_telemetry.py --validate-only spike\godot-rurix\bench\samples\fallback_telemetry_luminance_disabled_example.json`(GRX-009 disabled/fallback wiring 状态记录,FORMAT PASS;非 measured telemetry)
- `py -3 spike\godot-rurix\bench\fallback_telemetry.py --validate-only spike\godot-rurix\bench\samples\fallback_telemetry_luminance_callsite_wired_disabled_example.json`(GRX-009 segment 2 callsite-wired disabled/fallback 状态记录,FORMAT PASS;非 measured telemetry)
- `py -3 ci\godot_rurix_toolchain_probe_validation_failed_test.py`(GRX-009 segment 3a `validation_failed` temp fixture红绿回归,并覆盖 DXC texture descriptor/RTS0 cross-check red/green;不得改写真正 offline evidence)
- `py -3 ci\grx009_dxc_texture_descriptor_rts0_crosscheck_smoke.py`(GRX-009 segment 4k descriptor ↔ RTS0 cross-check;重新由 Rurix binding layout serialize RTS0 并要求与 `artifacts/dxc_texture_bridge/root_signature.rts0.bin` byte-for-byte match;保持 `runtime_mappable=false`、`real_gpu_pass=false`、canonical raw-buffer artifact 不变)
- `py -3 ci\godot_rurix_toolchain_probe.py`(GRX-009 segment 4c standalone real D3D12 dispatch smoke ready;输出 `grx009_patch_0006_applyable=true`、`grx009_segment4b_gated_dispatch_bringup_ready=true`、`grx009_real_d3d12_dispatch_smoke_ready=true`,historical predecessor `grx009_segment3a_compile_ready`/`grx009_segment3b_resource_mapping_ready`/`grx009_segment4a_runtime_binding_preflight_ready` 仍为 true,下一步为 `start_grx009_bridge_real_d3d12_dispatch_recording`;smoke success 只是 standalone measured evidence,非 bridge-recorded D3D12 dispatch、real GPU pass、measured telemetry 或性能证据)
- `$env:RURIX_DXC_DIR='H:\dxc-round7\extracted\bin\x64'; py -3 ci\grx009_luminance_d3d12_dispatch_smoke.py`(GRX-009 segment 4c real D3D12 dispatch smoke;真实 D3D12 adapter 记录 `status=success` 到 `real_d3d12_dispatch_smoke.json`,保持 `runtime_state=fallback_only`、`real_gpu_pass=false`;无真实 device harness 时显式 SKIP;非 bridge-recorded dispatch、real GPU pass、visual diff、measured telemetry 或性能证据)
- `py -3 ci\godot_rurix_patch_stack.py`(GRX-009 patch 栈状态与 0004 forward / 0005+0006+0007+0008 stacked applyability;0008 在已应用 0004+0005+0006+0007 的 scratch copy 中通过 `git apply --check`,不污染 `external/godot-master`)
- `$env:RURIX_DXC_DIR='H:\dxc-round7\extracted\bin\x64'; py -3 ci\grx009_godot_runtime_bridge_recording_smoke.py`(GRX-009 segment 4f Godot-runtime bridge dispatch recording smoke;须真实 D3D12 device、MSVC、signed DXC 且 `RURIX_GRX009_SEGMENT4F_GODOT_EXE` 指向用完整 0001..0008 栈重建的 Godot scratch build 才 `status=success`;缺任一前置写明确 SKIP,不推进 readiness;当前本机记 `status=skip`)

**状态更新(2026-07-08,GRX-009 stage A5)**:segment 4h gated real-pass enablement 已取得 strict MEASURED success——`real_pass_enablement_success_evidence.json` 记录 opt-in real dispatch(`enabled`+`dispatch_bringup`+`dispatch_real_pass`,均默认 false)在 0001..0010 scratch Godot(NVIDIA GeForce RTX 4070 Ti)上真正执行且完成(`RXGD_GODOT_RUNTIME_LUMINANCE_REAL_PASS` marker 入证),LDR visual gate 保持绿(max_abs=0),`forced_capability_downgrade` 红腿实测 `unsupported_device`,measured_local telemetry 通过 GRX-008 校验,0001..0010 patch-stack/溯源/日志审计全绿。canonical package 为 owner-approved HLSL bridge texture package(`texture_artifact_provenance_policy.json`);patch 0010 result writeback 仍是 scaffold(native continuation 重渲染全部级别,无净收益)。manifest 如实翻转为 `status=stage_a5_real_pass_measured_success_default_disabled`、`implemented=true`、`real_gpu_pass=true`(opt-in 实测口径;默认路径仍 fallback-only)、`runtime_state=fallback_only_by_default_real_pass_optin_measured`;probe 的 manifest 检查 fail-closed 放宽:仅当 `real_pass_enablement_success_evidence.json` 存在且通过 `grx009_segment4h_real_pass_enablement_issue` 全量审计时才接受上述新值(`grx009_real_pass_measured_success_active`),否则(含 placeholder/被篡改 success 文档)仍要求旧值 `implemented=false`/`runtime_state=fallback_only`/`real_gpu_pass=false`。owner 决策 `real_pass_default_enable_decision.json`/`.md` 记 `keep_default_disabled`(无 per-pass FPS 证据、writeback scaffold、math parity level-0-only),probe 新增 `grx009_real_pass_default_enable_decision_ready` 校验 gate(复用 provenance policy 模式:owner 签署、决策文档段落、`performance_claim=none`、manifest 引用一致);4h + 决策双 ready 后 `next_action` 由 `design_grx009_luminance_real_pass_default_enable_decision` 推进到 `start_grx010_tonemap_pass_contract`。`default_enable_state` 保持 `disabled`,默认 Godot config 下 bridge 仍返回 `RXGD_STATUS_FALLBACK`;上文 §7 大段中 `real_gpu_pass=false`/4f skip 的描述为历史快照,已被本段取代。无 FPS、p95、GPU timestamp 或任何性能提升宣称。新增验证命令:`py -3 ci\grx009_texture_artifact_provenance_policy_smoke.py`(stage A5 下仍必须绿:4h success 存在且审计通过时不再触发 `real_pass_enablement_success_evidence_must_not_exist`)。

**状态更新(2026-07-11，GRX-010 close-out)**:GRX-010 tonemap 已 close-out(stage-A5 对等)。tonemap gated real-pass enablement 取得 strict MEASURED success——`real_pass_enablement_success_evidence.json` 记录 opt-in real dispatch(`enabled`+`dispatch_real_pass`，均默认 false)在 `0001..0013` scratch Godot(Windows D3D12 Forward+)上真正执行且完成(`RXGD_GODOT_RUNTIME_TONEMAP_REAL_PASS` + patch 0013 writeback scaffold marker 入证),22 checks 全绿含 `forced_capability_downgrade` 红腿实测 `unsupported_device`,LDR visual gate `max_abs=0`/`mean_abs=0`,measured_local telemetry 通过 GRX-008 校验,`0001..0013` patch-stack/溯源/日志审计全绿。栈式 patch `0012`(runtime 资源绑定,`RenderingDevice::get_driver_resource` 真实 `ID3D12Resource*`)+`0013`(默认 false `dispatch_real_pass`/`dispatch_recording_smoke`/`real_pass_force_capability_downgrade` opt-in + `RXGD_CAP_TONEMAP_REAL_PASS`(1u<<4) + `d3d12-recording-shim` real dispatch path)入栈,`ci/godot_rurix_patch_stack.py`/`ci/godot_rurix_bridge_smoke.py` 纳入 0012/0013 stacked applyability。manifest 顶层如实翻转(`status=grx010_real_pass_measured_success_default_disabled`、`implemented=true`、`real_gpu_pass=true` opt-in 实测口径、`real_d3d12_dispatch_recorded=true`、`runtime_state=fallback_only_by_default_real_pass_optin_measured`、`default_enable_state` 保持 `disabled`),新增 `implementation_status.real_pass_measured_success` block;probe 的 manifest 检查 fail-closed 放宽(`grx010_manifest_*_ok` 系列,仅 `grx010_real_pass_measured_success_active`——即 strict success 存在且全量审计通过——时接受新值,placeholder/篡改 success 文档报 `grx010_real_pass_success_evidence_conflict` 并回落旧值)。owner 决策 `real_pass_default_enable_decision.json`/`.md` 记 `keep_default_disabled`(无 per-pass FPS 证据、仅 `TONEMAPPER_LINEAR`+sRGB 子集、patch 0013 writeback scaffold + raster-vs-compute output seam 未设计),probe 新增 `grx010_real_pass_enablement_ready`/`grx010_real_pass_default_enable_decision_ready`/`grx010_patch_0012_applyable`/`grx010_patch_0013_applyable` gate;enablement + 决策双 ready 后 `next_action` 由 `start_grx010_tonemap_runtime_resource_binding` 推进到 `start_grx011_ssao_blur_godot_patch_0014`。`ci/godot_rurix_toolchain_probe_validation_failed_test.py` 新增 grx010 close-out 红绿 case(pre-close-out fixture 归一化保持旧断言、篡改 success sha256 报 conflict 且 manifest 新值被拒、SKIP 不推进)。`default_enable_state` 保持 `disabled`,默认 Godot config 下 bridge 对 `RXGD_PASS_TONEMAP` 仍返回 `RXGD_STATUS_FALLBACK`、native tonemapper 接管;无 FPS、p95、GPU timestamp 或任何性能提升宣称。

**状态更新(2026-07-12,GRX-011 close-out / Wave 2)**:GRX-011 ssao_blur 已 close-out(stage-A5 对等,复用 GRX-009/GRX-010 模板)。ssao_blur gated real-pass enablement 取得 strict MEASURED success——`real_pass_enablement_success_evidence.json`(`status=success`、`strict_success=true`)记录 opt-in real dispatch(`enabled`+`dispatch_real_pass`,均默认 false)在 `0001..0016` scratch Godot(Windows D3D12 Forward+,RTX 4070 Ti)上执行且完成:candidate 腿 `RXGD_GODOT_RUNTIME_SSAO_BLUR_REAL_PASS recorded=1` + writeback scaffold marker 入证,checks 全绿含 `native_continuation_writeback_scaffold`/`real_pass_dispatched_and_completed`,`forced_capability_downgrade` 红腿实测 `first_missing_prerequisite=runtime_binding_preflight_failed`/`fallback_reason=unsupported_device`,LDR visual gate `max_abs=0`/`mean_abs=0`,measured_local telemetry 通过,`0001..0016` patch-stack/溯源/日志审计全绿;standalone dispatch smoke(`real_d3d12_dispatch_smoke.json`)CPU parity `max_abs_diff≈1.19e-07`(~1 ULP)。栈式 patch `0014`(pass-gate+call-site)/`0015`(runtime 资源绑定 `RenderingDevice::get_driver_resource` 真实 `ID3D12Resource*`)/`0016`(默认 false opt-in 三旋钮 + `RXGD_CAP_SSAO_BLUR_REAL_PASS`(1u<<5) + `d3d12-recording-shim` real dispatch path)入栈。manifest 顶层如实翻转(`status=grx011_real_pass_measured_success_default_disabled`、`implemented=true`、`real_gpu_pass=true` opt-in 实测口径、`real_d3d12_dispatch_recorded=true`、`runtime_state=fallback_only_by_default_real_pass_optin_measured`、`default_enable_state` 保持 `disabled`),新增 `implementation_status.real_pass_measured_success` block。owner 决策 `real_pass_default_enable_decision.json`/`.md` 记 `keep_default_disabled`(①无 per-pass FPS 证据②patch 0016 writeback 仍 scaffold(native ping-pong continuation 全量重跑、candidate bit-exact、无净收益)③仅 `MODE_SMART` 单遍单 slice 子集)。**GRX-011 起 gate 模块工业化**:每 pass 一个 `ci/grx_gates/grxNNN_*.py` 表驱动 gate 模块(接口 v1:`contract_ready`/`patch_applyability`/`dispatch_smoke_ready`/`enablement_ready`/`decision_ready` 五级 + `first_issue`/`next_action`),`ci/godot_rurix_toolchain_probe.py` 的 `GRX_GATE_SEQUENCE` 注册并 fail-closed walk;`grx011_ssao_blur` decision 文件落地后 `grx_gate_module_error` 消除、gate 全 ready,probe `next_action` 由 `start_grx011_ssao_blur_godot_patch_0014` 推进到 `start_grx012_taa_resolve_pass_contract`。本 wave 既有 pass 事实:GRX-009 luminance patch 0010 原地由 level-0 scaffold 改真实多级 pyramid writeback(`hook_contract_v2`:`[source,reduce[0..L-1],current,prev]` + 默认 false `try_record_luminance_pyramid()` 虚函数,既有 9-arg `try_record_luminance_reduction()` 字节不变;`levels=pyramid`/`native_continuation=skipped`/`one_frame_latency=1` declared_not_measured,LDR 仍 bit-exact `max_abs=0`,4h success evidence 经 scratch rebuild 重签);GRX-010 tonemap 4h evidence 对新 shim-v2 DLL 重签(writeback 仍 scaffold);shim `src/rurix-godot/shim/rxgd_luminance_record.cpp` 升级参数化执行模型 + `typed_view_format` typeless→typed 视图映射(如 `R8G8_TYPELESS→R8G8_UNORM`)+ `format_is_typeless` fail-closed 守卫(typeless view 是移除设备的非法 D3D12 调用,无法映射即干净 fallback);GRX-004b workload v2 evidence 级 full baseline(3 次 full 取中位 + 方差留档)随本 wave 落地(commit `eb918c8`)。`default_enable_state` 保持 `disabled`,默认 Godot config 下 bridge 对 `RXGD_PASS_SSAO_BLUR` 仍返回 `RXGD_STATUS_FALLBACK`、native SSAO blur 接管,`RXGD_PASS_SSIL_BLUR` 不接线;无 FPS、p95、GPU timestamp 或任何性能提升宣称。新增验证命令:`$env:RURIX_DXC_DIR='H:\dxc-round7\extracted\bin\x64'; py -3 ci\grx011_ssao_blur_d3d12_dispatch_smoke.py`(standalone 真实 dispatch smoke,无 adapter/签名 DXC/MSVC 时 SKIP)、`py -3 ci\grx011_ssao_blur_real_pass_enablement_smoke.py`(三腿 pass enable matrix,须 full 0001..0016 scratch build + 真实 D3D12 + signed DXC,缺前置写 SKIP)、`py -3 ci\grx_gates\grx011_ssao_blur.py`(gate 模块自评)。

**状态更新(2026-07-12,GRX-012/GRX-013 双 close-out / Wave 3)**:GRX-012 taa_resolve 与 GRX-013 particles_copy 均已 close-out(stage-A5 对等,复用 GRX-009/010/011 模板),两 pass gated real-pass enablement 均取得 strict MEASURED success(两 `real_pass_enablement_success_evidence.json`,`strict_success=true`:opt-in real dispatch 在 `0001..0022` scratch Godot(Windows D3D12 Forward+,RTX 4070 Ti)上执行且完成)。**GRX-012 taa_resolve**:candidate 腿 `RXGD_GODOT_RUNTIME_TAA_RESOLVE_REAL_PASS recorded=1` + writeback scaffold marker,`forced_capability_downgrade` 红腿实测 `runtime_binding_preflight_failed`/`unsupported_device`;**temporal 硬约束(GRX_PLAN DoD)兑现**——连续 8 帧序列(非单帧截图)逐帧对 reference diff 全 `max_abs=0`(scaffold 逐帧 bit-exact),reference 序列 `nonzero_delta_pairs=7/7` 证携带真实运动;栈式 patch `0017`(pass-gate+call-site,`using_taa` 分支 + `rb->has_texture(taa,history)` 守卫)/`0018`(六 native handle 绑定)/`0019`(默认 false opt-in 三旋钮 + `RXGD_CAP_TAA_RESOLVE_REAL_PASS`(1u<<6) + `d3d12-recording-shim` real dispatch path)入栈。**GRX-013 particles_copy**:candidate 腿 `RXGD_GODOT_RUNTIME_PARTICLES_COPY_REAL_PASS recorded=1` + writeback scaffold marker(`dispatch=64x1x1`/`dst_bytes=327680`=4096*80),`forced_capability_downgrade` 红腿实测 `dispatch_eligibility_failed`/`unsupported_device`(particles_copy 无 i64 preflight,forced 降级在下一级 dispatch-eligibility 关口 fail-closed,与纹理 pass 的 preflight 关口不同),LDR visual gate `max_abs=0`;场景 = 确定性 `GPUParticles3D`(fixed seed + fixed_fps,4096 粒子,`TRANSFORM_ALIGN_Z_BILLBOARD` 触发 cull-stage `particles_set_view_axis`,默认 `DRAW_ORDER_INDEX` 保 `do_sort=false` 选中在册 plain COPY_MODE_FILL_INSTANCES 子集);栈式 patch `0020`(pass-gate+call-site)/`0021`(两 structured-buffer 绑定)/`0022`(默认 false opt-in 三旋钮 + `RXGD_CAP_PARTICLES_COPY_REAL_PASS`(1u<<7) + real dispatch path)入栈。**shim 修复(W2-G 同类)**:`d3d12-recording-shim` 的 `typed_view_format` 扩充组合深度-模板 typeless 家族(`R32G8X24_TYPELESS`→`R32_FLOAT_X8X24_TYPELESS`、`R24G8_TYPELESS`→`R24_UNORM_X8_TYPELESS`),否则六资源 TAA record dispatch 在 depth SRV 处 fail-closed。两 manifest 顶层如实翻转(`implemented=true`、`real_gpu_pass=true` opt-in 实测口径、`real_d3d12_dispatch_recorded=true`、`runtime_state=fallback_only_by_default_real_pass_optin_measured`、`default_enable_state` 保持 `disabled`),各新增 `implementation_status.real_pass_measured_success` block;两 owner 决策 `real_pass_default_enable_decision.json`/`.md` 记 `keep_default_disabled`(无 per-pass FPS 证据、writeback 仍 scaffold 无净收益、仅各自子集覆盖)。gate 模块 `ci/grx_gates/grx012_taa_resolve.py`/`grx013_particles_copy.py` 的 `_contract_ready` 由 segment-A 断言(`implemented=false`)放宽为 close-out 断言(pass_id + `default_enable_state=disabled` + math_parity + patch-allocation 引用),decision+enablement 双 ready 后 probe `next_action` 经 grx011→grx012→grx013 全绿 walk 推进到 `start_grx014_cluster_store_pass_contract`,`grx_gate_module_error` 消除;probe validation `run_grx_gate_sequence_cases` (1b) 更新为三 gate 全 ready(3 records、无 module_error、advance 到 grx014)。默认 Godot config 下 bridge 对 `RXGD_PASS_TAA_RESOLVE`/`RXGD_PASS_PARTICLES_COPY` 仍返回 `RXGD_STATUS_FALLBACK`、native 接管;shipping feature-off bridge 仍 fail closed 为 `real_dispatch_path_not_linked`;无 FPS、p95、GPU timestamp、时序稳定性或任何性能提升宣称。新增验证命令:`$env:RURIX_DXC_DIR='H:\dxc-round7\extracted\bin\x64'; $env:RURIX_REQUIRE_REAL='1'; $env:RURIX_GRX012_TAA_RESOLVE_GODOT_EXE=...; py -3 ci\grx012_taa_resolve_real_pass_enablement_smoke.py`(三腿 pass enable matrix + temporal 8 帧序列,须 full 0001..0022 scratch build + 真实 D3D12 + signed DXC,缺前置写 SKIP)、`py -3 ci\grx013_particles_copy_real_pass_enablement_smoke.py`(三腿 pass enable matrix,GPUParticles3D 场景)、`py -3 ci\grx_gates\grx012_taa_resolve.py`、`py -3 ci\grx_gates\grx013_particles_copy.py`(gate 模块自评)。

**状态更新(2026-07-12,GRX-014 close-out + GRX-017 telemetry 首切片 + print 门控重签 / Wave 4)**:GRX-014 cluster_store 已 close-out(stage-A5 对等):`ci/grx014_cluster_store_real_pass_enablement_smoke.py` 在 `0001..0026` scratch Godot 上取得 strict MEASURED success(candidate 腿 real-pass/writeback/RECORD marker 入证,forced 红腿 `dispatch_eligibility_failed`/`unsupported_device`,LDR bit-exact `max_abs=0`),patch 0023/0024/0025 入栈,manifest 翻转 + owner `keep_default_disabled` 决策,gate 模块 `ci/grx_gates/grx014_cluster_store.py` 五级全 ready,probe `next_action` 推进到 `start_grx015_gpu_culling_pass_contract`(validation `run_grx_gate_sequence_cases` (1b) 同步为四 gate 全 ready、零 module_error)。GRX-017 telemetry-only 首切片(patch 0026,默认 false、verbose-only)落地并归档实测采样(`spike/godot-rurix/passes/material_sorting_telemetry/`,非 evidence 级 gate,无 gate 模块)。**print 门控修订与重签**:patch 0009/0010/0013/0016/0019/0022 原地修订把 per-dispatch REAL_PASS/WRITEBACK instrumentation marker 挂到 harness-only `dispatch_recording_smoke`(生产 real-pass 路径零 per-dispatch stdout),五个 enablement 冒烟(candidate/forced 腿开 recording_smoke;RECORD 断言改 reference/forced 禁止 + candidate 与 real-pass 耦合)与 probe 4h/grx010 严格审计(checks 键拆分)同步修订,并经逐级 scratch rebuild(0010b/0013b/0016b/0022b/0026)全部重跑回 strict success;4d bridge recording 与 4g visual fallback 冒烟独立重跑刷新(4f 证据 pin 的 0001..0008 栈字节未变,保留 historical 口径)。runner `VALID_PASS_MATRIX_KEYS` 追加 cluster_store 四键 + material_sorting telemetry 键。全程 default disabled、fallback-only、零性能宣称;不改 `external/godot-master`。

**状态更新(2026-07-13,GRX-019 fused_post_chain close-out / Wave 4)**:GRX-019 fused_post_chain(post-FX 融合:luminance 末级 + tonemap)已 close-out(stage-A5 对等,复用 GRX-010/013 纹理型模板)。`ci/grx019_fused_post_chain_real_pass_enablement_smoke.py` 在 `0001..0026 + 0036..0038` scratch Godot(Windows D3D12 Forward+;**0027-0035 为 GRX-015/016/018 预留孔**,fused 块 0036-0038 直接叠 0026 tip,合法单调孔 PATCH_ALLOCATION.md §4 rule 2)上取得 strict MEASURED success(`real_pass_enablement_success_evidence.json`,`status=success`、`strict_success=true`、`real_gpu_pass=true`、`real_d3d12_dispatch_recorded=true`)。三腿 pass enable matrix:reference(零 marker)/ candidate 腿 `RXGD_GODOT_RUNTIME_FUSED_POST_CHAIN_REAL_PASS recorded=1` + writeback + RECORD marker / `forced_capability_downgrade` 红腿实测 `dispatch_eligibility_failed`/`unsupported_device`(**fused runtime binding preflight 不校验 int64,只有 bridge dispatch eligibility 校验,故 forced 降级在下一级 dispatch-eligibility 关口 fail-closed,与其它纹理 pass 的 preflight 关口不同**),LDR visual gate `max_abs=0`/`mean_abs=0`。场景 = 确定性 3D + `CameraAttributesPractical.auto_exposure_enabled=true`(驱动原生 luminance_reduction 使 1×1 current luminance buffer 有效——fusion call site 的 luminance 输入前置)+ FILMIC tonemapper,`fixed_fps` 使 auto-exposure 时域 EMA 确定性收敛;capture frame 40 @ 256×144。栈式 patch `0036`(pass-gate + **fusion-first** call-site,置于逐成员单 pass tonemap gate 之前,接线两级回退 fusion→member→native)/`0037`(五 native texture handle 绑定 + 64-byte i64-dims b0)/`0038`(默认 false opt-in 三旋钮 + `RXGD_CAP_FUSED_POST_CHAIN_REAL_PASS`(1u<<13) + forced downgrade + `d3d12-recording-shim` real dispatch path)入栈,`0001..0026 + 0036..0038` scratch Godot 编译干净(console exe 产出、`tracked_patch_stack_only=true`)。**Scaffold binding 诚实声明**:fused luminance 段 distinct ≤8×8 reduce 级与双缓冲 1×1 prev/dst luminance 目标未被公开 Luminance API 在 tonemap call site 暴露,patch 0037 把唯一公开 1×1 current luminance buffer 别名给 lum_source/prev_luminance(只读)、把 LDR tonemap 目标(native 完全覆写)别名给 dst_color/dst_luminance;strict success 只断言「一次真实 D3D12 fused dispatch 被录制且 native-continuation 视觉门逐字节稳定」,**不**断言 fused 输出正确或净收益——真替代仍需 draw_graph 路线 B / Luminance-API 扩展(PASS_CONTRACT.md §3.4/§5)。manifest 顶层如实翻转(`status=grx019_real_pass_measured_success_default_disabled`、`implemented=true`、`real_gpu_pass=true` opt-in 实测口径、`real_d3d12_dispatch_recorded=true`、`runtime_state=fallback_only_by_default_real_pass_optin_measured`、`default_enable_state` 保持 `disabled`),新增 `implementation_status.real_pass_measured_success` block;owner 决策 `real_pass_default_enable_decision.json`/`.md` 记 `keep_default_disabled`(无 per-pass FPS 证据、writeback 仍 scaffold 无净收益、fused binding 为 scaffold 不宣称正确性、仅 LINEAR+SDR+auto-exposure 子集)。gate 模块 `ci/grx_gates/grx019_fused_post_chain.py` 五级全 ready(`PREREQ_ORDINALS` 修正为 `0004..0026 + 0036 + 0037` 以反映 0027-0035 孔)。**probe next_action 如实**:`GRX_GATE_SEQUENCE` 顺序 grx011..grx019 fail-closed walk,grx019 gate 独立 `all_ready`,但序列中 grx015(gpu_culling)未 author,走链在 grx015 停,故 probe `next_action` 仍停在 `start_grx015_gpu_culling_pass_contract`——grx019 gate ready 但不推进 next_action(GRX-015/016/018 各自切片未 author,如实反映,非回归)。runner `VALID_PASS_MATRIX_KEYS` 追加 fused_post_chain 四键,`bridge_smoke` 扩 0036-0038 stacked applyability + require_text。默认 Godot config 下 bridge 对 `RXGD_PASS_FUSED_POST_CHAIN` 仍返回 `RXGD_STATUS_FALLBACK`、native luminance_reduction + tonemap 链接管;shipping feature-off bridge 仍 fail closed 为 `real_dispatch_path_not_linked`;无 FPS、p95、GPU timestamp、fused-输出正确性或任何性能提升宣称。新增验证命令:`$env:RURIX_DXC_DIR='H:\dxc-round7\extracted\bin\x64'; py -3 ci\grx019_fused_post_chain_d3d12_dispatch_smoke.py`(standalone 真实 dispatch smoke)、`py -3 ci\grx019_fused_post_chain_real_pass_enablement_smoke.py`(三腿 pass enable matrix,须 full 0001..0026 + 0036..0038 scratch build + 真实 D3D12 + signed DXC,缺前置写 SKIP)、`py -3 ci\grx_gates\grx019_fused_post_chain.py`(gate 模块自评)。

**状态更新(2026-07-13,GRX 诚实天花板收官 Phase 2 / close-out)**:用户终裁(主会话 AskUserQuestion「诚实天花板收官」)下里程碑收口。GRX-023 终局 benchmark(`spike/godot-rurix/bench/rd_native_final_20260713/`:rb4 × workload v2.3 × per-pass GPU timestamp;baseline v2.3 geomean 212.74、measured all5 geomean **0.9942**、Amdahl 零成本 geomean 硬上限 **1.0669x**、per-pass µs 归因)、GRX-024 视觉 parity(`spike/godot-rurix/bench/grx024_visual_20260713/`:4/7 byte-exact + many_mesh ±1 LSB + mixed/particles temporal/非确定 floor;七 pass strict success `passes/*/rd_native_enablement_success_evidence.json`)、GRX-025 默认启用矩阵(`spike/godot-rurix/passes/DEFAULT_ENABLE_MATRIX.md` + `bench/grx025_default_enable_20260713/`:五 rd_native pass engaged-geomean 0.9906~0.9993 全过 ≥0.95 默认启用**成本**门)均已归档。**G-GRX-5 strict 1.5x 门 NOT MET → 归档为结构性不可达**(1.50 vs Amdahl 1.0669x 硬上限;三阈值 1.5/0.3/0.95、`perf_gate.py`、schema 一律未改未放宽——「归档不可达」非「降门」)。probe `next_action` 推进到 `grx_milestone_closed_ceiling_archived`(close-out marker `spike/godot-rurix/passes/GRX_MILESTONE_CLOSEOUT.json` + validation_failed_test fixture 覆盖 marker 缺失→回落 frontier)。per-pass 终态:五 rd_native pass 门达成 default disabled(启用=per-project 容器 staging opt-in)/ fused engaged 净零 shadow-recompute + AE parity out-of-tolerance keep_disabled / gpu_culling·016·018 mechanism_blocked(RDG compute→INDIRECT 同帧 sync 缺口)/ 022 frozen。GRX_CONTRACT §8.1 close-out 签署段草案就绪(签署行占位由主会话 close-out commit 生效)。全程 `performance_claim=none`,不写「显著提升已达成」。

当前未完成(收官归档口径):

- ~~7 场景 full baseline benchmark 实测~~(v1.31 已完成:`baseline_full_20260708.json`;但 per-pass FPS >= 0.95x baseline 的默认启用前置**对比**证据仍未完成)。
- baseline / Rurix 两组 full measured_local 对比数据。
- 真实 pass-vs-baseline visual diff evidence(v1.31 已完成 bench 层 reference capture 与 self-diff 基线;pass 对比 visual evidence 仍未完成)。
- 真实 fallback 接入与 measured telemetry(当前 telemetry 全部为 scaffold placeholder)。
- Godot-runtime bridge-recorded D3D12 dispatch 与 real GPU luminance pass(GRX-009 已完成 segment 4a preflight、4b gated dispatch bring-up、4c standalone D3D12 dispatch smoke success、4d bridge recording smoke success、4e native handle mapping ready、4f harness/patch/evidence 接线;但 segment 4f runtime smoke 需完整 0001..0008 Godot scratch build,当前本机记 `status=skip`,尚未在 Godot runtime 上实测录制;默认 bridge 仍恒 FALLBACK、默认 disabled、pass 本体未实现)。
- 任何实际加速 pass(含 GRX-009 Tier 1 luminance reduction pass)。
- 达成 1.5x / p95 -30% 的性能提升声明:**收官归档为结构性不可达**(GRX-023 量化 Amdahl 零成本 geomean 硬上限 1.0669x « 1.50;门数学/三阈值/`perf_gate.py`/schema 未改未放宽)。里程碑以诚实天花板收口,不以此门达标收口,全程 `performance_claim=none`。上方 baseline/rurix 对比、pass-vs-baseline visual diff 已由 GRX-023/024/025(`rd_native_final_20260713`/`grx024_visual_20260713`/`grx025_default_enable_20260713`)归档;剩余 Godot-runtime luminance 全 pass 与 GPU-culling 机制项按 mechanism_blocked / 后续 upstream 归档(GRX_PLAN §8 收官终态)。

## 8. 修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-07-01 | 初版。定义 PR smoke、Local Godot gate、Benchmark strict gate、Visual/fallback gate、ABI gate、red/green 程序和当前验证状态。 |
| v1.1 | 2026-07-01 | 收紧 GRX.0 文档基线口径。把 strict 指标中文解释统一为 `p95 frame time reduction >= 30%`,并明确当前阶段只是文档任务,不跑 Godot build/benchmark,也不宣称性能提升。 |
| v1.2 | 2026-07-01 | 统一 `GRX.0` / `GRX.1` 边界:明确 detector/build/load 执行项属于 `GRX.1`,避免将 `GRX-001` 误读为 GRX.0 交付。 |
| v1.3 | 2026-07-01 | 收紧 GRX.1 detector/build 口径:缺少 SCons launcher 时 `recommended_scons_command` 必须为 `null` 并给出下一步;明确 `dxv.exe` 属于后续 DXIL/device validation warning,不阻塞 `GRX-002` Godot SCons build。 |
| v1.4 | 2026-07-01 | 统一 GRX.1 probe readiness 语义:默认只在 workspace-local AccessKit 与 D3D12 deps 都齐备时才允许 `build_ready PASS` 并推荐默认 SCons;不再用 `accesskit=no` 作为 `build_ready=false` 状态下的默认推荐命令。 |
| v1.5 | 2026-07-01 | 将 `py -3 ci\godot_rurix_scons_build.py` 提升为推荐 Local Godot Gate;明确裸 SCons 是规范目标、当前本机 MSVC 14.44 使用 wrapper single-job/ANGLE-disabled workaround;要求 GRX-002 summary 记录 exe/console exe/module lib 的 artifact evidence,并把 `godot_rurix_load_smoke.py` 纳入当前验证状态。 |
| v1.6 | 2026-07-01 | 修正 `GRX.1` close-out hardening 口径:明确 `GRX-001/002/003` 已本地通过;要求 load smoke 项目文件仅落在 `target/grx/godot-load-smoke`,missing-DLL case 需对 session-ready marker 做反向断言并记录 `unexpected_markers`;probe 在 build + load evidence 完整后推进到 `start_grx2_tier0_benchmark_skeleton`,但不得据此宣称 benchmark 已开始或性能已提升。 |
| v1.7 | 2026-07-01 | 以 fresh path-overrides rebuild / smoke 收口更新 Local Godot Gate:要求 build summary 显式给出 `disable_path_overrides=no` readiness 证据,probe 的 `build_artifacts_ready` 同时检查 required artifacts 与 path-overrides 条件,并允许把 latest wrapper 非零退出降为 warning;同时把 `main.tscn` 旧残留清理口径收紧为 marker 或精确 fingerprint。 |
| v1.8 | 2026-07-01 | 收口 `GRX-004`/`GRX-005` 建设期 benchmark gate:明确 `GRX-004` 已以 fresh per-scene smoke 通过,当前接续任务是固定 `warmup 300 / sample 2000 / vsync off / 1920x1080` 的 tracked runner;允许 `gpu_timestamps_available=false` 但必须显式记录,同时写死本次不触发 baseline schema/perf gate、visual diff 或任何性能提升声明。 |
| v1.9 | 2026-07-01 | 收口 `GRX-005` 硬化 / `GRX-006` 交付:Benchmark Gate 新增 runner failure marker 扫描规则(allowlist global script cache warning、记录 `failure_markers` / `warnings` / `warning_count`)与 GRX-006 schema/`--strict`/`--validate-only` 说明;strict close-out 命令改为 `perf_gate.py --strict <results.json>` 并指向 `perf_gate_input.schema.json`;当前验证状态纳入两个样例 JSON 校验命令,并写死 full baseline 实测、visual diff、加速 pass 与性能提升仍未完成、quick-smoke 不可用于 close-out。 |
| v1.10 | 2026-07-01 | 收口 `GRX-006` hardening / 接续 `GRX-007` scaffold:Benchmark Gate 新增 strict forbidden marker 词边界正则、baseline `sample_count`(且 `== sample_frames`)、strict `thresholds` 固定值(1.5/0.3/0.95)三条强化规则与两个红测样例说明;Visual/Fallback Gate 新增 `GRX-007` scaffold 建设期规则(7 scene × ≥1 frame、LDR per-channel absolute diff 需真实帧、HDR/temporal 仅声明、SKIP 需具体原因、无真实帧不得写视觉验证通过);当前验证状态更新为 probe `next_action=start_grx007_visual_diff_scaffold`,纳入两个红测与 `visual_diff.py --validate-only` 命令,并重申 full baseline / 真实 visual diff / Rurix 加速 pass / 性能提升仍未完成。 |
| v1.11 | 2026-07-01 | 收口 `GRX-007` hardening / `GRX-008` scaffold hardening / 接续 `GRX-009` 准备:Visual/Fallback Gate 新增 `GRX-007` skip-禁止伪造 diff/帧路径规则与红测 `visual_diff_skip_with_fake_ldr_example.json`,并补齐 pass 缺 ldr_diff / diff 不一致 / diff 一致 / `--write-output` 红绿说明;新增 `GRX-008` scaffold/full 区分规则(scaffold 允许 timestamp/frame=null 但必须 disabled + `godot_fallback_active=true`;full/measured_local 要求 timestamp 非空、frame 非负整数;measured_local 禁止 `placeholder_` pass_id)与两个红测样例说明,placeholder 仍 FORMAT PASS;当前验证状态更新为 probe `grx007_visual_ready=true`/`grx008_telemetry_ready=true`、`next_action=start_grx009_luminance_reduction_pass_prep`,纳入新增 visual/fallback 红绿命令,并把“未完成”补充真实 fallback 接入/telemetry 与 GRX-009 仅为准备。本轮不实现任何实际 Rurix 加速 pass,不宣称视觉验证、fallback 真接入或性能提升已完成。 |
| v1.12 | 2026-07-01 | 收尾 `GRX-007`/`GRX-008` hardening / 产出 `GRX-009` 准备:Visual/Fallback Gate 新增 `GRX-007` 收尾规则(`status=pass` 帧在帧文件缺失/不可读/非合法 channel 文档/channel 数量不一致时必 DIFF FAIL 且非零退出,`--write-output` 拒绝写出)与红测 `visual_diff_pass_missing_frame_artifact_example.json`;注明 `GRX-008` `measured_local` 禁止 `placeholder_` pass_id 现为 schema 与脚本双侧约束;新增 `GRX-009` luminance reduction pass 准备期规则(准备产物 `PASS_CONTRACT.md` + `pass_manifest.json`、`grx009_prep_ready` 证据判定、`next_action=start_grx009_luminance_reduction_pass_contract`/就绪后 `start_grx009_luminance_reduction_pass_implementation`);当前验证状态同步更新。GRX-009 仍只是准备;本轮不实现任何实际 Rurix 加速 pass,不宣称视觉验证、fallback 真接入或性能提升已完成。 |
| v1.13 | 2026-07-02 | 收口 GRX-009 准备 / 交付 gated implementation 第一段:标题版号补齐至修订行(v1.12 后续);`grx009_prep_ready` 加强为校验 manifest 记录的 Godot source/header/shader/call-site 文件存在于 `external/godot-master`(相对路径、不逃逸快照根、只读);新增 GRX-009 第一段 gated 规则(bridge `LuminanceReductionGate` 恒 `RXGD_STATUS_FALLBACK` 且不累加 estimated time、栈式 0002 module patch 默认关且非 OK 走原生路径、bridge smoke patch 栈三态检查、disabled telemetry 样例 FORMAT PASS 但非 measured);当前验证状态纳入 pass-缺帧红测与 luminance disabled 样例命令,并把 GRX-009 未完成口径改为第一段 gated scaffold/pass 本体未实现。 |
| v1.14 | 2026-07-02 | 收口 GRX-009 第二段 core call-site fallback wiring:Visual/Fallback Gate 的 GRX-009 规则改为分段 gate 语义,保留 `grx009_prep_ready` 只做准备产物/manifest path 校验,新增 `grx009_segment1_ready` / `grx009_segment2_ready` 与 segment-aware `next_action`;新增 0003 core call-site patch 规则,bridge smoke 改为 patch 栈四态检查,并补入 callsite-wired disabled scaffold 样例命令。当前验证状态同步改为 segment 2 完成态,同时明确默认仍是 disabled/fallback,真实 GPU pass、真实 visual diff、measured telemetry 与性能提升声明仍未完成。 |
| v1.15 | 2026-07-02 | 收口 GRX-009 review findings 并接续 `segment 3a`:明确 `grx009_segment1_ready` 是历史交付点而非当前 segment 2 的必经真值;把 `grx009_segment2_ready` 收紧为必须同时通过 callsite-wired telemetry sample 的 `fallback_telemetry.py --validate-only` 与共享 patch 栈四态检查(`0001+0002+0003`),不再接受仅 patch 文本/JSON 可解析;新增 `grx009_segment3a_compile_ready` 规则,要求真实 `DXIL + root signature + descriptor layout` artifact 三者齐备且 runtime 仍 `fallback_only`,compile_failed 仅算 blocker evidence complete,不得推进 segment 3。 |
| v1.16 | 2026-07-02 | 修复 GRX-009 segment 3a artifact gate:ready gate 现在拒绝 LLVM IR 文本、`artifact_kind=dxil_ir_text`、`semantic_status=entry_shell_only` 与 toolchain SKIP stderr;当前 evidence 为 `compile_failed/body_lowering_missing`,current artifacts 只代表 latest compile attempt 产物,manifest 保持 segment 2,next action 指向真实 DXIL container/body lowering blocker,不进入 resource mapping。 |
| v1.17 | 2026-07-03 | 收口 GRX-009 `validation_failed` probe 回归门禁:PR Smoke 新增 `py -3 ci\godot_rurix_toolchain_probe_validation_failed_test.py`,该脚本必须使用 temp fixture 与 `RURIX_GRX009_PASS_DIR`,不得改写真正 luminance manifest/offline evidence;`validation_failed` 与 `compile_failed`、`toolchain_missing` 一样只能作为 blocker evidence complete,不得推进 segment 3 或 resource mapping。当时真实状态仍是第二段 `toolchain_missing/fallback_only`,下一硬 blocker 是提供或定位 patched DXIL `llc`(`RURIX_LLC`) 与 signed DXC validator suite。 |
| v1.18 | 2026-07-03 | 收口 GRX-009 segment 3a DXIL toolchain preflight hardening:DXC validator suite PASS 必须同时满足 `dxc.exe`/`dxv.exe` runnable probe、identity marker 与 `dxil.dll` sha256 evidence,不能让 Python/任意可执行文件凭 exit code 0 伪装通过;所有外部 probe 命令必须带 timeout 并把 timeout 写入 evidence 或 missing reason。当前真实状态仍是 `toolchain_missing/fallback_only/segment 2`;`RURIX_LLC` 未设置时,即使 signed DXC suite PASS,`dxil_toolchain_preflight_ready` 仍必须为 false。 |
| v1.19 | 2026-07-03 | 执行 GRX-009 segment 3a 真实 offline DXIL compile evidence:本机 `RURIX_LLC` 与 signed DXC validator suite preflight ready,latest compile 产出 `DXIL container + root signature + descriptor layout`,但 DXC validator rejected emitted DXIL container,因此 evidence/manifest 同步为 `validation_failed`,runtime 继续 `fallback_only`,manifest 当时保持第二段,不得进入 resource mapping 或宣称 real GPU pass。 |
| v1.20 | 2026-07-03 | 修复 GRX-009 segment 3a DXIL validator rejection 并取得 success:validator 拒因共三层——指针值 resource global 的 load/store 类型不匹配、手搓 external global/自造 intrinsic 违反 DXIL 结构规则(External declaration unused / not a DXIL function)、模块缺 `!dx.valver` 致容器级 `PSVRuntimeInfoSize` mismatch;compute lowering 改经上游 `llvm.dx.*` intrinsic(`handlefrombinding`/`load.rawbuffer`/`store.rawbuffer`/`getpointer`/`thread.id`,root constants 建模为 cbuffer b0)+ `!dx.valver 1.8`,真实 `RURIX_LLC` + signed DXC suite 重跑 offline compile,`dxv.exe` 接受 container(`Validation succeeded`),evidence/manifest 同步 `success`/segment 3,runtime 仍 `fallback_only`、`real_gpu_pass=false`,resource mapping 未开始,无 visual/telemetry/性能宣称;`tests/dxil/cs_noop` 两层 golden 因 `!dx.valver` 重 bless(bless_log 留痕)。 |
| v1.21 | 2026-07-03 | GRX-009 segment 3a 收官清理:同步当前 offline compile success/segment 3 语义到 CI gate 当前状态,保留 `validation_failed` temp fixture 作为历史/回归分支规则,明确 next_action=`start_grx009_luminance_segment3_resource_mapping`;本次只清理 index/evidence/doc gate/diff check policy,resource mapping 未开始,runtime 仍 `fallback_only`,`real_gpu_pass=false`,无 visual/perf/measured telemetry 声明。 |
| v1.22 | 2026-07-04 | 同步 GRX-009 segment 4a close-out gate 文案:新增 `grx009_segment4a_runtime_binding_preflight_ready` 规则,明确该 gate 只表示 preflight/fallback-only ready;0005 stacked applyability 必须通过 scratch copy 检查且不得污染 `external/godot-master`;当前状态推进到 `start_grx009_luminance_segment4b_gated_dispatch_bringup`。runtime 仍 `fallback_only`,`real_gpu_pass=false`,bridge 对 luminance 仍返回 `RXGD_STATUS_FALLBACK`,无 D3D12 dispatch、真实 GPU pass、真实 visual diff、measured telemetry 或性能提升声明。 |
| v1.23 | 2026-07-04 | 执行 GRX-009 segment 4b gated dispatch bring-up:新增 `0006-rurix-accel-luminance-gated-dispatch-bringup.patch`(栈在 0001+0002+0003+0004+0005,scratch copy 校验,不污染 `external/godot-master`)、默认 `false` 的 `.../dispatch_bringup` opt-in 与保留 flag `RXGD_CAP_LUMINANCE_DISPATCH_BRINGUP`(承载在既有 `RxGdCaps.flags`,不改 ABI struct layout、`RXGD_ABI_VERSION` 保持 1、header/patch/smoke 同步);bridge `record_gated_dispatch_bringup` 在 preflight 之后加 dispatch eligibility gate(opt-in flag、int64 能力、native device/queue/resource handle 非空、compiled package layout/摘要匹配),任一失败即 fallback,即便全部 eligible explicit dispatch gate 仍关闭返回 `RXGD_STATUS_FALLBACK`;新增 `grx009_patch_0006_applyable`/`grx009_segment4b_gated_dispatch_bringup_ready` probe 与 4 条 `cargo test` 红绿(disabled/missing handle/layout-hash mismatch/valid-eligibility-still-gated)。无真实 D3D12 device harness,real local D3D12 dispatch smoke 显式 SKIP,`next_action=provide_grx009_luminance_real_d3d12_dispatch_smoke`。runtime 仍 `fallback_only`、`real_gpu_pass=false`、`real_d3d12_dispatch_recorded=false`,无 D3D12 dispatch、真实 GPU pass、真实 visual diff、measured telemetry 或性能提升声明。 |
| v1.24 | 2026-07-04 | 同步 GRX-009 segment 4c standalone real D3D12 dispatch smoke success 并做一致性收口:`ci/grx009_luminance_d3d12_dispatch_smoke.py` 在真实 D3D12 adapter(NVIDIA GeForce RTX 4070 Ti)上完成一次最小 compute dispatch(tracked luminance DXIL container + RTS0 root signature + descriptor layout,RTS0 直建 root signature、DXIL 建 compute PSO、SRV t0/UAV u0/b0 绑定、dispatch=1,1,1、fence_completed_value=1、readback_checksum=0x4b95f515),evidence `real_d3d12_dispatch_smoke.json` 记录 `status=success`、`runtime_state=fallback_only`、`real_gpu_pass=false`,artifact SHA-256 与 segment 3a offline evidence 一致;新增 `grx009_real_d3d12_dispatch_smoke_ready` gate 规则,`next_action` 由 `provide_grx009_luminance_real_d3d12_dispatch_smoke` 推进到 `start_grx009_bridge_real_d3d12_dispatch_recording`。probe predecessor gate 改为 historical/cumulative:manifest 推进到 segment 4b 后 `grx009_segment3a_compile_ready`/`grx009_segment3b_resource_mapping_ready`/`grx009_segment4a_runtime_binding_preflight_ready` 仍为 true(artifact hash、patch applyability、fallback-only、real_gpu_pass=false 检查一律未削弱)。本次仅做文档/manifest/probe 一致性收口,不开始 bridge real D3D12 dispatch recording,不让 `rxgd_record_pass` 返回 OK,不启用 Godot luminance Rurix path;segment 4c smoke 为 standalone measured evidence,不宣称 bridge-recorded D3D12 dispatch、real GPU pass、真实 visual diff、measured telemetry 或性能提升。 |
| v1.26 | 2026-07-05 | 执行 GRX-009 segment 4f Godot-runtime bridge dispatch recording smoke 接线并先修两处状态一致性:§7 当前验证状态改写为 segment 4e 已 ready、probe 下一步 `start_grx009_godot_runtime_bridge_dispatch_recording_smoke`,`pass_manifest.json` 的 `implementation_status.segment_detail` 由 `4b_gated_dispatch_bringup` 改为 `4f_godot_runtime_bridge_recording_smoke`(仍 `segment=4`、`runtime_state=fallback_only`、`real_gpu_pass=false`)。新增栈式 patch `0008-rurix-accel-luminance-godot-runtime-bridge-recording-smoke.patch`(叠在 0001..0007,scratch copy 校验,不污染 `external/godot-master`):默认 `false` 的 `.../dispatch_recording_smoke` opt-in、module 侧 harness-only flag `RXGD_CAP_LUMINANCE_DISPATCH_RECORD (1u << 2)`(`RxGdCaps.flags`,`RXGD_ABI_VERSION` 保持 1)、仅 opt-in 开启时在 `try_create_session` 置 flag、`try_record_luminance_reduction` 仅 `rxgd_record_pass` 返回 OK 时打印 `RXGD_GODOT_RUNTIME_LUMINANCE_RECORD` marker。新增 harness `ci/grx009_godot_runtime_bridge_recording_smoke.py` 与 evidence `godot_runtime_bridge_recording_evidence.json`,并更新 `ci/godot_rurix_patch_stack.py`/`ci/godot_rurix_bridge_smoke.py`/`ci/godot_rurix_toolchain_probe.py` 纳入 `grx009_patch_0008_applyable`/`grx009_segment4f_godot_runtime_bridge_recording_ready`,ready 后 `next_action=start_grx009_luminance_real_visual_diff_and_measured_fallback_telemetry`;smoke 需真实 D3D12 device、MSVC、signed DXC 且 `RURIX_GRX009_SEGMENT4F_GODOT_EXE` 指向完整 0001..0008 栈重建的 Godot scratch build 才 success,缺任一前置写明确 SKIP、不推进 readiness(当前本机 `status=skip`)。默认(未开 opt-in)module 仍不设置 record flag,shipping/feature-off bridge 与默认 Godot config 仍 FALLBACK。本次只完成 segment 4f 接线:不启用默认 Godot luminance Rurix path、不完成 Godot runtime pass、不改 `RXGD_ABI_VERSION`、不把 `real_gpu_pass`/`real_d3d12_dispatch_recorded` 改成 true,不宣称 visual diff、measured telemetry、FPS、p95、GPU timestamp 或性能提升;不改 `external/godot-master`。 |
| v1.27 | 2026-07-06 | GRX-009 segment 4g preflight hardening + segment 4h gated real-pass enablement gate 落地:(a) 4g smoke/probe 加严——reference/candidate 双腿全量 stdout+stderr `runtime_log_audit`(只容忍带 rationale 的 `Could not load global script cache`,其余 `ERROR:` 一律 FAIL)、telemetry 条目强制 `enable_state=enabled`/`fallback_reason=validation_failed`/`godot_fallback_active=true`/`telemetry_frame == visual.capture_frame_index`(实测帧号),并补 stale frame / 错 reason / 错 state / 意外 ERROR 四类 red 回归;4g 证据经 hardened smoke 真机重测后保持 ready=true。(b) 新增 segment 4h:bridge 侧 `RXGD_CAP_LUMINANCE_REAL_PASS (1u << 3)`(ABI v1 不变)real-pass attempt 依次跑 preflight → eligibility → 新的 kernel-binding-kind conformance check(tracked segment 3a kernel 为 raw-buffer views(`target("dx.RawBuffer")`),Godot runtime 提供 Texture2D `ID3D12Resource*` 句柄,failing closed 即 `validation_failed`),任一失败返回 `RXGD_STATUS_FALLBACK` 并每 session 一次打印机读 `RXGD_REAL_PASS_BLOCKED first_missing_prerequisite=...` 诊断;栈式 patch `0009-rurix-accel-luminance-real-pass-optin.patch`(默认 false 的 `.../dispatch_real_pass` opt-in + harness-only `.../real_pass_force_capability_downgrade` 强制失败旋钮 + 仅 bridge 返回 OK 才打印的 `RXGD_GODOT_RUNTIME_LUMINANCE_REAL_PASS` marker);新增 harness `ci/grx009_segment4h_real_pass_enablement_smoke.py`(三腿 pass enable matrix:disabled_default / enabled_real_pass_optin / forced_capability_downgrade,measured_local LDR visual gate `max_abs<=2`/`mean_abs<=0.25`,GRX-008 telemetry,0001..0009 scratch 溯源审计,shipping feature-off DLL)与 latest/success 双证据 + schema + probe gate(`grx009_segment4h_real_pass_enablement_ready`、`grx009_patch_0009_applyable`)+ 全套 red 回归。**用 tracked artifact 严格 success 按设计不可达**:gate 如实上报 first missing prerequisite = runtime-mappable(texture-capable、math-parity)kernel artifact round。默认语义全程不变:pass 默认 `disabled`、`runtime_state=fallback_only`、`real_gpu_pass=false`、`RXGD_ABI_VERSION` 不变,不宣称 real GPU pass、visual 成功、measured telemetry 提升、FPS、p95、GPU timestamp 或任何性能提升;不改 `external/godot-master`。 |
| v1.28 | 2026-07-08 | 执行 GRX-009 segment 4k descriptor/RTS0 cross-check:新增 `ci/grx009_dxc_texture_descriptor_rts0_crosscheck_smoke.py` 与 evidence `dxc_texture_descriptor_rts0_crosscheck_evidence.json`,严格校验 descriptor resources(`src_luminance` SRV t0 space0 texture2d、`dst_luminance` UAV u0 space0 rwtexture2d、`root_constants=none`)、descriptor/RTS0 hash 与 metadata/evidence 一致,并经 Rurix binding layout 重新 serialize RTS0 后要求与 tracked `root_signature.rts0.bin` byte-for-byte match。probe 新增 `grx009_dxc_texture_descriptor_rts0_crosscheck_ready` 与红绿回归,ready 后 `next_action=define_grx009_texture_artifact_provenance_policy`。runtime 仍 closed:`provenance=hlsl_bridge_workaround`,`rurix_owned=false`,`runtime_mappable=false`,`real_gpu_pass=false`,canonical raw-buffer artifact 与 `offline_compile_evidence.json status=compile_failed` 不变;不宣称 visual/GPU timestamp/FPS/性能。 |
| v1.30 | 2026-07-08 | GRX-009 stage A5 收口:segment 4h strict MEASURED success(opt-in real dispatch 执行且完成 + LDR visual gate max_abs=0 + `forced_capability_downgrade` 红腿实测 `unsupported_device` + measured_local telemetry + 0001..0010 溯源/日志审计全绿)入证 `real_pass_enablement_success_evidence.json`;patch 0010 result writeback scaffold 入栈(native continuation 重渲染全部级别,无净收益);canonical package 为 owner-approved HLSL bridge texture package(owner provenance policy)。manifest 顶层如实翻转(`implemented=true`、`real_gpu_pass=true` opt-in 实测口径、`runtime_state=fallback_only_by_default_real_pass_optin_measured`、`default_enable_state` 保持 `disabled`);probe manifest 检查 fail-closed 放宽:仅当 4h success 存在且通过全量审计(`grx009_real_pass_measured_success_active`)才接受新值,placeholder/篡改文档仍被 `real_pass_enablement_success_evidence_must_not_exist` 拒绝;`ci/grx009_texture_artifact_provenance_policy_smoke.py` 同步该语义且不再强行改写 manifest 新值。新增 owner 默认启用决策文件 `real_pass_default_enable_decision.json`/`.md`(`keep_default_disabled`:无 per-pass FPS 证据、writeback scaffold、math parity level-0-only;full baseline + per-pass benchmark 后复评)与 probe `grx009_real_pass_default_enable_decision_ready` gate,双 ready 后 `next_action=start_grx010_tonemap_pass_contract`;`ci/godot_rurix_toolchain_probe_validation_failed_test.py` fixture 以 `pre_a5_fixture_manifest` 归一化回 pre-A5 fail-closed 形态并保持全绿。无 FPS、p95、GPU timestamp 或任何性能提升宣称。 |
| v1.29 | 2026-07-08 | 执行 GRX-009 segment 4l texture artifact provenance policy:新增 `ci/grx009_texture_artifact_provenance_policy_smoke.py` 与 owner-approved policy evidence `texture_artifact_provenance_policy.json`(`status=success`、`policy_ready=true`)+ `texture_artifact_provenance_policy.md`,严格校验 owner decision(`approve_hlsl_bridge_workaround_as_temporary_runtime_mappable_canonical`)、`canonical_switch_exception=owner_approved_hlsl_bridge_workaround`、`rurix_owned_rts0=true`、revert 条件非空、policy/bridge 文档段落齐备且 `dxc_texture_artifact_bridge.md` 引用 policy 文档,并要求 fail-closed 标志全 false(`runtime_mappable`/`real_gpu_pass`/`canonical_artifact_replaced`/`offline_compile_status_changed`)。probe 新增 `grx009_texture_artifact_provenance_policy_ready` 与红绿回归(依赖 descriptor/RTS0 cross-check gate ready),双 ready 后 `next_action=provide_grx009_runtime_mappable_luminance_kernel_artifact`。runtime 仍 closed:`provenance=hlsl_bridge_workaround`,`rurix_owned=false`,`runtime_mappable=false`,`real_gpu_pass=false`,canonical raw-buffer artifact 与 `offline_compile_evidence.json status=compile_failed` 不变;不宣称 visual/GPU timestamp/FPS/性能。 |
| v1.31 | 2026-07-08 | GRX 阶段 B 收尾:full baseline measured 完成 + GRX-007 bench 层 reference capture 实装。(a) 7 场景 full baseline benchmark 已实测(run_id=`20260708T134948Z_full`,run_mode=full,warmup 300 / sample 2000 / 1920x1080 / vsync false / measured_local,7 场景全 success,raw per-scene JSON 在 `target/grx/godot-bench-runs/20260708T134948Z_full/raw/`);新增聚合工具 `spike/godot-rurix/bench/aggregate_baseline_evidence.py`,把 runner summary + per-scene raw JSON 交叉校验后聚合为入库 baseline evidence `spike/godot-rurix/bench/baseline/baseline_full_20260708.json`(kind=baseline,per-scene avg_fps/p95_frame_time_ms/sample_count/raw_artifact_path + raw artifact SHA-256 hash-pin),该文档通过 `perf_gate.py --kind baseline --strict --validate-only`。(b) `capture_reference_frames.py` 由 scaffold(恒 SKIP)改为真实 capture backend:在生成的 bench project 内安装确定性 capture runner(GDScript,`--fixed-fps 60`,等待固定 600 帧后 `get_viewport().get_texture().get_image()` 存 RGB8+PNG+meta),tracked Godot exe 以 D3D12 Forward+ 跑 7 场景各截一帧 reference(256x144 确定性 capture 分辨率,防 channel JSON 膨胀),输出 `target/grx/godot-visual/reference/<scene>.rgb8` + `capture_summary.json`(含 SHA-256/尺寸/frame index),并生成 rgb8→`{"pixels":[[r,g,b],...]}` channel 文档 + 经 `visual_diff.py --write-output` 计算/复验的 7 场景 visual evidence `target/grx/godot-visual/visual_reference_capture_evidence.json`(7 帧全部 status=pass,LDR self-diff 全 0)。本机 7 场景 capture 全部 success,无 SKIP。probe `bench_runner_ready` 按既有规则识别 full 形态 summary(status/scene_count/failure_count,不限 run_mode),`grx007_visual_ready=true`,`next_action=start_grx010_tonemap_pass_contract` 无回归。**边界声明:baseline evidence 只是基线(单配置实测,无任何对比),visual evidence 是 reference-capture 自比对基线(reference==candidate,仅证明 capture/diff 管线),两者均不构成 baseline-vs-Rurix 对比、pass 视觉验证或任何性能提升宣称。** |
| v1.32 | 2026-07-08 | 启动 GRX-010 tonemap pass segment A 门禁(模板复制 GRX-009):PR Smoke 追加 `py -3 ci\grx010_tonemap_offline_compile_smoke.py`(DXC cs_6_0 + DXV + `emit_grx010_tonemap_rts0` Rurix-owned RTS0 + descriptor layout,audit `offline_compile_evidence.json`:`status=success`、`provenance=hlsl_bridge_workaround`、`rurix_owned=false`、`runtime_mappable=true`、三 artifact 磁盘 SHA-256 重算逐字节匹配,缺 toolchain 记诚实 SKIP)与 standalone 真实 dispatch smoke `py -3 ci\grx010_tonemap_d3d12_dispatch_smoke.py`(真实 D3D12 adapter,RTS0 直建 root signature、tracked DXIL 建 compute PSO、SRV t0/UAV u0/28-byte b0 按 descriptor layout 绑定、dispatch + fence + readback,且首像素必须与 CPU reference `linear_to_srgb(src*luminance_multiplier*exposure)` 容差内一致,evidence `real_d3d12_dispatch_smoke.json`;本机实测 success,RTX 4070 Ti,match=1)。probe 新增 `grx010_tonemap_contract_ready`(契约三件套 marker + manifest fail-closed 字段 + offline evidence 反伪造 hash 复算 + bridge `TonemapGate`/`RXGD_CAP_TONEMAP_REAL_PASS` marker + patch 0011 marker)、`grx010_patch_0011_applyable`(0011 在 0004..0010 scratch 栈上 `git apply --check`)、`grx010_tonemap_d3d12_dispatch_smoke_ready`(strict success + artifact/offline 双向 hash 复核 + checks 全绿含 `dst_matches_cpu_reference`;SKIP 永不 ready),next_action 链:contract 未就绪保持 `start_grx010_tonemap_pass_contract` → `fix_grx010_tonemap_patch_0011_applyability` → `provide_grx010_tonemap_real_d3d12_dispatch_smoke` → 全绿后 `start_grx010_tonemap_runtime_resource_binding`;`godot_rurix_toolchain_probe_validation_failed_test.py` 新增 grx010 红绿 case(真实 pass dir 双 gate 绿;tampered offline dxil hash → `grx010_offline_artifact_hash_mismatch`,SKIP smoke → `grx010_dispatch_smoke_status_skip`,均不推进)。`godot_rurix_patch_stack.py` / `godot_rurix_bridge_smoke.py` 纳入 0010/0011 stacked applyability 与 bridge/patch 0011 marker。ABI gate:`RXGD_CAP_TONEMAP_REAL_PASS (1u << 4)` 复用 `RxGdCaps.flags` 位,`RXGD_ABI_VERSION` 保持 1;`RXGD_PASS_TONEMAP` 由历史占位 estimated-timing 路径改走 fail-closed `TonemapGate`(默认恒 `RXGD_STATUS_FALLBACK`,shipping feature-off bridge 在 opt-in arm 上 fail closed 为 `real_dispatch_path_not_linked`)。tonemap pass 默认 `disabled`、manifest `implemented=false`、`runtime_state=fallback_only`、`real_gpu_pass=false`;本切片 measured 上限为 standalone dispatch + CPU parity,引擎内实测(4f/4g/4h 级)与其余 tonemapper 模式留后续切片;无 FPS、p95、GPU timestamp 或任何性能提升宣称;不改 `external/godot-master`。 |
| v1.25 | 2026-07-04 | 同步 GRX-009 segment 4d evidence artifact hygiene 到 CI gate,对齐 §2 `grx009_bridge_real_d3d12_dispatch_recording_ready` / evidence hygiene 规则与 §5 ABI gate / §7 当前验证状态:`bridge_dispatch_recording_evidence.json` 为**某一次历史 measured run** 产物;smoke 在 feature build 成功后记录当次 feature-built DLL 指纹 `dll_fingerprint`(`dll_path_at_run`/`dll_sha256`/`dll_size_bytes`/`dll_mtime_utc`/`build_profile=debug`/`features=["d3d12-recording-shim"]`),并把该 DLL 复制到 `target/grx009_bridge_recording_smoke/rurix_godot_d3d12_recording_shim.dll`(记 `snapshot_dll_path`/`snapshot_dll_sha256`;二进制在 `target/` 下 gitignored,不纳入 Git)。因 `target/debug/rurix_godot.dll` 是 mutable artifact(feature-off `cargo build -p rurix-godot` 会原地覆盖),4d readiness **不**把当前 `target/debug/rurix_godot.dll` hash 纳入 gate:覆盖后 `grx009_bridge_real_d3d12_dispatch_recording_ready` 仍为 true(historical measured evidence),probe 输出 `grx009_bridge_recording_evidence_dll_sha256`/`grx009_bridge_recording_current_dll_sha256`/`grx009_bridge_recording_current_dll_matches_evidence`,不匹配时追加 `current_dll_matches_evidence=false` warning(非 failure),提示重跑 `ci/grx009_luminance_bridge_recording_smoke.py` 可刷新当前 artifact 指纹。`RXGD_ABI_VERSION` 不变。本次仅做证据卫生一致性收口:不启用 Godot luminance Rurix path,不完成 Godot runtime pass,不把 `real_gpu_pass`/`real_d3d12_dispatch_recorded` 改成 true,`next_action` 仍为 `start_grx009_godot_native_resource_handle_mapping`;segment 4d 仍是 historical measured bridge evidence,不宣称 real GPU pass、visual diff、measured telemetry、GPU timestamp 或性能提升;不改 `external/godot-master`。 |
| v1.33 | 2026-07-11 | GRX-010 tonemap close-out(stage-A5 对等,模板复制 GRX-009):`pass_manifest.json` 顶层翻转(`status=grx010_real_pass_measured_success_default_disabled`、`implemented=true`、`real_gpu_pass=true` opt-in 实测口径、`real_d3d12_dispatch_recorded=true`、`runtime_state=fallback_only_by_default_real_pass_optin_measured`、`default_enable_state` 保持 `disabled`、新增 `implementation_status.real_pass_measured_success` block)。probe `ci/godot_rurix_toolchain_probe.py` 新增 tonemap real-pass enablement 严格审计(subject/checks 22 项/artifact 双向 hash 复核/`patch_stack_identity` 0001..0013/visual 帧 sha+diff 复算/telemetry 校验/三腿 matrix)、manifest 检查 fail-closed 放宽(`grx010_manifest_*_ok` + `grx010_real_pass_measured_success_active` + `grx010_real_pass_success_evidence_conflict`,篡改/缺失/SKIP 回落旧值)、0012/0013 stacked applyability gate(复用 `evaluate_stacked_patch_applyability`)、owner default-enable decision gate(`decision=keep_default_disabled`),next_action 链展开为 `fix_grx010_patch_0012_applyability` → `fix_grx010_patch_0013_applyability` → `provide_grx010_tonemap_real_pass_enablement_success` → `design_grx010_tonemap_real_pass_default_enable_decision` → 双 ready 后 `start_grx011_ssao_blur_godot_patch_0014`。新增 owner 决策文件 `real_pass_default_enable_decision.json`/`.md`(`keep_default_disabled`:无 per-pass FPS 证据、仅 `TONEMAPPER_LINEAR`+sRGB 子集、patch 0013 writeback scaffold + raster-vs-compute seam 未设计)。`ci/godot_rurix_bridge_smoke.py` 纳入 0012/0013 stacked applyability + require_text marker;`ci/godot_rurix_toolchain_probe_validation_failed_test.py` 新增 grx010 close-out 红绿 case(pre-close-out fixture 归一化、篡改 success sha256 报 conflict 且 manifest 新值被拒、SKIP 不推进)。无 FPS、p95、GPU timestamp 或任何性能提升宣称;native Godot tonemapper 仍作 continuation/backstop;不改 `external/godot-master`。 |
| v1.34 | 2026-07-12 | GRX-011 ssao_blur close-out(Wave 2,stage-A5 对等)+ 表驱动 gate 模块框架首用:ssao_blur gated real-pass enablement 取得 strict MEASURED success(`real_pass_enablement_success_evidence.json`,`strict_success=true`:opt-in real dispatch 在 `0001..0016` scratch Godot(Windows D3D12 Forward+,RTX 4070 Ti)上执行且完成、candidate 腿 `RXGD_GODOT_RUNTIME_SSAO_BLUR_REAL_PASS recorded=1`+writeback scaffold marker 入证、checks 全绿含 `native_continuation_writeback_scaffold`/`real_pass_dispatched_and_completed`、`forced_capability_downgrade` 红腿实测 `runtime_binding_preflight_failed`/`unsupported_device`、LDR visual gate max_abs=0/mean_abs=0、measured_local telemetry 通过、`0001..0016` 溯源/日志审计全绿;standalone dispatch smoke CPU parity ~1 ULP)。栈式 patch `0014`(pass-gate+call-site)/`0015`(runtime 资源绑定 `RenderingDevice::get_driver_resource` 真实 `ID3D12Resource*`)/`0016`(默认 false opt-in 三旋钮 + `RXGD_CAP_SSAO_BLUR_REAL_PASS`(1u<<5) + `d3d12-recording-shim` real dispatch path)入栈。**gate 模块工业化**:GRX-011 起每 pass 一个 `ci/grx_gates/grxNNN_*.py` 表驱动 gate 模块(接口 v1 五级 ready:contract/patch/dispatch-smoke/enablement/decision + `first_issue`/`next_action`),`ci/godot_rurix_toolchain_probe.py` 的 `GRX_GATE_SEQUENCE` fail-closed walk——任一 module import 错误/缺 `evaluate`/非 dict/缺键/非空 `first_issue`/false readiness 记 `grx_gate_module_error` 且 `next_action` 不变;`grx011_ssao_blur` gate 的 `_decision_ready` 校验决策文件顶层 `default_enable_decision` 非空,`_enablement_ready` 校验 `strict_success=true`;`real_pass_default_enable_decision.json`/`.md` 落地后 `grx_gate_module_error` 消除、gate 全 ready,probe `next_action` 由 `start_grx011_ssao_blur_godot_patch_0014` 推进到 gate 模块 `NEXT_ACTION=start_grx012_taa_resolve_pass_contract`。manifest 顶层如实翻转(`status=grx011_real_pass_measured_success_default_disabled`、`implemented=true`、`real_gpu_pass=true` opt-in 实测口径、`real_d3d12_dispatch_recorded=true`、`runtime_state=fallback_only_by_default_real_pass_optin_measured`、`default_enable_state` 保持 `disabled`),新增 `implementation_status.real_pass_measured_success` block;owner 决策记 `keep_default_disabled`(无 per-pass FPS 证据、patch 0016 writeback scaffold(native ping-pong continuation 全量重跑、candidate bit-exact、无净收益)、仅 `MODE_SMART` 单遍单 slice 子集)。本 wave 既有 pass 事实收录:GRX-009 luminance patch 0010 原地由 level-0 scaffold 改真实多级 pyramid writeback(`hook_contract_v2`,`levels=pyramid`/`native_continuation=skipped`/`one_frame_latency=1` declared_not_measured,LDR 仍 bit-exact,4h evidence 经 scratch rebuild 重签);GRX-010 tonemap 4h evidence 对新 shim-v2 DLL 重签(writeback 仍 scaffold);shim `rxgd_luminance_record.cpp` 升级参数化执行模型 + `typed_view_format` typeless→typed 视图映射(如 `R8G8_TYPELESS→R8G8_UNORM`)+ `format_is_typeless` fail-closed 守卫;GRX-004b workload v2 evidence 级 baseline(3 次 full 取中位 + 方差留档)随本 wave 落地。新增验证命令:`py -3 ci\grx011_ssao_blur_d3d12_dispatch_smoke.py`、`py -3 ci\grx011_ssao_blur_real_pass_enablement_smoke.py`、`py -3 ci\grx_gates\grx011_ssao_blur.py`。`default_enable_state` 保持 `disabled`,默认 Godot config 下 bridge 对 `RXGD_PASS_SSAO_BLUR` 仍返回 `RXGD_STATUS_FALLBACK`、native SSAO blur 接管,`RXGD_PASS_SSIL_BLUR` 不接线;无 FPS、p95、GPU timestamp 或任何性能提升宣称;luminance/tonemap 证据与 patch 由各自 wave 提交;不改 `external/godot-master`。 |
| v1.35 | 2026-07-12 | GRX-012 taa_resolve + GRX-013 particles_copy 双 close-out(Wave 3,stage-A5 对等,复用 GRX-009/010/011 模板 + 表驱动 gate 框架):两 pass gated real-pass enablement 均取得 strict MEASURED success(两 `real_pass_enablement_success_evidence.json`,`strict_success=true`:opt-in real dispatch 在 `0001..0022` scratch Godot(Windows D3D12 Forward+,RTX 4070 Ti)上执行且完成)。**GRX-012 taa_resolve**:candidate 腿 `RXGD_GODOT_RUNTIME_TAA_RESOLVE_REAL_PASS recorded=1`+writeback scaffold marker,`forced_capability_downgrade` 红腿实测 `runtime_binding_preflight_failed`/`unsupported_device`;**temporal 硬约束(GRX_PLAN DoD)兑现**——`ci/grx012_taa_resolve_real_pass_enablement_smoke.py` 捕获连续 8 帧序列(非单帧截图),逐帧对 reference diff 全 `max_abs=0`(scaffold 逐帧 bit-exact),reference 序列 `nonzero_delta_pairs=7/7` 证携带真实运动;栈式 patch `0017`(pass-gate+call-site,`using_taa` 分支 + `rb->has_texture(taa,history)` 守卫)/`0018`(六 native handle 绑定)/`0019`(默认 false opt-in 三旋钮 + `RXGD_CAP_TAA_RESOLVE_REAL_PASS`(1u<<6) + `d3d12-recording-shim` real dispatch path)入栈。**GRX-013 particles_copy**:`ci/grx013_particles_copy_real_pass_enablement_smoke.py` candidate 腿 `RXGD_GODOT_RUNTIME_PARTICLES_COPY_REAL_PASS recorded=1`+writeback scaffold marker(`dispatch=64x1x1`/`dst_bytes=327680`=4096*80),`forced_capability_downgrade` 红腿实测 `dispatch_eligibility_failed`/`unsupported_device`(particles_copy 无 i64 preflight,forced 降级在下一级 dispatch-eligibility 关口 fail-closed,与纹理 pass 的 preflight 关口不同),LDR visual gate `max_abs=0`;场景 = 确定性 `GPUParticles3D`(fixed seed + fixed_fps,4096 粒子,`TRANSFORM_ALIGN_Z_BILLBOARD` 触发 cull-stage `particles_set_view_axis`,默认 `DRAW_ORDER_INDEX` 保 `do_sort=false` 选中在册 plain COPY_MODE_FILL_INSTANCES 子集);栈式 patch `0020`/`0021`(两 structured-buffer 绑定)/`0022`(+ `RXGD_CAP_PARTICLES_COPY_REAL_PASS`(1u<<7))入栈。**shim 修复(W2-G 同类)**:`src/rurix-godot/shim/rxgd_luminance_record.cpp` 的 `typed_view_format` 扩充组合深度-模板 typeless 家族(`R32G8X24_TYPELESS`→`R32_FLOAT_X8X24_TYPELESS`、`R24G8_TYPELESS`→`R24_UNORM_X8_TYPELESS`,depth-plane SRV 读格式),否则六资源 TAA record dispatch 在 depth SRV 处 fail-closed 于 "unmapped typeless format"。两 manifest 顶层如实翻转(`implemented=true`、`real_gpu_pass=true` opt-in 实测口径、`real_d3d12_dispatch_recorded=true`、`runtime_state=fallback_only_by_default_real_pass_optin_measured`、`default_enable_state` 保持 `disabled`),各新增 `implementation_status.real_pass_measured_success` block;两 owner 决策 `real_pass_default_enable_decision.json`/`.md` 记 `keep_default_disabled`(无 per-pass FPS 证据、writeback 仍 scaffold 无净收益、仅各自子集覆盖)。gate 模块 `ci/grx_gates/grx012_taa_resolve.py`/`grx013_particles_copy.py` 的 `_contract_ready` 由 segment-A 断言(`implemented=false`/`runtime_state=fallback_only`/`real_gpu_pass=false`)放宽为 close-out 断言(pass_id + `default_enable_state=disabled` fail-closed 不变量 + math_parity + patch-allocation 全文件名引用),`_enablement_ready`/`_decision_ready` 断言与写入证据对齐(`strict_success=true` + `default_enable_decision` 非空),decision+enablement 双 ready 后 probe `next_action` 经 grx011→grx012→grx013 全绿 walk 推进到 `start_grx014_cluster_store_pass_contract`,`grx_gate_module_error` 消除。`ci/godot_rurix_toolchain_probe_validation_failed_test.py` 的 `run_grx_gate_sequence_cases` (1b) 更新为三 gate 全 ready(real walk 3 records、无 module_error、advance 到 `start_grx014_cluster_store_pass_contract`)。新增验证命令:`py -3 ci\grx012_taa_resolve_real_pass_enablement_smoke.py`、`py -3 ci\grx013_particles_copy_real_pass_enablement_smoke.py`、`py -3 ci\grx_gates\grx012_taa_resolve.py`、`py -3 ci\grx_gates\grx013_particles_copy.py`。默认 Godot config 下 bridge 对 `RXGD_PASS_TAA_RESOLVE`/`RXGD_PASS_PARTICLES_COPY` 仍返回 `RXGD_STATUS_FALLBACK`、native 接管;shipping feature-off bridge 仍 fail closed 为 `real_dispatch_path_not_linked`;无 FPS、p95、GPU timestamp、时序稳定性或任何性能提升宣称;不改 `external/godot-master`。 |
| v1.36 | 2026-07-12 | GRX-014 cluster_store close-out(Wave 4)+ GRX-017 telemetry-only 首切片 + print 门控修订与全量重签:新增 `ci/grx014_cluster_store_real_pass_enablement_smoke.py`(三腿 clustered-lights 场景,栈 `0001..0026`,env 前缀 `RURIX_GRX014_CLUSTER_STORE_*`)并取得 strict MEASURED success;patch 0023/0024/0025(cluster_store 三片)与 0026(material_sorting telemetry-only)入栈,PATCH_ALLOCATION §1 收录 0017-0026 在用 + Wave 4 print 门控修订说明;patch 0009/0010/0013/0016/0019/0022 原地修订(per-dispatch instrumentation marker 挂 `dispatch_recording_smoke`,生产路径零 per-dispatch stdout),五个 enablement 冒烟 + probe 4h/grx010 严格审计同步修订(RECORD 断言:reference/forced 禁止、candidate 与 real-pass 耦合;checks 键 `record_marker_absent_all_runs` 拆分),逐级 scratch rebuild 后五冒烟全部重签 strict success,4d/4g 独立重跑刷新;`ci/grx_gates/grx014_cluster_store.py` 注释同步 close-out,validation `run_grx_gate_sequence_cases` (1b) 更新为四 gate 全 ready(零 module_error,advance 到 `start_grx015_gpu_culling_pass_contract`);runner `VALID_PASS_MATRIX_KEYS` 追加 cluster_store 四键 + telemetry 键。新增验证命令:`py -3 ci\grx014_cluster_store_real_pass_enablement_smoke.py`、`py -3 ci\grx_gates\grx014_cluster_store.py`。零性能宣称;不改 `external/godot-master`。 |
| v1.37 | 2026-07-13 | GRX 诚实天花板收官 Phase 2(用户终裁「诚实天花板收官」):§7 新增 close-out 状态更新块并把「当前未完成」改为收官归档口径;§8 记录本行。GRX-023 终局 benchmark(`spike/godot-rurix/bench/rd_native_final_20260713/`:baseline v2.3 geomean 212.74、measured all5 geomean 0.9942、Amdahl 零成本 geomean 硬上限 1.0669x、per-pass µs 归因)、GRX-024 视觉 parity(`bench/grx024_visual_20260713/` + 七 pass `passes/*/rd_native_enablement_success_evidence.json` strict success)、GRX-025 默认启用矩阵(`passes/DEFAULT_ENABLE_MATRIX.md` + `bench/grx025_default_enable_20260713/`:五 rd_native pass engaged-geomean 0.9906~0.9993 全过 ≥0.95 默认启用**成本**门)归档。**G-GRX-5 strict 1.5x 门 NOT MET → 归档为结构性不可达**(1.50 vs Amdahl 1.0669x 硬上限;三阈值 1.5/0.3/0.95、`perf_gate.py`、schema 一律未改未放宽——「归档不可达」非「降门」;≥0.95 是默认启用成本门,与 1.5x geomean 是两个门,不混淆达标)。`ci/godot_rurix_toolchain_probe.py` 加 close-out 覆盖:读 `spike/godot-rurix/passes/GRX_MILESTONE_CLOSEOUT.json`(status closed + `performance_claim=none` + evidence 引用齐备)后把 `next_action` 推进到 `grx_milestone_closed_ceiling_archived`(marker 缺失/篡改则回落 gate frontier `start_grx015_gpu_culling_pass_contract`);`ci/godot_rurix_toolchain_probe_validation_failed_test.py` 新增 marker 红绿(present→terminal、absent→frontier)fixture,`walk_grx_gate_sequence` (1b) 诚实 walk 断言不变(仍在 grx015 frontier 停,close-out 覆盖在 walk 之外)。GRX_CONTRACT §8.1 close-out 签署段草案就绪(签署行占位由主会话 close-out commit 生效);per-pass 终态见 GRX_PLAN §8 收官终态总表。全程 `performance_claim=none`,不写「显著提升已达成」;不改 `external/godot-master`。 |
