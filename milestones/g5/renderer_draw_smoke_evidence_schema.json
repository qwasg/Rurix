{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "渲染器 draw 派发桥冒烟证据 schema(G5.2-B,步骤 83;G-G5-4;RFC-0016 章 B)",
  "description": "ci/renderer_draw_smoke.py 写 evidence/renderer_draw_smoke_<ts>.json。host 段(恒跑,无 GPU):rurix-rt render_exec host 单测(pipeline cache/屏障表/set0 约定/校验拒绝例/FFI 布局锚)全过。device 段(gate real,Vulkan 在位;SKIP=dev-env-degrade,RURIX_REQUIRE_REAL=1 翻硬红):rurix-rt --features vulkan render_exec 含 4 项 device 真跑(三角形真 draw 像素断言/compute 写 buffer/raster→compute 混合/能力探测,RTX 4070 Ti,validation 零报错)。镜像 g4 体例。",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "subject",
    "milestone",
    "step",
    "host_section_pass",
    "device_section_rc",
    "checks",
    "run_url",
    "timestamp"
  ],
  "properties": {
    "schema_version": { "type": "integer", "const": 1 },
    "subject": { "type": "string", "const": "renderer_draw_smoke" },
    "milestone": { "type": "string" },
    "step": { "type": "integer", "const": 83 },
    "host_section_pass": { "type": "boolean" },
    "device_section_rc": { "type": "integer" },
    "checks": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "render_exec_host_pass": { "type": ["boolean", "string"] },
        "render_exec_host_count": { "type": ["integer", "boolean", "string"] },
        "render_exec_device_pass": { "type": ["boolean", "string"] },
        "render_exec_device_count": { "type": ["integer", "boolean", "string"] },
        "render_exec_device_tests_present": { "type": ["boolean", "string"] }
      }
    },
    "toolchain_skip": { "type": ["string", "null"] },
    "dev_env_degrade": { "type": "boolean" },
    "run_url": { "type": "string" },
    "timestamp": { "type": "string" }
  }
}
