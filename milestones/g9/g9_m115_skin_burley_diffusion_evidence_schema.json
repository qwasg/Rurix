{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "G9.5 M115 skin_burley_diffusion evidence",
  "description": "evidence/g9_m115_skin_burley_diffusion_<UTC>.json(门 evidence,含 symbolic_gate_key 面;harness 直出件无该面,路由分派 g9_world_harness 共享 schema)。G9.5 P1 硬门 g9.p1.m115.skin_burley_diffusion(G9_CONTRACT §8.1 裁决① P1 全进):Burley normalized diffusion 屏单 pass separable SSS(颜色/深度双 kernel)对拍 golden;扩散 profile(RGB 三通道 falloff)资产化侧表通道;pre-integrated LUT 低端回退档与主档画质差纳入 golden 对照;profile 全零衰减注入必须退化纯漫反射(否则 RED);触 MaterialClosure 32B 冻结面经 RFC-0025 §4.L 🔒 显式修订行(资产化侧表扩展通道按材质槽 ID 索引,32B 定长布局/flags 位段 0-byte,缺省侧表 ≡ 既有材质输出逐位不变,禁静默扩)。host 纯 host 确定性门(device_section_state=not_applicable,按 harness evidence 实记 host-only 口径):host 段 display::skin 4 单测逐名锚定 + conformance display_pipeline M115 双件语料锚 + 冻结带 g9_m115_skin_band.json provenance 机器核验(含 closure_32b_layout_digest);harness 段持锁 g9_m115_skin 全档真跑(13 判据闭集全真)+ --red-arm zero-falloff-not-diffuse/slot-overreach 子模式独立复跑抽检。",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "subject",
    "symbolic_gate_key",
    "matrix_row",
    "milestone",
    "assertion_id",
    "status",
    "wave",
    "numeric_step",
    "source_ref",
    "base_commit",
    "host_section_pass",
    "device_section_state",
    "checks",
    "commands",
    "evidence_level",
    "run_url",
    "timestamp",
    "environment",
    "notes"
  ],
  "properties": {
    "schema_version": { "type": "integer", "const": 1 },
    "subject": { "type": "string", "const": "g9_m115_skin_burley_diffusion" },
    "symbolic_gate_key": { "type": "string", "const": "g9.p1.m115.skin_burley_diffusion" },
    "matrix_row": { "type": "string", "const": "M115" },
    "milestone": { "type": "string", "const": "M115" },
    "assertion_id": { "type": "string", "const": "g9.p1.m115.skin_burley_diffusion" },
    "status": { "type": "string", "enum": ["pass", "fail"] },
    "wave": { "type": "string", "const": "G9.5" },
    "numeric_step": { "type": "integer", "const": 160 },
    "source_ref": {
      "type": "string",
      "const": "RFC-0025 §4.F;spec/display_pipeline.md RXS-0373;G9_ACCEPTANCE_MAP §3 M115"
    },
    "base_commit": { "type": "string" },
    "host_section_pass": { "type": "boolean" },
    "device_section_state": {
      "type": "string",
      "enum": ["not_applicable"]
    },
    "checks": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "host_module_tests_anchored",
        "conformance_corpus_anchored",
        "band_provenance_frozen",
        "harness_full_pass",
        "harness_checks_closed_set_green",
        "harness_red_arm_submode_detected"
      ],
      "properties": {
        "host_module_tests_anchored": { "type": "boolean" },
        "conformance_corpus_anchored": { "type": "boolean" },
        "band_provenance_frozen": { "type": "boolean" },
        "harness_full_pass": { "type": "boolean" },
        "harness_checks_closed_set_green": { "type": "boolean" },
        "harness_red_arm_submode_detected": { "type": "boolean" }
      }
    },
    "commands": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["seq", "command", "exit_code"],
        "properties": {
          "seq": { "type": "integer" },
          "command": { "type": "string" },
          "exit_code": { "type": "integer" }
        }
      }
    },
    "evidence_level": { "type": "string", "const": "measured_local" },
    "run_url": { "type": "string" },
    "timestamp": { "type": "string" },
    "environment": {
      "type": "object",
      "additionalProperties": false,
      "required": ["os", "python_version", "cargo_version", "rustc_version"],
      "properties": {
        "os": { "type": "string" },
        "python_version": { "type": "string" },
        "cargo_version": { "type": "string" },
        "rustc_version": { "type": "string" }
      }
    },
    "notes": { "type": "string" }
  }
}
