# G9 CI_GATES — UE5 级渲染器与物理引擎正式建造期机器门

> 契约：[G9_CONTRACT.md](G9_CONTRACT.md) · 计划：[G9_PLAN.md](G9_PLAN.md) v1.1 · 能力矩阵：[G9_CAPABILITY_MATRIX.md](G9_CAPABILITY_MATRIX.md) v1.0。
> 当前状态（v1.0，2026-08-09）：**G9.1 governance-only，G9.2+ blocked**（`implementation_status: blocked`）。本文 §4 的 15 个 P0 key 当前全部未 materialize——脚本、schema、workflow 步骤一件未落；任何「G9.2 开工」叙述都不得当作 PASS。治理 validator 落地后必须诚实输出 `BLOCKED`，直到 §5 互锁六条件同时为真。

---

## 1. 互锁与编号纪律

### 1.1 实现互锁

稳定治理 validator 名为 `ci/check_g9_implementation_interlock.py`，属于 `check_*` 类未编号守卫。其实现后必须读取事实源并逐项输出：

1. `milestones/g8/G8_CONTRACT.md` §8.26 的有效 status 是否为 `closed`（flip commit `b4189e79`）；G9.0 不可变 ref `1d9460a1` 是否已登记；
2. RFC-0022/0023/0024 是否均 Agent Approved；
3. `G9_CANDIDATE_DECISIONS.md` 是否无空行，M52→M108 / M61→M109 的 strategic_override 是否已登记 `registry/deferred.json` history（只追加）；
4. 用户 G9.2 开工指令是否留痕；workflow 与 ledger 的实际末号/`next_free` 是否一致。

全假或任一为假时 `BLOCKED` 是唯一正确结论；禁止把 `--expect-blocked` 一类测试模式当成互锁 PASS——它只能证明 validator 能识别阻断。G9.2 起每个实现 PR 必须把 `--require-ready` 作为前置 required check。互锁全绿后才允许 `src/`/`spec/`/`conformance/` 改动，且 spec 条款 PR 先于实现 PR（G9_PLAN §5 G9.2 实现门）。

### 1.2 数字步骤延迟分配

- G9 的稳定身份是本文件中的 `symbolic_gate_key` 与 `script`。所有未来编号栏统一写 **`post-interlock actual-next-free allocation`**。
- 只有 §5 互锁全绿后，才可同时读取 `.github/workflows/pr-smoke.yml` 与 `registry/number_ledger.json`，按合入时实际 `next_free` 给即将 materialize 的脚本分配数字步骤，并在同一 PR 追加 ledger 校准。
- **禁止沿用 `design/` 草案的建议编号区间**（如 D3 §⑨ RXS-0322 起与 G8 M50 实际消费段 RXS-0322~0327 冲突，R-G9-7）；一切编号（RXS/RD/U/RX/CI step/RFC）以领取时实测 `next_free` 为准。RFC-0022/0023/0024 已按 2026-08-09 实测 `namespaces.RFC next_free=22` 顺位领取。
- 不创建“预留” workflow step、空 YAML job、空脚本、永远 PASS 的 schema 壳或注释占位。脚本 + RED/GREEN 自检 + schema + workflow 真步骤 + ledger 校准同一实现 PR 落。

## 2. 既有守卫与 0-byte 边界

G9.1 可运行且不得改弱的既有守卫：

```text
cargo fmt --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
py -3 ci/check_number_ledger.py
py -3 ci/check_schemas.py
py -3 ci/check_structure.py
py -3 ci/check_guardrails.py <g8-close-ref-or-pr-base>
py -3 ci/check_contribution.py
py -3 ci/trace_matrix.py --check
py -3 ci/budget_eval.py
```

G8 已 materialize 的全部数字 CI 步骤判据 0-byte 只增；G8 四件套/决策表/evidence schema/budget 0-byte，G8 closed 判据不回写。G9.1 不改 `.github/workflows/pr-smoke.yml`，也不以文档表格冒充 workflow 接线；spec/conformance/registry 在 G9.1 期 0-byte（registry 登记/翻转/history 追加归立项治理动作，与本文件无关）。

## 3. G9.1 governance-only 机器门

| Symbolic gate key | 稳定脚本/检查 | 数字步骤 | 判据 |
|---|---|---|---|
| `g9.gov.structure` | 既有 `ci/check_structure.py` + `ci/check_schemas.py` | 不编号（`check_*`） | CONTRACT/CI/decision/map/RFC 结构一致；map 中预定 schema 名唯一，实际 schema 只与对应脚本同 PR 落，不预建空壳 |
| `g9.gov.number_isolation` | 既有 `ci/check_number_ledger.py` | 不编号（`check_*`） | RFC-0022~0024 claim 与既有命名空间隔离；RXS/RD/U/RX/数字 CI 零推测 claim、零草案建议号沿用 |
| `g9.gov.implementation_interlock` | `ci/check_g9_implementation_interlock.py` | 不编号（`check_*`） | 当前应诚实报 `BLOCKED`；仅 §5 互锁六条件全绿时才输出 READY receipt |
| `g9.gov.acceptance_coverage` | `ci/check_g9_acceptance_map.py` | 不编号（`check_*`） | 15 个 P0 key/script/schema/check 双向全覆盖；MAP §2 / CONTRACT 验收章 / 本文 §4 三向逐字一致；候选决策表无缺行 |
| `g9.gov.measured_baseline` | 既有 `ci/budget_eval.py` + `ci/check_g9_budget_baseline.py` | 不编号（`check_*`） | `g9_budget.json` 非空 measured_local、零 estimated（P-09），counter 与 evaluator 同步；当前不得声称实现性能通过 |

这些 validator 可以在 G9.1 落地，但不得带数字“步骤 NN”，也不得把 G9.2 目标脚本接进 workflow。

## 4. 15 个 P0 独立机器断言

下表的 key 与脚本名冻结，与 [G9_ACCEPTANCE_MAP.md](G9_ACCEPTANCE_MAP.md) §2 逐字一致，由 `ci/check_g9_acceptance_map.py` 三向比对强制。每一行均须独立 evidence subject 和独立结果；同一 workflow 进程可以顺序调用多个脚本，但任一行 `FAIL`、`SKIP` 或 `DEV_ENV_DEGRADE` 都必须保持可见，聚合结果不得 PASS。`numeric_step` 一律为 `post-interlock actual-next-free allocation`。

| symbolic_gate_key | M## | 最晚波次 | script | 判据摘要 |
|---|---:|---|---|---|
| `g9.p0.m90.cluster_dag_deepening` | M90 | G9.2 | `ci/g9_cluster_dag_deepening_smoke.py` | DAG 误差单调逐边核验 + 双构建字节一致 + 破坏单调性 fixture 被拒 |
| `g9.p0.m91.page_format_v2_abi` | M91 | G9.2 | `ci/g9_page_format_v2_abi_smoke.py` | 页格式 v2 编解码往返无损 golden + M04 v1 0-byte 兼容 + 篡改 digest 页被拒 |
| `g9.p0.m102.dgc_abstraction` | M102 | G9.2 | `ci/g9_dgc_abstraction_smoke.py` | DGC token 限制装配期 fail-closed + layout 违规声明被拒 + 缺 capability 禁模拟 |
| `g9.p0.m103.descriptor_global_table` | M103 | G9.2 | `ci/g9_descriptor_global_table_smoke.py` | 全局 descriptor 索引与 shader 实际索引双向精确相等 + ≥65536 条目出图正确 |
| `g9.p0.m104.accesskind_indirect_edge` | M104 | G9.2 | `ci/g9_accesskind_indirect_edge_smoke.py` | 新 AccessKind 边 barrier 推导 golden + 漏声明 indirect 读边装配期 strict 拒 |
| `g9.p0.m121.physics_particle_view` | M121 | G9.2 + G9.6 | `ci/g9_physics_particle_view_smoke.py` | 五域 adapter 全实现 + 写路径仅 impulse/force + M68 journal 迁移无损（双 phase） |
| `g9.p0.m122.gameplay_field` | M122 | G9.2 + G9.6 | `ci/g9_gameplay_field_smoke.py` | 过滤默认空匹配零影响断言 + persistent 全 journal replay hash 一致（双 phase） |
| `g9.p0.m93.visible_cluster_set` | M93 | G9.3 | `ci/g9_visible_cluster_set_smoke.py` | selection cut 无重叠无空洞 + 未驻留页父簇兜底 + 空洞注入 RED |
| `g9.p0.m94.clas_rt_convergence` | M94 | G9.3 | `ci/g9_clas_rt_convergence_smoke.py` | CLAS 腿与回退腿逐命中一致 + 可见集/BLAS 错开一簇即 RED + 静态帧零 AS 构建 |
| `g9.p0.m95.single_source_truth` | M95 | G9.3 | `ci/g9_single_source_truth_smoke.py` | 蒙皮簇 VisBuffer SW/HW diff=0 + 旁路单源真相 variant provenance RED |
| `g9.p0.m96.path_tracer_reference` | M96 | G9.4 | `ci/g9_path_tracer_reference_smoke.py` | 固定 seed 位级一致 + pbrt-v4 收敛曲线容差带 + 改 seed/跳 RR/关 MIS 三臂 RED；门序前置 |
| `g9.p0.m97.surface_cache` | M97 | G9.4 | `ci/g9_surface_cache_smoke.py` | Card 空洞漏光检测臂 RED 有效 + 只丢能量不漏光断言 + 按匹配深度对 M96 golden |
| `g9.p0.m98.tracing_fallback_chain` | M98 | G9.4 | `ci/g9_tracing_fallback_chain_smoke.py` | 四级命中率/耗时计数非空 + 逐级强关回归可检测 + 禁静默回退 |
| `g9.p0.m110.world_partition` | M110 | G9.5 | `ci/g9_world_partition_smoke.py` | 预算违约注入必排队降级 + hitch p99 soak + cell 事件序列逐字 golden |
| `g9.p0.m118.display_pipeline_view_transform` | M118 | G9.5 | `ci/g9_display_pipeline_view_transform_smoke.py` | 四插件逐一 golden + 非 HDR 交换链携带 PQ 输出即 RED；设备标定未触发 SKIP 不充绿 |

> **单一命名空间**：本文件、`G9_CONTRACT.md` 验收章、`G9_ACCEPTANCE_MAP.md` §2/§3 与 RFC-0022~0024 必须引用同一份 key/脚本；`g9.p{0,1}.m##.<slug>` + `ci/g9_<slug>_smoke.py` 为唯一合法形态，由 `ci/check_g9_acceptance_map.py` 三向比对强制。

## 4A. 已 go P1 独立机器断言（G9.3 波四行 + G9.4 波三行 + G9.5 波九行 + G9.6 波三行）

G9.3 波 P1 全进裁决（[G9_CONTRACT.md](G9_CONTRACT.md) §8.1 裁决①）只追加登记：下表四行与 [G9_ACCEPTANCE_MAP.md](G9_ACCEPTANCE_MAP.md) §3 逐字一致，由 `ci/check_g9_acceptance_map.py` 双向比对强制（§4 P0 三向比对 0-byte 不改弱）。`numeric_step` 一律为 `post-interlock actual-next-free allocation`，待各门脚本/schema/workflow 步骤 materialize 时按 §1.2 纪律落盘实测回填；本节不预建空脚本、空 schema 壳或占位 workflow 步骤。G9.4 波 P1 全进裁决（同一裁决①）第二批只追加登记 M99/M100/M101 三行（2026-08-12，与 MAP §3 同一双向比对强制；M99 世界级 clipmap 未 measured 举证、M100 高档 ReSTIR workload 证据不足，两分项 not-triggered 不充绿，RD-040 校准见 G9_CANDIDATE_DECISIONS v1.3 注）。G9.5 波 P1 全进裁决（同一裁决①）第三批只追加登记 M111~M120 九行（2026-08-12，与 MAP §3 同一双向比对强制；承接面 = G9_PLAN §2 G9.5 顺位行 + D4 草案 D1~D17 + RFC-0025 §4 语义面——D4 无伞形 RFC 缺口经起草最小伞形 Full RFC-0025 处置，M114/M115 触 `MaterialClosure` 32B 经其 §4.L 🔒 显式修订行前置登记；M114 strand 档依赖 M120 精确档 benchmark 数据不足，分项 not-triggered 不充绿〔承接锚「M120 精确档数据落地后重判，兜底 G9.7 穷举」〕；M120 仅测量不定档；deferred.json 0-byte——无整项 defer，沿 M99/M100 先例）。G9.6 波 P1 全进裁决（同一裁决①）第四批只追加登记 M124/M125/M126 三行（2026-08-13，与 MAP §3 同一双向比对强制；承接面 = G9_PLAN §2 G9.6 波次段 + RFC-0024 §4.D/§4.E 语义面——M124 浮力走 Field 通道禁旁路 API、M125 Jolt 5.6 A/B 七步程序两臂诚实登记、M126 Rapier 基准同场景同输入同 determinism 画像 + RD-044 字面不变；**M123 双通道判档 = no-go 不充绿**——判档硬前置 Jolt 单线程成本 measured 未满足，维持 M75 no-go 留档不入表，承接锚 G9.7 穷举，RFC-0024 v1.1 修订行落定）。

| symbolic_gate_key | M## | 最晚波次 | script | 判据摘要 |
|---|---:|---|---|---|
| `g9.p1.m92.gpu_skinning_lod_update` | M92 | G9.3 | `ci/g9_gpu_skinning_lod_update_smoke.py` | GPU 蒙皮输出与 host 参照逐顶点一致 + 保守包围体含全部蒙皮顶点 + 距离分级更新率档位切换确定性 + 蒙皮簇 AS 更新计数非空 |
| `g9.p1.m105.command_build_node` | M105 | G9.3 | `ci/g9_command_build_node_smoke.py` | command build node 全链路零 CPU 回读（readback_counter=0 断言）+ 构建产物与 host 参照逐字节一致 |
| `g9.p1.m106.execution_set_pso` | M106 | G9.3 | `ci/g9_execution_set_pso_smoke.py` | Execution Set 与 PSO 衔接出图正确 + 失效重建确定性 + capability 缺失 fail-closed |
| `g9.p1.m107.shader_library_ir_link` | M107 | G9.3 | `ci/g9_shader_library_ir_link_smoke.py` | IR 链接 interface hash 双构建相等 + 变体预算超限硬失败 RED 臂 |
| `g9.p1.m99.spg_radiance_cache` | M99 | G9.4 | `ci/g9_spg_radiance_cache_smoke.py` | 屏幕级 SPG 自适应细分 + Radiance Cache 双级产物 digest golden + 关 product IS 方差回归可检测 RED；世界级 clipmap 未举证 not-triggered 不充绿 |
| `g9.p1.m100.multi_light_low` | M100 | G9.4 | `ci/g9_multi_light_low_smoke.py` | 低档多灯直接光默认档 golden + 验证射线零跳过偏置门 RED + 高档 ReSTIR 证据不足 not-triggered 不充绿 |
| `g9.p1.m101.if_tier_ladder` | M101 | G9.4 | `ci/g9_if_tier_ladder_smoke.py` | IF L0~L3 共享 probe 着色/八面体编码内核只换空间索引 + 每档 AS 更新预算行消费 AsStats + 超预算强制降档 RED |
| `g9.p1.m111.hlod_baking` | M111 | G9.5 | `ci/g9_hlod_baking_smoke.py` | HLOD 离线烘焙按 Component 分发产物即资产 + 双构建 hash 相等 + 运行时零合并断言 RED + screen-size 互斥切换 golden |
| `g9.p1.m112.atmosphere_froxel` | M112 | G9.5 | `ci/g9_atmosphere_froxel_smoke.py` | Froxel 统一基础设施云雾共用断言 + 雾/云前端 golden + weather map 篡改签名拒录 RED + 预算字段逐帧 evidence |
| `g9.p1.m113.water_dual_pipeline` | M113 | G9.5 | `ci/g9_water_dual_pipeline_smoke.py` | 大洋 IFFT/浅水波方程双管线分离断言 + IFFT 三贴图与 host FFT 参考对拍 + 非法谱参数资产拒录 RED + 浮力接口预留不实现 |
| `g9.p1.m114.hair_marschner` | M114 | G9.5 | `ci/g9_hair_marschner_smoke.py` | Marschner R/TT/TRT 逐瓣 golden + 单瓣置零无差异 RED + 股替换烘焙确定性；strand 档依赖 M120 精确档数据不足 not-triggered 不充绿 |
| `g9.p1.m115.skin_burley_diffusion` | M115 | G9.5 | `ci/g9_skin_burley_diffusion_smoke.py` | Burley 屏单 pass 对拍 golden + 扩散 profile 资产化 + 全零衰减未退化纯漫反射 RED + 触 32B 经 RFC-0025 §4.L 修订行 |
| `g9.p1.m116.terrain_chunk_cell` | M116 | G9.5 | `ci/g9_terrain_chunk_cell_smoke.py` | 地形 chunk ≡ cell 禁第二套分格 + 全 compute LOD/剔除/缝合 + 零 SVT 依赖断言 + 邻级 LOD 差>1 注入裂缝 RED |
| `g9.p1.m117.decal_dbuffer` | M117 | G9.5 | `ci/g9_decal_dbuffer_smoke.py` | DBuffer 三通道帧图设计期占位断言 + screen-space cluster 化受界 + 前向回退档语义等价 golden + 超界注入降级 RED |
| `g9.p1.m119.post_processing_skeleton` | M119 | G9.5 | `ci/g9_post_processing_skeleton_smoke.py` | 后处理骨架显式排序（曝光/bloom/tonemap/LUT/输出变换）+ 全程 HDR 线性域 + 隐式 SDR clamp 注入 RED + 曝光状态帧间持久 |
| `g9.p1.m120.oit_benchmark_harness` | M120 | G9.5 | `ci/g9_oit_benchmark_harness_smoke.py` | OIT benchmark harness 七算法对照 evidence 非空 + 仅测量不定档 + 无 benchmark 数据选型提交判 RED + 排序 fallback 永保留 |
| `g9.p1.m124.buoyancy_field_channel` | M124 | G9.6 | `ci/g9_buoyancy_field_channel_smoke.py` | 解析浮力走 Field 通道（persistent field + Buoyancy 语义）+ 旁路 API 注入即 RED + 细长/翻滚 corpus fixture + capture→replay 逐 tick hash + 变帧率逐位一致 |
| `g9.p1.m125.jolt_56_ab_evaluation` | M125 | G9.6 | `ci/g9_jolt_56_ab_evaluation_smoke.py` | RFC-0021 §4.A4 七步程序逐字 + 5.6 独立 vendor 并存不覆盖 5.3 基线 + 新摩擦模型重点实测 + GPU compute 只评估不接权威 + 两臂诚实登记禁伪绿 |
| `g9.p1.m126.rapier_benchmark_ab` | M126 | G9.6 | `ci/g9_rapier_benchmark_ab_smoke.py` | 同场景同输入同 determinism 画像 A/B 夹具 + measured 报告含确定性偏差统计 + 基准不作 replay oracle + RD-044 字面不变 |

## 5. G9.2 互锁

`G9.GOV.G9_2.ENTRY_INTERLOCK` 六条件与判据字面见 [G9_ACCEPTANCE_MAP.md](G9_ACCEPTANCE_MAP.md) §5（G8 closed + G9.0 不可变 ref 登记 + 三 RFC Agent Approved + 决策表无空行且 M52/M61 override 已登记 deferred history + 数字步骤按互锁后 actual next_free 分配 + 用户 G9.2 开工指令留痕）。互锁未输出 READY 前：禁止合入 G9.2 的 `spec/`、`conformance/`、`src/` 或 workflow 实现改动；禁止 claim 任何数字 CI step；spec-first + RED 先行自互锁通过后才启动。

## 6. 修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-09 | G9.1 初版：冻结治理/实现双门、15 个 P0 独立 key 与脚本（与 G9_ACCEPTANCE_MAP §2 逐字一致）；`g9.gov.*` 五个 governance-only 机器门全不编号；全部 numeric_step 延迟为 `post-interlock actual-next-free allocation`；禁沿用 design/ 草案建议编号区间；零 workflow/script/schema 预放，当前实现门诚实 blocked。 |
| v1.1 | 2026-08-09 | G9.2 实现波 agent D（M121+M122 骨架期）：`g9.p0.m121.physics_particle_view` → 步骤 **136**（`ci/g9_physics_particle_view_smoke.py --gate … --phase g9.2`，host 门）、`g9.p0.m122.gameplay_field` → 步骤 **137**（`ci/g9_gameplay_field_smoke.py --gate … --phase g9.2`，host 门）按 §1.2 互锁后 actual-next-free 纪律落盘时实测领取（ledger next_free=131，领 136/137，与蜂群预分配计划一致；A/B/C 同波领号以各自落盘实测为准，撞号顺位记 revision_log）。同 PR 落 evidence schema 双件 + check_schemas 三处纯追加 + 新建 apps/g9-physics-gates harness（G8 门 0-byte：g8-physics-gates 字面不动）+ conformance/physics/particle_view/、conformance/physics/field/ 语料。双 phase 纪律：骨架期 `phase_g9_6_pass` 恒 false 不充绿，`--phase g9.6` 调用诚实非零退出。§4 表 M121/M122 行 key/脚本字面不动。 |
| v1.2 | 2026-08-10 | G9.2 波聚合门: 数字步骤按互锁后实测  顺位 materialize 为 **步骤 138**( G9.2 wave2.exit aggregate;脚本  薄壳 + ,evidence schema ;七 P0 subject 只读汇总 + RFC-0022/0023/0024 Approved + RD-039/040 维持 open;M121/M122 骨架期 phase_g9_6_pass=false 不充绿); 由  转为实测 138(落盘前实测 CI_step.next_free=138 顺位;agent A 132/133、B 131、C 134/135、D 136/137 已落满 131~137)。 |
| v1.2 | 2026-08-09 | G9.2 M102 实现波：`g9.p0.m102.dgc_abstraction` 数字步骤按互锁后实测 `next_free` 顺位 materialize 为 **步骤 131**（`pr-smoke.yml` G9.2 M102 DGC abstraction smoke，device 门 env 双置 `RURIX_REQUIRE_REAL=1` + `RURIX_VK_VALIDATION=1`；脚本 `ci/g9_dgc_abstraction_smoke.py`，evidence schema `milestones/g9/g9_m102_dgc_abstraction_evidence_schema.json`，13 checks 双闭集）；`numeric_step` 由 `post-interlock actual-next-free allocation` 转为实测 131(与 agent D 的 136/137 合并;M102 实领 131)；实现面 = `src/rurix-rt/src/dgc.rs`（IndirectCmdLayout token 闭集装配期核验 + DgcBuffer 无 host 读接口结构性断言 + 三后端映射 + capability snapshot 阻塞性前置）+ `src/rurix-rt/src/vk.rs` DGC FFI 段（U54，`VK_EXT_device_generated_commands`）+ `src/rurixc/src/capability_check.rs` 闭集加性两位实位（RXS-0349）+ `src/rurix-rt/src/bin/vk_dgc.rs` device 最小链路。 |
| v1.3 | 2026-08-10 | G9.2 实现 agent A 双 P0 门落地（只追加）：步骤 132（`g9.p0.m90.cluster_dag_deepening`，host 纯 host 门 `ci/g9_cluster_dag_deepening_smoke.py`）+ 步骤 133（`g9.p0.m91.page_format_v2_abi`，host+device 门 `ci/g9_page_format_v2_abi_smoke.py`，env 双置 `RURIX_REQUIRE_REAL=1` + `RURIX_VK_VALIDATION=1`）插入 `.github/workflows/pr-smoke.yml` 步骤 131 块后、步骤 134 块前；evidence schema 双闭集（`milestones/g9/g9_m90_cluster_dag_deepening_evidence_schema.json` device_section_state=not_applicable / `g9_m91_page_format_v2_abi_evidence_schema.json` enum 含 executed）与 `ci/check_schemas.py` 前缀路由（`g9_m90_cluster_dag_deepening_` / `g9_m91_page_format_v2_abi_`）同 PR 落；ledger CI_step on_tree_max 137→133 / next_free 138→134（v1.48，落盘前实测 next_free=138 撞号顺位领取 132/133）。§4 表体冻结不动，本行只追加。 |
| v1.4 | 2026-08-11 | G9.3 波 P1 全进裁决只追加登记（G9_CONTRACT §8.1 裁决①）：新增 **§4A 已 go P1 独立机器断言表**四行——`g9.p1.m92.gpu_skinning_lod_update` / `g9.p1.m105.command_build_node` / `g9.p1.m106.execution_set_pso` / `g9.p1.m107.shader_library_ir_link`（最晚波次均 G9.3，与 G9_ACCEPTANCE_MAP §3 逐字一致，`ci/check_g9_acceptance_map.py` MAP §3 ↔ 本文 §4A 双向比对强制）；numeric_step 一律 `post-interlock actual-next-free allocation` 待 materialize 实测回填，零脚本/schema/workflow 预放。§4 表体 0-byte；G5~G8 closed 判据 0-byte。 |
| v1.5 | 2026-08-12 | G9.3 实现波七 CI 门 materialize（只追加）：数字步骤按 §1.2 互锁后 actual-next-free 纪律落盘前实测 `CI_step.next_free=139` 顺位领取 **139~145**——§4 P0 三行 `g9.p0.m93.visible_cluster_set` → 步骤 **139**（`ci/g9_visible_cluster_set_smoke.py`，host 纯 host 门）、`g9.p0.m94.clas_rt_convergence` → 步骤 **140**（`ci/g9_clas_rt_convergence_smoke.py`，host+device 门，env 双置 `RURIX_REQUIRE_REAL=1` + `RURIX_VK_VALIDATION=1`）、`g9.p0.m95.single_source_truth` → 步骤 **141**（`ci/g9_single_source_truth_smoke.py`，host+device 门，env 双置）；§4A P1 四行 `g9.p1.m92.gpu_skinning_lod_update` → 步骤 **142**（`ci/g9_gpu_skinning_lod_update_smoke.py`，host+device 门，env 双置）、`g9.p1.m105.command_build_node` → 步骤 **143**（`ci/g9_command_build_node_smoke.py`，host+device 门，env 双置）、`g9.p1.m106.execution_set_pso` → 步骤 **144**（`ci/g9_execution_set_pso_smoke.py`，host+device 门，env 双置）、`g9.p1.m107.shader_library_ir_link` → 步骤 **145**（`ci/g9_shader_library_ir_link_smoke.py`，host 纯 host 门）。同批落：七 evidence schema 双闭集（`milestones/g9/g9_m{93,94,95,92,105,106,107}_*_evidence_schema.json`，沿 M102 体例 const 钉死；M93/M107 `device_section_state=not_applicable`）+ `ci/check_schemas.py` 三处纯追加（load/validator/前缀路由，与既有 g9_* 全族互不包含）+ `.github/workflows/pr-smoke.yml` 步骤 138 块后追加七步骤（注释块含「落盘前实测 CI_step.next_free=139 顺位领取」留痕）。numeric_step 由 `post-interlock actual-next-free allocation` 转为实测真号（ledger v1.85：CI_step on_tree_max 138→145 / next_free 139→146）。§4/§4A 表体 0-byte；G5~G8 closed 判据与 G9.2 门脚本 0-byte；零新 RXS/RD/U/SG/RX_error/MR/D/RFC。 |
| v1.6 | 2026-08-12 | G9.3 波聚合门 `g9.wave.3.exit` materialize（只追加）：数字步骤按 §1.2 纪律落盘前实测 `CI_step.next_free=146` 顺位领取 **步骤 146**（`ci/g9_wave3_exit_check.py --gate g9.wave.3.exit`，薄壳聚合 + `ci/g9_wave_exit_lib.py` 复用同构；evidence schema `milestones/g9/g9_wave3_exit_evidence_schema.json`，required_gates minItems=maxItems=7、aggregate_read_only const true、numeric_step const 146）——七门最新 evidence 只读汇总（§4 三 P0：`g9.p0.m93.visible_cluster_set` 步骤 139 / `g9.p0.m94.clas_rt_convergence` 步骤 140 / `g9.p0.m95.single_source_truth` 步骤 141；§4A 四 P1：`g9.p1.m92.gpu_skinning_lod_update` 步骤 142 / `g9.p1.m105.command_build_node` 步骤 143 / `g9.p1.m106.execution_set_pso` 步骤 144 / `g9.p1.m107.shader_library_ir_link` 步骤 145）+ RFC-0022/0023 Agent Approved 字面维持 + RXS-0350~0356 条款头在树（spec/virtual_geometry.md 0350~0353 + spec/gpu_driven_submit.md 0354~0356）+ U56/U57 unsafe 登记在树（unsafe-audit/rurix-rt.md）；聚合不代绿、不重跑 smoke、不设 `RURIX_REQUIRE_REAL`，聚合 PASS 不遮蔽任一子断言 FAIL/SKIP/DEV_ENV_DEGRADE。同 PR 落 `ci/check_schemas.py` 三处纯追加（load/validator/前缀路由 `g9_wave3_exit_`，与 `g9_wave2_exit_` 及 g9_* 全族互不包含，既有路由 0-byte）+ `pr-smoke.yml` 步骤 145 块后追加步骤 146（注释块留痕体例沿步骤 138/145）。ledger v1.86：CI_step on_tree_max 145→146 / next_free 146→147。§4/§4A/§5 表体 0-byte；G5~G8 closed 判据 0-byte；零新 RXS/RD/U/SG/RX_error/MR/D/RFC。 |
| v1.7 | 2026-08-12 | G9.4 波 P1 全进裁决只追加登记（G9_CONTRACT §8.1 裁决①）：§4A 追加三行——`g9.p1.m99.spg_radiance_cache` / `g9.p1.m100.multi_light_low` / `g9.p1.m101.if_tier_ladder`（最晚波次均 G9.4，与 G9_ACCEPTANCE_MAP §3 逐字一致，`ci/check_g9_acceptance_map.py` MAP §3 ↔ 本文 §4A 双向比对强制）；M99 仅屏幕级判 go、M100 仅低档默认判 go（RD-040 条件分项触发举证校准：世界级 clipmap 未 measured 举证、高档 ReSTIR workload 证据不足，两分项 not-triggered 不充绿，G9_CANDIDATE_DECISIONS v1.3 注）、M101 全档判 go；numeric_step 一律 `post-interlock actual-next-free allocation` 待 materialize 实测回填，零脚本/schema/workflow 预放。§4 P0 表体与 §4A 既有四行 0-byte；G5~G8 closed 判据 0-byte；零新 RXS/RD/U/SG/RX_error/MR/D/RFC/CI_step。 |
| v1.8 | 2026-08-12 | G9.4 实现波六 CI 门 materialize（只追加）：数字步骤按 §1.2 互锁后 actual-next-free 纪律落盘前实测 `CI_step.next_free=147` 顺位领取 **147~152**——§4 P0 三行 `g9.p0.m96.path_tracer_reference` → 步骤 **147**（`ci/g9_path_tracer_reference_smoke.py`，host+device 门，env 双置 `RURIX_REQUIRE_REAL=1` + `RURIX_VK_VALIDATION=1`）、`g9.p0.m97.surface_cache` → 步骤 **148**（`ci/g9_surface_cache_smoke.py`，host+device 门，env 双置）、`g9.p0.m98.tracing_fallback_chain` → 步骤 **149**（`ci/g9_tracing_fallback_chain_smoke.py`，host+device 门，env 双置）；§4A P1 三行 `g9.p1.m99.spg_radiance_cache` → 步骤 **150**（`ci/g9_spg_radiance_cache_smoke.py`，host+device 门，env 双置）、`g9.p1.m100.multi_light_low` → 步骤 **151**（`ci/g9_multi_light_low_smoke.py`，host+device 门，env 双置）、`g9.p1.m101.if_tier_ladder` → 步骤 **152**（`ci/g9_if_tier_ladder_smoke.py`，host+device 门，env 双置）。门序机器阻断（D2-Q7 硬约束）：步骤 148~152 五门 `--gate` 模式前置机器核验 `evidence/` 最新 `g9_m96_path_tracer_reference_<UTC>.json` 须 `status=="pass"` 且 `assertion_id=="g9.p0.m96.path_tracer_reference"`（共享小库 `ci/g9_gi_interlock.py`，harness 直出件/他门件不充绿），缺失/非 pass 即门 FAIL 退 1；五门 `--selftest` 均含「M96 evidence 缺失必红」臂。同批落：六 evidence schema 双闭集（`milestones/g9/g9_m{96_path_tracer_reference,97_surface_cache,98_tracing_fallback_chain,99_spg_radiance_cache,100_multi_light_low,101_if_tier_ladder}_evidence_schema.json`，沿 M102 体例 const 钉死 numeric_step/gate key，device_section_state enum 含 executed）+ harness 直出件共享 schema `g9_gi_harness_evidence_schema.json`（identity 三件套 const + checks 键集闭集；守门刚需：实现波 12 件 harness 直出 evidence 在原 gpu fallthrough 路由下红，本波按 `symbolic_gate_key` 有无内容分派门 schema/harness schema 修正路由）+ `ci/check_schemas.py` 三处纯追加（load/validator/前缀路由，与既有 g9_* 全族互不包含）+ `.github/workflows/pr-smoke.yml` 步骤 146 块后追加六步骤（注释块含「落盘前实测 CI_step.next_free=147 顺位领取」留痕）。numeric_step 由 `post-interlock actual-next-free allocation` 转为实测真号（ledger v1.88：CI_step on_tree_max 146→152 / next_free 147→153）。§4/§4A 表体 0-byte；G5~G8 closed 判据 0-byte；零新 RXS/RD/U/SG/RX_error/MR/D/RFC。 |
| v1.9 | 2026-08-12 | G9.4 波聚合门 `g9.wave.4.exit` materialize（只追加）：数字步骤按 §1.2 纪律落盘前实测 `CI_step.next_free=153` 顺位领取 **步骤 153**（`ci/g9_wave4_exit_check.py --gate g9.wave.4.exit`，薄壳聚合 + `ci/g9_wave_exit_lib.py` 复用同构；evidence schema `milestones/g9/g9_wave4_exit_evidence_schema.json`，required_gates minItems=maxItems=6、aggregate_read_only const true、numeric_step const 153）——六门最新 evidence 只读汇总（§4 三 P0：`g9.p0.m96.path_tracer_reference` 步骤 147 / `g9.p0.m97.surface_cache` 步骤 148 / `g9.p0.m98.tracing_fallback_chain` 步骤 149；§4A 三 P1：`g9.p1.m99.spg_radiance_cache` 步骤 150 / `g9.p1.m100.multi_light_low` 步骤 151 / `g9.p1.m101.if_tier_ladder` 步骤 152）+ RFC-0022 Agent Approved 字面维持 + RXS-0357~0362 条款头在树（spec/global_illumination.md）+ 门序机器阻断留痕（M97~M101 五门最新 evidence 均含 `checks.gate_order_m96_passed=true`，D2-Q7 前置机器核验）+ 六冻结带在树（g9_m96_pbrt_tolerance_band / g9_m97_depth_band / g9_m98_depth_band / g9_m99_spg_rc_band / g9_m100_multi_light_band / g9_m101_if_tier_band）；聚合不代绿、不重跑 smoke、不设 `RURIX_REQUIRE_REAL`，聚合 PASS 不遮蔽任一子断言 FAIL/SKIP/DEV_ENV_DEGRADE。同 PR 落 `ci/check_schemas.py` 三处纯追加（load/validator/前缀路由 `g9_wave4_exit_`，与 `g9_wave2_exit_`/`g9_wave3_exit_` 及 g9_* 全族互不包含，既有路由 0-byte）+ `pr-smoke.yml` 步骤 152 块后追加步骤 153（注释块留痕体例沿步骤 146/152）。ledger v1.89：CI_step on_tree_max 152→153 / next_free 153→154。§4/§4A/§5 表体 0-byte；G5~G8 closed 判据 0-byte；零新 RXS/RD/U/SG/RX_error/MR/D/RFC。 |
| v1.10 | 2026-08-12 | G9.5 波 P1 全进裁决只追加登记（G9_CONTRACT §8.1 裁决①）：§4A 追加九行——`g9.p1.m111.hlod_baking` / `g9.p1.m112.atmosphere_froxel` / `g9.p1.m113.water_dual_pipeline` / `g9.p1.m114.hair_marschner` / `g9.p1.m115.skin_burley_diffusion` / `g9.p1.m116.terrain_chunk_cell` / `g9.p1.m117.decal_dbuffer` / `g9.p1.m119.post_processing_skeleton` / `g9.p1.m120.oit_benchmark_harness`（最晚波次均 G9.5，与 G9_ACCEPTANCE_MAP §3 逐字一致，`ci/check_g9_acceptance_map.py` MAP §3 ↔ 本文 §4A 双向比对强制）；承接面按 G9.5 大世界×专项波口径（G9_PLAN §2 G9.5 顺位行 + D4 草案 D1~D17 + RFC-0025 §4）；**D4 伞形 RFC 缺口处置**：G9.5 D4（大世界×专项渲染器×显示管线）无伞形 RFC——Grep 实测 RFC-0016/0019/0022/0023 冻结面与 D4 链路面无重叠，MR（Mini-RFC）体例不承载新语义面 + G5 冻结面修订行（M104 先例 = RFC-0023 §4.4.3），判档向上取严起草最小伞形 Full **RFC-0025**（`rfcs/0025-world-and-specialty-renderers.md`，2026-08-12 实测 RFC next_free=25 顺位领取，D-409 第 1 轮评审〔单实例偏差如实登记 + 效力自限〕后 Agent Approved；M114/M115 触 `MaterialClosure` 32B 经其 §4.L 🔒 显式修订行前置登记——资产化侧表扩展通道，32B 布局 0-byte，禁静默扩）；M114 条件 go（strand 档依赖 M120 精确档 benchmark 数据不足，分项 not-triggered 不充绿，承接锚「M120 精确档数据落地后重判，兜底 G9.7 穷举」）；M115 判 go（触 32B 修订行前置已登记）；M120 判 go（仅测量不定档，evidence 非空）；numeric_step 一律 `post-interlock actual-next-free allocation` 待 materialize 实测回填，零脚本/schema/workflow 预放；deferred.json 0-byte（无整项 defer——M114 strand 档为分项 not-triggered，沿 M99/M100 先例不动 RD）。§4 P0 表体与 §4A 既有七行 0-byte；G5~G8 closed 判据 0-byte；零新 RXS/RD/U/SG/RX_error/MR/CI_step（RFC-0025 消费登记于 ledger v1.90，随 spec-first 同批落）。 |
| v1.11 | 2026-08-13 | G9.5 实现波十一 CI 门 materialize（只追加）：数字步骤按 §1.2 互锁后 actual-next-free 纪律落盘前实测 `CI_step.next_free=154` 顺位领取 **154~164**——§4 P0 两行 `g9.p0.m110.world_partition` → 步骤 **154**（`ci/g9_world_partition_smoke.py`，host 纯 host 确定性门）、`g9.p0.m118.display_pipeline_view_transform` → 步骤 **155**（`ci/g9_display_pipeline_view_transform_smoke.py`，host 纯 host 确定性门）；§4A P1 九行 `g9.p1.m111.hlod_baking` → 步骤 **156**（`ci/g9_hlod_baking_smoke.py`）、`g9.p1.m112.atmosphere_froxel` → 步骤 **157**（`ci/g9_atmosphere_froxel_smoke.py`）、`g9.p1.m113.water_dual_pipeline` → 步骤 **158**（`ci/g9_water_dual_pipeline_smoke.py`）、`g9.p1.m114.hair_marschner` → 步骤 **159**（`ci/g9_hair_marschner_smoke.py`）、`g9.p1.m115.skin_burley_diffusion` → 步骤 **160**（`ci/g9_skin_burley_diffusion_smoke.py`）、`g9.p1.m116.terrain_chunk_cell` → 步骤 **161**（`ci/g9_terrain_chunk_cell_smoke.py`）、`g9.p1.m117.decal_dbuffer` → 步骤 **162**（`ci/g9_decal_dbuffer_smoke.py`）、`g9.p1.m119.post_processing_skeleton` → 步骤 **163**（`ci/g9_post_processing_skeleton_smoke.py`）、`g9.p1.m120.oit_benchmark_harness` → 步骤 **164**（`ci/g9_oit_benchmark_harness_smoke.py`）。十一门全 host 确定性面（按 harness evidence 实记 host-only 口径，`device_section_state=not_applicable`；`RURIX_REQUIRE_REAL=1` 以 host 确定性为准），每门三段判据：host 段（对应模块单测逐名锚定 + conformance accept/reject 语料 `//@ spec:` 锚与门 key 留痕 + golden 冻结带 provenance 机器核验）+ harness 段（持 `gpu_device_lock` 真跑全档，`schema`/`spec_anchor`/`assertion_id`/`status==pass` + harness 判据闭集全真 + `--red-arm` 1~2 子模式独立复跑抽检；门 evidence 落 `evidence/g9_m1**_<UTC>.json` 新文件不覆盖 harness 直出件）+ not-triggered 字段机器核验（M118 HDR 标定 `hdr_calibration.status==not-triggered` 且 `counts_as_green=false` 不否决 SDR 面；M114 strand 档 `strand_tier.status==not-triggered` 不充绿并如实记录 M120 测量可用性；M120 仅测量不定档 `tier_selection.committed==false`）。命名差如实登记：M111/M119 门 key 按 MAP §3 实记（`hlod_baking`/`post_processing_skeleton`），harness 直出件 `assertion_id` 实记字面 `g9.p1.m111.hlod_runtime`/`g9.p1.m119.post_chain`，门 harness 段按 harness 实记字面核验不改写。同批落：十一 evidence schema（`milestones/g9/g9_m{110_world_partition,118_display_pipeline_view_transform,111_hlod_baking,112_atmosphere_froxel,113_water_dual_pipeline,114_hair_marschner,115_skin_burley_diffusion,116_terrain_chunk_cell,117_decal_dbuffer,119_post_processing_skeleton,120_oit_benchmark_harness}_evidence_schema.json`，沿 M102 体例 const 钉死 numeric_step/gate key；`device_section_state` enum 仅 `not_applicable` 如实反映 host-only）+ harness 直出件共享 schema `milestones/g9/g9_world_harness_evidence_schema.json`（G9.5 十一 harness 直出件共享：anyOf 十一支 identity 四件套 const + checks 键集闭集全 boolean；守门刚需——实现波 22 件 harness 直出 evidence 在原 gpu fallthrough 路由下红，本波按 `doc.symbolic_gate_key` 有无内容分派门 schema/harness schema，沿 G9.4 `g9_gi_harness` 先例，evidence/ 只增不删不改纪律维持 0-byte）+ `ci/check_schemas.py` 三处纯追加（load/validator/前缀路由，与既有 g9_* 全族互不包含，既有路由 0-byte）+ `.github/workflows/pr-smoke.yml` 步骤 153 块后追加十一步骤（注释块含「落盘前实测 CI_step.next_free=154 顺位领取」留痕）。numeric_step 由 `post-interlock actual-next-free allocation` 转为实测真号（ledger v1.91：CI_step on_tree_max 153→164 / next_free 154→165；§4/§4A 表体 0-byte 不回写）。G5~G8 closed 判据与 G9.2/G9.3/G9.4 门脚本 0-byte；零新 RXS/RD/U/SG/RX_error/MR/D/RFC；wave5 聚合门归下一批不碰。 |
| v1.12 | 2026-08-14 | G9.5 波聚合门 `g9.wave.5.exit` materialize（只追加）：数字步骤按 §1.2 纪律落盘前实测 `CI_step.next_free=165` 顺位领取 **步骤 165**（`ci/g9_wave5_exit_check.py --gate g9.wave.5.exit`，薄壳聚合 + `ci/g9_wave_exit_lib.py` 复用同构；evidence schema `milestones/g9/g9_wave5_exit_evidence_schema.json`，required_gates minItems=maxItems=11、aggregate_read_only const true、numeric_step const 165）——十一门最新 evidence 只读汇总（§4 两 P0：`g9.p0.m110.world_partition` 步骤 154 / `g9.p0.m118.display_pipeline_view_transform` 步骤 155；§4A 九 P1：`g9.p1.m111.hlod_baking` 步骤 156 / `g9.p1.m112.atmosphere_froxel` 步骤 157 / `g9.p1.m113.water_dual_pipeline` 步骤 158 / `g9.p1.m114.hair_marschner` 步骤 159 / `g9.p1.m115.skin_burley_diffusion` 步骤 160 / `g9.p1.m116.terrain_chunk_cell` 步骤 161 / `g9.p1.m117.decal_dbuffer` 步骤 162 / `g9.p1.m119.post_processing_skeleton` 步骤 163 / `g9.p1.m120.oit_benchmark_harness` 步骤 164；门 key 按 G9_ACCEPTANCE_MAP §2/§3 实记，M111=hlod_baking / M119=post_processing_skeleton 命名差 v1.11 已如实登记）+ RFC-0025 Agent Approved 字面维持 + RXS-0363~0373 条款头在树（spec/world_partition.md 0363~0368 + spec/display_pipeline.md 0369~0373，共 11 枚）+ M115 MaterialClosure 32B 布局 digest 冻结面核验（g9_m115_skin_band.json 冻结值 ↔ spec RXS-0373 修订行断言字面 ↔ harness 直出件 material_closure_32b 机核面〔size=32B/reserved/flags 未分配位全零/revision_line=RFC-0025 §4.L〕三面逐字一致）+ M114 strand 档强制精确 OIT 分项 not-triggered 登记字面（spec RXS-0372 条款头 + 门 evidence checks.not_triggered_field_verified + harness checks.strand_tier_not_triggered_registered/m120_measurements_availability_recorded）+ M120 仅测量不定档字面（spec RXS-0371 条款头 + 门 evidence checks.not_triggered_field_verified + harness tier_selection.committed==false 且 policy 含 NotMeasuredYet）；聚合不代绿、不重跑 smoke、不设 `RURIX_REQUIRE_REAL`，聚合 PASS 不遮蔽任一子断言 FAIL/SKIP/DEV_ENV_DEGRADE。同 PR 落 `ci/check_schemas.py` 三处纯追加（load/validator/前缀路由 `g9_wave5_exit_`，与 `g9_wave2_exit_`/`g9_wave3_exit_`/`g9_wave4_exit_` 及 g9_* 全族互不包含，既有路由 0-byte）+ `pr-smoke.yml` 步骤 164 块后追加步骤 165（注释块留痕体例沿步骤 153/164）。ledger v1.92：CI_step on_tree_max 164→165 / next_free 165→166。§4/§4A/§5 表体 0-byte；G5~G8 closed 判据 0-byte；零新 RXS/RD/U/SG/RX_error/MR/D/RFC。 |
| v1.13 | 2026-08-13 | G9.6 波 P1 全进裁决只追加登记（G9_CONTRACT §8.1 裁决①）：§4A 追加三行——`g9.p1.m124.buoyancy_field_channel` / `g9.p1.m125.jolt_56_ab_evaluation` / `g9.p1.m126.rapier_benchmark_ab`（最晚波次均 G9.6，与 G9_ACCEPTANCE_MAP §3 逐字一致，`ci/check_g9_acceptance_map.py` MAP §3 ↔ 本文 §4A 双向比对强制）；承接面按 G9.6 物理波口径（G9_PLAN §2 G9.6 波次段 + RFC-0024 §4.D/§4.E）：M124 解析浮力走 Field 通道（persistent field + `FieldPhysicsType::Buoyancy`；旁路 API 注入即 RED；细长/翻滚 corpus fixture；capture→replay 逐 tick hash + 变帧率逐位一致；M77 锚承接）、M125 Jolt 5.3→5.6 A/B（RFC-0021 §4.A4 七步逐字；5.6 独立 vendor 并存不覆盖 5.3 基线；新摩擦模型重点实测；GPU compute 只评估不接权威；两臂诚实登记禁伪绿；G8.6a 评估窗锚承接）、M126 Rapier 深造对标基准（同场景同输入同 determinism 画像 A/B 夹具 + measured 报告含确定性偏差统计；基准不作 replay oracle；RD-044 字面不变；M65b 锚承接）；**M123 双通道判档 = no-go 不充绿**（判档硬前置 Jolt 单线程成本 measured 未满足——树内零 measured artifact，本治理/spec 波零 cargo 构建不产测量；维持 M75 no-go 留档，`physics-async-decorative`/`DecorativePhysicsTickId` 维持「仅判档 go 时生效」字面不启用；承接锚 G9.7 穷举；no-go 项不入表——G9_CANDIDATE_DECISIONS v1.5 校准注 + RFC-0024 v1.1 修订行落定；deferred.json 0-byte）；numeric_step 一律 `post-interlock actual-next-free allocation` 待 materialize 实测回填，零脚本/schema/workflow 预放。§4 P0 表体与 §4A 既有十六行 0-byte；G5~G8 closed 判据 0-byte；零新 RXS/RD/U/SG/RX_error/MR/D/RFC/CI_step（spec-first 条款批 RXS-0374~0379 消费登记于 ledger v1.93，随 spec PR 同批落）。 |
| v1.14 | 2026-08-14 | G9.6 实现波 M121/M122 完整期腿 materialize（只追加；判据事实源 = G9_ACCEPTANCE_MAP §2 M121/M122 行 + spec/physics.md RXS-0374/RXS-0375 + RFC-0024 §4.A/§4.B + v1.1 章 F2）：**双 phase 双腿复用步骤 136/137，CI_step 字段 0-byte 不消费新号**——MAP §2 M121/M122 行 invocation cell 已列 `--phase g9.2` 与 `--phase g9.6` 双命令同一 numeric_step，`.github/workflows/pr-smoke.yml` 步骤 136/137 单 step `run: |` 顺位追加完整期腿 invocation（逐行 `$LASTEXITCODE` 守卫，沿步骤 131~164 多行 run 体例；骨架期腿在先、完整期腿在后，聚合门/门序取最新件语义与 CI 顺序一致）。同批落：**①** `ci/g9_physics_particle_view_smoke.py` / `ci/g9_gameplay_field_smoke.py` 落 `--phase g9.6` 完整期分支（骨架期 `--phase g9.2` 分支 0-byte 维持）——M121 完整期 = 骨架期回归（phase_g9_2_pass 实测）+ `particle-view-full --freeze` 12 判据（场求解器耦合驱动运动/双跑逐位一致/场置零退化 digest 逐位等于无场基线/写路径结构性断言与旁路写 RED 0-byte 维持/analytic-surface 闭集/场 journal 并入 M66 capture 主流往返/capture→replay 主流逐 tick hash/World-Field GpuScene 只读扩面 F2/渲染侧写 typed Err/锚定语料消费/measured 冻结带对拍）+ 自证红臂四臂实测必红（journal 缺失/乱序/篡改 + coupling_not_wired 场置零不退化检测面活性）；M122 完整期 = 门序前置 + 骨架期回归 + `field-full --freeze` 8 判据 + 自证红臂两臂实测必红（render_write_injection/bypass_submit）。**②** 新建 `ci/g9_physics_interlock.py`（RXS-0375 门序硬约束机器阻断，沿 `ci/g9_gi_interlock.py` D2-Q7 先例：M122 完整期门前置核验 evidence/ 最新 M121 件须 assertion_id 相符 + status==pass + **phase_g9_6_pass==true**——骨架期件/harness 直出件/他门件不充绿；`--selftest` 4 RED + 1 GREEN）。**③** 两件 evidence schema 加性演进 anyOf 双支（`g9_m121_physics_particle_view_evidence_schema.json` / `g9_m122_gameplay_field_evidence_schema.json`：v1 支 = 骨架期形态字面 0-byte〔phase_g9_6_pass const false〕，v2 支 = 完整期形态〔wave const G9.6 + 完整期 checks 键集闭集 + phase_g9_2_pass/phase_g9_6_pass 各自实测 boolean〕；MAP M121/M122 行「schema 同时要求 phase_g9_2_pass=true 与 phase_g9_6_pass=true」由 v2 支承载，骨架期绿不替完整期充绿纪律字面维持）；`ci/check_schemas.py` 既有 `g9_m121_physics_particle_view_`/`g9_m122_gameplay_field_` 前缀路由 0-byte（同 subject 双 phase 件同路由，anyOf 自适应分派）。**④** measured 冻结带两件（`milestones/g9/g9_m121_field_solver_coupling_freeze.json` / `g9_m122_world_field_readonly_freeze.json`，harness `--emit-freeze` 首次真跑 measured_local 回填后字节冻结，provenance 全字段，禁手写 golden P-09）。**⑤** 实现面（harness `apps/g9-physics-gates` 增 `particle-view-full`/`field-full` 及各自 `-selftest` 红臂子命令；`src/rurix-physics/src/field/` 增 eval/couple/capture_merge 三面 + def.rs analytic-surface 闭集 + journal 主流变体；`src/rurix-render/src/geometry/gpu_scene.rs` World-Field 只读扩面）随实现 PR 同批落。§4/§4A/§5 表体 0-byte；G5~G8 closed 判据与 G9.2/G9.3/G9.4/G9.5 门脚本 0-byte；零新 RXS/RD/U/SG/RX_error/MR/D/RFC/CI_step。 |
| v1.15 | 2026-08-14 | G9.6 实现波 M124 P1 门 materialize（只追加；判据事实源 = G9_ACCEPTANCE_MAP §3 M124 行 + spec/physics.md RXS-0376 + RFC-0024 §4.D）：数字步骤按 §1.2 互锁后 actual-next-free 纪律落盘前实测 `CI_step.next_free=166` 顺位领取 **步骤 166**——`g9.p1.m124.buoyancy_field_channel`（`ci/g9_buoyancy_field_channel_smoke.py`，host 纯 host 确定性门，`device_section_state=not_applicable`，Jolt 5.3 lockstep 单线程 host 确定性面）。三段判据：host 段（`field::buoyancy` 5 单测 + `field::buoyancy_capture` 3 单测逐名锚定 + conformance/physics M124 accept/reject 双件语料 `//@ spec: RXS-0376` 锚与门 key 留痕 + 细长/翻滚 corpus fixture 六件×2 场景 provenance + 冻结带 `milestones/g9/g9_m124_buoyancy_freeze.json` provenance 机器核验〔schema/spec_anchor/16 digest 键 64-hex/禁手写字面〕）+ harness 段（持 `gpu_device_lock` 真跑 `g9_m124_buoyancy --evidence`〔直出件落 .tmp 工作区不进 evidence/〕，`schema`/`spec_anchor`/`assertion_id`/`status==pass` + 9 判据闭集全真 + `--red-arm` bypass-api/field-unwired/framerate-drift 三子模式独立复跑抽检）。实现面：`src/rurix-physics/src/field/buoyancy.rs`（解析浮力求值器消费 FieldEvaluator 同一求值实例——水面采样/场梯度同管线，浮力 impulse = ρ_f·V_sub·|g|·dt 取场 occupancy 梯度反向 + 线性/角阻力浸没分式加权；形状闭集 Sphere/Box/Capsule 解析 clip 含球帽质心修正，闭集外 fail-closed → voxel table cooked 版本化注册面；介质参数为场定义一部分进 digest；旁路 API `reject_buoyancy_bypass` typed Err 单一字面）+ `src/rurix-physics/src/field/buoyancy_capture.rs`（细长体 ρ=500 半浸漂浮/翻滚体 ρ=1200 全浸下沉 canonical 场景 + record/replay 驱动并入 M66 主流 + 变帧率 replay 逐位一致 + 帧率漂移注入检测 + corpus fixture 持久化/逐字对拍）+ `Cargo.toml` `physics-buoyancy` feature（RFC-0024 §4.D + R-7 🔒 冻结名，未编译档 bins fail-closed FeatureNotCompiled）+ 冻结带/corpus fixture 首批落盘（`--emit-freeze --write-corpus` 首次真跑 measured_local 回填字节冻结，禁手写 P-09）。同批落 evidence schema `milestones/g9/g9_m124_buoyancy_field_channel_evidence_schema.json`（沿 M102 体例 const 钉死 numeric_step=166/gate key；device_section_state enum 仅 not_applicable 如实反映 host-only）+ `ci/check_schemas.py` 三处纯追加（load/validator/前缀路由 `g9_m124_buoyancy_field_channel_`，与既有 g9_* 全族互不包含，既有路由 0-byte）+ `pr-smoke.yml` 步骤 165 块后追加步骤 166。§4/§4A/§5 表体 0-byte（numeric_step 经 ledger v1.95 校准回填，不回写表体）；G5~G8 closed 判据与 G9.2~G9.5 门脚本 0-byte；零新 RXS/RD/U/SG/RX_error/MR/D/RFC（零新 FFI——浮力贡献经既有 ParticleAdapter::set_force_impulse 写路径进求解器主流）。 |
| v1.16 | 2026-08-14 | G9.6 实现波 M126 P1 门 materialize（只追加；判据事实源 = G9_ACCEPTANCE_MAP §3 M126 行 + spec/physics.md RXS-0378 + RFC-0024 §4.E2）：数字步骤按 §1.2 互锁后 actual-next-free 纪律落盘前实测 `CI_step.next_free=167` 顺位领取 **步骤 167**——`g9.p1.m126.rapier_benchmark_ab`（`ci/g9_rapier_benchmark_ab_smoke.py`，host 纯 host 确定性门，`device_section_state=not_applicable`；rapier feature 默认 off 纪律维持——本门仅 `--features physics-capture,rapier` 构建档产绿，未编译档 fail-closed）。三段判据：host 段（`benchmark` 2 单测逐名锚定 + conformance/physics M126 accept/reject 双件语料 `//@ spec: RXS-0378` 锚与门 key 留痕 + measured 报告 `milestones/g9/g9_m126_rapier_benchmark.json` provenance 机器核验〔双臂 world_digest 64-hex/step_ns_median measured 正整数/double_run_bitwise 双 true/determinism 画像字面/glam 留档/replay oracle 字面〕+ 跨 solver 偏差统计字段机核〔bool/数值闭集 + 不变量 + 容差〕+ RD-044 verdict 诚实登记机核〔condition_literal_unchanged + verdict 闭集字面 + verdict_basis 非空 + 不升格深造字面〕）+ harness 段（持 `gpu_device_lock` 真跑 `g9_m126_rapier_benchmark --evidence`〔直出件落 .tmp 工作区不进 evidence/〕，`schema`/`spec_anchor`/`assertion_id`/`status==pass` + 10 判据闭集全真 + `--red-arm` replay-oracle/rd044-without-measured 双子模式独立复跑抽检）。实现面：`src/rurix-physics/src/benchmark.rs`（CanonicalStackSpec canonical 大堆叠 6 层 + 双臂 A/B 真跑——同场景同输入 input_digest 逐位相等硬断言 + 各自后端双跑位级一致硬断言 + 逐 tick world 摘要链 digest + 接触事件计数 + step_ns wall-clock measured 采样 + 跨 solver 逐元 abs diff/不变量/0.05m 容差对拍如实记录 + `compare_as_replay_oracle` 僭越 fail-closed typed Err + `validate_rd044_application` 无 measured 数据申请 fail-closed + `rd044_verdict` measured 优势裁决）+ `lib.rs` benchmark 模块导出（`all(feature = "physics-capture", feature = "rapier")` 门控）。measured 结论（measured_local，禁 estimated）：Jolt step 中位 40400ns vs Rapier 197900ns（canonical 堆叠 Jolt 显著占优）→ verdict=**maintain_no_go**，RD-044 条件字面不变维持 open-留档（G9_CANDIDATE_DECISIONS v1.6 校准注 + deferred.json RD-044 history 只追加；不升格深造、不作验收依赖与生产默认）；跨 solver 差异如实记录（world 链非逐位一致，max translation abs diff ≈ 3.9e-2 m 在 0.05m 容差内，接触事件计数差 11，rest_above_ground 不变量成立）。同批落 evidence schema `milestones/g9/g9_m126_rapier_benchmark_ab_evidence_schema.json`（沿 M102 体例 const 钉死 numeric_step=167/gate key；device_section_state enum 仅 not_applicable）+ `ci/check_schemas.py` 三处纯追加（load/validator/前缀路由 `g9_m126_rapier_benchmark_ab_`，与既有 g9_* 全族互不包含，既有路由 0-byte）+ `pr-smoke.yml` 步骤 166 块后追加步骤 167。§4/§4A/§5 表体 0-byte（numeric_step 经 ledger v1.96 校准回填，不回写表体）；G5~G8 closed 判据与 G9.2~G9.5 门脚本 0-byte；零新 RXS/U/SG/RX_error/MR/D/RFC（RD-044 为既有号 verdict 登记非新消费；零新 FFI——基准经既有 PhysicsWorld 后端抽象双臂驱动）。M125（Jolt 5.6 A/B）不在本批，其门脚本/schema/步骤归其自身实现波按落盘实测领取。 |
| v1.17 | 2026-08-14 | G9.6 实现波 M125 P1 门 materialize（只追加；判据事实源 = G9_ACCEPTANCE_MAP §3 M125 行 + spec/physics.md RXS-0377 + RFC-0024 §4.E1 + RFC-0021 §4.A4）：数字步骤按 §1.2 互锁后 actual-next-free 纪律落盘前实测 `CI_step.next_free=168` 顺位领取 **步骤 168**——`g9.p1.m125.jolt_56_ab_evaluation`（`ci/g9_jolt_56_ab_evaluation_smoke.py`，host 纯 host 确定性门，`device_section_state=not_applicable`；jolt56 feature 默认 off 纪律维持——本门仅 `--features physics-capture,jolt56` 构建档产绿，未编译档 fail-closed Jolt56BackendNotCompiled）。三段判据：host 段（`ab_eval` 2 单测逐名锚定〔同进程并存/双臂双跑位级/各自 capture→replay 一致/逐字段分类 + vendor 覆盖·GPU 接权威·伪写 PASS 三 RED 面〕+ `rurix-physics-sys56` 单测〔ffi_layout_anchors 编译期布局锚〕+ 5.3 基线默认档回归〔cargo test --lib 全绿〕+ conformance/physics M125 accept/reject 双件语料 `//@ spec: RXS-0377` 锚与门 key 留痕 + measured 报告 `milestones/g9/g9_m125_jolt56_ab.json` provenance 机器核验〔baseline_freeze corpus digest 64-hex/replay corpus ≥10 场景全 PASS/measured baseline 正整数 + vendor56 pin 实测 tag/commit 与 5 件适配补丁 + 偏差画像与逐字段分类闭集 + 摩擦模型专项字面 + 七步记录 + verdict 闭集诚实登记〕）+ harness 段（持 `gpu_device_lock` 真跑 `g9_m125_jolt56_ab --evidence`〔直出件落 .tmp 工作区不进 evidence/〕，`schema`/`spec_anchor`/`assertion_id`/`status==pass` + 18 判据闭集全真 + `--red-arm` vendor-overwrite/gpu-authority/fake-pass 三子模式独立复跑抽检）。实现面：新建 `src/rurix-physics-sys56` crate（独立 vendor 线 = JoltC@2982004〔与 5.3 基线同一 commit，上游未跟进 5.6〕+ JoltPhysics v5.6.0 e77f175 + 5.6 适配补丁 5 件〔ShapeCastSettings/CollideShapeSettings/BodyManager_DrawSettings 新字段 + CollisionEstimationResult 新摩擦模型重排 + ConstraintSettings protected ctor shim〕+ 全量机械符号重命名 `JPC_→JPC56_`/`namespace JPH→JPH56`〔dumpbin 实测隔离，同进程并存零冲突〕+ GPU compute 四开关 OFF 编译期排除〔只评估不接权威结构性断言〕；裁剪/LF 归一沿 5.3 VENDOR.md 体例，5.3 vendor 0-byte）+ `tools/layout_dump56.cpp` 探针入库（所有消费面 *Settings sizeof/offsetof 重测进 ffi_layout_anchors）+ `src/rurix-physics` `jolt56` feature 与 `BackendKind::Jolt56` 加性后端面（生产默认 Jolt 0-byte）+ `ab_eval.rs` 七步程序夹具（基线冻结 digest/replay corpus 重跑/双臂 replay 一致/canonical A/B 偏差画像/逐字段分类/摩擦模型专项/GPU compute typed Err/verdict 闭集 + 三 RED 面）+ capture header/replayer 加性（`new_jolt_56` 版本锚 + Jolt56 backend 快照臂，5.3 字面钉住不动）。measured 结论（measured_local，禁 estimated）：5.3 step 中位 23500ns vs 5.6 20800ns（canonical 堆叠+滑块场景 5.6 中位约快 11%）；双臂各自双跑位级一致 + capture/replay 120 tick 全 PASS；接触事件计数 244==244 exact；跨版本 world 链分叉如实记录（新摩擦模型求解器语义变化预期），max translation abs diff ≈ 6.0e-4 m（0.05m 容差内）、滑块行程差 ≈ 4.8e-7 m、堆叠 z 差 ≈ 1.6e-5 m、rest_above_ground 不变量成立；逐字段分类 translation/rotation/linvel/angvel=tolerance、contact_events=exact、world_chain=invariant；verdict=**maintain_5_3_default**（评估完成不升格默认；采纳臂⑦三件 not-triggered 登记——corpus 迁移/replay 门重跑/判据字面修订均未触发，「Jolt 5.3」字面钉住处 0-byte；零 budget counter 写入）。同批落 evidence schema `milestones/g9/g9_m125_jolt_56_ab_evaluation_evidence_schema.json`（沿 M102 体例 const 钉死 numeric_step=168/gate key；device_section_state enum 仅 not_applicable）+ `ci/check_schemas.py` 三处纯追加（load/validator/前缀路由 `g9_m125_jolt_56_ab_evaluation_`，与既有 g9_* 全族互不包含，既有路由 0-byte）+ `pr-smoke.yml` 步骤 167 块后追加步骤 168。§4/§4A/§5 表体 0-byte（numeric_step 经 ledger v1.97 校准回填，不回写表体）；G5~G8 closed 判据与 G9.2~G9.5 门脚本 0-byte；零新 RXS/RD/U/SG/RX_error/MR/D/RFC（U 复用 U33~U53 既有审计边界——sys56 为同构镜像 + 5.6 delta 登记于 unsafe-audit/rurix-physics-sys.md §M125 追加段，U 命名空间 0-byte）。 |
| v1.18 | 2026-08-14 | G9.6 波聚合门 `g9.wave.6.exit` materialize（只追加）：数字步骤按 §1.2 纪律落盘前实测 `CI_step.next_free=169` 顺位领取 **步骤 169**（`ci/g9_wave6_exit_check.py --gate g9.wave.6.exit`，薄壳聚合 + `ci/g9_wave_exit_lib.py` 复用同构；evidence schema `milestones/g9/g9_wave6_exit_evidence_schema.json`，required_gates minItems=maxItems=5、aggregate_read_only const true、numeric_step const 169）——五门最新 evidence 只读汇总（§4 两 P0 完整期：`g9.p0.m121.physics_particle_view` 步骤 136 / `g9.p0.m122.gameplay_field` 步骤 137——**同 step 双 phase 聚合裁定：核验最新件 `status=="pass"` 且 `phase_g9_6_pass==true`，骨架期绿不替完整期充绿**；§4A 三 P1：`g9.p1.m124.buoyancy_field_channel` 步骤 166 / `g9.p1.m125.jolt_56_ab_evaluation` 步骤 168 / `g9.p1.m126.rapier_benchmark_ab` 步骤 167；门 key 按 G9_ACCEPTANCE_MAP §2/§3 实记）+ spec/physics.md 在树且 RXS-0374~0379 条款头齐（共 6 枚；RXS-0376 条款头「解析浮力走 Field 通道」字面核验）+ RFC-0024 v1.1 章 F1/F2 字面在树（F1 M123 双通道判档落定 no-go / F2 World-Field GpuScene 只读扩面显式修订行）+ M123 no-go `counts_as_green=false` 诚实登记字面（spec RXS-0379 + G9_CANDIDATE_DECISIONS v1.5 校准注 + MAP 登记句三面一致；no-go 不入 MAP §3——零 m123 gate key 机器核验；承接锚 G9.7 穷举）+ M125 verdict=`maintain_5_3_default` + 5.3 基线 0-byte 事实（`g9_m125_jolt56_ab.json` verdict 机核 + `src/rurix-physics-sys/VENDOR.md` 5.3 pin 字面不动核验）+ M126 RD-044 `maintain_no_go` 登记字面（`g9_m126_rapier_benchmark.json` rd044.verdict + condition_literal_unchanged 机核）+ 门序 interlock 留痕（`ci/g9_physics_interlock.py`：M121 完整期最新件 status==pass 且 phase_g9_6_pass==true 前置满足 + M122 最新件 `checks.gate_order_m121_full_passed==true`，RXS-0375「M121 完整期未绿 M122 完整期不得验收」）；聚合不代绿、不重跑 smoke、不设 `RURIX_REQUIRE_REAL`，聚合 PASS 不遮蔽任一子断言 FAIL/SKIP/DEV_ENV_DEGRADE。同 PR 落 `ci/check_schemas.py` 三处纯追加（load/validator/前缀路由 `g9_wave6_exit_`，与 `g9_wave2_exit_`/`g9_wave3_exit_`/`g9_wave4_exit_`/`g9_wave5_exit_` 及 g9_* 全族互不包含，既有路由 0-byte）+ `pr-smoke.yml` 步骤 168 块后追加步骤 169（注释块留痕体例沿步骤 165/168）。ledger v1.98：CI_step on_tree_max 168→169 / next_free 169→170。§4/§4A/§5 表体 0-byte；G5~G8 closed 判据 0-byte；零新 RXS/RD/U/SG/RX_error/MR/D/RFC。 |
| v1.19 | 2026-08-14 | G9.7 P2 穷举决策门 `g9.wave.7.decisions` materialize（只追加；判据事实源 = G9_CONTRACT G-G9-9 + G9_PLAN §G9.7 候选行集 + G9_CANDIDATE_DECISIONS v1.0~v1.6 实记全集 + G9_ACCEPTANCE_MAP §1~§3 34 key + registry/deferred.json v1.79）：数字步骤按 §1.2 纪律落盘前实测 `CI_step.next_free=170` 顺位领取 **步骤 170**——`ci/g9_p2_decisions_check.py --gate g9.wave.7.decisions`（普通检查门**非聚合门**，required_gates 空；G9.1 蜂群基设骨架按候选全集口径回填：FROZEN_IDS 由骨架十行扩为 **33 行闭集**——CANDIDATE 47 行实记全集未进 34 key 验收面者 + G9.3~G9.6 新增 not-triggered/no-go 登记面〔M98-l4 / M114-strand / M118-hdr-cal / M125-adopt3 / M123 / M126-rd044〕去重，M114-strand/M126-rd044 等子项级 key 与整项 go 的 M114/M126 不互斥）。机器核验：G9_P2_DECISIONS.md v1.0 在树 + 33 行闭集全等（候选全集基数对账）+ 裁决枚举合法（go/no-go/defer-to-G10+/strategic_override）+ 零空行（全列非空）+ 承接锚「重判条件 + 兜底」字面全行必填、defer 行必含 G10+ 重评窗 + go 行 evidence 义务 + no-go 行 RD/矩阵/契约锚义务 + 横向机核①G9_ACCEPTANCE_MAP 34 key（15 P0 + 19 已 go P1 实解自 MAP 文件）互斥（P2 行 ID 不得命中已 go M## 裸 token）②deferred.json history 对账（G9.7 P2 defer 登记恰好 RD-039 +1〔M61〕/ RD-040 +3〔M52/M99-clipmap/M100-high〕，零新 RD max=RD-044，RD-039/040 status open 维持 0-byte）。裁决结果：no-go 23 行 + defer-to-G10+ 10 行 + go 0 行；no-go/defer 如实保持 open 不写进全绿叙述、不阻塞 G9.8a。同批落：`milestones/g9/G9_P2_DECISIONS.md` v1.0（33 行穷举表 + 裁决口径 + 承接锚清单 + 互斥对账面）+ evidence schema `milestones/g9/g9_p2_decisions_evidence_schema.json`（required_gates maxItems=0、extra_facts minItems=37、aggregate_read_only const true、numeric_step const 170，沿 wave6 体例）+ `ci/check_schemas.py` 三处纯追加（load/validator/前缀路由 `g9_p2_decisions_`，与 `g9_wave2_exit_`~`g9_wave6_exit_` 及 g9_* 全族互不包含，既有路由 0-byte）+ `pr-smoke.yml` 步骤 169 块后追加步骤 170 + `registry/deferred.json` history 只追加四条（RD-039 +1 / RD-040 +3，含「G9.7 P2」字面与承接锚）+ revision_log v1.79 + ledger v1.99（CI_step on_tree_max 169→170 / next_free 170→171）。§4/§4A/§5 表体 0-byte；G5~G8 closed 判据与 G9.2~G9.6 门脚本 0-byte；零新 RXS/RD/U/SG/RX_error/MR/D/RFC；evidence/ 只增不删不改纪律维持。 |
