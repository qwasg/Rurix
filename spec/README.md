# spec/ — Rurix 语言规范

> 地位:进入实现期后的**唯一语义事实源**(10 §4,D-403);规范领导实现——先写条款再写实现,缺条款的语义 PR 必须先补 spec。
> 本 README 规定条款体例与变更纪律,自身变更同样适用 §3 档位标记。

---

## 1. 条款编号

- 编号 `RXS-####`(四位,分配制递增),**永不复用**(10 §9.5);弃用条款标注 deprecated 并保留原文,不删除。
- 编号在全 spec 范围内唯一,不按文件分段——文件只是组织载体,条款号是稳定锚点。
- 已分配区间登记于 §4 文件清单。

## 2. 条款格式(FLS 风格,10 §4)

每条款一个三级标题:`### RXS-#### 标题`,按需分节:

| 节 | 内容 |
|---|---|
| Syntax | 文法产生式(EBNF 式记法)或词法形式定义 |
| Legality | 静态合法性规则;违例引用错误码 `RX####`([../registry/error_codes.json](../registry/error_codes.json)) |
| Dynamic Semantics | 运行期语义(词法/纯语法条款不适用) |
| UB | 未定义行为边界(**经 Full RFC 由 agent 自主落笔**,10 §7.5 高敏面) |
| Implementation Requirements | 对实现的强制要求(错误恢复/诊断质量/平台行为) |

**测试锚定**(10 §4):每条款 ≥1 测试,测试侧以 `//@ spec: RXS-####` 注释(conformance 样例)或单测注释引用条款号;traceability 矩阵由工具生成(M1.4 起)。

## 3. 变更纪律与档位标记

- spec 文件变更必须走对应档位(10 §3):初版条款化已选定决策(D-1xx 等)为 **Direct**;规范内 bug fix 为 **Mini-RFC**;新语法/语义变更为 **Full RFC**(RFC 合入后才可改 spec)。判档争议向上取严;AI 无权自行判档为 Direct。
- **机器核对**(M1 CI_GATES §4 第 7 项):每个 spec 文件末尾维护修订记录表,含"档位"列;任何对 spec 文件的变更必须**新增修订行**(既有行 0-byte),否则 guardrail FAIL。
- 错误码语义可加不可改(10 §6);条款引用的错误码一经发布含义冻结。

## 4. 文件清单与编号区间

| 文件 | 内容 | 已分配条款 | 起始里程碑 |
|---|---|---|---|
| [lexical.md](lexical.md) | 词法结构 | RXS-0001 ~ RXS-0010 | M1.2 |
| [syntax.md](syntax.md) | 语法结构 | RXS-0011 ~ RXS-0031 | M1.3 |
| [names.md](names.md) | 名称与模块语义 | RXS-0032 ~ RXS-0038 | M2.1 |
| [types.md](types.md) | 类型与检查语义 | RXS-0039 ~ RXS-0047 | M2.2 |
| [borrow.md](borrow.md) | 所有权与借用语义(desugar/穷尽性/drop scope 首批;move/init/Drop 执行语义;借用/生命周期主体) | RXS-0048 ~ RXS-0061 | M3.1 |
| [consteval.md](consteval.md) | const 求值语义(const fn 子集 / const item 求值 / const 泛型 / 求值失败) | RXS-0062 ~ RXS-0065 | M3.4 |
| [device.md](device.md) | device 语义(函数着色与跨着色调用 / 地址空间类型与一致性 / barrier uniform 可达性保守骨架 / 着色与地址空间诊断要求 / NVPTX codegen 目标与调用约定 / 地址空间 codegen 建模 / 线程索引与 launch bounds / ptxas 干验证关卡 / launch 类型契约与诊断要求 / PTX 装载协商 / poisoned context 状态机 / views 算子集语义与子 view 不相交证明 / shared+barrier 一致性数据流 / scoped atomics 类型契约与 PTX 映射 / device 数学函数 intrinsic 集与求值语义 / libdevice bitcode 链接流程与 codegen 诊断) | RXS-0066 ~ RXS-0082 | M4.1 |
| [toolchain.md](toolchain.md) | 工具链语义(rx CLI 子命令语义面与退出码约定 / 包管理 rurix.toml·rurix.lock 格式与依赖三来源解析 / rx test 子进程隔离 / workspace 多包 / 离线重建复现门 / LSP 能力面契约) | RXS-0083 ~ RXS-0103（rx CLI 首批 + 包管理 manifest/lock/vendor + rx test/workspace/repro + LSP MVP M6.4） | M6.1 |
| [stdlib.md](stdlib.md) | 标准库语义(core 数学库类型面:Vec `VecN<T>` N∈{2,3,4} / Mat `MatRxC<T>` / swizzle / 几何原语 Point·Vector·Normal·AABB·Ray 的构造·分量访问与 swizzle·逐元素算术·点积/叉积/范数·矩阵乘·几何谓词;全 safe、host+device 双路径同义) | RXS-0104 ~ RXS-0113（M7.1 Vec/Mat/swizzle 类型面 RXS-0104~0109:Vec 构造·分量访问与 swizzle·逐元素算术·点积/叉积/范数 + Mat 构造·逐元素算术与矩阵乘;几何原语 / 谓词 RXS-0110~0113:Point3·Vector3·Normal3 语义区分与互转 / AABB·Ray 类型与构造 / Point∈AABB 包含与点到 AABB 距离 / Ray–AABB 相交） | M7.1 |
| [imageio.md](imageio.md) | image-io 接口语义(host-only 确定性图像序列输出:图像缓冲与像素类型面 Rgb·Rgba·ImageBuffer / 无损格式优先与格式选择 PPM P6 优先·PNG 次 / 确定性字节布局与 header 规范化 / 图像序列落盘接口逐帧 content SHA-256 可核对;全 safe) | RXS-0114 ~ RXS-0117（M7.2 image-io 接口语义面:RXS-0114 图像缓冲与像素类型面 / RXS-0115 无损格式优先与格式选择 / RXS-0116 确定性字节布局与 header 规范化 / RXS-0117 图像序列落盘接口） | M7.2 |
| [softraster.md](softraster.md) | 软光栅 kernel 语义面(G0 compute 软光栅:图元分桶到 tile binning / tile 光栅覆盖判定·重心坐标·边函数 / 深度 z-buffer 写入与深度测试 / tonemap HDR→LDR 像素量化;全 safe 代码目标·device 路径 NVPTX 标量子集·确定性帧像素,对接 M7.1 数学库与 M7.2 image-io 像素口径) | RXS-0118 ~ RXS-0121（M7.3 G0 软光栅 kernel 语义面:RXS-0118 图元分桶到 tile binning / RXS-0119 tile 光栅覆盖判定·重心坐标·边函数 / RXS-0120 深度 z-buffer 写入与深度测试 / RXS-0121 tonemap HDR→LDR 像素量化） | M7.3 |
| [interop.md](interop.md) | 互操作语义面(`rx build --emit=pyd` PYD 产出约定 / `__cuda_array_interface__` v3 + DLPack 双协议零拷贝接入 PyTorch/CuPy / C ABI 边界;C ABI·PYD 唯一通道·永不 Python 原生嵌入·PTX-only·affine 所有权不设 UB) | RXS-0122 ~ RXS-0125（M8.1：RXS-0122 `rx build --emit=pyd` PYD 产出约定 / RXS-0123 `__cuda_array_interface__` v3 零拷贝消费 / RXS-0124 DLPack 双协议零拷贝与设备指针所有权 / RXS-0125 C ABI 边界，每条 ≥1 `src/rurix-interop` 单测锚定） | M8.1 |
| [cublas.md](cublas.md) | cublas 绑定语义面(GEMM/GEMV 三层绑定:raw FFI / safe wrapper / 高层 API;`cublasHandle_t` 不透明句柄·C ABI 通道·永不 Python 原生嵌入·PTX-only·affine 所有权不设 UB；cublas runtime DLL 按需附带与 Attachment A 白名单约定·完整 Toolkit/驱动/Nsight 永不捆绑) | RXS-0126 ~ RXS-0129（M8.2：RXS-0126 cublas raw FFI 边界 / RXS-0127 safe wrapper / RXS-0128 高层 GEMM·GEMV API / RXS-0129 runtime DLL Attachment A 白名单，每条 ≥1 `src/rurix-cublas` 单测锚定） | M8.2 |
| [pipeline.md](pipeline.md) | UC-02 流水线类型化语义面(affine Context/Stream/Event/Buffer 所有权与销毁纪律 / Event 记录·等待与跨 stream 同步 / 流序分配类型化 / 跨线程所有权转移 / 资源生命周期错误类别与编译期拦截判据;**复用既有 RXS-0066/0074/0077 device·运行时条款，新增仅补缺口**;复用 src/rurix-rt 运行时对象·永不 Python 原生嵌入·PTX-only·affine 所有权不设 UB·资源生命周期错误类别由 Rust 类型系统原生编译期拦截不新增 RX 码) | RXS-0130 ~ RXS-0134（M8.3：RXS-0130 affine Context/Stream/Event/Buffer 所有权与 RAII 销毁纪律 / RXS-0131 Event 记录·等待与跨 stream 同步 / RXS-0132 流序分配类型化 / RXS-0133 跨线程所有权转移 / RXS-0134 资源生命周期错误类别与编译期拦截判据，每条 ≥1 `src/rurix-rt` 单测锚定） | M8.3 |
| [release.md](release.md) | 发布产物语义面(编译器/运行时/标准库按版本原子分发与 content-tree 完整性 / 语言本体与 NVIDIA 再分发组件分离打包（Attachment A 白名单最小集·完整 Toolkit/驱动/Nsight 永不捆绑 r6）/ 签名清单约定与验签发布前置（Authenticode + 时间戳·of-record Azure Artifact Signing）/ SBOM SPDX 构建视图 + CycloneDX 发布视图约定 / Release 层 hard-block 发布门;**复用 M6 包管理 content-tree/lock RXS-0090/0092/0094 与 M5.4 NVIDIA 再分发白名单审计，新增仅补发布产物缺口**;PTX-only·永不 Python 原生嵌入·EULA 白名单 pending-human-review·发布门以机器事实定义不设 UB) | RXS-0135 ~ RXS-0139（M8.4：RXS-0135 原子分发与 content-tree 完整性 / RXS-0136 语言本体与 NVIDIA 再分发组件分离打包 / RXS-0137 签名清单约定与验签发布前置 / RXS-0138 SBOM SPDX+CycloneDX 约定 / RXS-0139 Release 层 hard-block 发布门，每条 ≥1 `src/rurixup` 单测锚定）；**RXS-0150 ~ RXS-0152（G1.5/MR-0005 续承：RXS-0150 分发产物变体模型与按架构预编 cubin + 保守 PTX fallback / RXS-0151 fatbin 装载协商序 / RXS-0152 lockfile `[[artifact]]` 变体 digest 与内容寻址锁定；复用 RXS-0073 ptxas 干验证 + RXS-0076/0077 PTX 装载协商 + RXS-0090/0093 content-tree SHA-256，每条 ≥1 `src/rurix-rt` / `src/rurix-pkg` 单测锚定）**；**RXS-0185 ~ RXS-0186（V1.2/MR-0008 续承：RXS-0185 stable channel 清单存在性·字段语义·确定性序列化（channel ∈ {stable}·bundle 内容寻址引用·无时间戳逐字节确定）/ RXS-0186 channel 与 bundle 同版号一致性判据 + Release 层第 8 子门 channel-manifest（既有 7 门相对顺序 0-byte）；RXS-0181~0184 已被 GRX showcase 分支 claim 跳号避撞；复用 RXS-0093/0138/0139，每条 ≥1 `src/rurixup` 单测锚定）**；**RXS-0187 ~ RXS-0188（post-V1/MR-0009 续承：RXS-0187 工具链版本注册表与默认切换（ToolchainRegistry 多版本幂等 + default + 确定性 round-trip）/ RXS-0188 stable channel 消费与 install 内容寻址校验（channel 一致性 RXS-0186 + bundle digest RXS-0135/0093，全有或全无）；rurixup install/list/default 纯 host/零网络/零真实 FS 物化；真实 FS 物化 + 网络拉取 defer RD-025；复用 RXS-0135/0186/0093，每条 ≥1 `src/rurixup` 单测锚定）** | M8.4（fatbin 续承 G1.5;channel 清单续承 V1.2;工具链前端续承 post-V1） |
| [interop_d3d12.md](interop_d3d12.md) | CUDA–D3D12 互操作呈现语义面（`ExternalBuffer`/`ExternalSemaphore` affine 类型与 import 句柄生命周期 / 生成式 context brand 与跨 context 编译期拦截 / `Ready→Acquired→Presentable` typestate 与共享 fence 偶奇值 handoff / D3D12 committed resource import ABI 与 shim 私有 present pass 布局；薄 C/C++ shim 不进语言·D-130·PTX-only·G0 软光栅 kernel RXS-0118~0121 语义 0-byte·affine+生成式 brand+typestate 不设 UB·档位 Full RFC/RFC-0001） | RXS-0140 ~ RXS-0143（预留，G1.1：RXS-0140 affine 类型与 import 句柄生命周期 / RXS-0141 生成式 context brand 与跨 context 拦截 / RXS-0142 `Ready→Acquired→Presentable` typestate 与偶奇 fence 协议 / RXS-0143 D3D12 committed resource import ABI 与 present pass 布局；脚手架仅登记区间预留、不落裸条款头，条款体随 G1.1 实现 PR 同落） | G1.1 |
| [async_buffer.md](async_buffer.md) | 流序分配 `AsyncBuffer<'stream,T>` 类型契约语义面（运行时流序分配器 `cuMemAllocAsync` + `CUmemoryPool` + `AsyncBuffer<'stream,T>` affine 所有权与流序分配/释放 RAII / 分配未完成访问被 stream 序排除（在途态无 host 读接口）/ 释放后访问编译期生命周期错误 / 跨 stream 经 `share_with(other,event)` 显式时序边 / 三类流序分配生命周期错误编译期拦截判据；**复用 M8.3 RXS-0130~0134 affine 资源 + `InFlight` 流序分配类型化先例，新增仅补流序分配器缺口**；复用 src/rurix-rt 运行时对象·PTX-only·affine 所有权不设 UB·不触 VMM/多 GPU·Graph API 仅评估不立项·流序分配生命周期错误类别由 Rust 类型系统原生编译期拦截大概率不新增 RX 码） | RXS-0144 ~ RXS-0148（预留，G1.2：RXS-0144 `AsyncBuffer` affine 所有权与流序分配/释放 RAII / RXS-0145 分配未完成访问被 stream 序排除 / RXS-0146 释放后访问编译期生命周期错误 / RXS-0147 跨 stream 经 `share_with` 显式时序边 / RXS-0148 三类流序分配生命周期错误编译期拦截判据；脚手架仅登记区间预留、不落裸条款头，条款体随 G1.2 实现 PR（步骤 42）同落） | G1.2 |
| [engine_integration.md](engine_integration.md) | 引擎集成语义面（Rurix DLL 经 C ABI 嵌入 C++/D3D12 框架承担 compute pass：引擎集成 DLL 打包约定 + C ABI 头文件与导出 ABI 逐一对应；**复用** M8.1 RXS-0125 既有手写 `extern "C"` C ABI 语义 0-byte·`cdylib` 产 DLL（rurix-cublas 先例）·自建最小 C++/D3D12 harness·仅承担 compute pass 不进图形着色阶段（G2，D-131）·永不 Python 原生嵌入（红线 1，SG-008）·PTX-only·affine 所有权 + 确定性诊断不设 UB·`#[export(c)]` 编译器 codegen + 内建头文件生成 defer（RD-009）·档位 Mini-RFC/MR-0002） | RXS-0149（G1.3：RXS-0149 引擎集成 DLL 打包与 C ABI 头文件对应，≥1 `src/rurix-engine` 单测锚定） | G1.3 |
| [shader_stages.md](shader_stages.md) | 着色阶段类型面（vertex/fragment/compute/mesh/task + RT raygen/closesthit/anyhit/miss 作为 kernel 着色扩展进语言的类型面/语法面：着色阶段函数着色规则（扩展 RXS-0066）/ 阶段专属 I/O 语义类型（插值限定 type-level + 内建变量类型化）/ 阶段间接口类型契约（vertex out → fragment in 兼容性编译期校验）/ 资源句柄·纹理采样器参数化类型的类型面（`Texture2D<F>`/`Sampler` 平行 `View<space,T>`）；复用 kernel 子语言+views·device⊂host 单向可达·trait 单态化子集·PTX-only·DXIL codegen（G2.2，D-131）/ 绑定布局推导（G2.3，P-11）/ 🔒 纹理路径内存模型映射（06 §4.2）均不在本文件·编译期诊断不设 UB·档位 Full RFC/RFC-0002） | RXS-0153 ~ RXS-0156（G2.1：RXS-0153 着色阶段函数着色规则 / RXS-0154 阶段专属 I/O 语义类型 / RXS-0155 阶段间接口类型契约 / RXS-0156 资源句柄·纹理采样器参数化类型面；**条款体已随 G2.1 实现 PR（PR-B2）落地**——带编号条款体 + 每条 ≥1 测试锚定，trace_matrix 全锚定 156/156，错误码 RX3011~3013 + 复用 RX3001） | G2.1 |
| [dxil_backend.md](dxil_backend.md) | DXIL 第二后端语义面（MIR→DXIL 第二后端，承 RFC-0003 + RFC-0004：codegen target 分发与 DXIL 后端分叉（MIR 之后 target 选择，PTX/DXIL 并存，strict-only 无 fallback）/ 着色阶段着色 → DXIL 着色器类型降级对应 / 阶段 I/O → DXIL 签名·系统值语义降级 / 阶段间接口 → DXIL 阶段链接一致性核对 / 图形=B 着色 body I/O 数据流降级最小切片；承 RFC-0002 着色阶段类型面 RXS-0153~0156、D-131=混合 compute=A（LLVM DirectX 后端直接 emit DXIL）/图形=B（MIR→SPIR-V→SPIRV-Cross→HLSL→dxc→DXIL）、device 维持 PTX-only 并存（07 §7）、gate `cargo feature dxil-backend`、`rx build --target dxil`；🔒 纹理路径内存模型映射（06 §4.2 禁区）/ FFI ABI 二进制布局（RFC-0003 §4.6 / §9 Q-Builtin；RFC-0004 §4.6）/ 绑定布局推导（G2.3，P-11）/ 对外通用多后端架构承诺（D-008/SG-003）均不在本文件、target 不支持/降级失败/非法 I/O 映射以编译期 6xxx codegen 诊断定义不设 UB·档位 Full RFC/RFC-0003+RFC-0004） | RXS-0157 ~ RXS-0162; RXS-0171（G2.2/G2.4：**RXS-0157（target 分发）/ RXS-0158（着色阶段→DXIL 着色器类型）/ RXS-0159（阶段 I/O→DXIL 签名·系统值语义，按 B）/ RXS-0160（阶段间接口→阶段链接一致性核对，按 B）/ RXS-0161（图形阶段 MIR→SPIR-V 降级面）/ RXS-0162（B 转译链确定性+validator gate+供应链 pin+strict-only）/ RXS-0171（RD-013 body I/O 数据流降级最小切片）条款体已落地**——带编号条款体（Syntax/Legality/Dynamic Semantics/Implementation Requirements，严禁 UB），错误码 RX6007（A 路 / mesh·task·RT stub）+ RX6010~RX6013（B 路 strict-only：转译失败 / 签名不一致 / 声明输入被消除 / 不可映射，RXS-0171 白名单外 body 构造复用 RX6013），每条 ≥1 `//@ spec` 测试锚定（`dxil_sig_gate`/`dxil_codegen`/`dxil_spirv` 单测 + conformance/dxil），trace_matrix 全锚定 **171/171**；图形=B 承 [RFC-0004](../rfcs/0004-spirv-dxil-graphics-backend.md)（owner Approved 2026-06-25，D-131 v1.4 混合 compute=A/图形=B）。RXS-0171 仅承诺源码层 I/O 元素/方向/字段序/语义名/系统值/类型和值语义 body dataflow，不冻结 register/mask/packing/byte layout/稳定 Location；资源/纹理/采样/显式布局触及即升 agent Full RFC。golden/device bless、CI step 48、G-G2-4 签字仍归 agent 后续确认，RD-013 status 维持 open） | G2.2 |
| [binding_layout.md](binding_layout.md) | 绑定布局推导语义面（descriptor / root signature 编译器推导：RXS-0156 资源句柄 → SPIR-V DescriptorSet/Binding 装饰 / register-space 分配推导 / root signature 形态推导 + RTS0 序列化 / PSV0 反射一致性校验门；承 RFC-0005 owner Approved 2026-06-28；feature gate 复用 `dxil-backend`；Q-Space=按资源种类分轴，首期默认 `space0`；Q-RootShape=CBV root descriptor + SRV/UAV/Sampler descriptor table；Q-Sampler=dynamic；Q-Bindless=defer RD-018；显式 `#[binding(...)]` 覆盖留后期；🔒 绑定二进制 ABI 布局 / 纹理路径内存模型映射 / DXIL-SPIR-V UB 边界只作边界声明，不在本文件落语义本体） | RXS-0163 ~ RXS-0166（G2.3：**条款体已随 PR-E2a 落地**——带编号条款体 `### RXS-0163 ~ ### RXS-0166`（FLS 体例，严禁 UB 节）：RXS-0163 资源句柄→SPIR-V 资源绑定降级 / RXS-0164 register-space 分配推导（按资源种类分轴 b/t/u/s）/ RXS-0165 root signature 形态推导+RTS0 容器机械序列化 / RXS-0166 一致性校验门（PSV0 反射 vs 推导意图）+strict-only 推导失败；配套 host 侧 safe 推导 `src/rurixc/src/binding_layout.rs`（纯函数 over `mir::ResourceBinding`，零新 unsafe）+ `mir.rs` 资源种类建模 + 每条 ≥1 `//@ spec` 单测锚定（**trace_matrix 166/166 全锚定**）。**PR-E2a 不接线生产 emit、不改 error_codes.json（占位「6xxx」）、不落 golden、不 device**——生产接线 + 6xxx 落码 + golden/bless + device 真跑归 PR-E2b（agent 闸门，G-G2-3）。bindless defer RD-018） | G2.3 |
| [d3d12_runtime.md](d3d12_runtime.md) | UC-04 deferred 渲染器 / 原生 D3D12 运行时出图语义面（运行时把 RFC-0004 DXIL 着色器对象 + RFC-0005 推导 RTS0 装配为多 pass deferred 管线：DXIL+RTS0→graphics PSO 装配一致性 / deferred 多 pass 编排（G-buffer MRT→单光源 lighting→offscreen readback）/ 资源状态+barrier 编排锚点（首期手动 RT→SRV→RT/Copy/Readback）/ offscreen readback 像素对照；承 [RFC-0006](../rfcs/0006-uc04-deferred-renderer.md)（owner Approved 2026-06-28，§9 全裁）；Q-Present=offscreen-first→RD-019 / Q-DemoCrate=src/uc04-demo（unsafe_code=deny，U23）/ Q-RuntimeShape=safe wrapper（运行时 ABI non-stable）/ Q-Barrier=首期手动编排→RD-020 / Q-Texture=不落纹理内存模型本体→RD-021 / Q-Gate=d3d12-runtime/uc04-demo 专属（dxil-backend 仅 codegen 前置）/ Q-Err=6xxx 自 RX6018（不预造）/ Q-CIStep=step 48 offscreen REQUIRE_REAL；🔒 纹理路径内存模型映射（06 §4.2）/ barrier 并发语义本体 / 运行时 stable ABI / FFI ABI 二进制布局 / DXIL·SPIR-V UB 边界只作边界声明，不落语义本体） | RXS-0167 ~ RXS-0170（G2.4：**条款体已随 PR-F2 blocked-honest interim slice 落地**——带编号条款体 `### RXS-0167 ~ ### RXS-0170`（FLS 体例，按需分 Syntax/Legality/Dynamic Semantics/Implementation Requirements，严禁 UB 节，Legality 引 6xxx 实码）：RXS-0167 DXIL+RTS0→graphics PSO 装配一致性（host 侧装配核验，渲染目标失配 RX6018 / RTS0↔绑定失配 RX6019 复用 RFC-0005 `check_binding_consistency` P-11）/ RXS-0168 deferred 多 pass 编排（几何 MRT→lighting SRV 采样→readback 结构核验，pass 顺序/目标缺失 RX6020；**device 像素对照 blocked-on-RD-013**）/ RXS-0169 资源状态+barrier 编排锚点（RT→SRV→RT/Copy/Readback 锚点存在性/合法性，缺/非法 RX6021；🔒 并发语义本体不落，「需升档」）/ RXS-0170 offscreen readback+像素对照（host 锚=readback 布局 RX6022；**device 像素对照 REQUIRE_REAL blocked-on-RD-013**）。配套 host 侧 safe 装配/编排模型 [`src/uc04-demo`](../src/uc04-demo)（纯 host/safe，**零新 unsafe → 不消费 U23**；复用 `binding_layout` RTS0 推导 P-11）+ 新 feature gate `d3d12-runtime`（不复用 `dxil-backend`）+ 每条 ≥1 `//@ spec` 单测锚定（`ci/trace_matrix.py` 增扫 `src/uc04-demo`，**166→170 全锚定**）+ 6xxx 装配期错误码 RX6018~RX6022。**device 段 blocked-honest**：hardware 多 pass deferred draw + offscreen 像素对照 / CI step 48 接线 / device run URL / G-G2-4 签字均未达成（前置 RD-013 open，无 Rurix 自产可出图着色器），按 G-G2-4 防降级硬门不以替代物伪造 device 绿、标 blocked 不签 | G2.4 |
| [edition.md](edition.md) | edition 机制语义面（语义版本边界声明：`[package].edition` 声明语义与缺省兼容 / edition 解析校验规则（合法集 `{ "2026" }`，确定性纯函数）/ edition 不匹配·未知诊断（未知 edition → RX7020 strict-only，无 fallback，P-01；值类型错误复用 RX7005）/ stable 面与 edition 关系（edition 作 stable 面版本锚边界，语言 1.0 stable 面以首个 edition `"2026"` 为基准，加性演进，快照字节内容非语言 ABI 保证）；承 [RFC-0008](../rfcs/0008-edition-stabilization.md)（agent Approved 2026-06-30，§9 全裁）；首个 edition `"2026"` 仅机制锚点（edition-gated 行为差异 = 空集）；RD-008 stable API 快照冻结机制经本里程碑激活（stable 面定义 + 快照比对 + bless 守卫 + 首份 bless，RD-008 open→closed）；🔒 无 UB / 内存模型映射 / FFI ABI 二进制布局 / 安全包络边界（edition 是编译期/host 工具链声明语义）；不解多后端（D-008/SG-003）/ 不触 Python 嵌入（SG-008）/ registry 维持 not_triggered（D-312/SG-007）） | RXS-0177 ~ RXS-0180（G2.5：**条款体已落地**——带编号条款体 `### RXS-0177 ~ ### RXS-0180`（FLS 体例，按需分 Syntax/Legality/Dynamic Semantics/Implementation Requirements，**严禁 UB 节**）：RXS-0177 edition 声明语义 / RXS-0178 edition 解析校验规则 / RXS-0179 edition 不匹配·未知诊断（RX7020）/ RXS-0180 stable 面与 edition 关系；配套 edition 解析/校验实现落 [`src/rurix-pkg/src/manifest.rs`](../src/rurix-pkg/src/manifest.rs)（纯 host/safe，零新 unsafe）+ 每条 ≥1 `//@ spec` 测试锚定（`src/rurix-pkg` 单测 + `conformance/edition/` accept|reject fixtures，**trace_matrix 176→180 全锚定**）+ 错误码 RX7020（`toolchain.edition_unknown`，7xxx 段续号接 RX7019，append-only）+ en/zh message-key（bilingual 对齐）；stable 快照机制 [`ci/stable_snapshot.py`](../ci/stable_snapshot.py) + [`tests/stable/stable_api.snapshot`](../tests/stable/stable_api.snapshot) + `check_guardrails` `check_stable_snapshot_bless` 守卫分支 + agent bless 首份快照） | G2.5 |
| [host_orchestration.md](host_orchestration.md) | single-source 宿主 GPU 编排语义面(host `.rx` 经 std::gpu 首期收敛子集 Context/Stream/Buffer/PinnedBuffer + launch/同步/传输编排 GPU / 同源 PTX(+可选 sm cubin)描述表嵌入单 EXE / rxrt C ABI staticlib 绑定 rurix-rt / 前端机械面 extern "C" 符号保名·`#[link]` 接线·`mod name;` out-of-line 模块;复用 RXS-0074/0075 launch 类型契约·RXS-0130~0134 affine 资源语义·RXS-0150/0151/0076 装载协商·RXS-0073/0082 编译期纪律·RXS-0125 手写 extern "C" 口径;present typestate 面(affine 消费式帧状态机,fence 偶/奇协议引用 RXS-0142)+ 宿主图像落盘桥(write_ppm → rxio_write_ppm,RXS-0114~0117 复用)MS1.2b 已落体;运行期失败=确定性诊断+终止+poisoned(对齐 RXS-0077)不占 RX 码·严禁 UB 节·档位 Full RFC/RFC-0009) | RXS-0189 ~ RXS-0199(MS1.2:**条款体已落地**(MS1.2 单 PR,条款 commit 先行)——RXS-0189 std::gpu 宿主编排类型面与 affine 语义(宿主 API 着色违例 RX3015)/ RXS-0190 宿主 GPU 方法签名与元素类型推断(T ∈ {f32,i32,u32},元素类型不可定型 RX2010)/ RXS-0191 launch 宿主 lowering 与实参借用/marshalling(实参子集外 RX6024,🔒 slot+kinds marshalling)/ RXS-0192 single-source device 产物嵌入与装载协商(嵌入失败 RX6025)/ RXS-0193 宿主运行期错误与 poisoned 语义(无 UB 正面语义条款)/ RXS-0194 rxrt C ABI 运行时边界(🔒 FFI,含义冻结·布局不冻结为语言 ABI,RXS-0180 L3 口径)/ RXS-0195 extern "C" 符号保名与 `#[link]` 接线(RX7022;rurix_rt_cabi 定位/构建失败 RX7021 按内聚并入)/ RXS-0196 out-of-line 模块文件加载(缺失/循环 RX1005);每条 ≥1 测试锚定随 MS1.2 实现 commit 同 PR 落,trace_matrix 184→192 全锚定);**RXS-0197 ~ RXS-0199(MS1.2b:条款体已落地——RXS-0197 present 宿主 typestate 面(Present/Ready/Acquired/Presentable 非 Copy affine 消费式帧状态机,错序 = 编译期 move 违例 RX4001/RX4003 零新码,fence 偶/奇协议引用 RXS-0142 不重述,D-130 shim 边界维持)/ RXS-0198 present backbuffer 借用缓冲与 blit 契约(仅 Acquired 态借用句柄 Buffer<C,f32>·drop 不释放·可作 launch 实参,布局对齐 RXS-0143/RXS-0121)/ RXS-0199 宿主图像落盘桥(write_ppm 编译器已知自由函数 → rxio_write_ppm,RXS-0114~0117 确定性 PPM 语义 0-byte 复用,尺寸失配 = 运行期确定性失败);零新 RX 码(RX3015/RX4xxx/RX2xxx 复用),每条 ≥1 测试锚定随 MS1.2b 实现 commit 同 PR 落,trace_matrix 192→195 全锚定)** | MS1.2 / MS1.2b |
## 5. 修订记录

| 版本 | 日期 | 变更 | 档位 |
|---|---|---|---|
| v1.0 | 2026-06-11 | 占位 README 升级为正式体例(编号规则/条款格式/档位标记约定);依据 10 §3 §4 既定治理决策,无新语义 | Direct |
| v1.1 | 2026-06-11 | §4 文件清单追加 syntax.md(RXS-0011 ~ RXS-0031,M1.3;D-M1-3 条款化登记,无体例变更) | Direct |
| v1.2 | 2026-06-11 | §4 文件清单追加 names.md(RXS-0032 ~ RXS-0038,M2.1 names 条款化登记,无体例变更) | Direct |
| v1.3 | 2026-06-12 | §4 文件清单追加 types.md(RXS-0039 ~ RXS-0047,M2.2 types 条款化登记,无体例变更) | Direct |
| v1.4 | 2026-06-12 | §4 文件清单追加 borrow.md(RXS-0048 ~ RXS-0052,M3.1 desugar/穷尽性/drop scope 首批条款化登记,无体例变更) | Direct |
| v1.5 | 2026-06-13 | §4 borrow.md 行区间更新至 RXS-0056(M3.2 move/init/Drop 执行语义条款追加登记,无体例变更) | Direct |
| v1.6 | 2026-06-13 | §4 borrow.md 行区间更新至 RXS-0061(M3.3 借用/生命周期主体条款追加登记,无体例变更) | Direct |
| v1.7 | 2026-06-13 | §4 文件清单追加 consteval.md(RXS-0062 ~ RXS-0065,M3.4 const eval 首批条款化登记,无体例变更) | Direct |
| v1.8 | 2026-06-13 | §4 文件清单追加 device.md(RXS-0066 ~ RXS-0069,M4.1 device 着色/地址空间首批条款化登记;codegen/launch 条款随 M4.2/M4.3 续写本文件,无体例变更) | Direct |
| v1.9 | 2026-06-13 | §4 device.md 行区间更新至 RXS-0073(M4.2 NVPTX codegen 目标与调用约定 / 地址空间 codegen 建模 / 线程索引与 launch bounds / ptxas 干验证关卡条款追加登记;launch 类型契约条款随 M4.3 续写,无体例变更) | Direct |
| v1.10 | 2026-06-13 | §4 device.md 行区间更新至 RXS-0075(M4.3 launch 类型契约与诊断要求条款追加登记;运行时对象/装载协商/poisoned 状态机随 rurix-rt 实现 PR,无体例变更) | Direct |
| v1.11 | 2026-06-13 | §4 device.md 行区间更新至 RXS-0077(M4.3 运行时 PTX 装载协商 / poisoned context 状态机条款追加登记;rurix-rt 运行时落地,无体例变更) | Direct |
| v1.12 | 2026-06-14 | §4 device.md 行区间更新至 RXS-0078(M5.1 views 算子集语义与子 view 不相交证明条款追加登记;MIR 借用检查 device 扩展,条款 PR 先于实现 PR,无体例变更) | Direct |
| v1.13 | 2026-06-14 | §4 device.md 行区间更新至 RXS-0080(M5.2 shared+barrier 一致性数据流 / scoped atomics 类型契约与 PTX 映射条款追加登记;MIR 借用检查 device 扩展数据流 + scoped atomics 映射 D-406 自主落笔,条款 PR 先于实现 PR,无体例变更) | Direct |
| v1.14 | 2026-06-14 | §4 device.md 行区间更新至 RXS-0082(M5.3 device 数学函数 intrinsic 集与求值语义 / libdevice bitcode 链接流程与 codegen 诊断条款追加登记;libdevice 按需引入 06 §7 + 编译流程 07 §7 D-205/D-207 条款化,条款 PR 先于实现 PR,gpu 基元 kernel codegen 接通随实现 WP,无体例变更) | Direct |
| v1.15 | 2026-06-15 | §4 文件清单追加 toolchain.md（RXS-0083 起续号预留，起始里程碑 M6.1：rx CLI 子命令语义面 / 包管理 rurix.toml·rurix.lock 格式与三来源解析 / LSP 能力面契约）。M6 开工脚手架仅登记新文件名 + 预留区间，**不落裸条款头**——条款体与测试锚定随 M6.1+ 实现 PR 同落（条款 PR 先于实现 PR，trace_matrix 维持全锚定），无体例变更 | Direct |
| v1.16 | 2026-06-15 | §4 toolchain.md 行区间更新至 RXS-0088（M6.1 rx CLI 子命令语义面首批条款化登记：总入口与子命令分发 + 退出码约定 / build / run / check / fmt 收编 RD-005 / bench 收编 RD-003；07 §2 §6 §9 单一前端 + 08 §7 D-239 + BENCH_PROTOCOL §3 已锁定决策的条款化，条款 PR 先于实现 PR，每条 ≥1 测试锚定，无体例变更）。包管理 manifest/lock 格式条款（M6.2）与 LSP 能力面条款（M6.4）续写 toolchain.md | Direct |
| v1.17 | 2026-06-15 | §4 toolchain.md 行区间更新至 RXS-0094（M6.2 包管理 manifest/lock/vendor 条款化登记：rurix.toml 清单格式与声明式无 build.rs / 依赖三来源 path·git·archive 解析规则 / 依赖解析图与 feature additive-v1 加性合一 + 冲突检测 / rurix.lock 精确解析图格式 / 内容树规范化 SHA-256 / vendor 与离线解析路径；09 §7.1/§7.2 已锁定决策 D-308~D-311 的条款化，条款 PR 先于实现 PR，每条 ≥1 测试锚定，无体例变更）。LSP 能力面条款（M6.4）续写 toolchain.md | Direct |
| v1.18 | 2026-06-15 | §4 toolchain.md 行区间更新至 RXS-0097（M6.3 rx test 子进程隔离 / workspace members 多包 / G-M6-1 三包离线重建逐字节复现门条款化登记：`#[test]`/`#[test(gpu)]` 逐测试子进程 harness、workspace members 进入单根 lock 图、`rx build --locked --offline` reproducible profile 两次 host EXE SHA-256 一致且 lock/vendor 不改写；14 §6 + 09 §7.1/§7.2 + M6 契约 D-M6-3/G-M6-1 的条款化，条款 PR 先于实现 PR，每条 ≥1 测试锚定，无体例变更）。LSP 能力面条款（M6.4）续写 toolchain.md | Direct |
| v1.19 | 2026-06-15 | §4 toolchain.md 行区间更新至 RXS-0103（M6.4 LSP MVP 条款化登记：`rurixc --tooling-server` 常驻 query 层 / publishDiagnostics 诊断 JSON / completion / definition+references / documentHighlight / rename；07 §9 D-210 单一前端 + RD-004 无损语法树通道接通，条款 PR 先于实现 PR，每条 ≥1 测试锚定，无体例变更） | Direct |
| v1.20 | 2026-06-15 | §4 文件清单追加 stdlib.md（RXS-0104 起续号预留，起始里程碑 M7.1：core 数学库类型面 Vec `VecN<T>` N∈{2,3,4} / Mat `MatRxC<T>` / swizzle / 几何原语 Point·Vector·Normal·AABB·Ray 的构造·分量访问与 swizzle·逐元素算术·点积/叉积/范数·矩阵乘·几何谓词，全 safe、host+device 双路径同义）。M7 开工脚手架仅登记新文件名 + 预留区间，**不落裸条款头**——条款体与测试锚定随 M7.1 实现 PR 同落（条款 PR 先于实现 PR，trace_matrix 维持全锚定；01 §6 UC-03 + 08 §5 stdlib 充实 + 05 §1 device⊂host + 11 §3 M7，M7_CONTRACT D-M7-1 `rfc_required: none` 授权），无体例变更 | Direct |
| v1.21 | 2026-06-15 | §4 stdlib.md 行区间更新为 RXS-0104 ~ RXS-0109（M7.1 core 数学库 Vec/Mat/swizzle 类型面首批条款体落地：Vec 类型与构造 / 分量访问与 swizzle / 逐元素算术 / 点积·叉积·范数 / Mat 类型与构造 / Mat 逐元素算术与矩阵乘，每条 ≥1 conformance 锚定 `conformance/stdlib/**`，host 结构体 API 真跑 + device 标量分量原语 codegen，trace_matrix 维持全锚定）。几何原语 Point/Vector/Normal/AABB/Ray + 几何谓词保留为 stdlib.md §3 预留（不落裸条款头），随 M7.1 后续小步续号。Legality 仅引用既有 2xxx 类型类诊断（RX2001/RX2002/RX2003/RX2004），不新增错误码，无体例变更 | Direct |
| v1.22 | 2026-06-16 | §4 stdlib.md 行区间更新为 RXS-0104 ~ RXS-0113（M7.1 几何原语 / 谓词条款体落地：RXS-0110 几何向量类语义区分与互转 Point3·Vector3·Normal3 / RXS-0111 AABB·Ray 类型与构造 / RXS-0112 Point∈AABB 包含与点到 AABB 距离 / RXS-0113 Ray–AABB 相交,每条 ≥1 conformance 锚定 `conformance/stdlib/{host,device,reject}/**`,host 结构体 API 真跑 + device 标量分量谓词原语 codegen + 类型互斥 reject → RX2001,trace_matrix 维持全锚定）。stdlib.md §3 预留骨架升格为「几何原语条款落地说明」。Legality 仅引用既有 2xxx 类型类诊断（RX2001/RX2002/RX2003），不新增错误码,零编译器改动,无体例变更 | Direct |
| v1.23 | 2026-06-16 | §4 文件清单追加 imageio.md（RXS-0114 ~ RXS-0117,起始里程碑 M7.2：image-io 接口语义面条款体落地——RXS-0114 图像缓冲与像素类型面 Rgb·Rgba·ImageBuffer（复用 M7.1 标量 f32 像素口径）/ RXS-0115 无损格式优先与格式选择（PPM P6 优先·PNG 次）/ RXS-0116 确定性字节布局与 PPM P6 header 规范化（行主序·通道序·f32→u8 确定量化）/ RXS-0117 图像序列落盘接口（逐帧 content SHA-256 可核对·两次落盘逐字节一致），每条 ≥1 锚定（`src/image-io` crate 内确定性单测,trace_matrix 维持全锚定）。实现裁决：**host-only 单路径**（不引入 device codegen,区别于 stdlib 双路径）,纯函数确定性编码 + 标准库落盘,全 safe（`unsafe_code=deny`）;维度以具体像素类型名 + 运行期宽高编码,不用 const 泛型（RD-007 不触碰）。错误码：Legality 仅引用既有 2xxx 类型类诊断（RX2001/RX2003）,不新增 / 不预造错误码、不改 error_codes.json 与 en.messages;运行期失败（格式不支持 / 写入失败）以库层 Result 错误值表达,若确需编译器侧 RX 诊断则停手升档。授权:01 §6 UC-03 + 08 §5 stdlib 充实 + 09 §5/§7 image-io 包形态 + 11 §3 M7,M7_CONTRACT D-M7-2 / G-M7-1 子集 / G-M7-5 `rfc_required: none`,无体例变更 | Direct |
| v1.24 | 2026-06-16 | §4 文件清单追加 softraster.md（RXS-0118 ~ RXS-0121,起始里程碑 M7.3：G0 compute 软光栅 kernel 语义面条款体落地——RXS-0118 图元分桶到 tile binning（确定性遍历序·每桶 owner 线程独写·atomics-free）/ RXS-0119 tile 光栅覆盖判定·重心坐标·边函数 / RXS-0120 深度 z-buffer 写入与深度测试（less 约定·确定性深度合成序）/ RXS-0121 tonemap HDR→LDR 像素量化（对接 imageio RXS-0116 f32→u8 确定量化与 M7.1 标量 f32 像素口径），每条 ≥1 锚定（`conformance/soft_raster/**` device codegen 样例 + `src/soft-raster` crate 确定性单测,trace_matrix 维持全锚定）。实现裁决：device 路径 NVPTX codegen 标量子集（`kernel`/`device fn` + `View<global, f32>` + `shared` + `ThreadCtx` + `block.sync()`,复用 M7.1 标量分量原语），host 路径全 safe CPU 参考（`src/soft-raster`,复用 imageio `ImageBuffer`/`Rgb`/PPM 确定编码）;全 safe 代码目标（`unsafe_code=deny`）,本轮零 unsafe。确定性：固定输入两次运行帧像素逐字节一致（固定归约 / tile 调度 / 分桶 / 深度合成序）。维度以运行期 usize/View 长度编码,不用 const 泛型（RD-007 不触碰）;不引入 device 原子（D-406/RD-008 禁区）。错误码：Legality 仅引用既有 6xxx codegen 段位诊断（RX6003）,不新增 / 不预造错误码、不改 error_codes.json 与 en.messages;运行期边界以确定性返回值/哨兵表达,若确需编译器侧 RX 诊断则停手升档。授权:01 §6 UC-03 + 06 GPU 模型 + 07 §7 device codegen + 05 §1 device⊂host + 11 §3 M7,M7_CONTRACT D-M7-3 / G-M7-3 / G-M7-1 子集 / G-M7-5 `rfc_required: none`,无体例变更 | Direct |
| v1.25 | 2026-06-16 | §4 文件清单追加 interop.md（RXS-0122 起续号预留，起始里程碑 M8.1：互操作语义面——`rx build --emit=pyd` PYD 产出约定 / `__cuda_array_interface__` v3 + DLPack 双协议零拷贝接入 PyTorch/CuPy / C ABI 边界；C ABI·PYD 唯一通道、永不 Python 原生嵌入（红线 1，SG-008）、device 维持 PTX-only、设备指针所有权以 affine 所有权 + 确定性诊断定义不设 UB 节）。M8.1 开工脚手架**仅登记新文件名 + 预留区间，不落裸条款头**——条款体（RXS-0122 起）与每条 ≥1 测试锚定随 M8.1 实现 PR 同落（条款 PR 先于实现 PR，trace_matrix 维持全锚定；09 §6 + 02 §U1 + 01 §6 + 05 §FFI + 07 §7 + 11 §3 M8 已锁定决策的条款化，M8_CONTRACT D-M8-1 / G-M8-1 / G-M8-7 `rfc_required: none` 授权）。识别新决策面 PYD/C ABI 边界 unsafe 策略带档位标记 **Mini**（口径已由 M8_CONTRACT §5 guardrail 锁定），agent 自主判档，判档争议向上取严，无体例变更 | Direct |
| v1.26 | 2026-06-16 | §4 interop.md 行区间更新为 RXS-0122 ~ RXS-0125（M8.1 实现 PR 条款体落地：RXS-0122 `rx build --emit=pyd` PYD 产出约定 / RXS-0123 `__cuda_array_interface__` v3 零拷贝消费 / RXS-0124 DLPack 双协议零拷贝与设备指针所有权 / RXS-0125 C ABI 边界，每条 ≥1 `src/rurix-interop` crate 单测锚定，trace_matrix 维持全锚定）。错误码新段位首批分配 RX7013~RX7015（互操作诊断：协议不支持 / 设备指针非法 / 形状不匹配，7xxx 续接、含义冻结，registry/error_codes.json v1.20 + en.messages）。实现裁决:rurix-interop FFI 边界 crate unsafe_code 经裁决豁免 + unsafe-audit 注册（对齐 rurix-rt）;PTX-only、不触 RD-007 / D-406 / 红线 1。PYD/C ABI 边界 unsafe 策略档位 **Mini**，agent 自主判档，判档争议向上取严，无体例变更 | Direct |
| v1.27 | 2026-06-16 | §4 文件清单追加 cublas.md（RXS-0126 起续号预留，起始里程碑 M8.2：cublas 绑定语义面——GEMM/GEMV 三层绑定 raw FFI / safe wrapper / 高层 API（`cublasHandle_t` 不透明句柄、C ABI 通道、永不 Python 原生嵌入（红线 1，SG-008）、device 维持 PTX-only、设备指针所有权以 affine 所有权 + 确定性诊断定义不设 UB 节）；cublas runtime DLL 按需附带与 Attachment A 白名单约定（完整 Toolkit/驱动/Nsight 永不捆绑，许可红线 r6）。M8.2 开工脚手架**仅登记新文件名 + 预留区间，不落裸条款头**——条款体（RXS-0126 起）与每条 ≥1 测试锚定随 M8.2 实现 PR 同落（条款 PR 先于实现 PR，trace_matrix 维持全锚定；09 + 05 §FFI + 01 §6 + 07 §7 + 11 §3 M8 已锁定决策的条款化，M8_CONTRACT D-M8-2 / G-M8-2 / G-M8-7 `rfc_required: none` 授权）。识别新决策面 cublas FFI 边界 unsafe 策略带档位标记 **Mini**（口径已由 M8_CONTRACT §5 guardrail 锁定），agent 自主判档，判档争议向上取严，无体例变更 | Direct |
| v1.28 | 2026-06-16 | §4 cublas.md 行区间更新为 RXS-0126 ~ RXS-0129（M8.2 实现 PR 条款体落地：RXS-0126 cublas raw FFI 边界 / RXS-0127 cublas safe wrapper / RXS-0128 cublas 高层 GEMM·GEMV API（row-major ↔ col-major 适配）/ RXS-0129 cublas runtime DLL 按需附带与 Attachment A 白名单约定，每条 ≥1 `src/rurix-cublas` crate 单测锚定，trace_matrix 维持全锚定）。错误码新段位首批分配 RX7016~RX7019（cublas 诊断：句柄初始化失败 / 设备指针非法 / 维度不匹配 / 执行运行时失败，7xxx 续接、含义冻结，registry/error_codes.json v1.21 + en.messages）。实现裁决：rurix-cublas FFI 边界 crate unsafe_code 经裁决豁免 + unsafe-audit 注册（对齐 rurix-rt / rurix-interop）；cublas runtime DLL 动态加载（对齐 nvcuda 先例）、PTX-only、不触 RD-007 / D-406 / 红线 1、完整 Toolkit 永不捆绑（r6）。cublas FFI 边界 unsafe 策略档位 **Mini**，agent 自主判档，判档争议向上取严，无体例变更 | Direct |
| v1.29 | 2026-06-17 | §4 文件清单追加 pipeline.md（RXS-0130 起续号预留，起始里程碑 M8.3：UC-02 流水线类型化语义面——affine Context/Stream/Event/Buffer 所有权与销毁纪律 / Event 记录·等待与跨 stream 同步 / 流序分配类型化 / 跨线程所有权转移 / 资源生命周期错误类别与编译期拦截判据；**复用既有 RXS-0066/0074/0077 device·运行时条款，新增仅补缺口**；复用 src/rurix-rt 运行时对象、永不 Python 原生嵌入（红线 1，SG-008）、device 维持 PTX-only、跨 stream/跨线程资源所有权以 affine 所有权 + 确定性诊断不设 UB 节、不触高级 GPU intrinsics（SG-001/002））。M8.3 开工脚手架**仅登记新文件名 + 预留区间，不落裸条款头**——条款体（RXS-0130 起）与每条 ≥1 测试锚定随 M8.3 实现 PR 同落（条款 PR 先于实现 PR，trace_matrix 维持全锚定；02 §U2 + 05 §1/§FFI + 06 + 08 §1/§2 + 01 §6 + 07 §7 + 11 §3 M8 已锁定决策的条款化，M8_CONTRACT D-M8-3 / G-M8-3 / G-M8-7 `rfc_required: none` 授权）。识别新决策面跨线程所有权转移类型化机制（Send 化 primary-context 共享句柄 + 流序分配类型化）带档位标记 **Mini**（口径已由 M8_CONTRACT §5 guardrail 锁定：src/rurix-rt unsafe 边界 undocumented_unsafe_blocks=deny + 每块 // SAFETY: + unsafe-audit 注册，safe 类型层对上全 safe），agent 自主判档，判档争议向上取严。**本里程碑不新增 RX 错误码**（资源生命周期错误类别由 Rust 类型系统原生编译期拦截 rustc 诊断，registry/error_codes.json 与 en.messages 零追加），无体例变更 | Direct |
| v1.30 | 2026-06-17 | §4 pipeline.md 行区间落地为 RXS-0130 ~ RXS-0134（M8.3 实现 PR 条款体落地，去「起续号预留」）：RXS-0130 affine Context/Stream/Event/Buffer 所有权与 RAII 销毁纪律 / RXS-0131 Event 记录·等待与跨 stream 同步语义 / RXS-0132 流序分配类型化（`InFlight` typestate）/ RXS-0133 跨线程所有权转移（Send 化 primary-context 共享句柄）/ RXS-0134 资源生命周期错误类别与编译期拦截判据，每条 ≥1 `src/rurix-rt/src/pipeline.rs` 单测锚定，trace_matrix 维持全锚定（129→134）。**本里程碑不新增 RX 码**（四类资源生命周期错误由 rustc 原生编译期诊断拦截：use-after-free E0382 / double-free E0382·E0599 / 跨 stream 未同步 E0599 / 跨线程非法转移 E0277，registry/error_codes.json 与 en.messages 零追加）。新决策面跨线程所有权转移类型化机制档位 **Mini**（unsafe-audit/rurix-rt.md U11~U16 注册），agent 自主判档，判档争议向上取严，无体例变更 | Direct |
| v1.31 | 2026-06-17 | §4 文件清单追加 release.md（RXS-0135 起续号预留，起始里程碑 M8.4：发布产物语义面——编译器/运行时/标准库按同一版号原子分发与 content-tree 完整性 / 语言本体与 NVIDIA 再分发组件分离打包（Attachment A 白名单最小集、完整 Toolkit/驱动/Nsight 永不捆绑 r6）/ 签名清单约定与验签发布前置（Authenticode + 时间戳、of-record Azure Artifact Signing）/ SBOM SPDX 构建视图 + CycloneDX 发布视图约定 / Release 层 hard-block 发布门（签名/SBOM/许可审计/bench 严格任一缺失即阻断上传）；**复用 M6 包管理 content-tree/lock RXS-0090/0092/0094 与 M5.4 NVIDIA 再分发白名单审计，新增仅补发布产物缺口**；PTX-only（cubin/fatbin 真分发 → G1 out_of_scope）、永不 Python 原生嵌入（红线 1，SG-008）、registry sumdb 维持 not_triggered（D-312/G2）、发布门以 content-tree SHA-256 + 验签判定 + 白名单审计机器事实定义不设 UB 节）。M8.4 开工脚手架**仅登记新文件名 + 预留区间，不落裸条款头**——条款体（RXS-0135 起）与每条 ≥1 测试锚定随 M8.4 实现 PR 同落（条款 PR 先于实现 PR，trace_matrix 维持全锚定；08 §9 D-241 r6 + 14 §8 Release 层 + 10 §6 工具链发布门 + 09 §6/§7 + 01 §6 已锁定决策的条款化，M8_CONTRACT D-M8-4 / G-M8-4 / G-M8-7 / RD-001 `rfc_required: none` 授权）。agent 裁定 **Azure Artifact Signing 为 of-record 签名后端**（生产签名经 CI secret + 人工门控，本机/CI 不自动调用真实证书，带档复议留痕不擅自切换）；**NVIDIA EULA Attachment A 白名单法律签署维持 `pending-human-review`**（agent 起草机器事实，自主签署，对齐 M5.4 redistribution_audit 先例）。**本里程碑发布门诊断错误码按需段位 7 RX70xx 续接**（脚手架不预造，最终随实现 PR 按 07 §5 裁定），无体例变更 | Direct |
| v1.32 | 2026-06-18 | §4 文件清单追加 interop_d3d12.md（RXS-0140 起续号预留，起始里程碑 G1.1：CUDA–D3D12 互操作呈现语义面——`ExternalBuffer`/`ExternalSemaphore` affine 类型与 import 句柄生命周期 / 生成式 context brand 与跨 context 编译期拦截 / `Ready→Acquired→Presentable` typestate 与共享 fence 偶奇值 handoff / D3D12 committed resource import ABI 与 shim 私有 present pass 布局；薄 C/C++ shim 不进语言（D-130）、PTX-only、G0 软光栅 kernel RXS-0118~0121 语义 0-byte、affine+生成式 brand+typestate 不设 UB 节）。**G1.1 spec 脚手架仅登记新文件名 + 预留区间 RXS-0140~0143，不落裸条款头**——条款体（RXS-0140 起）与每条 ≥1 测试锚定随 G1.1 实现 PR（步骤 40）同落（条款 PR 先于实现 PR，trace_matrix 维持全锚定）。档位 **Full RFC**：依据 [RFC-0001](../rfcs/0001-cuda-d3d12-interop.md)（agent 2026-06-18 已自主落笔/批准 FFI ABI / 内存模型映射(信号时序) / 安全包络 🔒 禁区 + Q1~Q5），有别于 M8 互操作/cublas/pipeline 的 Direct（契约 rfc_required:none 初版条款化）；agent 自主判档，判档争议向上取严。授权 G1_CONTRACT D-G1-1 / G-G1-1 / G-G1-6 + G1_PLAN §1，无体例变更 | **Full RFC**（RFC-0001） |
| v1.33 | 2026-06-19 | §4 文件清单追加 async_buffer.md（RXS-0144 起续号预留，起始里程碑 G1.2：流序分配 `AsyncBuffer<'stream,T>` 类型契约语义面——运行时流序分配器 `cuMemAllocAsync` + `CUmemoryPool` + affine 所有权与流序分配/释放 RAII / 分配未完成访问被 stream 序排除（在途态无 host 读接口）/ 释放后访问编译期生命周期错误 / 跨 stream 经 `share_with(other,event)` 显式时序边 / 三类流序分配生命周期错误编译期拦截判据；**复用 M8.3 RXS-0130~0134 affine 资源 + `InFlight` 流序分配类型化先例，新增仅补流序分配器缺口**；复用 src/rurix-rt 运行时对象、PTX-only、affine 所有权不设 UB、不触 VMM/多 GPU（G2，D-232）、Graph API 仅评估不立项）。**G1.2 spec 脚手架仅登记新文件名 + 预留区间 RXS-0144~0148，不落裸条款头**——条款体（RXS-0144 起）与每条 ≥1 测试锚定随 G1.2 实现 PR（步骤 42）同落（条款 PR 先于实现 PR，trace_matrix 维持全锚定）。档位 **Mini-RFC**：依据 [MR-0001](../rfcs/mini-0001-async-buffer.md)（owner 2026-06-19 经 AskUserQuestion 批准 §2 API 形态 + §3 判档 + §4 零新码 + §6 范围），有别于 M8 互操作/cublas/pipeline 的 Direct（纯类型级 typestate + 生成式 `'stream` brand，不改 rustc/MIR、不触内存模型映射/FFI ABI 禁区；`cuMemAllocAsync` 等为稳定 Driver API 薄层绑定，与 M8.3 `cuEvent*` 同类）；agent 自主判档，判档争议向上取严。授权 G1_CONTRACT D-G1-2 / G-G1-2 + G1_PLAN §2，无体例变更 | **Mini-RFC**（MR-0001） |
| v1.34 | 2026-06-20 | §4 文件清单追加 engine_integration.md（RXS-0149 起续号预留，起始里程碑 G1.3：引擎集成语义面——引擎集成 DLL 打包约定 + C ABI 头文件与导出 ABI 逐一对应；**复用** M8.1 RXS-0125 既有手写 `extern "C"` C ABI 语义 0-byte、`cdylib` 产 DLL（rurix-cublas 先例）、自建最小 C++/D3D12 harness、仅承担 compute pass 不进图形着色阶段（G2，D-131）、永不 Python 原生嵌入（红线 1，SG-008）、device 维持 PTX-only、affine 所有权 + 确定性诊断不设 UB 节）。**G1.3 spec 脚手架仅登记新文件名 + 预留区间 RXS-0149，不落裸条款头**——条款体（RXS-0149）与每条 ≥1 测试锚定随 G1.3 实现 PR（步骤 43）同落（条款 PR 先于实现 PR，trace_matrix 维持全锚定）。档位 **Mini-RFC**：依据 [MR-0002](../rfcs/mini-0002-engine-integration.md)（owner 2026-06-20 经 AskUserQuestion 裁决 §3 判档 Mini 复用 C ABI + §2 宿主自建 harness），有别于 G1.1 的 Full RFC（复用既有 C ABI 不扩 ABI 表面、不实现 `#[export(c)]` codegen——`#[export(c)]` 编译器 codegen + 内建头文件生成 defer RD-009）；agent 自主判档，判档争议向上取严。授权 G1_CONTRACT D-G1-3 / G-G1-3 + G1_PLAN §3，无体例变更 | **Mini-RFC**（MR-0002） |
| v1.35 | 2026-06-20 | §4 engine_integration.md 行区间更新为 RXS-0149（G1.3 实现 PR 条款体落地：RXS-0149 引擎集成 DLL 打包与 C ABI 头文件对应——`cdylib` 产物 rurix_engine.dll + import lib、导出符号集与随附头文件声明逐一对应、compute pass C ABI **复用** RXS-0125 既有 `extern "C"` 形态不扩 ABI 表面、宿主自建最小 C++/D3D12 harness 经头文件 + import lib 链接承担 compute pass、复用既有 device kernel saxpy/reduce 语义 0-byte，≥1 `src/rurix-engine` 单测锚定 `c_abi_header_matches_exports`，trace_matrix 维持全锚定）。错误码**零新增**（复用 RX7013~7015，对齐 G1.1/G1.2 零新码先例）。实现裁决：rurix-engine 引擎边界 crate `unsafe_code` 经裁决最小开 C ABI 导出属性 + unsafe-audit U21 注册（对齐 rurix-interop / rurix-cublas），前向 rurix-interop safe API 本层无 unsafe 块；`#[export(c)]` 编译器 codegen defer RD-009；仅承担 compute pass 不进图形着色阶段（G2，D-131）。档位 **Mini-RFC**（MR-0002），agent 自主判档，判档争议向上取严 | **Mini-RFC**（MR-0002） |
| v1.36 | 2026-06-22 | §4 release.md 行区间延伸补 RXS-0150 ~ RXS-0152（G1.5/MR-0005 生产分发 fatbin 语义面**续承 release.md**，agent 裁定不新建文件：RXS-0150 分发产物变体模型与按架构预编 cubin + 保守 PTX fallback / RXS-0151 fatbin 装载协商序 / RXS-0152 lockfile `[[artifact]]` 变体 digest 与内容寻址锁定；条款体落 spec/release.md §2.5，复用 RXS-0073 ptxas 干验证 + RXS-0076/0077 PTX 装载协商 + RXS-0090/0093 content-tree SHA-256，**M8.4 既有条款 RXS-0135 ~ RXS-0139 条款体 0-byte**，每条 ≥1 `src/rurix-rt` / `src/rurix-pkg` 单测锚定）。档位 **Mini-RFC**：依据 [mini-0005-fatbin-distribution.md](../rfcs/mini-0005-fatbin-distribution.md)（owner 2026-06-22 经 AskUserQuestion 裁决档位 Mini-RFC + 落点 release.md + `[[artifact]]` 落 rurix.lock + 不立性能门），有别于 G1.1 Full RFC（复用既有 PTX 装载协商不扩 FFI ABI / 安全包络，cubin 经稳定 `cuModuleLoadData` 装载、装载协商降级而非 reject）；agent 自主判档，判档争议向上取严，无体例变更 | **Mini-RFC**（MR-0005） |
| v1.37 | 2026-06-23 | §4 文件清单追加 shader_stages.md（RXS-0153 起续号预留，起始里程碑 G2.1：着色阶段类型面——vertex/fragment/compute/mesh/task + RT raygen/closesthit/anyhit/miss 作为 kernel 着色扩展进语言的类型面/语法面：着色阶段函数着色规则（扩展 RXS-0066）/ 阶段专属 I/O 语义类型（插值限定 type-level + 内建变量类型化）/ 阶段间接口类型契约（vertex out → fragment in 兼容性编译期校验）/ 资源句柄·纹理采样器参数化类型的类型面（`Texture2D<F>`/`Sampler` 平行 `View<space,T>`）；复用 kernel 子语言+views、device⊂host 单向可达（05 §1）、trait 单态化子集（D-104）、device 维持 PTX-only（07 §7）、DXIL codegen（G2.2，D-131）/ 绑定布局推导（G2.3，P-11）/ 🔒 纹理路径内存模型映射（06 §4.2 禁区）均不在本文件、着色阶段误用/阶段间接口不匹配/资源句柄违例以编译期诊断 P-01 strict-only 定义不设 UB 节）。**G2.1 spec 脚手架仅登记新文件名 + 预留区间 RXS-0153~0156，不落裸条款头**——条款体（RXS-0153 起，已锁定 4 条 RXS-0153~0156，RFC-0002 §9 Q5）与每条 ≥1 测试锚定随 G2.1 实现 PR（PR-B2，步骤 45）同落（条款 PR 先于实现 PR，trace_matrix 维持全锚定 152/152）。档位 **Full RFC**：依据 [RFC-0002](../rfcs/0002-shader-stages.md)（agent 2026-06-23 自主裁决 §9 Q1~Q6 与 §4.5 🔒 禁区边界处置/批准，AI agent 自主），有别于 M8 互操作/cublas/pipeline 的 Direct（本设计触新语法 + 类型系统扩张——着色阶段函数着色/阶段 I/O type-level 标注/阶段间接口契约/纹理采样器参数化类型，AGENTS 硬规则 5 / 10 §3 Full RFC 触发面）；agent 自主判档，判档争议向上取严。错误码**脚手架不预造、不预留**（着色阶段语义诊断归 3xxx 段 RX3011+ 续号，随 PR-B2 按真实可达类别只追加）。授权 G2_CONTRACT D-G2-1 / D-G2-6 / G-G2-1 / G-G2-6 + G2_PLAN G2.1 首子里程碑，无体例变更 | **Full RFC**（RFC-0002） |
| v1.38 | 2026-06-23 | **§4 shader_stages.md 区间格更新为条款体已落地**(G2.1 实现 PR / PR-B2):shader_stages.md §2 计划骨架升格为带编号条款体 `### RXS-0153 ~ ### RXS-0156`(FLS 体例,按需分 Syntax/Legality/Dynamic Semantics/Implementation Requirements 节,严禁 UB 节;Legality 引用 RX 码)——RXS-0153 着色阶段函数着色规则(前缀式 `<stage> fn`,kernel 入口着色,直接调用/跨着色非法调用复用 RX3001)/ RXS-0154 阶段专属 I/O 语义类型(`#[interpolate]`/`#[builtin]` 属性式,无标注字段 → RX3011)/ RXS-0155 阶段间接口类型契约(vertex out→fragment in varying 兼容 → RX3012)/ RXS-0156 资源句柄·纹理采样器参数化类型面(`Texture2D<F>`+`Sampler` 仅着色阶段签名形参 → RX3013)。配套 rurixc 着色阶段前端(parser 上下文关键字 `<stage> fn` + AST 层类型面检查,gate cargo feature `shader-stages`)+ conformance accept/reject(`conformance/shader/`)+ UI golden(`tests/ui/shader/*.stderr` 经 bless)+ 每条 ≥1 `//@ spec` 锚定(**trace_matrix 152→156 全锚定**)。错误码 RX3011~3013 落 error_codes.json(3xxx 段续号,着色阶段误用复用 RX3001)+ en/zh message-key(bilingual 覆盖)。仅类型面/语法面 + 编译期拦截,不碰 DXIL codegen(G2.2)/绑定布局推导(G2.3)/🔒 纹理内存模型映射(06 §4.2 禁区);区间锁定 4 条不拆条。档位 **Full RFC**(RFC-0002),无体例变更 | **Full RFC**（RFC-0002） |
| v1.39 | 2026-06-24 | §4 文件清单追加 dxil_backend.md（RXS-0157 起续号预留，起始里程碑 G2.2：DXIL 第二后端语义面——MIR→DXIL 第二后端，承 RFC-0003：codegen target 分发与 DXIL 后端分叉（MIR 之后 target 选择，PTX/DXIL 并存，strict-only 无 fallback）/ 着色阶段着色 → DXIL 着色器类型降级对应 / 阶段 I/O → DXIL 签名·系统值语义降级 / 阶段间接口 → DXIL 阶段链接一致性核对；承 RFC-0002 着色阶段类型面 RXS-0153~0156、**D-131=A**（LLVM DirectX 后端直接 emit DXIL，owner 凭 G2.2 round-1~8 双路 spike 证据裁定，RFC-0003 §9 Q-D131 C→A + 13 §D-131 待决→A 经独立勘误 PR；A 路 dev 工具链偏差由 RD-011 跟踪）、device 维持 PTX-only 并存（07 §7）、gate `cargo feature dxil-backend`、`rx build --target dxil`；🔒 纹理路径内存模型映射（06 §4.2 禁区）/ FFI ABI 二进制布局（RFC-0003 §4.6 / §9 Q-Builtin）/ 绑定布局推导（G2.3，P-11）/ 多后端架构承诺（D-008/SG-003）均不在本文件、target 不支持/降级失败/非法 I/O 映射以编译期 6xxx codegen 诊断 P-01 strict-only 定义不设 UB 节）。**PR-C1 spec 脚手架沿 README v1.32 interop_d3d12.md / v1.33 async_buffer.md / v1.37 shader_stages.md 脚手架先例:仅登记文件名 + 预留区间 RXS-0157~，不落裸条款头**——dxil_backend.md **零 `### RXS-####` 条款头**，条款体（RXS-0157 起）与每条 ≥1 测试锚定随 PR-C2（DXIL 后端实现 PR）同落（条款 PR 先于实现 PR，**trace 维持全锚定 156/156**，零新 RXS、无悬空锚点）。区间大小未锁定（RFC-0003 §9 Q-Range：随路径裁定与条款数一并定），故 RXS 末号锚定数不变（156 不动）。档位 **Full RFC**：依据 [RFC-0003](../rfcs/0003-dxil-backend.md)（owner Approved 2026-06-23，触 codegen 第二后端 + target 分发，AGENTS 硬规则 5 / 10 §3 Full RFC 触发面），有别于 M8 互操作/cublas/pipeline 的 Direct；agent 自主判档，判档争议向上取严。错误码**脚手架不预造、不预留**（DXIL codegen 诊断归 6xxx 段，随 PR-C2 按真实可达类别只追加）。授权 G2_CONTRACT D-G2-2 / G-G2-2 + G2_PLAN G2.2 子里程碑，无体例变更 | **Full RFC**（RFC-0003） |
| v1.40 | 2026-06-24 | §4 dxil_backend.md 区间格更新为**条款体已落地（PR-C2 分片1）**：dxil_backend.md §2 升格为带编号条款体 `### RXS-0157`（codegen target 分发与 DXIL 后端分叉，FLS 体例分 Syntax/Legality/Dynamic Semantics/Implementation Requirements，严禁 UB 节；Legality L1 后端可用性/L2 最小子集/L3 降级失败 → RX6007）。配套 rurixc DXIL 后端最小实现（`dxil_codegen` 模块 + `--target dxil` 分发 + cargo feature `dxil-backend` + MIR→DirectX 三元组 LLVM IR + patched llc 经 `RURIX_LLC` dev env 定位 RD-011 + dxc validator accept）+ conformance accept/reject（`conformance/dxil/`）+ DXIL golden（文本反汇编经 validator + bless）+ 每条 ≥1 `//@ spec: RXS-0157` 锚定（**trace_matrix 156→157 全锚定**）。错误码 RX6007 落 error_codes.json（6xxx codegen/目标段续接 RX6006，只追加）+ en/zh message-key（bilingual 覆盖）。RXS-0158/0159/0160 仍为 §9 Q-Range 计划映射（非裸条款头），随后续分片落地；本片不碰 DXIL codegen 完整阶段着色器类型对应/绑定布局推导（G2.3）/🔒 纹理内存模型映射（06 §4.2 禁区）。档位 **Full RFC**（RFC-0003），无体例变更 | **Full RFC**（RFC-0003） |
| v1.41 | 2026-06-25 | §4 dxil_backend.md 行同步**图形=B 条款计划映射（[RFC-0004](../rfcs/0004-spirv-dxil-graphics-backend.md)，SPIR-V→DXIL 图形后端，混合 compute=A/图形=B，owner Approved 2026-06-25）**：登记 **RXS-0159 保号重构（按 B）**（A 路签名 ISG1/OSG1 `elemcount=0` 不可达 → D-131 v1.4 裁定图形=B → RXS-0159 保号、PR-D2 按 B 路径 SPIR-V `BuiltIn`/`Location` decoration + 译后强制签名一致性校验门重写；**#97 A 路 RXS-0159 类型面 stub 不入 main**，由 PR-D2 统一重写）+ 新增 **RXS-0160**（阶段间接口 → DXIL/SPIR-V 阶段链接一致性核对）/ **RXS-0161**（图形着色阶段 MIR→SPIR-V 降级面：execution model / I/O decoration / 资源句柄）/ **RXS-0162**（B 转译链确定性 + validator gate + 供应链 pin `[[toolchain]]`+SHA256 + strict-only 核验，含签名一致性校验门与 golden 形态）**预留区间与重构说明**（落 dxil_backend.md §2.1 计划映射）。区间裁决已锁定（RFC-0004 §9 Q-Range-B）；feature gate **复用 `dxil-backend`**（Q-Gate-B，不新增子 gate）；golden 仅 DXIL 文本反汇编入 bless（Q-Golden-B）；错误码归 6xxx 段（承 RFC-0003 RX6007~，只追加）。RXS-0158（着色阶段→DXIL 着色器类型，#96 HOLD）维持 §9 Q-Range 计划映射不动。**PR-D1 沿 spec 脚手架先例仅登记计划映射 / 预留区间，不落条款体、零 `### RXS-####` 裸条款头**——`ci/trace_matrix.py --check` 维持全锚定 **157/157**（无新增可锚条款、无悬空锚点、零新 RXS）。条款体随 **PR-D2**（B 转译实现 PR）同落（条款 PR 先于实现 PR，硬规则 7）。禁区不动：🔒 签名二进制 ABI 布局（RFC-0004 §4.6(a)）/ 纹理路径内存模型映射（06 §4.2）/ DXIL·SPIR-V UB 边界 只引边界声明，不落禁区语义本体。档位 **Full RFC**：依据 RFC-0004（owner Approved 2026-06-25，触 codegen 新路径 + 第二中间表示 SPIR-V + 外部转译依赖 + P-01 strict-only 达标要求 + 三处 🔒 禁区边界，10 §3 / 硬规则 5 触发面），agent 自主判档，判档争议向上取严。授权 G2_CONTRACT D-G2-2 / G-G2-2 + RFC-0004 §6 栈式 PR（PR-D1 spec 脚手架先于 PR-D2），无体例变更 | **Full RFC**（RFC-0004） |
| v1.42 | 2026-06-27 | §4 dxil_backend.md 行**升格为图形=B 条款体已落地（PR-D2）**：RXS-0157（target 分发）/ RXS-0158（着色阶段→DXIL 着色器类型）/ RXS-0159（阶段 I/O→DXIL 签名·系统值语义，按 B：`UserSemantic` 保名 + 强制签名一致性校验门 `signature_gate::check` 不可裁剪）/ RXS-0161（图形阶段 MIR→SPIR-V 降级面：`dxil_spirv::emit_spirv` 纯 safe `Vec<u32>` 经 spirv-val）/ RXS-0162（B 转译链确定性 ×N SHA256 + dxc validator gate + 供应链 `[[toolchain]]` pin + strict-only）条款体落 dxil_backend.md §2（FLS 体例，严禁 UB）。错误码 **RX6010~RX6013**（B 路 strict-only：转译失败 / 签名不一致 / 声明输入被消除 / 不可映射，6xxx 段续接 RX6007 只追加 + en/zh message-key）。每条 ≥1 `//@ spec` 锚定（`dxil_sig_gate`/`dxil_codegen`/`dxil_spirv` 单测），`ci/trace_matrix.py --check` 全锚定 **157→161/161**。**RXS-0158 收口**:经 agent 裁决复用 **RD-012**（不新开 RD-016）、mesh/task/RT deferred 发码由 RX6008 对齐实现实发的 **RX6007**（RX6008 维持 RD-012 预留）。**RXS-0160（阶段间接口→阶段链接一致性核对）维持 §2.1 计划映射**（需 vertex+fragment 多阶段联编点，本片单阶段编译尚无该接缝，非裸条款头、承后续里程碑）。禁区不动：🔒 签名二进制 ABI 布局（RFC-0004 §4.6(a)）/ 纹理访问语义（§4.6(b)）/ DXIL·SPIR-V UB 边界（§4.6(c)）只边界声明；golden 仅 DXIL 文本反汇编、本机 NOT BLESSED 由 agent pin 环境重 bless，device 真跑 / 呈现对照 / run URL / evidence 归 agent（G-G2-2）。供应链 `[[toolchain]]` pin + CI 步骤 46（`ci/dxil_codegen_smoke.py`）随供应链子分片回填。档位 **Full RFC**（RFC-0004 / PR-D2），无体例变更 | **Full RFC**（RFC-0004 / PR-D2） |
| v1.43 | 2026-06-27 | **v1.42 RXS-0159「`UserSemantic` 保名」叙述勘误（append-only，既有行 0-byte；据 dxil_backend.md §3 v1.5 实测收窄、机制① 缺口闭合后口径对齐）**：v1.42 行述「RXS-0159（阶段 I/O→DXIL 签名·系统值语义，按 B：`UserSemantic` 保名 + 强制签名一致性校验门）」中**「`UserSemantic` 保名」表述 over-broad**——据 dxil_backend.md §3 v1.5 实测（本机 dxc 1.8.0.4739 / spirv-cross vulkan-sdk-1.3.296.0）收窄：**(1)** SPIR-V `UserSemantic` 装饰**仅作 provenance**（经 spirv-val 干净）、**spirv-cross 不消费**为 HLSL 语义、**非保名机制**；**(2)** 保名**按方向收窄**＝系统值名**恒保真**（SV_Position/SV_VertexID elemcount>0，B-over-A 核心不受影响）＋ vertex 输入名经 `spirv-cross --set-hlsl-vertex-input-semantic <location> <semantic>`（由 `dxil_codegen::vertex_input_semantic_flags` 经 io_sig 导出、按 location 覆盖）**measured 保真**（机制① 已接入生产）＋ 输出/片元 varying 名**当前不可保真 → 显式 RX6011 拒、缺口 deferred RD-017**（spirv-cross 无输出/片元语义旗标）。强制签名一致性校验门（`signature_gate::check`，不可裁剪）表述**不变、仍准确**。**不改 v1.42 行本身**（append-only）；§4 dxil_backend.md 活动单元格「按 B」已准确、无需动；RD-017 status 翻转 / golden bless / device 真跑归 agent（G-G2-2）。档位 **Full RFC**（RFC-0004），无体例变更 | **Full RFC**（RFC-0004） |
| v1.44 | 2026-06-27 | §4 dxil_backend.md 行**补 RXS-0160（阶段间接口→阶段链接一致性核对，按 B）条款体已落地 + 条款数 161→162**：RFC-0004 §5 下游条款全落地，dxil_backend.md §2 落 `### RXS-0160`（vertex out ↔ fragment in 跨阶段 varying 经 B 路降级后以语义名等价为链接键核实语义名/类型/插值一致性，错链 strict-only 显式 6xxx；FLS 体例分 Syntax/Legality/Dynamic Semantics/Implementation Requirements，严禁 UB）+ §2.1 收口（无尚未落地计划映射）。配套 `signature_gate::check_stage_link`（链接核对入口，复用 RXS-0159 `semantic_name_matches` 语义名等价，纯 host/safe 零新 unsafe）+ `dxil_codegen::link_graphics_stages`（vertex+fragment 多阶段联编点接缝，收集两阶段 io_sig；单阶段/缺一阶段 NoPair 零漂移）+ 单测（accept 链接一致 + reject 缺链接键/类型失配/插值失配，`//@ spec: RXS-0160`）+ conformance accept（`conformance/dxil/graphics/accept/` vertex+fragment 链接一致配对）。`ci/trace_matrix.py --check` 全锚定 **161→162**。**判档点（需升档，agent 闸门）**：错链错误码语义归类——旧 §2.1 映射 RX6011，但 RX6011 现由 `codegen.dxil_sig_mismatch`（单阶段输出未保真）占用；6xxx 下一空号 = **RX6014**（RX6008/RX6009 由 RD-012/RD-013 预留不复用）。「复用 RX6011」抑或「新开 RX6014」属语义归类裁决，**不擅自落码**：条款体先以占位「6xxx」表述，`check_stage_link` 返回类型化 `StageLinkError` **不**接线生产 emit、**不**改 `registry/error_codes.json` / message-key；落码 + 错链 conformance reject + golden 归 agent 裁码后实现步。禁区不动：🔒 寄存器/location 编号/mask/packing（§4.6(a)）链接核对不比对 location 数值（ABI 中立）。device 真跑 / golden bless / 新错误码语义归类裁决归 agent（G-G2-2）。档位 **Full RFC**（RFC-0004 / PR-D2），无体例变更 | **Full RFC**（RFC-0004 / PR-D2） |
| v1.45 | 2026-06-28 | §4 文件清单追加 binding_layout.md（RXS-0163~0166 计划区间，起始里程碑 G2.3：绑定布局推导语义面——RXS-0156 资源句柄 → SPIR-V DescriptorSet/Binding 装饰 / register-space 分配推导 / root signature 形态推导 + RTS0 序列化 / PSV0 反射一致性校验门 + strict-only 推导失败）。承 [RFC-0005](../rfcs/0005-binding-layout-inference.md)（owner Approved 2026-06-28，§9 全部裁决已锁：Q-Space=B 按资源种类分轴、Q-RootShape=B 混合 root descriptor/table、Q-Sampler=B dynamic、Q-Bindless=A defer RD-018、Q-Gate=A 复用 `dxil-backend`、Q-Err=6xxx 续号策略、Q-File=B 新建本文、Q-Range=4 条、Q-Inference-vs-Explicit=C 覆盖后期）。**PR-E1 spec 脚手架仅登记新文件名 + 计划区间，不落裸条款头**——条款体与每条 ≥1 `//@ spec` 测试锚定随 PR-E2 同落，trace_matrix 维持全锚定；错误码不预留、不预造。禁区不动：绑定二进制 ABI 布局 / 纹理路径内存模型 / DXIL-SPIR-V UB 边界只作边界声明，不落语义本体。档位 **Full RFC**（RFC-0005 / PR-E1），无体例变更 | **Full RFC**（RFC-0005 / PR-E1） |
| v1.46 | 2026-06-28 | §4 binding_layout.md 行**升格为条款体已落地（PR-E2a）**：RXS-0163~0166 计划映射升格为带编号条款体 `### RXS-0163 ~ ### RXS-0166`（FLS 体例，按需分 Syntax/Legality/Dynamic Semantics/Implementation Requirements，严禁 UB 节，Legality 引 RX 码或占位「6xxx」，镜像 dxil_backend.md RXS-0157~0162 先例）——RXS-0163 资源句柄→SPIR-V 资源绑定降级（opaque 资源类型 + DescriptorSet/Binding 装饰，按 io_sig 声明序确定性）/ RXS-0164 register/space 分配推导（§9 Q-Space=B 按资源种类分轴 CBV/SRV/UAV/Sampler→b/t/u/s 各自从 0 递增、首期单 space0 + register/layout 冲突核验）/ RXS-0165 root signature 形态推导（§9 Q-RootShape=B CBV root descriptor + SRV/UAV/Sampler descriptor table；Q-Sampler=B dynamic）+ RTS0 容器机械序列化（D3D12 既定 DXBC+RTS0 格式，类比 RXS-0162 DXIL 容器，非 stable）/ RXS-0166 一致性校验门（PSV0 反射 vs 推导意图比对）+ strict-only 推导失败。配套 host 侧 safe 推导逻辑 `src/rurixc/src/binding_layout.rs`（纯函数 over `mir::ResourceBinding`，零新 unsafe，照 RXS-0160 `check_stage_link` 范本）+ `mir.rs` 资源种类建模（`ResourceClass`/`MirResourceType`/`ResourceCount`/`ResourceBinding`，仅数据建模，不改既有标量/向量 I/O 路径语义）+ 每条 ≥1 `//@ spec: RXS-####` 单测锚定（accept + reject 真实红绿，feature gate 复用 `dxil-backend`，**trace_matrix 162→166 全锚定**）。**PR-E2a 不接线生产 codegen emit、不改 registry/error_codes.json（错误码占位「6xxx」，避开 RX6014 与 RXS-0160 争号）、不落 golden、不 device 真跑/bless**——生产接线 + 6xxx 落码 + golden/bless + device 真跑/run URL 归 PR-E2b（agent 闸门，G-G2-3）。bindless 维持 defer（RD-018）。禁区只作边界声明（register/space/mask/packing/descriptor 偏移/root parameter DWORD 物理布局 / 纹理路径内存模型 / DXIL·SPIR-V UB 不冻结为 stable）；deferred.json/spike_gating.json 不动、不开 SG-010、不碰 00–14。档位 **Full RFC**（RFC-0005 / PR-E2a），无体例变更 | **Full RFC**（RFC-0005 / PR-E2a） |
| v1.47 | 2026-06-28 | §4 文件清单追加 d3d12_runtime.md（RXS-0167~0170 计划区间，起始里程碑 G2.4：UC-04 deferred 渲染器 / 原生 D3D12 运行时出图语义面——运行时把 RFC-0004 DXIL 着色器对象 + RFC-0005 推导 RTS0 装配为多 pass deferred 管线：DXIL+RTS0→graphics PSO 装配一致性 / deferred 多 pass 编排（G-buffer albedo+normal+depth MRT→单光源 lighting→offscreen readback）/ 资源状态+barrier 编排锚点（首期手动 RT→SRV→RT/Copy/Readback）/ offscreen readback 像素对照）。承 [RFC-0006](../rfcs/0006-uc04-deferred-renderer.md)（owner Approved 2026-06-28，§9 全部裁决已锁：Q-Present=offscreen-first→RD-019 / Q-DemoCrate=src/uc04-demo（unsafe_code=deny，U23）/ Q-RuntimeShape=safe wrapper 运行时 ABI non-stable / Q-DeferredPass=G-buffer(albedo+normal+depth)→单光源→offscreen readback / Q-Barrier=首期手动编排→RD-020 / Q-Texture=不落纹理内存模型本体→RD-021 / Q-Range=4 条 / Q-Err=6xxx 自 RX6018（不预造）/ Q-Gate=d3d12-runtime/uc04-demo 专属（dxil-backend 仅 codegen 前置）/ Q-CIStep=step 48 offscreen REQUIRE_REAL）。落点（Q-File，owner §9 未单列）取新建 d3d12_runtime.md（镜像 RFC-0005 binding_layout.md 独立成文先例，请 owner 确认）。**PR-F1 spec 脚手架仅登记新文件名 + 计划区间，不落裸条款头**——条款体与每条 >=1 `//@ spec` 测试锚定随 PR-F2 同落，trace_matrix 维持全锚定；错误码不预留、不预造（6xxx 自 RX6018 落码归 PR-F2）。append-only 登记 RD-019（窗口 swapchain present defer）/ RD-020（自动资源状态跟踪推导 defer）/ RD-021（纹理内存模型映射 defer，须 agent Full RFC）入 registry/deferred.json。禁区不动：纹理路径内存模型映射 / barrier 并发语义本体 / 运行时 stable ABI / FFI ABI 二进制布局 / DXIL·SPIR-V UB 边界只作边界声明，不落语义本体。error_codes.json / spike_gating.json 不动、不开 SG、不改 CI、不动 src/、不碰 00–14。档位 **Full RFC**（RFC-0006 / PR-F1），无体例变更 | **Full RFC**（RFC-0006 / PR-F1） |
| v1.48 | 2026-06-28 | **Q-File 人工定调留痕（PR-F1）**:owner（Language Lead）在本工作会话确认 G2.4 UC-04 deferred 渲染器 / D3D12 运行时出图语义面的 spec 落点采用新建 `spec/d3d12_runtime.md`，不延伸既有 spec 文件；Codex 仅代录该人工决定，非 AI 代签 / 代决。§4 文件清单既有 `d3d12_runtime.md` 行维持有效；本行仅补 v1.47 所述“请 owner 确认”的人工闭环，不落裸条款头、不改实现/CI/error_codes/golden/device。 | **Full RFC**（RFC-0006 / PR-F1） |
| v1.49 | 2026-06-29 | §4 d3d12_runtime.md 行**升格为条款体已落地（PR-F2 blocked-honest interim slice）**：RXS-0167~0170 计划区间升格为带编号条款体 `### RXS-0167 ~ ### RXS-0170`（FLS 体例，按需分 Syntax/Legality/Dynamic Semantics/Implementation Requirements，严禁 UB 节，Legality 引 6xxx 实码，镜像 binding_layout.md RXS-0163~0166 先例）——RXS-0167 DXIL+RTS0→graphics PSO 装配一致性（host 侧装配核验，渲染目标失配 RX6018 / RTS0↔绑定失配 RX6019 复用 RFC-0005 `check_binding_consistency` P-11）/ RXS-0168 deferred 多 pass 编排（几何 MRT→lighting SRV 采样→readback 结构核验，pass 顺序/目标缺失 RX6020；device 像素对照 blocked-on-RD-013）/ RXS-0169 资源状态+barrier 编排锚点（RT→SRV→RT/Copy/Readback 锚点存在性/合法性，缺/非法 RX6021；🔒 并发语义本体不落「需升档」）/ RXS-0170 offscreen readback+像素对照（host 锚=readback 布局 RX6022；device 像素对照 REQUIRE_REAL blocked-on-RD-013）。配套 host 侧 safe 装配/编排模型 [`src/uc04-demo`](../src/uc04-demo)（纯 host/safe，**零新 unsafe → 不消费 U23**；复用 [`binding_layout`](../src/rurixc/src/binding_layout.rs) RTS0 推导 P-11）+ 新 feature gate `d3d12-runtime`（不复用 `dxil-backend`）+ 每条 ≥1 `//@ spec` 单测锚定（`ci/trace_matrix.py` 增扫 `src/uc04-demo`，**166→170 全锚定**）+ 6xxx 装配期错误码 **RX6018~RX6022**（registry/error_codes.json append-only + en/zh message-key `runtime.uc04_*`，ci/bilingual_coverage.py PASS）。**device 段 blocked-honest**（owner 2026-06-29 裁定 interim slice）：hardware 多 pass deferred draw + offscreen 像素对照 / DXIL·像素 golden bless / CI step 48 接线（本轮**不**接线 `pr-smoke.yml`，避免常驻红门）/ device run URL / **G-G2-4 签字**均**未达成**——前置 RD-013（图形=B 入口 body 数据流降级）open，无 Rurix 自产可出图着色器；按 G-G2-4 防降级硬门不以任何替代物伪造 device 绿，标 blocked 不签。deferred.json append-only 留痕（RD-013/019/020/021，无 status 翻转）。禁区不动：纹理路径内存模型（RD-021）/ barrier 并发语义本体 / 运行时 stable ABI / FFI ABI 二进制布局 / DXIL·SPIR-V UB 只作边界声明。档位 **Full RFC**（RFC-0006 / PR-F2），无体例变更 | **Full RFC**（RFC-0006 / PR-F2） |
| v1.50 | 2026-06-29 | §4 dxil_backend.md 行追加 **RXS-0171**（RD-013 图形=B 着色 body I/O 数据流降级最小切片）：记录 owner/owner 代表裁决 Q1=C1-a 值语义、Q2=C2-a 字段序绑定、Q3=C3-c ABI 中立分层、Q4=C4-c refined 白名单子集 + RX6013 strict-only；条款体落 spec/dxil_backend.md，生产分发接 full Body lowering，测试锚定覆盖字段序、OpLoad/OpStore、常量/算术、unsupported rvalue RX6013 与 conformance body dataflow。RXS-0167~0170 仍归 d3d12_runtime.md，不复用；RD-013 status 仍 open，golden/device bless、CI step 48、G-G2-4 签字归 agent。 | **Full RFC**（RFC-0004 / RD-013 agent 裁决） |
| v1.51 | 2026-06-30 | §4 文件清单追加 edition.md（RXS-0177~0180，起始里程碑 G2.5：edition 机制语义面——`[package].edition` 声明语义与缺省兼容 / edition 解析校验规则（合法集 `{ "2026" }`，确定性纯函数）/ edition 不匹配·未知诊断（未知 edition → **RX7020** `toolchain.edition_unknown` strict-only 拒，无 fallback，P-01；值类型错误复用 RX7005）/ stable 面与 edition 关系（edition 作 stable 面版本锚边界，语言 1.0 stable 面以首个 edition `"2026"` 为基准，加性演进，快照字节内容非语言 ABI 保证））。承 [RFC-0008](../rfcs/0008-edition-stabilization.md)（agent Approved 2026-06-30，§9 全裁：Q-Name=`"2026"` / Q-Scope=仅机制锚点空差异集 / Q-Decl=`[package].edition` / Q-Default=缺省取首个 edition / Q-Mismatch=strict-only 拒 / Q-ErrCode=新码 RX7020 / Q-File=新建 edition.md / Q-Range=4 条 / Q-RD008=激活 / Q-Stabilize=10 §5/§6/§2.2 FCP-lite）。**本轮已落带编号条款体 `### RXS-0177 ~ ### RXS-0180`**（FLS 体例，按需分 Syntax/Legality/Dynamic Semantics/Implementation Requirements，**严禁 UB 节**）+ edition 解析/校验实现落 [`src/rurix-pkg/src/manifest.rs`](../src/rurix-pkg/src/manifest.rs)（`Edition` 枚举 + `Edition::parse` 纯函数 + `Manifest.edition` 缺省 `Edition2026` + 未知 → `PkgError::EditionUnknown`(RX7020)，纯 host/safe，零新 unsafe）+ 每条 ≥1 `//@ spec: RXS-####` 测试锚定（`src/rurix-pkg` 单测 + `conformance/edition/` accept|reject fixtures，**trace_matrix 176→180 全锚定**）。错误码 **RX7020**（`toolchain.edition_unknown`，7xxx 工具链段续号接 RX7019，append-only）+ en/zh message-key（`ci/bilingual_coverage.py` 对齐）；edition 值类型错误复用 RX7005（不新增）。首个 edition `"2026"` 仅机制锚点（edition-gated 行为差异 = 空集）。**RD-008 stable API 快照冻结机制经本里程碑激活**（RFC-0008 §9 Q-RD008）：定义 stable 面（RXS 条款 ID 全集 + 冻结错误码 ID/含义 + edition 合法值集 + rx CLI 子命令面）+ 落 [`ci/stable_snapshot.py`](../ci/stable_snapshot.py) + [`tests/stable/stable_api.snapshot`](../tests/stable/stable_api.snapshot) + `ci/check_guardrails.py` `check_stable_snapshot_bless` 守卫分支 + agent 自主 bless 首份快照，RD-008 open→closed（registry/deferred.json append-only）。不触红线（D-008/SG-003 多后端 / SG-008 Python 嵌入 / D-312/SG-007 registry 维持 not_triggered）、不触 🔒 禁区（无 UB / 内存模型映射 / FFI ABI 二进制布局 / 安全包络）。 | **Full RFC**（RFC-0008） |
| v1.52 | 2026-07-14 | §4 release.md 行追加 **RXS-0185 ~ RXS-0186**（V1.2/MR-0008 续承：最小 stable channel 清单——语言 1.0 首个 stable 发行的发行渠道身份锚）：RXS-0185 channel 清单存在性·字段语义·确定性序列化（`rurixup release` 追加产出 `channel_manifest.json`，channel ∈ {stable} 首版合法集，`bundle_manifest_sha256` = bundle.json 字节流 SHA-256 内容寻址引用复用 RXS-0093，组件干名字典序，无时间戳同一输入两次逐字节一致；不实现 install/update/channel 切换，nightly 不建）/ RXS-0186 channel 与 bundle 同版号一致性判据 + Release 层发布门延伸（第 8 子门 channel-manifest 追加末位，既有 7 门相对顺序 0-byte，清单缺失/漂移/版号不符 → 发布阻断退出码 2）。**RXS-0181~0184 已被 GRX showcase 分支（未合 main）claim，跳号避撞（编号永不复用 10 §9.5）**。条款体与每条 ≥1 `//@ spec` 单测锚定（`src/rurixup/src/channel.rs` + gate/lib 端到端）同 PR 落（条款 commit 先于实现 commit，trace_matrix 180→182 全锚定）；零新 RX 码（工具层退出码 + failed_gates 枚举，确需则 RX7021 续接停手不预造）+ 零新 unsafe;stable 快照（RD-008）因条款计数 180→182 同 PR 重 bless + `tests/stable/bless_log.md` 追加（RXS-0180 L2 加性演进）;CI 步骤 50 `ci/channel_manifest_smoke.py`（纯 host,green + 确定性 + 漂移注入红 + 未知 channel 红 + 复原绿）。依据 [mini-0008-stable-channel-manifest.md](../rfcs/mini-0008-stable-channel-manifest.md)（agent Approved 2026-07-14）+ V1_CONTRACT D-V1-3 / G-V1-3。不触红线/禁区。 | Mini-RFC（MR-0008） |
| v1.53 | 2026-07-14 | §4 release.md 行追加 **RXS-0187 ~ RXS-0188**（post-V1/MR-0009 续承：rurixup 工具链前端首切片——兑现 MR-0008 预留的 rustup 式前端锚点,08 §9 D-241 locked 意图）：RXS-0187 工具链版号注册表与默认切换（ToolchainRegistry 多版号注册幂等 + default 指针 + set_default 未注册版号拒 + 确定性序列化 round-trip·无时间戳）/ RXS-0188 stable channel 消费与 install 内容寻址校验（channel 一致性 RXS-0186 + `bundle_manifest_sha256` == 实测 sha256(bundle_json) RXS-0093/0135,全有或全无不注册；幂等；首装成 default）。`rurixup install/list/default` 纯 host、纯确定性、零网络端点、零真实 FS 物化；复用 install.rs RXS-0135 原子安装内核 + channel.rs RXS-0186 判据。条款体与每条 ≥1 `//@ spec` 单测锚定（`src/rurixup/src/toolchain.rs`）同 PR 落（条款 commit 先于实现 commit，trace_matrix 182→184 全锚定）；零新 RX 码（ToolchainError 工具层 + 退出码 1）+ 零新 unsafe;stable 快照因条款计数 182→184 同 PR 重 bless + `tests/stable/bless_log.md` 追加（RXS-0180 L2 加性演进）;CI 步骤 51 `ci/toolchain_frontend_smoke.py`（纯 host,green + 幂等 + list + 篡改 bundle 红 + 未注册版号红 + 复原绿）。**真实 FS 物化 + 网络拉取 defer RD-025**。依据 [mini-0009-toolchain-frontend.md](../rfcs/mini-0009-toolchain-frontend.md)（agent Approved 2026-07-14）。不触红线/禁区。 | Mini-RFC（MR-0009） |
| v1.54 | 2026-07-14 | §4 文件清单追加 host_orchestration.md(MS1.2/RFC-0009 续承:single-source 宿主 GPU 编排语义面——host `.rx` 经 std::gpu 首期收敛子集编排 GPU + 同源 PTX(+可选 sm cubin)描述表嵌入单 EXE + rxrt C ABI staticlib 绑定 rurix-rt + 前端机械面)。**本轮落带编号条款体 RXS-0189 ~ RXS-0196**(FLS 体例,按需分 Syntax/Legality/Dynamic Semantics/Implementation Requirements,严禁 UB 节):RXS-0189 std::gpu 宿主编排类型面与 affine 语义(Context/Stream/Buffer/PinnedBuffer lang items 非 Copy affine,move/borrow 违例复用 RXS-0054/0057~0061 零新借用码,宿主 API 着色合法性 kernel/device 内使用 → RX3015)/ RXS-0190 宿主 GPU 方法签名与元素类型推断(编译器已知签名,T ∈ {f32,i32,u32},元素类型不可定型 → RX2010)/ RXS-0191 launch 宿主 lowering 与实参借用/marshalling(兑现 RXS-0074/0075 类型契约执行语义,kernel 引用编译期绑定同源 device 入口符号,ViewMut 位 &mut·View 位 & 调用期借用,实参子集外 → RX6024,🔒 slot+kinds marshalling 含义冻结非语言 ABI)/ RXS-0192 single-source device 产物嵌入与装载协商(PTX 必存 + 可选 sm cubin 描述表,复用 RXS-0150/0151/0076 协商与 RXS-0073/0082 编译期纪律,嵌入失败 → RX6025,工具链缺失嵌哨兵运行期确定性失败)/ RXS-0193 宿主运行期错误与 poisoned 语义(确定性诊断+终止,poisoned 对齐 RXS-0077,无 UB 无静默降级 P-01 正面语义条款)/ RXS-0194 rxrt C ABI 运行时边界(🔒 FFI:rxrt_* 符号面/u64 句柄/free 前 sync 销毁纪律,含义冻结·布局不冻结为语言 ABI RXS-0180 L3 口径,复用 RXS-0125 手写 extern "C" 口径,unsafe 集中 cabi + U25)/ RXS-0195 extern "C" 符号保名与 `#[link]` 接线(字面名链接 + .lib 追加,定位失败 RX7022;rurix_rt_cabi 定位/构建失败 RX7021 按内聚并入本条,host_orchestration.md §4 留痕)/ RXS-0196 out-of-line 模块文件加载(`mod name;` 同目录装配内联等价,缺失/循环 → RX1005)。**RXS-0197 ~ RXS-0199(present 宿主 typestate 面 / present backbuffer 借用缓冲与 blit 契约 / 宿主图像落盘桥)为该文件预留区间,仅登记说明不落裸条款头,条款体随 MS1.2b 实现 PR 同落**。编译期新码 spec 先行引用,registry/error_codes.json 落码 + en/zh message-key 随 MS1.2 实现 commit 同 PR 落(真实可达不预造);运行期失败走确定性诊断+终止不占 RX 码;每条 ≥1 测试锚定随实现 commit 同 PR 落(条款 commit 先于实现 commit,硬规则 7;trace_matrix 184→192 全锚定;stable 快照同 PR 重 bless,RXS-0180 L2 加性演进)。依据 [RFC-0009](../rfcs/0009-host-gpu-orchestration.md)(Agent Approved 2026-07-14,🔒 §4.3/§4.4 FFI 面 agent 亲笔)+ MS1_CONTRACT D-MS1-2 / G-MS1-1 / G-MS1-2。不触红线/禁区(RD-026/RD-027/D-130/D-008·SG-003/RXS-0171 白名单/RD-009 维持),无体例变更 | **Full RFC**(RFC-0009) |
| v1.55 | 2026-07-15 | §4 host_orchestration.md 行更新(MS1.2b/RFC-0009 续承):**RXS-0197 ~ RXS-0199 预留区间兑现,升格为条款体已落地**——RXS-0197 present 宿主 typestate 面(Present/Ready/Acquired/Presentable 非 Copy affine lang items + Present::create(&ctx, rw, rh, ww, wh) / ready() / wait() / backbuffer() / signal() / pump() / present() 编译器已知方法集;消费式转移 self 按值 move,错序 = 编译期 move 违例复用 RXS-0054(RX4001/RX4003)零新码;wait/signal/present 对应共享 fence acquire/cuda_done/d3d_done 步,偶/奇值协议引用 RXS-0142 不重述,单一事实源留 interop 帧机;ready() 纯类型面转移不落运行时符号;运行期错序/失败 = 确定性诊断 + 终止 RXS-0193 口径;窗/泵/交换链维持 C++ shim D-130 0-byte,cabi rxp_* 只做状态断言转发,RXS-0194 纪律)/ RXS-0198 present backbuffer 借用缓冲与 blit 契约(仅 Acquired 态可得,产借用句柄 Buffer<C, f32> 元素定型 f32;drop 不释放底层共享资源,对齐 RXS-0140/RXS-0124;可作 launch 实参 RXS-0191 子集内;内容布局 = 行主序紧密共享 f32 RGB 分量 0…255,引用 RXS-0143 / 对齐 RXS-0121)/ RXS-0199 宿主图像落盘桥(write_ppm(path: &str, w: u32, h: u32, data: &PinnedBuffer<C, f32>) **形态定案 = 编译器已知自由函数**(值位置兜底可遮蔽,解析锚点镜像 Context::create,不引入 Image 容器 lang item);→ rxio_write_ppm,RXS-0114~0117 确定性 PPM 序列化 0-byte 复用、量化 = RXS-0116;n ≠ w·h·3 / IO 失败 = 运行期确定性失败 RXS-0193;data 形参位为元素类型使用点约束 RXS-0190)。**零新 RX 码**(present 错序 RX4001/RX4003、非本态方法/元数/类型 RX2004/RX2003/RX2001、着色 RX3015 复用,RXS-0189 同识别面),registry/error_codes.json 与 en/zh messages 0-byte;零新 unsafe(语言面,cabi rxp_*/rxio_* 归运行时侧 U25 纪律)。每条 ≥1 测试锚定(conformance/host_orch/accept/present_loop·imageio_write + reject/present_out_of_order·present_in_kernel + rurixc corpus 单测)随 MS1.2b 实现 commit 同 PR 落(条款 commit 先于实现 commit,硬规则 7;trace_matrix 192→195 全锚定;stable 快照因条款计数 192→195 同 PR 重 bless,RXS-0180 L2 加性演进)。依据 [RFC-0009](../rfcs/0009-host-gpu-orchestration.md)(Agent Approved 2026-07-14,§9 Q-Present 裁决)+ MS1_CONTRACT D-MS1-2 / G-MS1-2。不触红线/禁区(D-130/RD-027 维持,RD-026/D-008·SG-003/RXS-0171 白名单/RD-009 不动),无体例变更 | **Full RFC**(RFC-0009) |
