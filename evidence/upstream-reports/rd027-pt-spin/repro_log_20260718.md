> **Status: DRAFT — do NOT file.** Owner review gate; agent does not file externally.

# repro log — RD-027 PT poison-path discrimination campaign (2026-07-18)

Verbatim commands + key outputs for the ptxas/driver-JIT BSYNC-deadlock report. Commands are
the campaign drivers (per `evidence/rd027_pt_poison_spike_report.md` §7) plus the manual
single-repro recipe; outputs are quoted from `build/spike-rd027/campaign.jsonl` (107-line
per-run JSONL). Every GPU run went through the `bench/proc_guard` watchdog (kill process
tree + honest RED exit 124 on timeout, NOT skip; hung exes quarantined); classification is
by **exit code**, never by grepping stdout. A known-green canary + nvidia-smi responsiveness
gate ran after every hang verdict: 14 passed / 0 failed.

- Date: 2026-07-18 (campaign 17:37–19:07 +08:00)
- Machine: Windows 11 build 10.0.28120, NVIDIA GeForce RTX 4070 Ti (CC 8.9, WDDM, HAGS on,
  TDR at OS defaults), driver 620.02 (NVML 13.620.02, CUDA driver API 13.2)
- Toolkit: CUDA 13.3 — ptxas `Cuda compilation tools, release 13.3, V13.3.33`;
  nvdisasm / compute-sanitizer same toolkit
- Artifact residency: everything under `build/spike-rd027/` (campaign.jsonl, bin/*.ptx,
  variants/, e5/*.sass, ptxas_wrap/) is a **workspace artifact — not committed, regenerable**
  (regeneration commands in `PROVENANCE.md`). Tracked sources: `spike/rd027-pt-poison/*`
  (run_e0a/e0b/e1/e4/e7a/e7b.py, spike_common.py, attribution.json, mrp/), plus the two
  evidence files under `evidence/`.

---

## 1. Build toolchain + variants (single-artifact check)

```
$ cargo build -p rurixc -p rx
$ py -3 spike/rd027-pt-poison/run_e0a.py
```

The driver copies `apps/ruridrop/src` per variant, patches `params.rx`
(`SPP 32→8 / SPP_BATCH 32→8 / PT_BOUNCES 2→{2,3,4} / REND_FRAMES 8→1`; poison_256 =
`SPP 256 / batch 32 / bounces 2`), then builds `target\debug\rx.exe build <variant>/offline.rx
-o <name>.exe`. All five configs produced **byte-identical PTX**:

```
build ctrl_b2    {spp 8,  batch 8,  bounces 2}  ptx_sha256=85d597dd22e2d05f...c738e315
build ctrl_32    {spp 32, batch 32, bounces 2}  ptx_sha256=85d597dd22e2d05f...c738e315
build poison_b3  {spp 8,  batch 8,  bounces 3}  ptx_sha256=85d597dd22e2d05f...c738e315
build poison_b4  {spp 8,  batch 8,  bounces 4}  ptx_sha256=85d597dd22e2d05f...c738e315
build poison_256 {spp 256,batch 32, bounces 2}  ptx_sha256=85d597dd22e2d05f...c738e315
single_artifact_check: distinct_ptx_digests=1, confirmed_single_artifact=true
```

Hang vs no-hang is therefore selected purely by runtime kernel parameters + data.

## 2. E0a — baseline on the 13.3 toolchain (ptxas AOT default = -O3)

```
run ctrl_b2    -> completed     exit=0    wall=0.62s   stdout: "RENDER_OK frames=8"
run ctrl_32    -> completed     exit=0    wall=0.94s   stdout: "RENDER_OK frames=8"
run poison_b3  -> hang_timeout  exit=124  wall=120.32s
    stderr: "[proc_guard] run:poison_b3: TIMEOUT 120s -> killed process tree (pid=48688),
             quarantined=1 exe(s); honest RED (exit 124, NOT skip)"
run poison_b4  -> hang_timeout  exit=124  wall=120.32s
run poison_256 -> hang_timeout  exit=124  wall=120.32s
e0a_summary: reproduced=true (2 green / 3 hang, same shape as the MS1.4 record)
```

GPU signature during every hang (nvidia-smi sampled every ~10 s, flat across the window):
`util 100% / ~63 W (62.15–64.50 W) / SM 2745 MHz / temp 39–42 C`. Sample (poison_b3,
t=60.3 s): `{"util_pct": 100.0, "power_w": 63.19, "sm_clock_mhz": 2745.0}`.

## 3. E0b — dual load path: driver JIT leg

Build with ptxas made unlocatable (`RURIXC_PTXAS` → nonexistent path, `CUDA_PATH` removed,
PATH scrubbed of CUDA dirs) so the runtime falls back to driver JIT:

```
$ py -3 spike/rd027-pt-poison/run_e0b.py
build jit_* : "rurixc: note: ptxas not found; embedding PTX-only artifacts (ptxas dry-gate SKIPPED, RXS-0073)"
              ptx_sha256=8d9c3c05035b92...5ff92a35  (PTX ISA 7.8 conservative header;
              diff vs AOT-leg PTX 8.8 = 4 version-header lines only)
run jit_ctrl_b2    -> completed     exit=0    wall=0.81s
run jit_poison_b3  -> hang_timeout  exit=124  wall=120.21s
run jit_poison_b4  -> hang_timeout  exit=124  wall=120.36s
run jit_poison_256 -> hang_timeout  exit=124  wall=120.32s
```

Both NVIDIA optimizing back ends (ptxas AOT and driver JIT) hang identically.

## 4. E7a — compute-sanitizer memcheck (first round invalidated + retest, honest record)

First round was **invalidated**: the poison exe had already been quarantined by the E0a
timeout handler, and the sanitizer actually reported
`========= Target application doesn't exist or is not a valid executable` (exit 1) — a
`correction` record voids the verdict in campaign.jsonl (original lines preserved).
After rebuilding:

```
$ py -3 spike/rd027-pt-poison/run_e7a.py
memcheck ctrl_b2   -> exit=0, tail: "========= COMPUTE-SANITIZER / RENDER_OK frames=8 /
                                     ========= ERROR SUMMARY: 0 errors"
memcheck poison_b3 -> exit=124 (300 s watchdog), tail:
    "[proc_guard] memcheck:poison_b3: TIMEOUT 300s -> killed process tree (pid=44176), ..."
e7a_summary: verdict=still_hangs_under_sanitizer
```

Control is memory-clean; the hang is not an OOB artifact and does not dissolve under the
sanitizer.

## 5. E1 — ptxas optimization-level scan (wrapper-injected -O0/1/2; O3 = default, already hung)

Wrapper env: `RURIXC_PTXAS` → `build/spike-rd027/ptxas_wrap/target/release/ptxas_wrap.exe`,
`RD027_REAL_PTXAS` → real CUDA 13.3 ptxas, `RD027_PTXAS_O` ∈ {0,1,2}. Same PTX
(sha256 85d597dd…) on every leg.

```
$ py -3 spike/rd027-pt-poison/run_e1.py
run aotO0_ctrl_b2   -> completed     exit=0    wall=0.62s
run aotO0_poison_b3 -> completed     exit=0    wall=0.66s      <-- GREEN at -O0
run aotO1_ctrl_b2   -> completed     exit=0    wall=0.40s
run aotO1_poison_b3 -> hang_timeout  exit=124  wall=120.24s    <-- hang appears at -O1
run aotO2_ctrl_b2   -> completed     exit=0    wall=0.55s
run aotO2_poison_b3 -> hang_timeout  exit=124  wall=120.28s
```

Workaround check at full production scale (256 spp / 4 bounces / 720p, 600 s line — this
config previously hung >15 min and was unmeasurable):

```
build aotO0_full_prod {spp 256, batch 32, bounces 4}
run   aotO0_full_prod -> completed  exit=0  wall=9.49s  stdout: "RENDER_OK frames=8"
```

## 6. E7b — all source loops hard-capped (DDA≤1000 / cell≤4096 / bounce≤8 / spp≤64)

```
$ py -3 spike/rd027-pt-poison/run_e7b.py
build e7b_cap_all  caps=[dda,cell,bounce,spp]  ptx_sha256=4c77f765bfa52315...59d07ce
run   e7b_cap_all -> hang_timeout  exit=124  wall=120.36s
    stderr: "[proc_guard] run:e7b_cap_all: TIMEOUT 120s -> killed process tree (pid=49768), ..."
```

Capping every source loop does not fix the hang — the spin is not source-loop iteration.

## 7. E4 — code-deletion ladder + d6 subdivision + data-dependence probe

Poison params (8 spp / bounces 3 / 1 frame) throughout:

```
$ py -3 spike/rd027-pt-poison/run_e4.py
d1 (drop NEE)              -> completed     0.41s
d2                         -> completed     0.41s
d3                         -> completed     0.37s
d4 (drop ground/sky)       -> completed     0.46s
d5 (inner single-particle) -> hang_timeout  120.25s
d6 (drop shadow_walk)      -> hang_timeout  120.21s
d7                         -> completed     0.41s
d6_nosim (SUBSTEPS=0)      -> completed     0.54s   <-- initial particle layout does not trigger
d6a (drop bounce-0 light)  -> hang_timeout  120.31s <-- minimized repro (276-line kernel source)
d6b                        -> completed     0.38s
d6c                        -> hang_timeout  120.20s
d6d                        -> completed     0.51s
```

Non-monotonic ladder = code-shape-sensitive optimizer trap, not a single source construct.
The d6a variant is tracked at `spike/rd027-pt-poison/mrp/render_pt_mrp_d6a.rx`.

## 8. E5 — SASS regeneration (pure CPU, no GPU runs)

```
ptxas -arch=sm_89 -O1 bin/ctrl_b2.ptx     -o e5/aot_O1.cubin   # + nvdisasm -c -> e5/aot_O1.sass
ptxas -arch=sm_89 -O2 bin/ctrl_b2.ptx     -o e5/aot_O2.cubin   # + nvdisasm -c -> e5/aot_O2.sass (byte-identical to aot_O3.sass)
ptxas -arch=sm_89 -O1 bin/e7b_cap_all.ptx -o e5/e7b_O1.cubin   # + nvdisasm -c -> e5/e7b_O1.sass
ptxas -arch=sm_89 -O3 bin/e7b_cap_all.ptx -o e5/e7b_O3.cubin   # + nvdisasm -c -> e5/e7b_O3.sass
```

Key counts (pt_render section): local-label `@!P0 CALL.REL.NOINC` latch exits = O0: 0 /
O1: 4 / O2=O3: 4; registers 141 (O0) vs 77 (O1/O2/O3); barrier ids B0–B8 all in use, dynamic
peak 9/9. Line-anchored evidence patterns A–F: `build/spike-rd027/e5/analysis.md` §6
(workspace artifact — regenerate per `PROVENANCE.md`, or fold into the filing package).

## 9. Manual single-repro recipe (without the campaign drivers)

```
1. cargo build -p rurixc -p rx
2. copy apps/ruridrop/src; replace render_pt.rx with spike/rd027-pt-poison/mrp/render_pt_mrp_d6a.rx;
   patch params.rx: SPP 32->8 / SPP_BATCH 32->8 / PT_BOUNCES 2->3 / REND_FRAMES 8->1
3. target\debug\rx.exe build <copy>/offline.rx -o poison.exe
4. py -3 bench/proc_guard.py --timeout 120 -- poison.exe        # ALWAYS under a watchdog
   - default build (ptxas -O3 AOT cubin): HANGS (util 100% / ~63 W / max clocks)
   - RURIXC_PTXAS invalidated => driver JIT:  HANGS
   - ptxas -O0 injected (AOT cubin):          completes 0.4-0.7 s
   - PT_BOUNCES=2 control:                    completes in seconds at every level
```
