# unsafe-audit: rurix-rt(CUDA Driver API FFI 边界)

> 注册依据:AGENTS.md 硬规则 9 / 10 §7.6(无注册条目的 unsafe 是 CI 错误);
> 14 §2 常驻集 unsafe-audit 完整性。M4.3 激活(D-M4-4 运行时落地,首个 unsafe 边界)。
> 决策依据:D-113(FFI 战略:`extern "system"` + `#[repr(C)]` + 原始指针,Windows x64
> 唯一 ABI)、D-230(运行时 = Driver API 薄层)、D-231/D-232/D-234(对象模型/内存
> 三件套/装载协商)。M4 契约 `rfc_required: none`(已锁定决策的条款化),会话授权
> 直接实现 + 块级豁免(不另走 RFC)。

## 范围与豁免

- crate:`src/rurix-rt`(`[lints.rust] unsafe_code = "allow"`;`undocumented_unsafe_blocks
  = "deny"` 维持——每个 unsafe 块强制 `// SAFETY:` 注释)。
- 全仓其余 crate(`rurixc`)维持 `unsafe_code = "deny"`(根 workspace 默认),不受影响。
- 全部 unsafe 集中于 `src/rurix-rt/src/sys.rs`(FFI 边界)+ `lib.rs` 中少量裸指针
  构造(launch 实参数组 / JIT 选项数组 / pinned 切片视图),逐块 `// SAFETY:` 在位。

## 原语清单与验证义务(RustBelt 式)

| # | 原语 | 位置 | 验证义务(SAFETY 不变量) |
|---|---|---|---|
| U1 | CUDA Driver 装载器动态加载(per-OS cfg 分叉 `mod loader`:`#[cfg(windows)]` `LoadLibraryA`/`GetProcAddress` + `nvcuda.dll` / `#[cfg(not(windows))]` `dlopen(RTLD_NOW)`/`dlsym` + `libcuda.so`;mb1 W8/RXS-0211 v1.12 扩注,同 nvcuda FFI 边界无新 U 号) | sys.rs `mod loader` + `Cuda::load` | 入参为 `c"..."` NUL 结尾字面量(`loader::CUDA_LIB` per-OS 库名);返回地址仅经 `cast_fn` 在 null 校验后转函数指针;`#[cfg(windows)]` 分支逐调用等价旧实现(`open`=`LoadLibraryA` / `sym`=`GetProcAddress`,字节零漂移,内 `unsafe` 块纯 lint 构造);`#[cfg(not(windows))]` = POSIX `dlopen`/`dlsym`(libc 提供,消除 aarch64-android 链接期未定义符号;android 无 `libcuda.so` → `open` 返回 null → `Cuda::load` 返回 `None` → CUDA 运行期不可用,诚实降级,08 §2.5,非 panic);loader 不 `close`/`FreeLibrary`(进程常驻) |
| U2 | `transmute_copy::<*mut c_void, FnT>` 符号 → 函数指针 | sys.rs `cast_fn` | `raw` 非 null;符号名 ⇔ 类型别名签名 ⇔ CUDA Driver API(`_v2`)ABI 逐一对应(D-113);指针宽度相等(debug_assert) |
| U3 | Driver API 函数指针调用(cuInit/cuCtx*/cuMem*/cuModule*/cuLaunchKernel/...) | sys.rs `Cuda::*` 方法 | 句柄(ctx/stream/module/function/deviceptr)有效且未释放,由上层所有权类型(Context/Stream/DeviceBuffer/Module/Kernel)RAII 维持;出参指针有效可写;字节范围在分配内 |
| U4 | `CStr::from_ptr`(cuGetErrorName/String) | sys.rs `error_name`/`error_string` | 成功返回时驱动写入进程生命期静态 NUL 结尾字符串 |
| U5 | `cuModuleLoadDataEx` 平行选项数组 + NUL 结尾 PTX image | lib.rs `Context::load_module` | image 为 `CString`(NUL 结尾);opts/vals 长度 4 平行有效;日志缓冲 `info_buf`/`err_buf` 调用期存活 |
| U6 | H2D/D2H 拷贝裸指针 | lib.rs `DeviceBuffer::copy_*` | 主机切片 `bytes` 字节有效;设备地址范围在分配内;`assert` 守长度 ≤ 容量 |
| U7 | `cuLaunchKernel` 实参指针数组 | lib.rs `Stream::launch` | `params` 各元素指向调用方维持的有效实参存储,长度与 kernel 形参匹配(编译期 launch_check 裁决,RXS-0074) |
| U8 | `slice::from_raw_parts(_mut)` pinned 视图 | lib.rs `PinnedBuffer::as_(mut_)slice` | ptr 为 cuMemAllocHost 返回的 `len*size_of::<T>()` 字节锁页内存,对齐满足;`&self`/`&mut self` 约束生命期与别名 |
| U9 | primary context retain/release/set_current(`cuDevicePrimaryCtxRetain` / `cuDevicePrimaryCtxRelease_v2` / `cuCtxSetCurrent`) | sys.rs `Cuda::{primary_ctx_retain,primary_ctx_release,ctx_set_current}` + lib.rs `Context::from_primary` / `Drop` | `device` 来自 `device_get`;retain/release 配对(引用计数,Drop 仅 release 一次);`ctx_set_current` 入参为刚 retain 成功的有效 context;set_current 失败回滚 release(M8.1 互操作零拷贝:与 PyTorch runtime API 共享 primary context,RXS-0125) |
| U10 | 借用外部设备指针构造缓冲(`from_device_ptr`,Drop 不 free) | lib.rs `Context::from_device_ptr` / `DeviceBuffer::drop`(`!owned` 早返) | 调用方(互操作 FFI 边界,经 `__cuda_array_interface__` v3 / DLPack capsule 取得)保证 `ptr` 在本 context 设备上有效、可读写、容纳 ≥ `len` 个 `T`,借用存活期内未被外部 deleter 释放;`owned=false` 故 Drop **不** `cuMemFree`(所有权留外部 deleter,不双重释放,M8.1 / RXS-0123/0124) |
| U11 | event 跨 stream 同步 FFI(`cuEventCreate` / `cuEventRecord` / `cuEventDestroy_v2` / `cuEventSynchronize` / `cuStreamWaitEvent`) | sys.rs `Cuda::{event_create,event_record,event_destroy,event_synchronize,stream_wait_event}`(M8.3 UC-02,RXS-0131) | event/stream 句柄有效、未销毁、同 current context,由上层所有权类型(`SharedEvent`/`SharedStream`)RAII 维持;出参指针有效可写;`event_record` 前 stream 有效,`stream_wait_event` 建立流序依赖不解引用数据 |
| U12 | 异步搬运 FFI(`cuMemcpyHtoDAsync_v2` / `cuMemcpyDtoHAsync_v2`) | sys.rs `Cuda::{memcpy_htod_async,memcpy_dtoh_async}`(M8.3 UC-02,RXS-0131) | 设备地址范围在分配内;主机端(宜锁页)`src`/`dst` ≥ `bytes` 字节,且**在 stream 异步操作完成前保持有效**(由 `InFlight` 持 `PinnedBox` 保活至同步,杜绝悬垂);`stream` 有效 |
| U13 | 跨线程共享 primary context(`SharedContext`/`SharedInner` 的 `unsafe impl Send + Sync` + 跨线程 `cuCtxSetCurrent` 重绑 + retain/release) | pipeline.rs `SharedInner`(Send/Sync/Drop)、`SharedContext::{from_primary,bind}`、各 affine 资源 `Drop`(重绑 current 后释放)(M8.3 UC-02,RXS-0133) | primary context 为**进程级**对象,多线程各自 `cuCtxSetCurrent` 后共享合法(Driver 线程模型);`SharedInner` 持句柄/设备序号纯数据,`Arc` 单点配对 retain/release(最后引用 Drop 仅 release 一次);任意线程的资源 Drop 先 `ctx_set_current(inner.raw)`(`Arc` 存活保证句柄有效)再释放 |
| U14 | 跨线程 event 句柄转移(`SharedEvent` 的 `unsafe impl Send`) | pipeline.rs `SharedEvent`(`unsafe impl Send` / Drop)(M8.3 UC-02,RXS-0133) | `cuEvent` 为绑 context 的进程级驱动对象,跨线程 `move` 合法(持有者线程 current 为同一 context;`Arc<SharedInner>` 保证 context 存活,Drop 前重绑 current);仅 `Send`(move 转移),不 `Sync`(不跨线程共享 `&`) |
| U15 | 跨 stream 流序依赖(`SharedStream::wait_event` 经 `cuStreamWaitEvent`) | pipeline.rs `SharedStream::wait_event` / `acquire`(M8.3 UC-02,RXS-0132 流序分配类型化) | `stream`/`event` 有效且同 current context;`acquire` 消费 `InFlight` 插入 wait 后重 brand 回 `DeviceBox`——「跨 stream 未同步访问」由类型系统(`InFlight` 无读接口)编译期拦截,不解引用未就绪数据 |
| U16 | 异步搬运裸指针 + pinned 经 `InFlight` 保活(`SharedStream::{upload,download}`) | pipeline.rs `SharedStream::{upload,download}`(M8.3 UC-02,RXS-0131/0132) | `upload` move 入 `PinnedBox` 并随 `InFlight` 存活至同步(异步 H2D 期 pinned 源不悬垂);`download` 末 `cuStreamSynchronize` 后方返回(异步 D2H 期 pinned 目标存活);设备地址范围在分配内,`assert` 守长度 ≤ 容量 |
| U17 | external memory import/map/destroy FFI + `ExternalBuffer` Drop(`cuImportExternalMemory` / `cuExternalMemoryGetMappedBuffer` / `cuDestroyExternalMemory` / mapped `cuMemFree`) | sys.rs `Cuda::{import_external_memory,external_memory_get_mapped_buffer,destroy_external_memory}` + interop.rs `build_session`(`scope` 与 `OwnedPresentSession::create` 的建链单一事实源,MS1.2b 提取,RXS-0197)/ `ExternalBuffer::drop`(G1.1 CUDA–D3D12,RXS-0140/0143;RFC-0001 §4.2.3/§4.4;feature `d3d12-interop`) | descriptor `#[repr(C)]` 与 CUDA 头文件 v1 布局一致(编译期 `const assert!` 核对 104/88 字节);`win32.handle` 为 shim `create` 移交的有效 NT HANDLE(import 后即 close,CUDA 不接管所有权);`ext_mem` 由 import 成功产出、`dptr` 由 map 产出、本类型独占;Drop 销毁序先 `cuMemFree(mapped)` 再 `cuDestroyExternalMemory`(RFC-0001 §4.4),**不释放 D3D12 resource**(COM owner 留 shim);单一所有权(非 Clone)Drop 仅一次,current context 一致 |
| U18 | external semaphore import/signal/wait/destroy FFI + `ExternalSemaphore` Drop(`cuImportExternalSemaphore` / `cuSignalExternalSemaphoresAsync` / `cuWaitExternalSemaphoresAsync` / `cuDestroyExternalSemaphore`) | sys.rs `Cuda::{import_external_semaphore,signal_external_semaphore,wait_external_semaphore,destroy_external_semaphore}` + interop.rs fence 助手 `core_wait_acquire`/`core_signal_done`(MS1.2b 提取的单一事实源,由 `ReadyFrame::wait`/`AcquiredFrame::signal` 与 `OwnedPresentSession::{wait,signal}` 消费,RXS-0197)/ `ExternalSemaphore::drop`(G1.1,RXS-0140/0142;RFC-0001 §4.3/§4.4;feature `d3d12-interop`) | descriptor `#[repr(C)]` 布局一致(`const assert!` 96/144 字节);`fence_handle` 为 shim 移交有效 fence NT HANDLE(import 后即 close);signal/wait 单信号量(numExtSems=1,`sems`/`params` 长度 1 栈数组)、fence 值按 §4.3 偶/奇协议 checked 递增;销毁前无在途 signal/wait(§4.4 shutdown 序);`ext_sem` 本类型独占 Drop 仅一次,current context 一致 |
| U19 | 流序分配/释放 FFI(`cuMemAllocAsync` / `cuMemFreeAsync`) | sys.rs `Cuda::{mem_alloc_async,mem_free_async}`(G1.2 流序分配,RXS-0144;Option 字段非致命解析,CUDA 11.2+) | 符号为 `Option`(老驱动缺失 → `has_stream_ordered_alloc()=false` → 上层 `DriverUnavailable`,核心 CUDA 不受影响);`stream` 句柄有效且同 current context,由上层所有权类型(`SharedStream`/`AsyncBuffer`)RAII 维持;出参 `dptr` 有效可写;`mem_free_async` 的 `ptr` 由 `mem_alloc_async` 产出且未释放;分配自 `stream` 当前 memory pool(默认 = 设备默认 `CUmemoryPool`),字节数由驱动裁决(0 合法) |
| U20 | 流序分配 RAII Drop + 跨 stream 时序边(`PoolAlloc::drop` 经 `cuMemFreeAsync` + `share_with` 经 `cuEventRecord`/`cuStreamWaitEvent`) | pipeline.rs `PoolAlloc::drop` + `SharedStream::alloc_async` + `AsyncBuffer`/`AsyncReady::share_with`(G1.2,RXS-0144/0146/0147) | 释放责任由 `PoolAlloc`(独占 Drop)**单点**承载——`share_with` 经 `move` 转移 `PoolAlloc`(wrapper 无 Drop,不双重释放);Drop 在**任意线程**先 `ctx_set_current(inner.raw)`(`Arc` 存活保证句柄有效)重绑本 context 再 `cuMemFreeAsync` 入所属 stream 流序释放,仅一次(单一所有权、非 `Clone`);`share_with` 在产出 stream `record` `event`、`other` stream `wait_event` 建立流序依赖(复用 U11/U15 event 同步骨架,不解引用未就绪数据),`event`/`stream` 句柄有效且同 current context |
| U22 | 按架构预编 cubin 装载 + compute capability 查询 FFI(`cuModuleLoadData` / `cuDeviceGetAttribute`) | sys.rs `Cuda::{module_load_data,device_compute_capability}` + lib.rs `Context::{load_module_artifacts,try_load_cubin}`(G1.5 生产分发 fatbin,RXS-0150/0151;Option 字段非致命解析)+ pipeline.rs `Bound::{load_module_artifacts,try_load_cubin}`(MS1.2 shared 族镜像,RFC-0009 §4.3 cabi 惰性装载缓存消费) | 符号为 `Option`(缺失 → `has_cubin_load()=false` → 装载协商降级**保守 PTX fallback**,核心 PTX 装载 `cuModuleLoadDataEx`(U5)路径不受影响,D-207);`image` 指向 `build.rs` `ptxas -arch=sm_89` 预编并嵌入的有效 cubin 二进制(RXS-0150,`DeviceArtifactSet` 持有保活);`device_compute_capability` 出参 `major`/`minor` 有效可写、`attrib` 为合法 `CUdevice_attribute` 常量、`dev` 来自 `device_get`;cubin 被驱动拒绝(架构不符 / 损坏等)→ 返回非 SUCCESS,上层 `try_load_cubin` 降级 PTX 重试(**降级而非 reject,不 poison context**,保守兜底);safe wrapper `Context::load_module_artifacts` 对上全 safe(签名无 `unsafe`,装载变体决策 `select_load_variant` 为纯函数,host 可测) |
| U26 | Vulkan compute 运行时 FFI 边界(`vulkan-1.dll` 动态加载 + ~35 Vulkan 命令 `extern "system"` 函数指针 + `#[repr(C)]` 结构 + 句柄线性生命周期;mb1,RXS-0207,RFC-0011 §4.6;跳 U23 空号 / U25=MS1.2b 占用避撞) | `src/rurix-rt/src/vk.rs`(feature `vulkan`,默认关闭)`run_compute` → `run_compute_inner` → `run_on_device` → `dispatch_and_readback` | `vulkan-1.dll` 经 `LoadLibraryA` + `vkGetInstanceProcAddr` 装载(镜像 U3 nvcuda.dll 纪律,缺失 → `Err` 非 panic);每个 `#[repr(C)]` VkStruct 与 Vulkan spec 逐字节对齐(`sType`/`pNext` 前缀 + 字段序/尺寸;**由 `VK_LAYER_KHRONOS_validation` 真跑零报错实证**——反证:错入口名触发 VUID 报错,证 layer 生效);句柄(instance/device/buffer/memory/shaderModule/pipeline/descriptorPool/commandPool)在 `run_compute` 内**线性配对 create/destroy**(逆序销毁,无泄漏、无双重释放);host-visible+coherent 内存(免 flush/invalidate);单 queue 同步提交 + `vkQueueWaitIdle` 后回读(无数据竞争);`cast_fn` null 校验 + size 断言(镜像 `sys::cast_fn`);gate feature `vulkan` 默认关闭,**NVIDIA-only(CUDA)构建零改动零回归**;safe 公共面 `run_compute(spv, entry, buffers, push_constants, groups) -> Result` 对上全 safe(无 `unsafe` 签名);**MB1.4 加载缝 cfg 分叉扩注(RXS-0211,同一 feature `vulkan` FFI 边界,无需新 U 号)**:`LoadLibraryA`/`GetProcAddress` 抽为 `mod loader` cfg 分支——`#[cfg(windows)]` = `vulkan-1.dll` + `LoadLibraryA`/`GetProcAddress`(Windows 路径逐调用等价旧实现、零漂移)、`#[cfg(not(windows))]` = `libvulkan.so` + `dlopen(RTLD_NOW)`/`dlsym`(`unsafe extern "C"`,Android/Linux libc 提供;缺失 → `Err` 非 panic 同旧);`load_vulkan_loader` 只调抽象 `loader::open`/`sym` + per-OS 库名常量 `VULKAN_LIB`,入参 NUL 结尾字面量,返回址 null 校验后 transmute 为已知 ABI 函数指针,不 `close`/`FreeLibrary`(进程常驻,镜像 U1 纪律);另 `#[cfg(target_os = "android")] pub mod android_present` 就位 `vkCreateAndroidSurfaceKHR` FFI(`#[repr(C)] AndroidSurfaceCreateInfoKHR` 逐字节对齐 + `ANativeWindow` opaque + safe 签名 `create_android_surface(...) -> Result<VkSurfaceKHR, String>`;compute 路径不启用 surface 扩展〔`enabled_extension_count=0`〕,on-device present 循环 = 尾门 G-MB1-7),仅 android target 编译 |
| U27 | Vulkan graphics 运行时 FFI 边界(offscreen 出图:render pass〔单 color attachment CLEAR→STORE→TRANSFER_SRC〕/ graphics pipeline〔vertex+fragment 双 stage,pName `"main"`〕/ framebuffer / color image + imageView / 顶点缓冲 / `vkCmdDraw` / `vkCmdCopyImageToBuffer` readback + `VK_EXT_debug_utils` messenger;mb1 Phase 3,RXS-0210,RFC-0011 §4.6) | `src/rurix-rt/src/vk.rs`(feature `vulkan`,默认关闭)`run_graphics_offscreen` → `run_graphics_inner` → `graphics_body`(graphics queue family,区别于 U26 compute) | 复用 U26 loader/instance/device 骨架(`vulkan-1.dll` `LoadLibraryA`+`vkGetInstanceProcAddr`,缺失 / 无 graphics queue / pipeline 建失败 → `Err` 非 panic,P-01);每个新增 graphics `#[repr(C)]` VkStruct(Image/ImageView/AttachmentDescription/Subpass/RenderPass/Framebuffer/GraphicsPipeline + 各 pipeline state CI / RenderPassBeginInfo / VkBufferImageCopy / VkImageMemoryBarrier / `DebugUtilsMessengerCreateInfoEXT` / `DebugUtilsMessengerCallbackDataEXT`)与 Vulkan spec 逐字节对齐(`sType`/字段序/尺寸;**由 `VK_LAYER_KHRONOS_validation` 真跑零报错实证**——反证:provenance-带保名 `.spv` 触 VUID-...-08742,证 layer 生效);句柄(image/imageView/renderPass/framebuffer/vertexBuffer/readbackBuffer/memory/shaderModule×2/pipeline/pipelineLayout/commandPool/**messenger**)在 `graphics_body`/`run_graphics_inner` 内**线性配对 create/destroy**(逆序销毁;**messenger 先于 `vkDestroyInstance` 销毁**——messenger 建于全部 instance-符号 `?` 查找**之后**、首个 Vulkan API 调用之前(其间纯 `vkGetInstanceProcAddr` 取址、无 messenger 需求),故创建点与首个销毁点之间**无 `?` 早退**;messenger 创建后的**每个** early-return(count==0 / 无 graphics queue / vkCreateDevice 失败)与末尾正常路径均经 `destroy_msgr!()` 拆除,避 VUID-vkDestroyInstance-instance-00629;无泄漏/双释放);color image 经 render pass finalLayout 转 `TRANSFER_SRC_OPTIMAL` 后 `vkCmdCopyImageToBuffer`;顶点/回读缓冲 host-visible+coherent(免 flush);单 graphics queue 同步提交 + `vkQueueWaitIdle` 后回读紧凑 RGBA8(无数据竞争);`VK_EXT_debug_utils` messenger 的 `extern "system"` 回调 `p_user_data` 指向 `run_graphics_inner` **栈上 `AtomicBool`**(messenger 生命周期严格短于该栈变量;回调仅经共享引用原子 `store`,无 `&mut` 别名),ERROR 级校验消息 → 末尾翻 `Err`(fail-closed,L3;仅 `RURIX_VK_VALIDATION=1` 装载);gate feature `vulkan` 默认关闭,**NVIDIA-only(CUDA)构建零改动零回归**;safe 公共面 `run_graphics_offscreen(vs, fs, vertices, stride, attrs, W, H, clear) -> Result<Vec<u8>, String>` 对上全 safe(无 `unsafe` 签名);**W6 win32 present 扩注(RXS-0210 L4,同 feature `vulkan` + `#[cfg(windows)]` FFI 边界,无需新 U 号)**:`run_graphics_present` → `run_graphics_present_inner` → `present_vk` → `present_body` 复用上述 loader/instance/device/pipeline/messenger 骨架,增量 = ① **win32 窗口 FFI**(`mod win32`:user32.dll `RegisterClassW`/`CreateWindowExW`(WS_POPUP 隐藏,不 ShowWindow)/`DestroyWindow`/`UnregisterClassW`/`Peek·Translate·DispatchMessageW` + kernel32 `GetModuleHandleW`;`#[link(name="user32")]`;`wnd_proc` 一律委派 `DefWindowProcW`;窗口在 `run_graphics_present_inner` 内 create/destroy 线性配对,class 名经 pid+原子计数唯一化避残留冲突);② **surface/swapchain/semaphore Vulkan FFI**(`vkCreateWin32SurfaceKHR`/`vkDestroySurfaceKHR` + surface support/caps/formats/present-modes 查询 + `vkCreateSwapchainKHR`/`vkGetSwapchainImagesKHR`/`vkAcquireNextImageKHR`/`vkQueuePresentKHR`/`vkCreateSemaphore`,`#[repr(C)]` `Win32SurfaceCreateInfoKHR`/`SwapchainCreateInfoKHR`/`PresentInfoKHR`/`SurfaceCapabilitiesKHR`/`SubpassDependency` 逐字节对齐);句柄(surface/swapchain/imageView×N/framebuffer×N/semaphore×2/vertexBuffer/readbackBuffer/pipeline/…)**线性配对 create/destroy**,**swapchain image 归 swapchain 所有——只销毁 imageView/framebuffer/swapchain,不 `vkDestroyImage` swapchain image**;binary semaphore(imageAvailable/renderFinished)逐帧 `vkQueueWaitIdle` 后复用(无未决信号);surface 先于 instance 销毁、messenger 先于 instance 销毁(避 VUID-vkDestroyInstance-instance-00629);由 `VK_LAYER_KHRONOS_validation` 本机 NVIDIA/Windows 真跑**零报错**实证 + present 路 messenger fail-closed(ERROR 级校验翻 `Err`,red_self_test VUID-08742 判红);`#[cfg(not(windows))]` stub 返回确定性 `Err`(windows-only);safe 公共面 `run_graphics_present(vs, fs, vertices, stride, attrs, W, H, clear, frames) -> Result<Vec<u8>, String>` 对上全 safe。**W7 Android on-device present 扩注(同 feature `vulkan` + `#[cfg(target_os = "android")]` FFI 边界,无需新 U 号;G-MB1-7 Phase B 兑现)**:`run_graphics_present_android_safe` → `run_graphics_present_android`(镜像 `present_vk`)复用同一 loader/instance/device/messenger/`present_body` 骨架,增量 = ① instance 扩展换 `VK_KHR_surface`+`VK_KHR_android_surface`(compute 路仍 `enabled_extension_count=0`);② surface 由 `ANativeWindow*` 经 `vkCreateAndroidSurfaceKHR`(复用 `android_present::create_android_surface`,其 `FnCreateAndroidSurfaceKHR`/`AndroidSurfaceCreateInfoKHR` 提 `pub` 供主作用域 `cast_fn` 解析,`#[repr(C)]` 逐字节对齐)取代 `vkCreateWin32SurfaceKHR`;③ `present_body` **解 win32 gate → `#[cfg(any(windows, target_os = "android"))]`**(本体平台无关,仅 win32/android 两调用方所在平台编入)+ 返回改 `(pixels, ext_w, ext_h)`——**全屏 present 真实 extent 由 surface `currentExtent` 决定**(非入参 w/h),win32 wrapper 丢弃 extent 保签名不变;④ `pre_transform`/`composite_alpha` 由 `caps` 派生(`caps.current_transform` / `pick_composite_alpha(caps.supported_composite_alpha)`,**非硬编码**——win32 得 IDENTITY+OPAQUE 数值零回归,Android 得设备旋转+INHERIT,硬编码 OPAQUE 触 VUID);⑤ android debug messenger 回调 `debug_messenger_cb_android`(`p_user_data` 指向本函数**栈上 `AtomicU32`** 计数,生命周期严格长于 messenger;每条 ERROR 级消息经 `__android_log_write` 落 logcat〔tag `RurixVK-VVL`〕+ 计数,末尾 **fail-closed** 翻 `Err`)——桌面 `debug_messenger_cb`(stderr + `AtomicBool`)不变;`__android_log_write` 由最终 cdylib 链接期 glue crate `-llog` 解析(rlib 不解析,桌面零影响);⑥ `red_selftest` 用模块内不存在的**假 pipeline 入口名**(vertex stage `pName`;SPIR-V 原样合法)触 `VUID-VkPipelineShaderStageCreateInfo-pName-00707`(反证 layer 真加载;pipeline create 直接返 `VK_ERROR` 亦判红,messenger instance 级先落 logcat)——**合法模块 + 假名不向驱动/layer 喂非法 SPIR-V**,消解旧机制(损坏 `.spv` 字节触 pCode-08742)在 layer 错误格式化路径的崩溃/UB 风险(HONOR Adreno/Android16+MTE 实测 layer 自身 SIGSEGV、VUID 未吐出即崩,致 RED 取证失败);与 `validation` 开关无关地内存安全。句柄(instance/surface/device/messenger/swapchain/imageView×N/framebuffer×N/semaphore×2/pipeline/…)线性配对 create/destroy、逆序销毁(surface/messenger 先于 instance);on-device present N 帧结构性像素校验 + `VK_LAYER_KHRONOS_validation` 零报错(RED 反证)由 `rurix-android-present`(U28,`librurix_vk.so`)在真机跑 = 尾门 **G-MB1-7**(证据待 owner 裁签)。**AMD 真卡 present 像素校验 = 尾门 G-MB1-6(本片不触)**。**G3.3 PR-S0 v2 descriptor 建面扩注(RXS-0230,RFC-0013 §4.B7/§6.4,同 feature `vulkan` graphics FFI 边界,0 新 U 号)**:`run_graphics_offscreen_v2`(**v1 签名/行为 0-byte 保留**;`resources` 空时直接委派 v1)→ `run_graphics_inner_v2` → `graphics_body_v2` **独立组装**(不触 v1 `graphics_body` 字节),复用同一 loader/instance/device/messenger(fail-closed)骨架,增量 = ① **新 FFI 命令 4**(`vkCreateSampler`/`vkDestroySampler`/`vkCmdCopyBufferToImage`/`vkGetPhysicalDeviceFeatures`)+ 新 `#[repr(C)]` `SamplerCreateInfo`/`DescriptorImageInfo`/`PhysicalDeviceFeatures`(Vulkan 1.0 固定 55 个 VkBool32 顺排,定长数组建模逐字节对齐,`FEATURE_*` 常量按 spec 字段序索引)逐字节对齐;② **descriptor 建面**——`vkCreateDescriptorSetLayout` × set-per-class(set0 CBV 空占位 / set1 SRV / set2 UAV / set3 Sampler,binding = 类内声明序,与 rurixc `binding_layout::infer_spirv_bindings_vk_native` 同一分配律〔单一 binding-号事实源同序〕)+ immutable sampler 进 set3 layout `pImmutableSamplers`(RXS-0230 L3;binding 号 = samplers 向量下标,同为声明序,向量在取指针后不增删)+ `vkCreateDescriptorPool`(pool sizes 纯函数 `plan_pool_sizes` 计划;immutable sampler 仍占 SAMPLER 容量)+ `vkAllocateDescriptorSets` + `vkUpdateDescriptorSets`(sampled image `SHADER_READ_ONLY_OPTIMAL` / storage image `GENERAL`;`DescriptorImageInfo` 向量先建全后取址,地址稳定)+ `vkCmdBindDescriptorSets`(GRAPHICS bind point,firstSet 0 一次绑全部 set);③ **mip 链上传**——单 staging buffer 逐层紧凑偏移(纯函数 `mip_staging_layout`,偏移恒纹素尺寸整倍满足 bufferOffset 对齐 VUID;写入与 copy 消费同源)→ `UNDEFINED→TRANSFER_DST` 全 mip 屏障 → 逐层 `vkCmdCopyBufferToImage` → `TRANSFER_DST→SHADER_READ_ONLY` 屏障(TRANSFER→VERTEX|FRAGMENT);storage image `UNDEFINED→GENERAL`(TOP_OF_PIPE→FRAGMENT,SHADER_READ|WRITE);④ **device feature 协商**(`vkGetPhysicalDeviceFeatures`:sampler `max_anisotropy>1` 需 `samplerAnisotropy`,缺失 → 确定性 `Err`〔RXS-0193 口径,不占 RX 码〕;storage image 在位时机会性启用 `fragmentStoresAndAtomics`,缺失不 Err——绑定本身不需该 feature);pipeline layout 由 v1 空 layout 扩为含 set layouts(v2 独立组装,v1 路径不动)。句柄(sampler×N / texture image·mem·view·staging×N / storage image·mem·view×N / setLayout×N / descriptorPool + v1 全集)**线性配对 create/destroy、末尾逆序销毁**(pool 先于 setLayout、sampler 在 pool/layout 之后销毁、view 先于 image 先于 memory);资源合法性(mip 尺寸序列 / 逐层数据长 / `SamplerDesc::is_valid` / storage 尺寸)在**任何句柄创建前**纯 host 预校验 fail-closed(`validate_resources`,P-01);safe 公共面 `run_graphics_offscreen_v2(v1 全参…, resources: &[GraphicsResource]) -> Result<Vec<u8>, String>` 对上全 safe;`bin/vk_desc_v2` 最小闭环 demo(v1/v2 像素逐字节相等断言 = 底座中性 + validation 零报错),device 数值见证(≥6 模式,步骤 63)归主循环。**G3.4 bindless 无界表运行时扩注(RXS-0235,RFC-0013 §4.C4/§6.4,同 feature `vulkan` graphics FFI 边界,0 新 U 号)**:`run_graphics_offscreen_bindless`(**v1/v2/present 路径 0-byte 不动,独立组装**;`resources` = 有界 set0~3 首期仅 Texture2D/Sampler〔storage 归 v2〕,`table` = 无界表注册纹理〔切片序 = 注册序 = 动态索引值〕)→ `run_graphics_inner_bindless` → `graphics_body_bindless`,复用同一 loader/instance/device/messenger(fail-closed)骨架,增量 = ① **instance apiVersion 升 1.2**(descriptor indexing = Vulkan 1.2 core,承 RXS-0212;v1/v2 维持 1.1 不动)+ device apiVersion < 1.2 → 确定性 `Err`(`VkPhysicalDeviceProperties` 以 **2048 字节 align(8) blob** 承载,仅读首字段 apiVersion,blob 为真实结构〔x64 约 824 B〕严格超集防越界/错位写);② **feature chain 四 bit 探测**——`vkGetPhysicalDeviceFeatures2`(1.1 core)+ pNext `VkPhysicalDeviceDescriptorIndexingFeatures`(`#[repr(C)]` sType/pNext 头 + **20 个 VkBool32 定长数组**逐字节对齐,`FEAT_*` 常量按 spec 字段序索引,布局经单测 size/align 锚定),摘取 shaderSampledImageArrayNonUniformIndexing / descriptorBindingSampledImageUpdateAfterBind / descriptorBindingPartiallyBound / runtimeDescriptorArray 四 bit 经纯函数 `check_descriptor_indexing_bits` 判定,**任一缺失 → 确定性 `Err` 全列缺失位名**(RXS-0193 封口,无静默降级 P-01;missing 路 mock 单测覆盖);③ **device 启用链**——`DeviceCreateInfo.pNext` 直挂仅置四 bit 的 indexing 结构(`pEnabledFeatures` 仍 1.0 结构,链中无 Features2 时两者并用合法);④ **set4 无界表建面**——set0~3 有界 layout 同 v2 分配律 + set4 独占 layout(binding 0 × `table_len`,`VkDescriptorSetLayoutBindingFlagsCreateInfo` 挂 `UPDATE_AFTER_BIND | PARTIALLY_BOUND` binding flags + layout `UPDATE_AFTER_BIND_POOL` flag,`BINDLESS_SET = 4` 镜像 rurixc `binding_layout::VK_BINDLESS_SET_BASE` 分配律,RXS-0233),pool 带 `UPDATE_AFTER_BIND` flag、SAMPLED_IMAGE 容量并入 `table_len`;descriptor 写入 `dstArrayElement = 注册序`,**仅发生在提交前**(in-flight 不更新,§8);⑤ **push-constant table_len 硬接线**——pipeline layout 声明 `PushConstantRange{VS|FS, offset 0, size 4}` + 录制期 `vkCmdPushConstants` 下发注册计数(stageFlags 与 range 逐位一致;= codegen 强制 clamp `UMin(idx, table_len-1)` 的上界源,RXS-0208 尾槽/RXS-0234——缺此注册则 shader 读未初始化 push-constant,clamp 到垃圾上界);⑥ 表纹理与有界纹理同一 staging 上传/迁移纪律(`build_tex` 闭包单点)。句柄(table 纹理 image·mem·view·staging×N + v2 有界全集 + set4 layout + pool)**线性配对 create/destroy、末尾逆序销毁**(pool 先于 setLayout,sampler 在 pool/layout 之后);入口纯 host 预校验(`validate_resources` ×2 + `validate_bindless_inputs`:空表/非纹理表元素/有界 storage 全拒)在**任何句柄创建前** fail-closed;safe 公共面 `run_graphics_offscreen_bindless(...) -> Result<Vec<u8>, String>` 对上全 safe(无 `unsafe` 签名);`bin/bindless_modes` 三判据 harness(四象限四色 / 篡改注册序换位 RED·复原 GREEN / feature 缺失 mock Err),device 数值判据阈值(步骤 64)归 owner 本机主循环。**G3.5 render graph 两 pass 执行器扩注(RXS-0240,RFC-0013 §4.D D5,同 feature `vulkan` graphics FFI 边界,0 新 U 号)**:`run_graph_offscreen`(**v1/v2/present/bindless 路径 0-byte 不动,独立组装**;入参 = `graph.rs` 纯 host 推导产物 `&[graph::PlannedBarrier]` + 两 `GraphPassDraw` + `SamplerDesc`)→ `run_graph_inner` → `run_graph_body`,复用同一 loader/instance/device/messenger(fail-closed)骨架,增量 = ① **两 render pass + 两 framebuffer + 两 pipeline**——pass0(三角形写 rt0,render pass `finalLayout = COLOR_ATTACHMENT_OPTIMAL` 停在写态,空 pipeline layout)+ pass1(全屏采样 rt0 出 final,render pass `finalLayout = TRANSFER_SRC_OPTIMAL` 供回读,含 4 set layouts);② **pass 边界 barrier 逐字重放**——`plan` 内 `at_pass==1` 的 image transition barrier 经 `graph_image_barrier_fields`(layout/stage/access 全取 graph.rs 同源表,**禁二次推导** P-11)重放为 `vkCmdPipelineBarrier`(rt0 `COLOR_ATTACHMENT_OPTIMAL→SHADER_READ_ONLY_OPTIMAL`);缺该 barrier(漏声明 read)→ rt0 停写态而 descriptor 声明 read-only → validation 报错(RXS-0239 hazard 红绿,`RURIX_VK_VALIDATION=1` fail-closed 翻 `Err`);③ **pass1 descriptor 建面**——set-per-class(set0/set2 空占位 / set1 SRV binding0 = rt0 view `SHADER_READ_ONLY_OPTIMAL` / set3 immutable sampler binding0)镜像 v2 分配律,故复用 `sampling_shaders_spv` 全屏采样语料;rt0 usage = `COLOR_ATTACHMENT | SAMPLED`,final usage = `COLOR_ATTACHMENT | TRANSFER_SRC`;终端回读 = final `TRANSFER_SRC_OPTIMAL` 色写→传输读可见性屏障 + `vkCmdCopyImageToBuffer`(手写终端胶水,镜像 v1/v2,非 pass 边界推导)。句柄(rt0/final image·mem·view / sampler / renderPass×2 / framebuffer×2 / setLayout×4 / descriptorPool / pipelineLayout×2 / pipeline×2 / shaderModule×4 / vertexBuffer×2 / readbackBuffer / commandPool)**线性配对 create/destroy、末尾逆序销毁**(pool 先于 setLayout,view 先于 image 先于 memory);单 graphics queue 同步提交 + `vkQueueWaitIdle` 后回读紧凑 RGBA8;safe 公共面 `run_graph_offscreen(plan, pass0, pass1, sampler, W, H) -> Result<Vec<u8>, String>` 对上全 safe(无 `unsafe` 签名);`bin/graph_modes` 三判据 harness(两 pass 自动 RT→SRV barrier 出图 / 漏声明 read 装配期 RX6030 拒 RED·声明 GREEN / 推导逐字重放对照),device 采样点数值阈值(步骤 65)归 owner 本机主循环。**G3.6 mesh 管线运行时扩注（RXS-0248 前半,RFC-0013 §4.E7/§6.4,同 feature `vulkan` graphics FFI 边界,0 新 U 号——mesh 管线在 graphics FFI 边界内,§6.4/E-2）**:`run_mesh_offscreen`(**present/采样 v2/graph run_graph 路径 0-byte 不动,独立组装**)→ `run_mesh_inner` → `mesh_body`,复用同一 loader/instance/device/messenger(fail-closed)骨架,增量 = ① **mesh feature 探测**——`vkGetPhysicalDeviceFeatures2` + pNext `VkPhysicalDeviceMeshShaderFeaturesEXT` 摘 `meshShader`(task 时并 `taskShader`)bit,缺失 → 确定性 `Err`(RXS-0210 L3,无静默降级 P-01);device 启用链仅置 `meshShader`/`taskShader`,**probe 阶段驱动填入的 `multiviewMeshShader`/`primitiveFragmentShadingRateMeshShader` 等余 bit 显式清零**(启用需连带 multiview/fragmentShadingRate feature,VUID-...-07032/07033,fail-closed);② **无 vertex-input mesh 管线**——graphics pipeline `pVertexInputState`/`pInputAssemblyState` 置 null(mesh 管线忽略,§4.E7)+ stage 集(task?)+mesh+fragment(`VK_SHADER_STAGE_{TASK,MESH}_BIT_EXT`)+ 新 FFI 命令 `vkCmdDrawMeshTasksEXT(x,y,z)`;fragment 见证 = 手编无输入 const-color `.spv`(`mesh_witness_fs_spv`,spirv-val vulkan1.2 accept)。句柄(color image·mem·view / renderPass / framebuffer / pipeline / pipelineLayout / shaderModule×N / readbackBuffer / commandPool)线性配对 create/destroy、末尾逆序销毁;单 graphics queue 同步提交 + `vkQueueWaitIdle` 后回读紧凑 RGBA8;safe 公共面 `run_mesh_offscreen(...) -> Result<Vec<u8>, String>` 对上全 safe;`bin/vk_mesh` covered 计数 + 篡改 SetMeshOutputs RED harness(步骤 66),device 真跑(本机 RTX 4070 Ti,validation 开**零报错**)判据阈值归 owner 本机主循环 |
| U30 | 🔒 **AS 构建 / SBT 布局 / device-address 细审计**(RXS-0248 后半,RFC-0013 §4.E8/§6.4/E-2;**全期最高危 unsafe 面 = 裸 GPU 指针 + 对齐算术,全期唯一新 U 号;U29 = EA1 预留显式跳让不回收**) | `src/rurix-rt/src/vk.rs`(feature `vulkan` 默认关闭)`run_ray_tracing_offscreen` → `run_rt_inner` → `rt_body`(同 U26/U27 loader/instance/device/messenger 骨架) | **present/采样 v2/graph/mesh 路径 0-byte 不动,独立组装**;RT 扩展集(`VK_KHR_acceleration_structure`+`ray_tracing_pipeline`+`deferred_host_operations`+`buffer_device_address`)经 `vkEnumerateDeviceExtensionProperties` + 纯 host [`negotiate_device_extensions`] 协商 + feature 链(accel_struct/rt_pipeline/bda)`vkGetPhysicalDeviceFeatures2` 探测,任一缺失 → 确定性 `Err`(RXS-0210 L3,无静默降级 P-01);每个新增 `#[repr(C)]` VkStruct(`AccelerationStructureBuildGeometryInfoKHR` / `AccelerationStructureGeometryKHR`〔**geometry `union{triangles,instances}` 64 字节,构造安全读取才 unsafe,本运行时只构造后经指针传驱动从不读回**〕 / `AccelerationStructureInstanceKHR`〔**bitfield `customIndex:24|mask:8` / `sbtOffset:24|flags:8` 手工打包为两 u32,64 字节 align 8**〕 / `RayTracingPipelineCreateInfoKHR` / `StridedDeviceAddressRegionKHR` / `MemoryAllocateFlagsInfo` 等)与 Vulkan spec **逐字节对齐**(sType 值 + 字段序 + 尺寸,由 vk::tests `mesh_rt_ffi_layout_anchors` size/align 锚 + `VK_LAYER_KHRONOS_validation` 本机 NVIDIA 真跑**零报错**双证);🔒 **device address 为原始 GPU 指针面**——`vkGetBufferDeviceAddress`(vertex/instance/scratch/SBT buffer)/ `vkGetAccelerationStructureDeviceAddressKHR`(BLAS 供 TLAS instance 引用)取址**仅作 build/SBT/instance 引用与 `VkStridedDeviceAddressRegionKHR` 填充,从不解引用为 host 指针**(裸指针不越界:BLAS/TLAS 两段构建 BLAS→内存屏障→TLAS 单 queue 全序,AS device address 于 `vkCreateAccelerationStructureKHR` 后即合法);🔒 **SBT 三 region 对齐铺设**——`vkGetRayTracingShaderGroupHandlesKHR` 取 handles,对齐算术经**纯 host** [`plan_sbt`]/[`align_up`](**不在 unsafe 内推算,可单测:raygen size==stride VUID-04023 / region offset baseAlignment 对齐 / stride ≥ handleSize 且 handleAlignment 整数倍**)铺 raygen/miss/hit,host-visible SBT buffer(`SHADER_BINDING_TABLE`+device-address 内存 `VK_MEMORY_ALLOCATE_DEVICE_ADDRESS_BIT`)按对齐偏移 `copy_from_slice` handle 字节;descriptor set-per-class(set0 TLAS `ACCELERATION_STRUCTURE_KHR`〔`WriteDescriptorSetAccelerationStructureKHR` pNext 链〕/ set1 storage image `STORAGE_IMAGE` GENERAL);`vkCmdTraceRaysKHR(W,H,1)` → storage image `GENERAL→TRANSFER_SRC` → `vkCmdCopyImageToBuffer` 回读。句柄(vertex/instance/scratch×2/AS-storage×2/SBT/readback buffer + BLAS/TLAS AS handle + storage image·mem·view + setLayout×2 / pool / pipelineLayout / pipeline / shaderModule×3 / commandPool)**线性配对 create/destroy、末尾逆序销毁**(**AS handle 先于其 storage buffer**;buffer 先于其 memory);单 graphics queue 一次 build+trace 提交 + `vkQueueWaitIdle` 后回读;messenger fail-closed 同 U27(ERROR 级校验翻 `Err`,退出码判红反 grep);gate feature `vulkan` 默认关闭,**NVIDIA(CUDA)构建零改动零回归**;safe 公共面 `run_ray_tracing_offscreen(raygen, miss, chit, &vertices, W, H) -> Result<Vec<u8>, String>` 对上全 safe(无 `unsafe` 签名);`bin/vk_rt` 三件套命中/miss 双色 + 移动顶点 RED harness(步骤 67),device 真跑(本机 RTX 4070 Ti,validation 开**全机构零报错**)像素判据阈值归 owner 本机主循环 |

> **G1.1 范围补注**:U17/U18 为 CUDA 侧 external-resource FFI(`src/rurix-rt`,feature `d3d12-interop`);D3D12/DXGI 侧 C/C++ shim 的 FFI 边界(`extern "C"` 声明 + COM)注册见 [`rurix-d3d12.md`](rurix-d3d12.md)(feature `real-shim`)。safe 类型层(`ExternalBuffer`/`ExternalSemaphore`/frame typestate)对上全 safe,三类资源生命周期错误由 Rust 类型系统编译期拦截(无 RX 码,RXS-0140~0142)。MS1.2b `OwnedPresentSession`(非闭包持有形态,RXS-0197/0198)对上同样全 safe——复用 U17/U18 原语与建链/fence 助手,错序降为运行期确定性错误(`InvalidState`),不新增 unsafe 原语。
>
> **G1.2 范围补注**:U19 为流序分配器 CUDA FFI(`src/rurix-rt/src/sys.rs`,`cuMemAllocAsync`/`cuMemFreeAsync`,Option 非致命解析),U20 为 `AsyncBuffer`/`AsyncReady` 流序分配 RAII(`pipeline.rs`,`PoolAlloc` 单点 Drop + `share_with` 跨 stream 时序边,复用 U11/U15 event 同步骨架)。safe 类型层(`AsyncBuffer`/`AsyncReady` typestate)对上全 safe,三类流序分配生命周期错误由 Rust 类型系统编译期拦截(无 RX 码,RXS-0144~0148;镜像 RXS-0132 `InFlight` 先例)。`AsyncBuffer` 随 `rurix-rt` **始终编译**(无可选依赖,无 feature 门控,默认 workspace 网全覆盖)。
>
> **G1.5 范围补注**:U22 为按架构预编 cubin 装载 FFI(`src/rurix-rt/src/sys.rs`,`cuModuleLoadData`/`cuDeviceGetAttribute`,Option 非致命解析)。U21 在 [`rurix-engine.md`](rurix-engine.md)(G1.3,C ABI 导出豁免),故 G1.5 续号取 **U22**。装载协商 [`select_load_variant`](../src/rurix-rt/src/fatbin.rs)(`fatbin.rs`)为**纯函数**(host 可测,无 FFI / device 依赖,RXS-0150/0151 锚定),safe wrapper `Context::load_module_artifacts` 对上全 safe;cubin 命中即用、未命中 / driver 无 cubin 装载 / cubin 拒绝 → **降级保守 PTX fallback**(既有 `cuModuleLoadDataEx` 装载协商 U5,RXS-0076/0077 语义 0-byte,**降级而非 reject、不 poison**,D-207)。`DeviceArtifactSet` / `ArtifactKind` / `SmTarget` 随 `rurix-rt` **始终编译**(无可选依赖,无 feature 门控,默认 workspace 网全覆盖),仅实际 cubin 装载 device 真跑经 GPU 门控。无新 RX 码(装载协商降级而非 reject)。
>
> **MS1.2 范围补注**:`pipeline.rs Bound::{load_module_artifacts,try_load_cubin}` 为 U22 同一
> 原语的 shared 族镜像(RFC-0009 §4.3):协商决策仍单点复用纯函数 `select_load_variant`,
> cubin 被拒降级保守 PTX fallback(不 poison,D-207);`SharedModule` 自持 `Arc<SharedInner>`
> (context 不早于模块,Drop 自行重绑 current 后卸载),返回 **`'static` brand** 供
> `rurix-rt-cabi` u64 句柄表跨调用惰性缓存(消费侧注册见 [`rurix-rt-cabi.md`](rurix-rt-cabi.md)
> U25)。

## 销毁纪律(D-231)

`Context::drop` 先 `cuCtxSynchronize`,再按种类释放:独占 context(`cuCtxCreate`)走
`cuCtxDestroy`,primary context(`from_primary` retain)走 `cuDevicePrimaryCtxRelease`
(引用计数,不 destroy 与 PyTorch 共享的 context)。Stream/Module/PinnedBuffer 的 Drop 在
各自资源上调用 free/unload;`DeviceBuffer` 的 Drop **仅当 `owned`** 才 `cuMemFree`——借用
缓冲(`from_device_ptr`,零拷贝互操作)所有权在外部 deleter,Drop 不释放(不双重释放)。
错误吞掉(Drop 无 panic)。生命周期 brand(`'ctx`)保证资源不晚于 context(借用检查 +
反向 Drop 序)。

**M8.3 UC-02 shared 族销毁纪律(`pipeline.rs`,RXS-0130/0133)**:`SharedContext` 经 `Arc`
引用计数包裹 primary context,`Clone` 仅 `Arc` +1(不重复 retain);`DeviceBox`/`PinnedBox`/
`SharedStream`/`SharedEvent`/`SharedModule` 各持一份 `Arc<SharedInner>` 克隆,故 `SharedInner`
(及其 `primary_ctx_release`)在**全部资源 Drop 之后**才发生(context 不早于其资源,跨线程亦
然)。各资源 Drop 在**任意持有线程**先 `cuCtxSetCurrent(inner.raw)` 重绑本 context 再
free/destroy/unload(`Arc` 存活保证句柄有效),Drop 仅一次(单一所有权、非 `Clone`,不双重
释放)。current context 线程绑定守卫 `Bound` 为 `!Send`(`PhantomData<*const ()>`),不得跨线程
转移;可跨线程的仅 `SharedContext`(`Send+Sync`)/ `DeviceBox`(`Send`)/ `SharedEvent`(`Send`)。

## 测试

- `cargo test -p rurix-rt`(子进程隔离 GPU 真跑,14 §6):装载→launch→拷回逐元素核对。
- 全链路真跑红绿见 M4 CI_GATES §2 步骤 21(M4.3 接入)/ close-out run URL。
