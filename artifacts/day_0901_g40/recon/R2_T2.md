# R2 侦察交接单 — T2 device cut P2 生产 dispatch(#77)

> 2026-09-01,主 agent 侦察。素材:g31_frame_cut_arm.rs 全读(2324 行)/
> g31_frame_cut_probe.rs(305 行)/ G39 t5_devicecut/DESIGN.md §2.7 P2 +
> §4-2 / render_exec.rs 会话构造面 / g31_cluster_cull.rx 头注。

## 1. 会话 resources/passes 面(表驻留 SSBO session 化落点)

- 现主会话(L1282-1373):3 资源(rays/hits/arena)+ 2 pass(fc_clear/
  fc_rq)+ 1 readback(hits)+ `new_with_accel_structs`(单 BLAS
  ALLOW_UPDATE)。
- **P2 结构约束**:帧 k 决策码必须在构造帧 k 的 FrameUpdate(uploads =
  cut 差集)之前拿到 ⇒ dispatch 不能进主会话同帧 pass 序 ⇒
  **独立常驻 cull 会话**(`DeviceFrameSession::new`,无 AS 面,L1122——
  compute-only 合法):10 资源 = kernel 10 buffer 布局(params 256B
  host-visible / cluster_f32 n×40B data 驻留 / lod_f32 n×32B data 驻留 /
  input_ids n×4B data / hzb_data 4B / hzb_meta 12B / counters 12B /
  decisions n×4B / vis_ids / occ_ids),1 pass(cull,Direct [n,1,1]),
  1 readback(decisions ~493KB)。每帧仅 params 256B 上传 +
  readback_subset [0](决策码回读);counters 不清零(溢出仅丢
  vis_ids 追加写,decisions 逐项无条件写,kernel 头注 L49-52 字面)。
- P1 的 `vk::run_compute` 逐帧建 device 路(82.7ms 上界)退役;
  表构造/NoContraction 注入/red-arm 篡改三函数(L577-752)原样复用。

## 2. 决策码消费形(host 由 d==4 构造 cut 集)

- 新纯函数 `frame_cut_select_from_decisions`:decisions(u32/簇,
  canonical 全局序)→ 逐块 pre bool 集(d==4)→ `verify_cut_coverage`
  (host 影子核直跑回读集,fail-closed 逐字保持)→
  `frame_cut_promote_min_level`(ml 照旧 host)→ 提升后再 verify →
  (sets, clusters, tris, tris_promoted)——与 `frame_cut_select_ext`
  的 select 后链字面同形,仅 select 源换 device。
- decisions∈{2,4} 闭集断言保持(中和面破坏检出);red-arm 篡改 ⇒
  决策翻转 ⇒ coverage 覆盖性必破 ⇒ rc≠0(构造性证明施加链真实消费
  device 决策;报文形态变化如实登记)。

## 3. cut_ms 分项计时(select/verify/promote)插桩点

- `frame_cut_select_ext`(L481)内三段计时:select_lod_cut_grouped 累计
  = select_ms;两处 verify_cut_coverage 累计 = verify_ms;
  promote 段 = promote_ms。签名加性尾参(&mut 计时槽或返回结构),
  调用点闭集 3 处机械补(L456 frame_cut_select / L1245 帧 0 / L1398 逐帧)。
- device 臂:select_ms = params 上传 + execute + 决策回读 + bool 集构造
  墙钟;verify/promote 同 host 口径。stat 加性字段:select_ms/verify_ms/
  promote_ms(Option 或恒在;evidence v1 保持 + 加性,G39 先例
  w4_verify 无 schema 断言)+ device_cut_dispatch_gpu_ms(cull 会话
  telemetry pass GPU)。
- 既有 `device_cut_probe_ms` 语义由 run_compute 全程 → 退役;P2 以
  select_ms(device)+ dispatch_gpu_ms 分项承载,命名区分不混口径。

## 4. 边界与不动面

- 缺省 `--cut-source host` 字面 0-byte(E2 默认值路径);kernel
  g31_cluster_cull.rx 冻结;rt/lane_body/窗口 bin/schema 0-byte;
  min-level 提升照旧 host;P3(直写竞技场)不预支。
- 验收 = 任务书 B1 六条;跨锚 = G38 t3_incr digest 序列 + G39 t5_dev;
  ml1×device 组合墙钟对 DESIGN §4-2 预期 ~15-19ms 登记不判红。
