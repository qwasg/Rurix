# R3 侦察交接单 — T3 AS 副本内存 evidence 补登 + T4 素材集

> 2026-09-01,主 agent 侦察。素材:g31_fif_dyn_probe.rs 账采集段 /
> g14_3_lane_body.rs slot_as 面 / bench receipt 组装段。

## T3:AS 副本组内存账来源与落点

- **账来源(probe 先例,机制直迁)**:session telemetry 全量 allocation
  ledger(`out.telemetry.allocations: Vec<AllocationLedgerEntry>`)按
  AS 表项 `resource_id == resources.len() + ai + 1` 过滤求和
  (g31_fif_dyn_probe.rs `slot_as_mem_from_ledger` L605-620;逐表项含
  instance buffer/BLAS 顶点/BLAS storage/TLAS storage/scratch 全部
  vkAllocateMemory 真账;首帧采集,ledger 项 session 生命周期稳定)。
- **lane_body 落点**(编辑面独占,禁改 render_exec*):
  1. lane 加字段 `slot_as_mem_bytes: Option<Vec<u64>>` +
     `slot_as_resources_len: usize`(`create_with_slot_as` L10408 存
     descs 资源数;UnifiedDescs 七变体 d.0.len() 同式);
  2. `collect_frame_dyn`(L11307)/`collect_frame_skin`(L11411)出队
     collect 后、rec_from_output 消费前,首帧过滤求和入字段(镜像
     probe 纯函数,lane_body 本地副本如实登记);
  3. bench receipt(L18631)加一个 `{}` 注入位:slot_as 臂产
     `"slot_as_mem": {per_slot_bytes, group_total_bytes, note},\n  `
     段;非 slot_as 臂 = 空串 ⇒ **off 面 receipt 字节 0 漂**。
- **预算门 0 动不混**:`g31.fif_dyn.slot_as_group_mem_bytes`(probe 口径
  44,544/66,816)条目/标定/evidence 路全不触;本登记面 = bench receipt
  notes(生产 bistro 规模,数百 MB 级),budget_eval 维持 330 pass 0 skip。
- 验收 B2:dyn/skin × inflight 2|3 各一跑字段在档 + flip-trace
  (RURIX_G14_FLIP_TRACE)轨迹对 G39 B1/B2 在案件位级不漂(G39 命令口径
  = gpu_b1_t2.py / gpu_b2_t3.py 逐字复用)。

## T4:bridge_ext×FIF rt 平行入口评估素材集(纯文档)

| 素材 | 用途 |
|---|---|
| `artifacts/day_0830_g38/t2_fifdyn/WIRING_PLAN.md` §5-4 | skin×FIF 与 bridge_ext 不混的原始留窗字面 |
| `artifacts/day_0831_g39/t2_skin/REPORT.md` §8 | 批次 B 后的留窗刷新(bridge_ext×FIF 须 rt 平行入口) |
| `src/rurix-rt/src/render_exec.rs` bridge_ext 面(`execute_with_frame_update_bridge_ext`/`BlasRefitBridgeExt`/多 region copy) | 现入口形态与单槽约束事实链 |
| `src/rurix-rt/src/render_exec_g37_fif_dyn.rs` + `submit_with_frame_update_slot_as` | slot_as FIF 入口形态(平行入口的形制参照) |
| RFC-0030 v1.1 §4.3 L2a | 每槽 AS 副本律法与 evidence 义务 |
| WP §4 模板(WIRING_PLAN) | 决策语义分析对照(host 在环判据) |

产出 = `t4_bridgeext/EVAL_BRIDGEEXT_FIF.md`:rt 平行入口形态案
(bridge_ext 多 region copy × 每槽 AS 副本提交面语义)+ 决策语义分析 +
工程量/风险 + GO/NO-GO 判档建议;判档留 owner,不预支实施。
