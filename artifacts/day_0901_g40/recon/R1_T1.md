# R1 侦察交接单 — T1 ReSTIR 画质补窗

> 2026-09-01,主 agent 侦察(本役无子 agent 工具,角色合并如实登记)。
> 素材:g31_realism.rx 全读(2148 行)/ g31_window_present.rs 检索面 /
> G39 t1_restir/REPORT.md / EVAL_RESTIR §5/§6.2。

## 1. reservoir SSBO 回读面可达性:**可达,无须动 rt**

- 窗口 bin 会话既有 readback 形态:lane 创建期注册 `Readback::Buffer{res,offset,size}`
  列表(如 unified 基座 0..=4),逐帧 `prepare_update` 经 `readback_subset:
  Some(vec![...])` 订阅子集(L5105 既有消费形);消费侧
  `rec_from_output` 按下标 `std::mem::take` 提取并有计数断言(L5150-5254)。
- 结论:T1a 无须 rt 加性入口——verify 模式在 descs 追加 reservoir 双份
  readback 注册(仅 verify on 时注册,off/on-非verify 面 readback 列表字节不变),
  逐帧订阅 parity-cur 一路。**T1a 开窗条件成立,不触 rt。**

## 2. 镜像可行性关键事实(kernel 全读结论)

- **臂⑨ reservoir 四元组(y/w_sum/m/phat_y)在验证射线之前定值**
  (L959-1091 WRS/merge;L1093-1099 W;L1128 起才发射线;写回 L1186-1194
  消费 WRS 链变量)——**host 镜像零射线求交**,只需 kernel 精确
  hit_pos(hx,hy,hz = near + d·th,L322-324)与最终法线 nx,ny,nz
  (mut,法线贴图臂后值)+ points 表 + params + prev_vp + 上帧 reservoir。
- 随机维全闭式:候选 R3(L971-975)/判定 R2(L976-979)/merge R3
  (L1080-1083),px/py/params[52] 驱动,host 可逐字复算。
- phat 口径(L1001-1002):`max3(li)·(cos_s/d2s)·(gate_d·gate_cs·gate_lc)`,
  全部由 hit_pos/normal/points/params[49] 重建。
- **承载面方案(选定):reservoir stride 4→12 f32/px**
  `[y,w_sum,m,phat_y | hx,hy,hz,hit_f | nx,ny,nz,rsv]`——零新增 binding
  (下标族 37-39/45-47 与 AE/资源计数断言全不动),仅 buffer 尺寸 ×3
  (两 descs builder 各一处)+ kernel 读写步幅。@1080p 内部分辨率
  双份 2×99.5MB(heap 282MB 先例内)。同一面同时服务 T1a(镜像输入)
  与 T1b(prev 深度/法线拒)。
- **T1a 位级先决 = NoContraction 注入——〔实施期修正:已在树〕**:初判
  「窗口 bin 对 realism 链 SPV 不注入」有误——`--textures on` 装载路
  L9454 `spv_inject_no_contraction(&load_spv(&spv_texture))` 对场景 SPV
  **恒注入**(B4 纹理接线既有面,evidence `no_contraction_injected:true`
  字段自证);restir 依赖 --textures on ⇒ 全部 restir 跑恒经该路,先决
  已成立,**零新增注入面**。f32 ULP 预案照 G39 T5 §3.2:mismatch≠0 ⇒
  量化+归因打印(px/两侧 y/phat/w_sum),不凑绿。

## 3. T1b 深度/法线拒方案(prev 承载面 = 上述 stride-12 扩槽,无新面)

- 拒点在 merge 循环体内(读 prev 槽后):折入槽有效门 `lrs_ok`
  (L1049)——`lrs_ok × rej_d × rej_n`;m_cl/wo 已 ×lrs_ok ⇒ 拒 =
  计数门零合并(m 不加、w_other=0、take 门自落 0),即「merge 计数门
  追加两拒」字面。
- 深度拒:prev 槽存 prev 命中位,以 prev_vp 行 3 投影得
  `pcw_prev = prev_vp[12..16)·prev_hit`,与既有 `lrs_pcw`(当前命中在
  上帧相机的视深,L1020)相对差 `|pcw_prev−lrs_pcw| ≤ dth·|lrs_pcw|`;
  dth = params[76](0 = 拒关)。
- 法线拒:`dot(n_cur, n_prev) ≥ nth`,nth = params[77](0 = 拒关)。
- 阈值形态 = CLI 子旗标 + 字面缺省(depth 0.10 / nrm 0.80),裁决数据 =
  B3 dolly A/B(拒开 vs 拒关〔=G39 语言形〕),登记 REPORT——「字面裁决
  登记」路径,阈值可复跑复核。
- params 扩面 76→80:[75] T1c 钳值(G39 预留槽启用,0=off)/[76] 深度拒
  阈/[77] 法线拒阈/[78..80) 预留 0。off 面恒 72/56 不扩(G39 律)。

## 4. T1c per-pixel phat 钳制

- 落点 = `lrs_wgt`(L1099)后:`W·phat_y ≤ clamp_v` 归一化钳
  (`s = min(1, cv/max(W·phat, tiny))`,branchless 门 params[75]>0);
  只钳输出消费,reservoir 写回四元组不动 ⇒ 镜像/时域链零扰动。
- 旗标 `--lamp-restir-clamp <f>`(0=off 缺省;域 [0,64] 闭集,须随 on);
  缺省裁决走 B3 A/B(26 簇 on:clamp off vs clamp 4 vs mcap 4 对照)。

## 5. 行号刷新(在树 HEAD 1478859a 实测)与 SPV 谱系

- kernel:门/让位 L733-736;臂⑨主块 L928-1194(WRS L967-1014/重投影
  L1015-1034/merge L1035-1091/W+验证射线+合成 L1092-1182/写回 L1183-1194);
  GI2 臂注 L1195 起。窗口 bin:SPV 常量 L376(`night_0831` 现值);
  readback 消费 L5040-5260;NoContraction helper 先例 L5586。
- SPV:现锚 `.tmp/night_0831/spv/g31_realism_restir.spv`(sha 5571e875…,
  365,724B);本役 kernel 必改 ⇒ 复编到 `.tmp/night_0901/spv/` 新 sha
  登记,`G31_DEFAULT_SPV_REALISM_RESTIR` 常量随迁;既有 9 下位工件
  (texture_nrm_gi 三件 + realism f0/ao/soft/refl/gitex/nrm/transp/ris)
  sha 全等维持,off 面字节隔离律不破。
- 编辑面:g31_window_present.rs + kernels/g31_realism.rx + 新 SPV;
  **禁改** g28_restir.rx / gi/restir_reservoir.rs / gi/multi_light.rs /
  lane_body / rt(收役 g31_restir_wiring_smoke --gate 维持证明)。

