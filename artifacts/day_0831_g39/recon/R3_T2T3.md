# R3 — T2 skin 批次 B × T3 slot_as 单源折叠交接单(2026-08-31;行号 = 侦察时快照,实施以字面锚为准)

**总要**:WIRING_PLAN(`artifacts/day_0830_g38/t2_fifdyn/WIRING_PLAN.md`)批次 A(§1-A1~A5 + §2-B1)已实施并 GPU 双硬门全绿;T2 剩余 = §1-A6(skin)+ §2-B2(CLI 解除)。**§2-B2 的「替换前」字面锚已过期**——现行字面在 `g14_3_pipeline_perf.rs` L648-650:

```rust
                if inflight != 1 {
                    fail("--skin-demo 要求 --inflight 1（蒙皮 × slot_as 批次 B 留窗：RFC-0030 v1.1 §4.3 L2a 通路 rt 侧已支持〔blas_refit 槽纪律同律〕,g14_3 接线计划 = artifacts/day_0830_g38/t2_fifdyn/WIRING_PLAN.md §1-A6;接线前 fail-closed 维持,蒙皮车道走顺序入口）");
                }
```

(闭集注释②也已改写 L634-641;B1 dyn 解除已完成 = 现 L621-626 登记注释。)

## T2 目标(§1-A6,目标文件 `g14_3_lane_body.rs` + `g14_3_pipeline_perf.rs`)

A6 快照行号漂移现值:`frame_skin` = L10855-11049(rec 组装段 L10974-11045、update 构造 L10960-10970、`blas_refit: Some(blas)` L10969);skin scene pass `g31_skin_scene` 创建期 `accel_structs: vec![0]` 现 L14246-14251。四步:
1. rec 组装段提取 `fn skin_rec_from_output(&self, out, readback_out, verify, debug_tris, ow,oh,iw,ih) -> Result<SkinFrameRec,String>`(原地逐字搬移,调用点 1 行替换,禁复制第二源)。
2. skin update 构造段加 `scene_as_override: Option<u32>` 分叉——skin 的 scene pass = **pass 1**(override 推 `(1, b)` 非 `(0, b)`);既有 (3,resample)/(4,resolve) 不动。
3. `submit_frame_skin_slot_as`:`blas_refit: Some(BlasRefitUpdate { as_index: target=base+slot, … })`(skin 臂无 tlas_update,仅 blas_refit 换槽);副本组创建期每表项 `updatable_blas: &[1]`;palette/params uploads 走既有 per-slot staging 零改动。
4. `PendingSkinFrame { ticket, frame_index, readback_out, verify, debug_tris }` + `collect_frame_skin` + `pending_skin_len`;核验(SkinVerifyFrame 组装)同 A5-2 提取闭包、collect 侧凭帧号复算 palette(骨骼动画 = 帧号纯函数);skin bench 循环 FIF 分支形同 A5-3(骨架镜像静态 FIF 分支,AS 副本组构造 + `if inflight > 1 { FIF 循环 } else { 既有循环 0-byte }`)。skin bench 现值:`SKIN_UPDATABLE_BLAS:[u32;1]=[1]` L16684、单表项 AS + `create(&descs,&accel_structs,1)` L16685-16693、`BlasRefitUpdate{as_index:0,blas_index:1,src:U_TRIS+1,src_offset:skin_tri_base*36,byte_len:tri_count*36,after_pass:0}` L16697-16704。
- 批次 A 既有件可参照:`create_with_slot_as`/`prepare_update_ext scene_as_override`/`submit_frame_dyn_slot_as`/`collect_frame_dyn`/`PendingDynFrame`/dyn bench FIF 分支(A1-A5 已在树)。
- **纪律**:skin 走普通 blas_refit 路;bridge_ext×FIF 不混(§5-4);禁改 `render_exec*.rs`;lane_body 为 7 个 include 消费方共享体,共享签名变动须机械补参(批次 A 曾补 `g35_particle_lane.rs` 一处 None)。
- skin_verify all_pass:lane_body L17108-17110(`all(pass) && motion_max >= SKIN_MV_HOST_MOTION_MIN_PX=1.0px` L13753);`skin_verify.json` schema `rurix.g31.skin_verify.v1`;真动门标定口径 = 100 帧窗(`--frames 120 --warmup 10` 满足)。
- refit 非纯降档:L2a = `rfcs/0030-...md` L99「Refit 非纯实测时按槽稳定判据显式降档登记」;登记先例 = gpu_batch1.py L231(eq_double 成立而 eq_across 破时不计 FAIL,note 登记)。

## T3 目标(§5-1,目标文件 `src/rurix-rt/src/render_exec.rs` + `render_exec_g37_fif_dyn.rs`)

- 折叠源登记:`artifacts/day_0830_delivery/w3_deep/fif_dyn/REPORT.md` L211 §7-3:复制适配体应折叠回单源——`submit_pipelined_frame` 加 `Option<&SlotAsGroup>` 参数,顺序路 0-byte 等价重构。
- 基函数 `submit_pipelined_frame` render_exec.rs **L10267-10563**(doc L10243-10266);复制体 `g37_submit_pipelined_frame_slot_as` render_exec_g37_fif_dyn.rs **L291-649**(include 点 render_exec.rs L13068);公共入口 `submit_with_frame_update_slot_as` L167-269 **签名不动**;`SlotAsGroup{base:u32,len:u32}` L49-55。
- 差异清单(折叠须吸收):①防御性复核换向——基 L10276-10278 一律拒 `prepared.tlas`;slot_as L303-320 改核 tlas/blas 目标 `== group.base+slot` 并拒 `tlas_b`/`blas_b` ②插入② L357-368:fence 等待后 `mgr.write_transforms(...)` 写本槽副本 instance buffer ③插入③ L488-526+L544-568:组装 `AsFrameOps{...}` 传 `record_frame_body(..., as_ops)`(基函数恒 None,None L10479-10480)④报错前缀 `"FIF:"` vs `"slot-AS FIF:"`(保持各路字面)。其余全为逐字同形块。
- 耦合冻结注记:render_exec.rs L5805-5809「两结构(BlasRefitRecord/AsFrameOps)被 render_exec_g37_fif_dyn.rs 以字面量构造(T2 冻结面),加字段即打崩其编译」——折叠后随实况更新注记。
- 消费方(签名不得变):lane_body 经 `submit_with_frame_update_slot_as` 消费;`g31_fif_dyn_probe` 同。

## GPU 验收(主 agent 锁内跑,子 agent 禁 GPU)

- T2 批:`g14_3_pipeline_perf.exe --bench --backend tsr_device --scene bistro-interior --tier 100 --frames 120 --warmup 10 --skin-demo --inflight <1|2|3>` × 双跑,env `RURIX_G14_FLIP_TRACE=<dir>`;判据 x1==x2==x3 逐字节 + 各臂双跑位级 + skin_verify all_pass + VUID=0;refit 非纯按 L2a 降档登记。fif probe 双臂 + `ci/calibrate_fif_budget.py --check`。skin 生产规模 AS 副本内存(角色 BLAS ×S)如实入 evidence notes 不混 probe 口径。
- T3 零语义批:静态 FIF(`--bench tsr_device --inflight 1|2|3` 无 dyn/skin)+ dyn rebuild/refit ×1|2|3 + skin ×1|2|3 + fif probe 双臂,全部 digest == 折叠前基线。
