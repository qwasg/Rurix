# R5 — T5 device cut 设计依据交接单(2026-08-31)

## frame_cut 臂事实链

- 臂本体 `src/rurix-render/src/bin/g14_3_lane/g31_frame_cut_arm.rs`(1799 行,probe/窗口 bin include 共享单源):竞技场 = 全簇固定槽位(单 BLAS 单实例 ALLOW_UPDATE);逐帧 **host 金标准 cut**(`frame_cut_select_ext` L450-518 = `select_lod_cut_grouped` + `verify_cut_coverage` fail-closed + min-level 提升映射)→ 槽位差集(进 cut 写真几何/出 cut 零面积折叠)→ `FrameUpdate::blas_refit`(bridge_ext 多 region copy + UPDATE build)→ RQ 渲染 → 命中缓冲回读 sha256。
- host cut 成本(CAMPAIGN_LOG L54 留窗登记「3-11.5ms(#77 device cut)」):t3_incr 帧 0=3.218ms 其余 6.29-12.83;t3_full 6.01-9.44;窗口真轨迹 5.99-10.10;ml1 9.68-15.25(提升+二次 verify +~5ms)。**UPDATE build ~21ms 地板才是主导;ml1 降档后 exec 9.2-12.4ms 进预算**。
- incr==full 位级门:16 帧 digest 序列逐字节等(t3_incr==t3_full 已核);跨进程双跑 + 臂内建双跑(L1306-1337);ml1 只自洽双跑不跨档比(几何表示不同,口径非缺陷)。
- 关键判定(与 WP §4 对照):cut = **相机纯函数**(帧 k 相机 host/device 同帧已知),无上帧回读反馈环——与 WP §4 NO-GO 场景(HZB masks 由上帧回读驱动的 host 在环闭环)结构相反。生产语义路 cut→竞技场写→refit→RQ 可全 device 闭环零回读;同帧 CPU 消费仅:差集/上传构造(下沉后由 device 写竞技场消失)、copy_regions 收集、回读判据(probe 判据面非生产语义)。

## 已验证 device kernel

`src/rurix-asset/kernels/g31_cluster_cull.rx`(331 行;**在 rurix-asset 非 rurix-render**):单 kernel 逐簇 invocation;关1 视锥球 L80-122 / 关2 背面锥 L123-148 / 关3 组共享球 LOD cut L149-208(`select_lod_cut_grouped` 字面同式)/ 关4 HZB rect 两阶段 L209-310;I/O = params 64 f32 + cluster_f32 10/簇 + lod_f32 8/簇 + input_ids + hzb + counters[3] 原子 + decisions(判定码 0-4)+ vis_ids/occ_ids 原子压实。harness `src/rurix-asset/src/bin/g31_cluster_cull_device.rs`(判据:判定码逐项全等 vs host 复算/可见集全等/裁判零假阳性/双跑位级/red-arm 篡改必变;NoContraction 注入先例 L87-119)。v1.1.5(2026-08-27)全绿;**仅 harness,未进生产,无 ci 门无 evidence 注册**。
`artifacts/day_0830_delivery/w3_deep/frame_cut_as/REPORT.md` L129-134:「device cut kernel(g31_cluster_cull 链)替换 host cut 归 #77 生产接线自身……cut 来源可插拔(判据不变)」。

## WP §4 先例原文(决策语义分析模板)

> 机制根据:HZB 车道的逐帧 TLAS 掩码更新(masks/uploaded_masks)由**上帧回读的可见性判定**驱动——host 在环反馈闭环。FIF 化会把决策延迟 S−1 帧(读到的是 k−S 帧判定),改变剔除语义本体,不是「每槽副本」能消解的写面竞争问题。

## digest 口径与等价门素材

- 现 digest = RQ 命中缓冲回读字节 sha256(非 cut 列表);cut 源替换若 cut 集逐帧全等 ⇒ 竞技场字节全等 ⇒ digest 门免重锚。等价门形 = cluster_cull harness 判据①(判定码逐项全等 vs host `select_lod_cut_grouped`),f32/NoContraction 风险须登记;`verify_cut_coverage` fail-closed 的 device 化等价(或 host 影子核)须设计。
- 三关超集 kernel 中和方案(等价所需):frame_cut 只用关 3——关 1/2/4 需中和(params 视锥退化/HZB all-visible 或新加性变体)。
- 回读基建:`Readback::Buffer`(render_exec.rs L577)/`FrameUpdate.readback_subset`;免回读 GPU-driven 先例:`DrawSpec::Indirect`/`DispatchSpec::Indirect`(L326-342)+ g35 `g35_indirect_args.rx`/`g35_compact_u32.rx`(device 组装 args→间接消费零回读);`acceleration_structure_indirect_build` 未启用(L4143/L8961 恒 0)。
- NO-GO 侧素材:device f32 等值须重证;host cut 3-11.5ms 与 GPU 段当前串行叠加,下沉省 host 段但不解 build ~21ms 地板(须与 --min-level 组合叙事);TODO #76(GPU compact+MDI 零回读)P1 独立行勿混。
