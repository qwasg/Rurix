# lamp-k 提档提案(G39 T1 判 GO;**只登记,默认翻转/重锚不预支——归 owner 裁决**)

> 2026-09-01。开窗条件(G38 CAMPAIGN_LOG L27「提灯数须 ReSTIR」)已被 G39 T1 measured 兑现。
> 本提案 = TODO 表 v1.2.2 随役登记件;默认字面任何变更触发整批重锚(G38 Wave3 程序),本役零重锚纪律下不执行。

## 事实链(measured,今晚机态,evidence = `t1_restir/ab/T1_AB_MATRIX.json` + `ev/`)

| 档 | grid / k | kept | off p50(render_wall) | on p50 | dark ROI p95 噪声(off→on,u8 归一) |
|---|---|---|---|---|---|
| s1 现默认 | 0.6 / 12 | 12 | 9.560ms | 7.390ms | 0.0093→0.0191(dark_arch) |
| s5 提档候选 | 0.15 / 48 | **26** | **11.546ms 超线** | **7.526ms(+3.58 余量)** | 0.0094→0.0241 |
| 探索档 | 0.10 / 96 | 38 | 13.755ms 超线 | 7.417ms | 0.0094→0.0245 |

- **机制兑现**:`--lamp-restir on` 帧时与簇数解耦(7.4-7.5ms 恒平;每像素 M=8 候选 WRS + 1 条验证射线,与灯数 O(1));26/38 簇逐盏路超线现象与 G38 阶梯方向一致(G38: 26 簇 12.96ms/margin −1.85)。
- **代价如实**:on 臂为 1-spp 随机换选 + 时域 merge(m_cap 8),dark ROI p95 时域噪声升 ~1.5-2.6×(绝对量 ≈2.4-6.2/255 LSB,TSR 后口径);点灯软阴影半影让位 + 玻璃后点灯影转硬影两税(T1 REPORT §四)。
- 确定性:on 臂 r1/r2 双跑位级 + dolly 240f digest_seq 双跑位级 + VUID=0;off==锚(all-off/full19 双锚 MATCH)。

## 提案形态(owner 三选一)

- **A(维持,零动作)**:默认 12/0.6 不动;26+ 簇场景以显式组合交付(`RURIX_G31_LAMP_GRID_M=0.15 --quality full --lamp-k 48 --lamp-restir on`,已验证进预算)。零重锚。
- **B(提灯数默认档)**:`GRID_M` 默认字面 0.6→0.15 + `--lamp-k` 默认 12→48 + `--lamp-restir` 默认 off→on(full dup 表进臂)。代价:full19 → full20 语义变更,**整批重锚**(G38 Wave3 程序:负控+不动抽验+谱系登记);dark ROI 噪声上升随默认面进锚;两税(半影/玻璃影)进默认画面。收益:缺省画面 26 簇提灯 + 帧时 7.5ms(现默认 9.6ms 反而更快)。
- **C(仅臂默认 on,不提灯数)**:`--lamp-restir` 默认 on、灯数维持 12/0.6。收益 = 缺省帧时 9.56→7.39ms(−2.17ms 余量);代价 = 12 簇下方差纯劣化(零随机→1-spp)+ 两税,画质无灯数收益——**不推荐**(EVAL §0:16 盏以下逐盏全算收益前提不成立)。

## 建议

倾向 **A 维持 + B 留窗**:B 的重锚窗口应与下一次触锚语义变更合批(重锚成本均摊);A 已把「26 簇进预算」作为可交付组合钉死在案。判档人:owner。
