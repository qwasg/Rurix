From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Rurix Godot Bridge <rurix@example.invalid>
Date: Wed, 8 Jul 2026 22:30:00 +0800
Subject: [PATCH] Add default-disabled GRX-010 tonemap gate and Tonemap
 call-site opt-in wiring

Stacked on 0001..0010. Template copy of the GRX-009 0002/0003 pattern for
the tonemap pass:

- modules/rurix_accel: new default-false per-pass setting
  rendering/rurix_accel/passes/tonemap/enabled and try_record_tonemap()
  (0002-level module gate; the call carries no resource bindings, native
  handle mapping belongs to a later slice).
- drivers/d3d12/d3d12_hooks.h: default-false virtual try_record_tonemap()
  so any hooks implementation that does not override it (or the absence of
  a hooks singleton) keeps the native Godot tonemap path.
- renderer_scene_render_rd.cpp Tonemap block (can_use_storage leg): the
  opt-in gate runs before the native tone_mapper->tonemapper(...); only
  when the gate returns true (module setting enabled AND bridge
  session/symbol present AND rxgd_record_pass returns RXGD_STATUS_OK) is
  the native call skipped. The tonemapper_mobile leg is not wired.

The Rurix bridge TonemapGate is fail-closed and always returns
RXGD_STATUS_FALLBACK for RXGD_PASS_TONEMAP (the shipping feature-off
bridge fails closed with real_dispatch_path_not_linked even on the opt-in
real-pass arm), and the per-pass setting defaults to false, so in practice
the gate returns false and the native Godot tonemap path runs. No real GPU
pass, no acceleration, and no performance improvement is claimed.
---
diff --git a/drivers/d3d12/d3d12_hooks.h b/drivers/d3d12/d3d12_hooks.h
index 29864a4..868dcc9 100644
--- a/drivers/d3d12/d3d12_hooks.h
+++ b/drivers/d3d12/d3d12_hooks.h
@@ -56,5 +56,9 @@ public:
 	// any hooks that do not override this (or the absence of a hooks singleton)
 	// keep the native Godot luminance path.
 	virtual bool try_record_luminance_reduction(uint64_t p_source_native_handle, uint32_t p_source_width, uint32_t p_source_height, uint64_t p_dest_native_handle, uint32_t p_dest_width, uint32_t p_dest_height, float p_max_luminance, float p_min_luminance, float p_exposure_adjust) { return false; }
+	// GRX-010: opt-in hook for the tonemap pass. The base implementation
+	// returns false so any hooks that do not override this (or the absence of
+	// a hooks singleton) keep the native Godot tonemap path.
+	virtual bool try_record_tonemap() { return false; }
 	static D3D12Hooks *get_singleton() { return singleton; }
 };
diff --git a/modules/rurix_accel/register_types.cpp b/modules/rurix_accel/register_types.cpp
index bee3d93..27aa305 100644
--- a/modules/rurix_accel/register_types.cpp
+++ b/modules/rurix_accel/register_types.cpp
@@ -49,6 +49,13 @@ void initialize_rurix_accel_module(ModuleInitializationLevel p_level) {
 	// keeps rendering. Defaults to false; never enabled by the shipping
 	// build/config.
 	GLOBAL_DEF_BASIC("rendering/rurix_accel/passes/luminance_reduction/real_pass_force_capability_downgrade", false);
+	// GRX-010: per-pass opt-in gate for the tonemap pass; the pass stays on
+	// the native Godot tonemapper unless this is explicitly enabled AND the
+	// bridge actually records the pass. Defaults to false; the bridge
+	// TonemapGate is fail-closed and returns RXGD_STATUS_FALLBACK for
+	// RXGD_PASS_TONEMAP, so the native tonemap path keeps rendering in
+	// practice and no acceleration is claimed.
+	GLOBAL_DEF_BASIC("rendering/rurix_accel/passes/tonemap/enabled", false);
 
 #if defined(D3D12_ENABLED)
 	if (GLOBAL_GET("rendering/rurix_accel/enabled")) {
diff --git a/modules/rurix_accel/rurix_accel.cpp b/modules/rurix_accel/rurix_accel.cpp
index 3fa6d37..dbd1bd2 100644
--- a/modules/rurix_accel/rurix_accel.cpp
+++ b/modules/rurix_accel/rurix_accel.cpp
@@ -317,4 +317,31 @@ bool RurixAccelD3D12Hooks::try_record_luminance_reduction(uint64_t p_source_nati
 	return true;
 }
 
+bool RurixAccelD3D12Hooks::try_record_tonemap() {
+	// GRX-010 gate: every early return below means the caller must keep the
+	// native Godot tonemap path. This is 0002-level module wiring: the call
+	// carries no resource bindings (native handle mapping belongs to a later
+	// slice), and the fail-closed bridge TonemapGate always returns
+	// RXGD_STATUS_FALLBACK for RXGD_PASS_TONEMAP today, so the native
+	// tonemapper keeps rendering in practice.
+	if (!GLOBAL_GET("rendering/rurix_accel/passes/tonemap/enabled")) {
+		// manual_disabled: the pass is opt-in and defaults to disabled.
+		return false;
+	}
+	if (session == nullptr || rxgd_record_pass == nullptr) {
+		// No bridge session or record entry point; equivalent to
+		// unsupported_device for this pass.
+		return false;
+	}
+	const int32_t rc = rxgd_record_pass(session, RXGD_PASS_TONEMAP, nullptr, 0, nullptr, 0);
+	if (rc != RXGD_STATUS_OK) {
+		if (!tonemap_fallback_logged) {
+			print_verbose(vformat("RurixAccel: tonemap fallback rc=%d; Godot native tonemap path remains active.", rc));
+			tonemap_fallback_logged = true;
+		}
+		return false;
+	}
+	return true;
+}
+
 #endif // D3D12_ENABLED
diff --git a/modules/rurix_accel/rurix_accel.h b/modules/rurix_accel/rurix_accel.h
index 4061d4b..a877f3b 100644
--- a/modules/rurix_accel/rurix_accel.h
+++ b/modules/rurix_accel/rurix_accel.h
@@ -13,6 +13,7 @@
 #define RXGD_RENDER_METHOD_FORWARD_PLUS 1u
 #define RXGD_RESOURCE_TEXTURE 1u
 #define RXGD_PASS_LUMINANCE_REDUCTION 4u
+#define RXGD_PASS_TONEMAP 5u
 // GRX-009 segment 4b reserved capability flag carried in RxGdCaps.flags
 // (ABI v1, no struct layout change). Set only when the Godot dispatch
 // bring-up opt-in setting is enabled.
@@ -75,6 +76,7 @@ class RurixAccelD3D12Hooks : public D3D12Hooks {
 	RxGdRecordPass rxgd_record_pass = nullptr;
 	RxGdDestroySession rxgd_destroy_session = nullptr;
 	bool luminance_fallback_logged = false;
+	bool tonemap_fallback_logged = false;
 	static constexpr uint32_t LUMINANCE_RESOURCE_MAPPING_ROOT_CONSTANT_BYTES = 28;
 	static constexpr uint32_t LUMINANCE_RESOURCE_MAPPING_RESOURCE_COUNT = 2;
 	static constexpr const char *LUMINANCE_RESOURCE_MAPPING_SCAFFOLD = "resource mapping scaffold";
@@ -106,6 +108,12 @@ public:
 	// preflight-only bridge) returns false and the caller must keep the native
 	// Godot luminance path.
 	bool try_record_luminance_reduction(uint64_t p_source_native_handle, uint32_t p_source_width, uint32_t p_source_height, uint64_t p_dest_native_handle, uint32_t p_dest_width, uint32_t p_dest_height, float p_max_luminance, float p_min_luminance, float p_exposure_adjust) override;
+	// GRX-010: returns true only when the Rurix bridge actually recorded the
+	// tonemap pass. Any other outcome (setting disabled, missing
+	// session/symbol, or a non-OK bridge status such as RXGD_STATUS_FALLBACK
+	// from the fail-closed TonemapGate) returns false and the caller must
+	// keep the native Godot tonemap path.
+	bool try_record_tonemap() override;
 };
 
 #endif // D3D12_ENABLED
diff --git a/servers/rendering/renderer_rd/renderer_scene_render_rd.cpp b/servers/rendering/renderer_rd/renderer_scene_render_rd.cpp
index 534d0f4..f7e8820 100644
--- a/servers/rendering/renderer_rd/renderer_scene_render_rd.cpp
+++ b/servers/rendering/renderer_rd/renderer_scene_render_rd.cpp
@@ -854,7 +854,21 @@ void RendererSceneRenderRD::_render_buffers_post_process_and_tonemap(const Rende
 		}
 
 		if (can_use_storage) {
-			tone_mapper->tonemapper(color_texture, dest_fb, tonemap);
+			bool rurix_recorded_tonemap = false;
+#ifdef D3D12_ENABLED
+			if (D3D12Hooks::get_singleton() != nullptr) {
+				// GRX-010 opt-in gate. Returns true only when the Rurix module
+				// setting is enabled AND the bridge session/symbol exist AND
+				// rxgd_record_pass returns RXGD_STATUS_OK. The fail-closed
+				// bridge TonemapGate currently always returns
+				// RXGD_STATUS_FALLBACK for RXGD_PASS_TONEMAP, so this stays
+				// false and the native Godot tonemapper below runs.
+				rurix_recorded_tonemap = D3D12Hooks::get_singleton()->try_record_tonemap();
+			}
+#endif
+			if (!rurix_recorded_tonemap) {
+				tone_mapper->tonemapper(color_texture, dest_fb, tonemap);
+			}
 		} else {
 			tone_mapper->tonemapper_mobile(color_texture, dest_fb, tonemap);
 		}
-- 
2.50.0
